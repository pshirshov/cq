# implement-reviewer — T328 (opus); verdict approve (0/0/0)
agent a63707e7defcd3526. check green 1353/0; all 6 acceptance points: (1) sidebar on the 3 tabs only (HelpDocsLayout), Shortcuts none; (2) DOM `id` added ADDITIVELY beside the existing data-testid (testids preserved); (3) helpActiveSectionSetters Map exposed + IntersectionObserver happy-dom-guarded; (4) scrollToHelpSection exported; (5) Agents tab still renders help-agent-<id> per AGENT_ROLES (cross-nav targets); (6) tests non-vacuous (exact scrollIntoView receiver + exactly-one aria-current + Shortcuts-no-sidebar). T324 .lw-help-body 90vh scroller unaffected (.lw-help-docs owns its own content-pane scroll). Non-blocking observation: scrollspy useEffect depends on a freshly-allocated `entries` array → re-runs per render (correctness-neutral, observer disconnected in cleanup) — not filed.
