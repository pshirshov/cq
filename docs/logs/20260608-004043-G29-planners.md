# Plan-flow candidate-planner logs — G29 (multi-planner round 1)

- **Goal:** G29 — provider-qualified pi token grammar (resolves D36)
- **Milestone:** M92 (coordination) / M94 (work)
- **Outcome:** synthesized plan persisted (9 tasks T231–T239 under M94); goal → planning

## Candidates
- **opus** (Agent a3332f6436f9afff5, claude:opus-4.8[1m]) — 8-task single-milestone candidate. Strongest base: caught the 3-layer surfacing (@cq/config ReviewerToken → @cq/ledger structural ResolvedReviewer/GetConfigResult → @cq/ledger-mcp projection), isolated the breaking-test-migration as a distinct task, added a cross-layer @cq/config↔extension consistency test. USED AS SYNTHESIS BASE.
- **grok** (pi:grok-build) — 9-task / 4-milestone candidate; finer parser/resolver/expose decomposition (type → parse → resolvers → expose as separate units). Folded: confirmed the layered split.
- **minimax** (pi:ollama-cloud/minimax-m3) — completed exit 0 **via ollama-cloud** (proving the user's fix / the D36 root cause: minimax-m3 is served by ollama-cloud, not the key-gated minimax provider). 6-milestone candidate; contributed the FINAL VERIFICATION GATE (folded as T239: bun run check + nix build .#ledger-{mcp,tui,web} + repo-wide `rg` bare-pi audit). Its "Resolve D36 / archive milestone" tasks were dropped — defect closure + archival are orchestrator-owned (Q20), not worker tasks.

## Synthesis (judge + fold-in)
Base = opus. Folded grok's finer decomposition + minimax's verification-gate. Result: 9 tasks under M94 — T231 (hinge: ReviewerToken.provider + slash-extraction + reject bare pi), T232 (resolvers + ledger config-capability surfacing), T233 (extension mirror reject-bare-pi), T234 (migrate live cq.toml + cq.toml.example, BREAKING), T235 (adapt broken pre-existing tests), T236 (decisions item: mirror must agree), T237 (docs), T238 (end-to-end + cross-layer consistency tests), T239 (verification gate). All fix tasks ledgerRef goals:G29 + defects:D36 (D36 closes on merge, orchestrator-owned).
Locked decisions honored: Q132 slash separator; Q133 structured provider field + mirror; Q134 DROP bare pi (breaking + migration); Q135 pi-only; Q136 unit + cross-layer tests, reuse T225 live evidence.
