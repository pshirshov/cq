# Session log — T377 implement-worker (opus)

- **Task:** T377 (M158, G45); worker; status **pass**; resultCommit 06103ac; check 1649/0.

### Session summary
- **Did:** converted static SERVER_INSTRUCTIONS in ledger-mcp/src/main.ts to a pure `buildServerInstructions(toolPrefix)` (SERVER_INSTRUCTIONS_TEMPLATE source). Empty prefix returns the original byte-identically; non-empty rewrites every whole-word LEDGER_TOOL_NAMES occurrence to its prefixed form via the shared prefixToolName helper (Q208 reuse), longest-first with \b boundaries + escapeRegExp to avoid partial/substring rewrites. assertToolPrefix once. McpServer construction calls buildServerInstructions(''). New serverInstructions.test.ts.
- **Achieved:** pass; resultCommit 06103ac; bun run check green (1649 pass/0 fail).
- **Discovered:** harness cut from stale base f941bce; applied sanctioned `git reset --hard b4cdb5f`. **Worker prose claim that derive_predicates is NOT in LEDGER_TOOL_NAMES was FALSE** (opus reviewer verified it IS at ledgerTools.ts:803) — but the code iterates the LIVE array so it CORRECTLY rewrites derive_predicates→myproj_derive_predicates regardless; no code defect.
- **Issues:** the false prose claim is a narrative error, not a code defect (opus-adjudicated).
