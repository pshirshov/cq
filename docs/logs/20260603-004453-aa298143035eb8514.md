# plan-advance — goal G6 (follow-ups #2/#3/#4) — review-requested

- Goal: G6 — full accumulated scope plan
- Role: planner (plan-advance subagent)
- Returned: `review-requested`

### Session summary
- **Did:** Advanced G6 by one step (rule 2(b)): Q55-Q72 answered, persisted grounding, transitioned G6 clarifying→planning, created three work milestones, emitted the full fine-grained plan for #2/#3/#4.
- **Achieved:** G6 `planning`; fields.milestones = [M28, M33, M31, M32]. M33 (#4A defect-lifecycle states, T116-T122), M32 (#3 ledger-mcp --reset, T123-T124), M31 (#2 /advance + N=4→8 + #4B sweep/cleanup, T125-T129) — 14 tasks, each with acceptance + bun run check gate + suggestedModel, ordered by dependsOn (M31 dependsOn M33). File-collisions serialized: plan/advance.md + plan-advance.md (T120), implement/advance.md (T121→T127→T128), advance.md (T125→T128); constants.ts schema (T116) gates all #4A dependents.
- **Discovered:** LIVE DEFECTS_SCHEMA (packages/ledger/src/constants.ts:102-126) is `[open, wip, blocked, resolved, abandoned]`, NOT the open/wip/resolved/wontfix stated in Q66's preamble. The locked target `[open, wip, root-caused, inconclusive, resolved, wontfix]` therefore also requires dropping `blocked` and renaming `abandoned`→`wontfix`; captured in grounding + T116/T122 (incl. live-defect migration: grep for any blocked/abandoned defect). `backupAndReinit` is private (init divergence branch only) → --reset needs a public reset() (T123). `.codex/prompts` symlinks committed separately from the link-prompts.ts `.claude` tree (T126).
- **Issues:** Q66 preamble/live-schema mismatch flagged for the reviewer; followed Q67 verbatim (open→{wip,resolved,wontfix}, no open→root-caused/inconclusive).
