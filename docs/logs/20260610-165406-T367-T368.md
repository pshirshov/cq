# Session log — T367 + T368 implement (opus/sonnet workers + opus reviews)

## T367 (M152) — advance-gate verdict tests; resultCommit 870a466 → 4eec74c (main); check 1622/0; review APPROVE (opus af7213f4269b5cb21)
WORKER: extended packages/cq-cli/test/advance-gate.test.ts into the full verdict+exit-code matrix (5 tests) via dispatch(['advance-gate',...]) with captured DispatchIo: (1a) marker+P-investigate TRUE⇒block/non-zero/'continue per D41'; (1b) marker+P-implement TRUE (planned goal + DAG-ready task)⇒block; (2) marker+all-FALSE⇒allow/0; (3) marker ABSENT with a TRUE ledger on disk⇒allow/0 (gate dormant, ledger unread, items=[]); (4) marker+external-signal⇒allow/0 even with TRUE predicate. advanceGate.ts unchanged.
REVIEW (opus approve): all 4 cases teethed + non-vacuous (cases 3/4 seed a genuinely-TRUE ledger on disk proving dormancy/override); block path exercised for 2 predicates; test-only diff; check 1622/0.

## T368 (M153) — advance.md detection→MCP rewire; resultCommit 6e77ef5 (r1) + a02659a (r2) → e0497ee (main); check 1621/0; review round1 REVISE → round2 APPROVE (opus ae28443724c9d95d0)
WORKER (r1): rewired advance.md §Bootstrap + §Detection-predicates to call `mcp__ledger__derive_predicates` as the authoritative predicate source (replacing snapshot() hand-derivation); retained prose definitions; added the anti-drift sentence (shared derivePredicates() backs both the MCP tool + the cq advance-gate CLI); regen agentsCatalogue.gen.ts; §cycle/§Stop-condition semantics untouched (T370's scope).
REVIEW r1 (opus DISAPPROVE, 1 criticism): the §Bootstrap-recipe documented return shape claimed `items: [{id,status,summary}]` but derivePredicates returns `items: string[]` (id strings) — doc-accuracy bug; everything else (tool name, source-rewire, retained defs, anti-drift sentence verified vs advanceGate.ts, freshness guard, check 1621/0) correct.
ROUND-2 FIX (orchestrator, a02659a): corrected the JSON shape to `"items": ["<itemId>", ...]` + the prose to "plain array of ledger-item id strings (e.g. [\"D50\",\"T361\"])"; regen agentsCatalogue.gen.ts; check 1621/0 + freshness guard green. Test-only/prose delta directly addressing the single criticism.
