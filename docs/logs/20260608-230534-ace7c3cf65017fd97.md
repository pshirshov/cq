# Implement worker log — T285 (worker: opus[claude], worktree)

- Task T285 (M116/G34). Branch implement/T285, commit 189a107, base 005c19c. status: pass. bun run check GREEN (1242 pass / 0 fail).

### Session summary
- Replaced the throwing T283 computeAgentModels stub in ledger-mcp configCapability with a real impl walking a NEW shared 19-role roster (@cq/config AGENT_ROLE_TIERS constant), resolving each model-configurable subagent's tier via resolveAgentTier + selectTokensForTier over the planners∪reviewers candidate union, grouped per harness exactly as gen-agents deriveModelMappings. Statuses: resolved / no-live-token / not-configured / not-model-configurable; re-reads cq.toml per call; configured = config !== null. gen-agents now imports the shared roster + asserts its ROLES table matches at codegen time (assertRosterMatchesShared); ledger-mcp test asserts roster shape (19 roles, 7 configurable) + all four statuses.
- **Worktree stale-base note:** the native worktree branch was worktree-agent-ace7c3cf65017fd97 on a stale 087b889 'wip' (pre-T283/T284); worker `git reset --hard 005c19c` then created implement/T285. Commit parent verified 005c19c.
- filesTouched: cq-config/src/agentRoster.ts, cq-config/src/index.ts, ledger-mcp/src/configCapability.ts, ledger-mcp/test/computeAgentModels.test.ts, ledger-web/scripts/gen-agents-catalogue.ts.
