# investigate-explorer session — D38 / H27

- defect: D38 (Pi-path subagent child emits off-enum verdict "fail" instead of go-ahead|revise)
- hypothesis: H27
- role: explorer
- lean: would-confirm (supports)

### Session summary
- **Defect:** D38 (Pi-path subagent child emits off-enum verdict `"fail"` instead of `go-ahead|revise`)
- **Hypothesis:** H27 (Pi dispatch never pins the child's verdict to the canonical enum, and no orchestrator-side validation rejects/normalizes off-enum verdicts)
- **Role:** explorer
- **Lean:** would-confirm
- **Why:** All three sub-claims are supported by reading: (1) both rubrics specify literal enums (`go-ahead|revise` plan-review.md:85, `approve|disapprove` implement-review.md:83); (2) the Pi dispatch trigger (pi-context.md:43-72) and the `dispatch_agent` tool (cq-subagent-dispatch.ts:607 passes `args.task` verbatim, injects no skeleton/enum) never re-assert the exact enum; (3) both orchestrators' abstention rules key on contract-key *parseability* not enum-literal validity, and reconcile is bare string-equality against the two literals with no normalization/rejection of an off-enum value (plan/advance.md:291-317, implement/advance.md:142-191).

### Caveats harvested
- Two Pi reviewer paths share the gap: the DIRECT `pi -p` shellout (K30, used by plan/advance.md + implement/advance.md reviewer panels) and the `dispatch_agent` extension (T224/T227 demo path). The defect was observed on the dispatch path; the gating logic lives on the shellout path. A fix must cover the orchestrator reconcile regardless of path.
- The rubrics DO specify the enum, but that is the CHILD's instruction; a model can still paraphrase and nothing downstream catches it.

### Evidence (all 9 validated [correct] by orchestrator)
1. plan-review.md:82-86 — `"verdict": "go-ahead | revise"` (enum source).
2. implement-review.md:81-83 — `"verdict": "approve | disapprove"` (enum source).
3. pi-context.md:51-67 — dispatch trigger maps convention onto `dispatch_agent({agent,task})`; no enum re-assertion.
4. cq-subagent-dispatch.ts:605-607 — passes `args.task` verbatim + agent md as append-system-prompt; no skeleton/enum injected.
5. cq-subagent-dispatch.ts:687-694 — returns child raw final text; no verdict parse/normalize/validate.
6. plan/advance.md:291-299 — abstention keys on parse-into-contract, not enum validity.
7. plan/advance.md:310-311 — reconcile is bare string-equality vs the two literals.
8. implement/advance.md:145-150 — same parseability-only abstention.
9. implement/advance.md:174-177 — strictest-wins string-equality vs approve|disapprove.
