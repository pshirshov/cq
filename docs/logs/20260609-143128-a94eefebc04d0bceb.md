# implement-worker — T323 (sonnet); D45 fix

- Task T323; agent a94eefebc04d0bceb; status **pass**; resultCommit 17a36e6; check green 1333/0; surgical 2-file diff.
Reproduce-first: added the createLedger mirror test cell FIRST, confirmed it FAILS with ENOENT (mirror's docs/ledgers.yaml absent after 'create') on unpatched code, THEN inserted `if (op === "create" || op === "archive") await mirrorFile(layout, mirrorRoot, layout.registryPath)` before the existing archive-only early return; docstring updated. New cell + existing 'on archive' cell both pass (7 cache-mirror tests green). **Discovered:** createLedger's schema needs statusValues/terminalStatuses (not just fields), and the test ledger name needs a unique initial letter to avoid an "M" prefix collision with `milestones` — used name "xledger" (prefix X).
