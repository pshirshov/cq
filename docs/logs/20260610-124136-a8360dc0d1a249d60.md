# Session log — T359 implement-worker (opus)

- **Task:** T359 (M149, G43); status pass; resultCommit 1ecf166 (worktree) → 857464d (main); check 1564/0

### Session summary
Added `packages/ledger/test/gitInvariants.test.ts` with all five K66/Q196 git-backend invariants as named, separable regression-guard tests: (1) host-checkout byte-identity across a real create+update+archive sequence (tree sha256 + HEAD + index `git ls-files -s` digest + `git status --porcelain` empty, before/after); (2) orphan-ref one-commit-per-mutation + parentless root via `git rev-list --max-parents=0` (exactly one parentless root; cat-file shows no parent line); (3) CAS stale-reject asserting the typed StaleRefError both directly on GitPlumbing.updateRef AND across a backend write raced by an out-of-band ref advance (Proxy interposes updateRef to advance the ref between the backend's read-old and its CAS — no last-writer-wins); (4) lock-not-committed enumerating the orphan tree (`git ls-tree -r`) for absence of any .lock/.locks path; (5) backup-tag asserting cq-ledger-backup-<ts> exists AND points at the prior head before reinit. Throwaway mkdtemp repo per test, afterAll cleanup. DISCOVERED: CAS-stale was NOT covered by T352 at all (only byte-identity/one-commit/no-lockfiles/backup-tag) — this adds genuinely new coverage; the orphan root after init() must be located via --max-parents=0, not the ref head. T352's gitObjectLedgerBackend.test.ts left intact. Based on main 1af178c.
