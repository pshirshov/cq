# Plan candidate planner — G38 follow-up #1 (minimax, pi ollama-cloud/minimax-m3 CANDIDATE)
Candidate plan emitted (7 milestones, 7 tasks — one per FU sub-item; tests folded INTO each task, no separate verification task). Same dependency layering: FU-4-data (agentId+roleKind+palette) → FU-4-render (clickable+keyboard+fills) → FU-4-catalogue (agentId map + all formalized ops + infra nodes); FU-2 → FU-3; FU-4-flowtab (cross-nav + legend) deps render+catalogue+sidebar. Strong operational acceptance per task (scrollIntoView spy, computed-style role/tabIndex/cursor/fill assertions, fake-fetch seam for FU-1). Its "tests in each task" model was adopted into the synthesis; the data/render split was merged into one renderer+data-model+palette task (T326) for cohesion.
