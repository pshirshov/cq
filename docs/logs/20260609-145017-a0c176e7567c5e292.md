# Plan-flow planner — G38 follow-up #1 (single-planner; role: planner); returned **awaiting-answers**
Agent a0c176e7567c5e292.

### Session summary
- **Did:** DEFAULT single-planner step on G38 — grounded the FU-1..FU-4 follow-up scope read-only against the ledger-web HelpOverlay and filed a clarifying-question batch.
- **Achieved:** Filed 7 open `questions` under M126, all linked goals:G38 — Q176 (FU-1 optional stale-server UX-message task scope), Q177 (FU-2 hard-90% vs responsive popup + internal scroll), Q178 (FU-3 sidebar form + entry set + jump behavior), Q179 (FU-4a/b agent depiction + clickable-node cross-nav + the role→agentId mapping), Q180 (FU-4c which git ops/handoffs to surface and how), Q181 (FU-4d color scheme by roleKind + legend + palette), Q182 (whole-follow-up web-only confirmation). Q167-Q173 terminal; G38 stays clarifying.
- **Discovered (FU-4 renderer/data gaps):** DiagramNode (diagramLayout.ts) carries only id/label/terminal/fill — NO click handler/href/agent linkage; DiagramSvg renders static <g>/<rect> with no onClick and never maps roleKind→color (single DEFAULT_FILL grey); RoleNode.roleKind exists but is unused by the renderer. CRITICAL: ROLE_FLOWS nodes are ABSTRACT role labels while the Agents tab is keyed by CONCRETE AGENT_ROLES catalogue ids (agentsCatalogue.gen.ts) — the namespaces don't line up, so FU-4(b) clickable cross-nav needs an explicit role→agentId map. Each tab already renders stable testid'd anchors (help-item-state-<ledger>, help-flow-<id>, help-agent-<id>) reusable for FU-3 scrollIntoView + FU-4 cross-nav.
- **Issues:** none blocking. FU-1's display issue is a deploy action (rebuild+restart), already diagnosed; only the optional UX-message task is plannable.
