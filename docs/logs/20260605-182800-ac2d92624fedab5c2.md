# Investigate-explorer session log

- **Defect:** D29 — empty/whitespace answer accepted on `answered` transition
- **Hypothesis:** H19 (backend: ledger core/MCP does not require a non-empty `answer`)
- **Role:** explorer (read-only)
- **Returned lean:** supports

---

### Session summary
- **Did:** gathered evidence for hypothesis H19 (ledger core/MCP does not require a non-empty `answer` when a question transitions to `answered`)
- **Achieved:** 6 numbered evidence items; lean supports
- **Discovered:** (a) `QUESTIONS_SCHEMA.answer` is declared `{ type: "string", required: false }` with no non-empty/min-length/whitespace/status-conditional constraint (constants.ts:217); (b) the `update_item` MCP handler passes `fields` straight to `store.updateItem` with no answer check, and the Zod `fieldsSchema`/`fieldValueSchema` accept any string including `""` (ledgerTools.ts:171-173, 298-318); (c) the store transition path `applyUpdateItem` runs only `validateFields` (type + required-on-create) and the F1 transition guard; the ONLY cross-ledger precondition hook is goal-phase-specific, nothing for questions/answer (core.ts:259-291, 559-588); (d) `assertFieldType` for `"string"` checks only `typeof value !== "string"`, so `""`/`"   "` pass (core.ts:852-859); `validateFields` has no non-empty check (core.ts:831-850); (e) no backend test asserts empty-answer rejection — the sole `answer`+`whitespace` hit is a frontend UI test (`answerLock.test.tsx`, T88/Web #15) that disables buttons client-side, not an MCP-boundary validation. No contradicting evidence found.
- **Issues:** none. Codegraph was not indexed for this repo (used Read + grep).

Validated by orchestrator: citations #1 (constants.ts:212-219), #4 (core.ts:267-286), #5 (core.ts:831-859) re-opened against source and confirmed exact. Precondition wiring confirmed goals-only at FsLedgerStore.ts:571-581.
