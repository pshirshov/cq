# Implement review log — T287 (reviewers: opus[claude] + pi:ollama-cloud/minimax-m3)

- Task T287 (M116/G34). Reconciled verdict: **approve** (opus approve agent a573023ce70eb4c26; minimax approve; grok+codex abstained — no API key). Cherry-picked to main d750167; integrated bun run check 1262/0.

### opus (approve)
get_agent_models registered in BOTH createLedgerMcpTools + registerLedgerStdioTools mirroring get_config (empty schema, ConfigNotImplementedError, computeAgentModels via jsonResult); HTTP/embedded exposure via shared buildServer; LEDGER_TOOL_NAMES + drift/count tests bumped 21->22 across 4 sites; server-level stdio tests assert the 19-entry roster (fixture + no-cfg) + not-implemented shape; diff single commit, no scope creep; bun run check green at facdfc2.

### minimax (approve)
Same conclusion: every acceptance clause satisfied; tool registered both paths, count bumped, drift tests updated, 19-roster stdio test. Raised non-blocking coverage-gap notes (HTTP path asserts only the tool-list membership not the return shape; no stdio not-implemented test) — explicitly out-of-scope for T287's 'server-level test' acceptance; verdict approve.
