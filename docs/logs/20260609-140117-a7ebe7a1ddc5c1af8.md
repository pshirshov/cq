# Plan candidate planner — G39 (opus, claude plan-advance CANDIDATE mode)
Agent a7ebe7a1ddc5c1af8. Candidate plan emitted: 1 milestone, 1 task (standard). Grounded against cacheMirror.ts + cache-mirror.test.ts + FsLedgerStore.createLedger (L730-761) — root cause holds verbatim (cacheMirror.ts:82 early return before :84 registry mirror; createLedger writeRegistry L756 + fireMutation 'create' L759). Cleanest fix: gate registry mirror on op==='create'||'archive'; existing test file has the XDG_CACHE_HOME redirect + dispose() drain seam + a "mirrors ledgers.yaml on archive" cell to parallel. Single task = correct granularity (fix+docstring+test share one verification; splitting adds ordering overhead).
