# plan-reviewer — goal G8 (D20+D21) — R125 (revise)

- Goal: G8; Role: reviewer; Verdict: `revise` — R125 (4 criticisms, 0 new questions)

### Session summary
- **Did:** reviewed G8 plan (M36: T130 D20-flakiness, T131 D21-reset-non-canonical) grounded against navMemo.test.tsx, app.test.tsx, FsLedgerStore.ts, LedgerSearchIndex.ts.
- **Achieved:** verdict revise, R125, 4 criticisms.
- **Discovered:** T131 (D21) sound + well-grounded — LedgerSearchIndex.removeLedger exists; reset() confirms orphan-.md survival (backupAndReinit never unlinks non-canonical) + stale-FTS survival (this.ledgers.clear() leaves searchIndex untouched; init() re-indexes only registry ledgers). T130 (D20) has 3 accuracy faults: (1) navMemo 'drop the wall-clock assertion' is STALE — the test already asserts counters via a waitQuiescent poll, no wall-clock assertion; the real residual flake is bun's default 5000ms PER-TEST timeout under contention. (2) cited flaky files scrolling.test.tsx/suggestions/questionDetail don't exist — the scroll test is in app.test.tsx. (3) poll-until-condition can't apply verbatim to the negative-assertion 's'/'e'-key-inert tests. D13 correctly excluded; parallel-safety + ledgerRefs correct.
- **Issues:** none.
