# Session log — plan-advance (planner), goal G23

- Goal: G23 (Describe all flow state machines + add a Flows help tab)
- Role: planner (plan-advance subagent), run inline under /cq:advance → /cq:plan:advance
- Returned status: review-requested
- Agent id: acec7ccba0d2b1f8c

## Plan
- **M77 (phase 1):** T200 — author the flow state-machine doc.
- **M78 (phase 2, dependsOn M77):** T201 (elkjs dep + Nix FOD hash + bundle closure) → T202 (elk layout adapter + thin SVG renderer) → T203 (migrate State-machines tab, retire homegrown layout) + T204 (flow render-data module, dependsOn T200+T202) → T205 (Flows tab UI, dependsOn T203+T204) → T206 (e2e verification, dependsOn T205).
- **K37 (proposed decision):** adopt elkjs — full rationale + alternatives, for the reviewer's adversarial check.

## Session summary
- **Did:** G23 was in `clarifying` with Q114/Q115/Q116 all answered; folded the three decisive constraints in, grounded in the repo (web help dialog, homegrown renderer, Nix packaging) and researched diagram libraries, then transitioned G23 `clarifying → planning` and emitted the full decomposed plan (rule 2(b)).
- **Achieved:** Recorded goal grounding; created work milestones M77 (phase-1 doc) and M78 (phase-2, dependsOn M77) and recorded them on G23.fields.milestones; recorded the library-selection decision K37 (status `proposed`, subject to adversarial review) proposing **elkjs**; created tasks T200 (M77) and T201–T206 (M78) with full headline/description/acceptance/suggestedModel and dependsOn ordering (T201→T202→{T203, T204(+T200)}→T205→T206). Reached `planning`, awaiting the reviewer.
- **Discovered:** The load-bearing library constraint is the happy-dom test env (no real layout: no getBBox/ResizeObserver/DOMMatrix), which rules out measurement-dependent renderers (mermaid, React Flow, cytoscape) and favors elkjs (pure layout → coords, thin in-repo SVG, structural tests preserved). The homegrown computeDagLayout is shared with DagView (milestone DAG), so the migration scope is the TWO diagram tabs only — DagView stays on the homegrown layout. A new web runtime dep requires four wiring points: package.json, bun.lock, FOD outputHash refresh, and the ledger-web `for dep` symlink loop in flake.nix (~line 345). elkjs needs the elkjs/lib/elk.bundled.js entry to avoid a web-worker asset under Bun.build. Codegraph not indexed; used Read + web search.
- **Issues:** None blocking. The library choice was explicitly delegated (proposed as K37, not a question). flowData.ts and the phase-1 doc are intentionally two artifacts kept in sync by hand (Q114), a drift risk flagged for the reviewer.
