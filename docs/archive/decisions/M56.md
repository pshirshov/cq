## M56

### K30 — locked

- createdAt: 2026-06-05T20:37:38.415Z
- updatedAt: 2026-06-05T20:37:38.415Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: pi non-interactive invocation contract (T169 spike — CONFIRMED)
- rationale: |
    Spike T169 confirmed `pi` IS non-interactively drivable in this sandbox for both target providers, byte-clean stdout, both OAuth-pre-authenticated. CONTRACT (downstream T175/T176 build on this):
    - Mode: `pi -p` / `pi --print` = non-interactive print mode. Add `--no-session` (ephemeral, no session persistence) + `--no-tools` (no FS/bash access for the reviewer model).
    - Provider+model selection: `--provider <P> --model <M>` OR combined `--model <P>/<M>`. Omitting both defaults to grok-build/grok-build.
    - (a) grok-build: `pi -p --no-tools --no-session --provider grok-build --model grok-build '<prompt>'`
    - (b) gpt-5.5: `pi -p --no-tools --no-session --provider openai-codex --model gpt-5.5 '<prompt>'`
    - Auth: BOTH authenticated via OAuth in ~/.pi/agent/auth.json (grok-build, openai-codex); no /login needed.
    - Output: `--mode text` (default) → stdout is the bare model reply + trailing \n, NO ANSI/TUI escapes (verified od -c), pipes directly to jq. `--mode json` is a JSONL event STREAM (not a single object) — extract assistant text via jq on message_end events. So for reviewer-json capture, use default text mode and parse the fenced/bare json from stdout.
    - Reproducible across reruns. Findings: docs/drafts/20260605-2130-pi-noninteractive-spike.md.
    Risks for downstream: model output non-determinism, OAuth token expiry, possible prose/fence wrapping (T175/T176 must tolerate fence-wrapped json).
- ledgerRefs: ["goals:G15","tasks:T169"]
