# Plan candidate planner — G38 follow-up #1 (opus, claude plan-advance CANDIDATE mode)
Agent aaf85331f25793153. Candidate plan emitted (1 milestone, 8 tasks; chained same-file tasks). Best grounding of the panel.

### Session summary
Grounded against the 7 locked answers (Q176-Q182) + the ledger-web HelpOverlay sources. KEY discoveries: `.lw-help` is currently `min(900px,92vw)×min(80vh,720px)`; **`.lw-help-body` ALREADY has overflow-y:auto + min-height:0 and `.lw-help-head` is ALREADY pinned** → FU-2 is mainly the 90vw×90vh size change. DiagramSvg already honors `n.fill ?? DEFAULT_FILL` → FU-4d is zero-renderer-change for color (palette + node.fill authoring + legend). DiagramNode (diagramLayout.ts) = id/label/terminal/fill, NO onClick/agentId → FU-4a/b widens it + threads an activate callback. ROLE_FLOWS nodes are abstract; Agents tab keyed by concrete AGENT_ROLES ids → explicit role→agentId map. testid scheme help-flow-<id>/-node-<id>/-edge-<from>-<to>, help-agent-<id>, help-item-state-<ledger> reusable for FU-3 scroll + FU-4b cross-nav. Decomposition: FU-2 + FU-1 leaves; FU-4 renderer affordance; roleActions tasks chained (palette → ops → agentId map); FU-3 (dep FU-2); FU-4b wiring+legend (dep renderer+agentId+FU-3). Synthesis adopted this structure; folded minimax's tests-in-each-task (no separate test task).
