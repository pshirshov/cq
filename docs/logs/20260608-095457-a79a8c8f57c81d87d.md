# implement-worker (opus) — T233

- task: T233 (reject bare pi in extension tokenToChildModel mirror); status: pass; resultCommit c92aa38
### Session summary
Changed cq-subagent-dispatch.ts tokenToChildModel so a pi token's model must carry a non-empty provider/model qualifier: bare (no '/') and empty-half ('p/'|'/m') now return null (refused) instead of dispatching {provider:null,model} (the D36 bug). Mirrors @cq/config parseReviewerToken THROW-on-bare (T231) per K50. Rewrote JSDoc + model param + explicit-override comment to qualified-only grammar + mirror obligation. claude/non-pi unchanged (null). Standalone tsc --strict vs pi 0.78.0 store types CLEAN (pi-extensions not in the bun workspace; injected as nix store path at runtime). 6/6 cases pass.
