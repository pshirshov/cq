## M85

### T216 — done

- createdAt: 2026-06-07T00:12:13.383Z
- updatedAt: 2026-06-07T00:32:15.026Z
- author: "opus-4.8[1m]"
- session: 059ff637-d28c-4785-8125-9c0d73ddf7a0
- headline: Add .lw-modal-backdrop / .lw-modal overlay CSS so LogModal renders as a popup (Q121)
- description: |
    ROOT CAUSE of 'opens inline, not a popup': packages/ledger-web/src/styles.css has NO rules for the `.lw-modal-backdrop` / `.lw-modal` classes that LogModal (App.tsx ~L3116-3123) uses, so the modal renders in normal document flow (inline, below the log links) instead of as a fixed overlay.
    
    Add CSS modeled on the existing `.lw-help-backdrop`/`.lw-help` rules (styles.css ~L152-171):
    - `.lw-modal-backdrop`: position:fixed; inset:0; z-index (>= the help dialog's 50, e.g. 60); display:flex; align-items:center; justify-content:center; background: rgba(0,0,0,0.5).
    - `.lw-modal`: width:min(900px, 92vw); max-height:min(85vh, 760px); padding; background:var(--panel); border:1px solid var(--line); border-radius:8px; box-shadow; display:flex; flex-direction:column.
    - `.lw-modal-head`: flex row, space-between, align-items:center, small bottom margin (mirror `.lw-help-head`).
    - `.lw-modal-body`: flex:1; min-height:0; overflow-y:auto (scroll long logs inside the popup).
    - `.lw-modal-title`: ellipsis/word-break for the long path.
    Reuse existing `.lw-close` for the ✕ (already styled). Match surrounding style; no unrelated refactors.
    
    No TSX change in this task (LogModal already emits these classes). This is the change that makes it a true popup.
- acceptance: "After the change, styles.css contains `.lw-modal-backdrop` (position:fixed; inset:0) and `.lw-modal` rules. Manually (or via the existing happy-dom modal test if it asserts layout-affecting attributes) the LogModal overlays the page rather than appearing inline. `bun run lint` passes (valid CSS). The CSS z-index is >= the help/batch overlay so the log popup layers above content."
- suggestedModel: fast
- ledgerRefs: ["goals:G26"]
- resultCommit: 607f098366ea7a2cfbf9c9685fe44500b94d9912
- completion: Added .lw-modal-backdrop/.lw-modal overlay CSS (fixed, z-index 60) so LogModal renders as a true popup instead of inline.
- sessionLogs: ["docs/logs/20260607-002428-a43de33fca57129ed.md","docs/logs/20260607-002809-afaf0ec3c00e29f38.md"]

### T217 — done

- createdAt: 2026-06-07T00:12:25.323Z
- updatedAt: 2026-06-07T00:44:06.935Z
- author: "opus-4.8[1m]"
- session: 059ff637-d28c-4785-8125-9c0d73ddf7a0
- headline: Render LogModal content via the Markdown component instead of <pre> (Q121)
- description: |
    In packages/ledger-web/src/App.tsx, LogModal's `state.kind === 'ok'` branch (~L3141-3149) renders the fetched log content as `<pre className="lw-log-content" data-testid="log-modal-content">{state.content}</pre>`. Replace the `<pre>` with the existing `Markdown` component (import from './Markdown.js' — confirm the import is present; it is already used elsewhere in App.tsx for field rendering): `<Markdown text={state.content} />` wrapped in a container that carries `data-testid="log-modal-content"` so existing tests that query that testid still resolve.
    
    Keep the loading / error branches unchanged. Markdown component already applies remark-gfm + rehype-sanitize (safe) and `.lw-md` styles exist, including `.lw-md pre`/`code` for fenced blocks. Keep `lw-log-content` class if any test/style depends on it, but the content is now rendered markdown, not preformatted text. Surgical change only.
- acceptance: LogModal's ok-branch renders `state.content` through `<Markdown>` (react-markdown), not a raw `<pre>`. The container still exposes `data-testid="log-modal-content"`. `bun run typecheck` passes. A log whose content is `# Heading` renders an `<h1>`/role=heading element (verified by the test in the next task), not literal `# Heading` text.
- suggestedModel: standard
- dependsOn: ["T216"]
- ledgerRefs: ["goals:G26"]
- resultCommit: "3591456"
- completion: LogModal ok-branch renders log content via the sanitized Markdown component instead of <pre>; testid + class preserved.
- sessionLogs: ["docs/logs/20260607-004046-aedee54a4d9ed36ce.md","docs/logs/20260607-004328-ada9337de7eeadbba.md"]

### T218 — done

- createdAt: 2026-06-07T00:12:36.068Z
- updatedAt: 2026-06-07T00:32:15.261Z
- author: "opus-4.8[1m]"
- session: 059ff637-d28c-4785-8125-9c0d73ddf7a0
- headline: Relax read_log truncation cap per decision K42 (Q124 'render everything')
- description: |
    Per locked decision K42: Q124 ('render everything without truncation') is satisfied by relaxing — NOT removing — the read_log byte cap, so real session logs are always returned whole while a generous safety bound prevents unbounded reads.
    
    In packages/ledger/src/mcp/readLog.ts, raise `MAX_READ_LOG_BYTES` from `256 * 1024` (256 KiB) to a generous bound (e.g. `4 * 1024 * 1024` = 4 MiB). Update the surrounding doc comment to reflect the new bound and the rationale (full-log rendering with a defensive cap). KEEP the `truncated` flag and the LogModal truncation notice as a defensive fallback for pathological files — do not delete them.
    
    Do NOT make the read unbounded (would risk MCP tool-output / browser overflow; violates fail-fast boundary validation). If any test asserts the exact 256KiB value (search for MAX_READ_LOG_BYTES usages / a truncation test in packages/ledger/test), update it to the new constant.
- acceptance: "`MAX_READ_LOG_BYTES` is raised to the K42 bound (e.g. 4 MiB); the doc comment is updated. Any existing read_log truncation test still passes against the new constant (i.e. it references the constant, not a hardcoded 262144). `bun test` for the ledger package passes. A typical session log (well under the new bound) returns with `truncated` absent/false."
- suggestedModel: fast
- ledgerRefs: ["goals:G26"]
- resultCommit: d730720
- completion: Raised read_log MAX_READ_LOG_BYTES 256KiB->4MiB per K42 (truncation safety net preserved). Cherry-picked onto main (worktree branch was based on a divergent commit).
- sessionLogs: ["docs/logs/20260607-002428-ada286ec3a35c8f81.md","docs/logs/20260607-002809-a2cd541e370d5e9dc.md"]

### T219 — done

- createdAt: 2026-06-07T00:12:48.458Z
- updatedAt: 2026-06-07T00:55:38.017Z
- author: "opus-4.8[1m]"
- session: 059ff637-d28c-4785-8125-9c0d73ddf7a0
- headline: "happy-dom test: log popup opens as overlay and renders markdown content"
- description: |
    Add/extend a ledger-web happy-dom test (look under packages/ledger-web/test for the existing LogModal/session-logs test — search for testids `session-logs-section`, `log-link-`, `log-modal`, `log-modal-content`). The test drives the real component path with a fake LedgerClient whose `readLog` resolves to markdown content.
    
    Assert:
    1. Initially no `log-modal` is present.
    2. Clicking a `log-link-<path>` button opens the modal: `log-modal` (role=dialog) and `log-modal-backdrop` are present.
    3. The fetched content is rendered as MARKDOWN, not literal text: feed content like `# Title\n\n- a\n- b` and assert a heading element (e.g. an `<h1>` / `role=heading` with text 'Title') exists inside `log-modal-content`, and that the literal string `# Title` does NOT appear as text. (This is the regression guard for the <pre>->Markdown swap.)
    4. Closing via the ✕ (`log-modal-close`) or Escape removes the modal.
    
    Note happy-dom caveat (CLAUDE.md): controlled text inputs don't fire onChange — not relevant here (buttons/clicks). Keep the test focused; reuse existing test harness/utilities.
- acceptance: "New/extended test in packages/ledger-web/test passes under `bun test`: it opens the log popup, asserts the modal+backdrop appear, asserts a `#` heading in the log content renders as a heading element (not literal `#` text), and asserts close behavior. The test fails if LogModal reverts to `<pre>` rendering or the modal classes are removed."
- suggestedModel: standard
- dependsOn: ["T217","T218"]
- ledgerRefs: ["goals:G26"]
- resultCommit: 2d57f42
- completion: "Added logModalOverlay.test.tsx (5 happy-dom tests): modal opens as overlay, content renders as markdown (h1 not literal '#'), close via x/Escape. Regression guard for the <pre>->Markdown swap."
- sessionLogs: ["docs/logs/20260607-005036-a7e478e781ffee21f.md","docs/logs/20260607-005408-abe6ead4020b67de0.md"]

### T220 — done

- createdAt: 2026-06-07T00:12:57.703Z
- updatedAt: 2026-06-07T01:03:58.257Z
- author: "opus-4.8[1m]"
- session: 059ff637-d28c-4785-8125-9c0d73ddf7a0
- headline: Run bun run check (typecheck + lint + test) green for the whole change
- description: Final gate. From nix/pkg/cq-ledgers/ run `bun run check` (tsc -b + eslint . + bun test) and fix anything the three earlier tasks introduced. Confirm no regressions in existing ledger-web or ledger tests. This is the operational success criterion for G26.
- acceptance: "`bun run check` exits 0 from nix/pkg/cq-ledgers/: typecheck clean, lint clean, full test suite green (including the new T219 popup/markdown test and the updated read_log truncation test)."
- suggestedModel: fast
- dependsOn: ["T219"]
- ledgerRefs: ["goals:G26"]
- resultCommit: b1cfed6
- completion: "G26 final check gate: bun run check green (1018 pass, 1 skip, 0 fail) with all G26 changes integrated. No code change needed."
- sessionLogs: ["docs/logs/20260607-010120-a616e9fbcf78cec18.md"]
