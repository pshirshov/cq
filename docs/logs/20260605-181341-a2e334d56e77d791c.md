# Plan-flow session log

- **Goal:** G15 (milestone M51) — Explorer RW access + pluggable parallel reviewers (cq.toml)
- **Role:** planner (plan-advance)
- **Agent id:** a2e334d56e77d791c
- **Returned status:** awaiting-answers
- **Timestamp (UTC):** 2026-06-05T18:13:41Z

---

### Session summary
- **Did:** Rule 2(a) — fresh `clarifying` goal G15 (milestone M51), not defect-seeded; grounded against the real harness, persisted a grounding summary on G15, and filed the first batch of clarifying questions.
- **Achieved:** Wrote `goals/G15.fields.grounding`; created six `open` questions linked `goals:G15` — Q88 (Feature 1: which RW design; I proposed a third option, a worktree-isolated `investigate-prober`), Q89 (Feature 1: scope — read+execute-in-throwaway-worktree vs persisted edits, network axis), Q90 (Feature 2: invocation mechanism — pi/codex are CLI harnesses shelled out via Bash, not Agent subagents; proposed a spike first), Q91 (Feature 2: reconciliation semantics — strictest-wins + union), Q92 (Feature 2: cq.toml location/schema/off-by-default), Q93 (Feature 2: on-the-fly active-set durability across stateless orchestrator runs). Goal left in `clarifying` (no phase transition).
- **Discovered:** investigate-explorer is hard read-only (`disallowedTools` incl. Bash) and shares the main checkout precisely because the orchestrator re-validates every citation — option (a) breaks that trust model. Reviewers are Claude-native `Agent` subagents returning structured JSON; pi (default `grok-build`) and codex (default `gpt-5.5`) are independent CLI harnesses with NO Agent mechanism (nix/hm/dev-llm.nix), so parallel non-Claude reviewers must be Bash shellouts emitting the same JSON contract. No `cq.toml` and no `/cq:*` namespace exist today. The cq-assets README.md is stale (omits the investigate/* assets that exist). Relevant files: `/home/pavel/work/safe/flakes/cq/nix/pkg/cq-assets/agents/investigate-explorer.md`, `.../commands/investigate/advance.md`, `.../agents/plan-reviewer.md`, `.../agents/implement-reviewer.md`, `.../commands/{plan,implement}/advance.md`, `/home/pavel/work/safe/flakes/cq/nix/hm/dev-llm.nix`.
- **Issues:** Codegraph is not indexed for this repo and no Glob/Grep/Bash tools were available in this runtime, so exploration was via direct Read + ledger search; the questions' design hinges on Q90's spike (can the orchestrator invoke pi/codex non-interactively in this sandbox), which only execution can confirm. None blocking — the user's answers (especially Q90) will settle the plan's task shape.
