# implement-reviewer — T109 (D12 part 1/2) — approve

- Task: T109 (M28, goal G6, defect D12) — round 1
- Role: reviewer (opus)
- Verdict: `approve` (0 criticism / 0 questions / 0 out-of-scope defects)

### Session summary
- **Did:** reviewed task T109 round 1 (diff f67e5ee..462debb in worktree agent-ac4f85711963fb950; orchestrator's stated base 9d6da3e is not this branch's parent — the branch forked from f67e5ee, which is the correct base to diff against).
- **Achieved:** verdict approve; 0 criticisms / 0 questions / 0 out-of-scope defects.
- **Discovered:** Backfill is FS-only at `FsLedgerStore.init()` (line 402, `isMilestones`-guarded, before `initialised=true`); `materialiseFetchedLedger` (InMemoryLedgerStore.ts:607 shallow-copies pointers) and the InMemory adapter are untouched, preserving D10/T106 parity. `fetch()` stays synchronous (FsLedgerStore.ts:431) and returns the mutated pointers. Reproduction is real: reverting the source to f67e5ee makes the backfill test fail with `Received: ""` (empty title) for the right reason; with the fix it passes. Fail-soft confirmed (missing archive → `init()` resolves, title/status stay `""`, no throw; ENOENT and parse errors both caught). Non-overwrite confirmed (`&&` early-skip plus per-field `=== ""` guards; M77 test preserves persisted values). `bun run check` green: tsc clean, eslint clean, 664 pass / 0 fail.
- **Issues:** none that block. Two non-blocking observations, not defects: (a) `backfillLegacyArchivePointers` adds the only path-traversal guard among the archive readers — `collectArchivedItems`/`fetchArchive` read the same files without it — a benign inconsistency that only fail-soft-skips escaping paths; (b) at init each legacy pointer's archive file is read twice (once by backfill, once by FTS indexing), but archives are immutable so this is safe and does not break the existing index read.
