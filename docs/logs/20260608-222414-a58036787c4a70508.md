# Plan candidate log — G34 follow-up #2 (planner, candidate mode: opus[claude])

- Goal G34 (M108), multi-planner round 1, CANDIDATE mode. Emitted a 3-milestone/10-task candidate for follow-up #2 (live-model runtime overlay). Wrote nothing; orchestrator synthesized + persisted.

### Session summary
- **Did:** CANDIDATE mode for G34 ff#2 (live-configured Agents-tab models). Grounded read-only; emitted one candidate task-DAG.
- **Achieved:** 3-milestone/10-task candidate. Decision on Q154: NEW dedicated get_agent_models MCP tool returning a server-resolved finished overlay (per-agent class + per-harness mappings + 4-variant status enum). Folded Q155 (fallback), Q156 (union(planners∪reviewers)), Q157 (4 distinguished states), Q158 (fixed 19-role roster keyed by AgentRole.id, shared constant to prevent drift), Q159 (FakeClient overlay + distinguished-fallback tests + server unit tests + green bun run check + nix build .#ledger-mcp/.#ledger-web).
- **Discovered:** configCapability.ts has the injectable ConfigCapability pattern + projectConfig T268 bridge; @cq/config exposes resolveAgentTier/classifyToken/selectTokensForTier/loadConfig; web LedgerClient has NO config method (must add); gen-agents derives model/modelMappings from cq.toml.example; agentsTab.test.tsx static-only.
- **NOTE (caught in review):** this candidate chose KEEP-build-time-as-fallback for Q155, which the reviewers found CONTRADICTS the answered Q155 (= DROP). Revised in round 2.
