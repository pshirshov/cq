# Implement worker+review log — T294 (M119/G36) — pi-extension effort mirror

- Worker: opus (worktree agent-a31268d282c37d91f r1 → commit f6c2519; agent-abf7fb81342a16c80 r2 → commit 8b1095f). Cherry-picked to main: f6c2519→a4700d3, 8b1095f→6e6e85c. base a0d7e2c (worktree-only resets — D43 honored). bun run check r1 1276/0, r2 1278/0, integrated 1285/0.

### Worker
Mirrored the effort change in the INLINED resolver of the standalone cq-subagent-dispatch.ts (cannot import @cq/config): CqToken.effort + inlined PI_EFFORTS/CLAUDE_EFFORTS/isEffort (keep-in-sync comment citing cq-config/src/types.ts); parseCqToken splits an optional trailing-LAST-':' effort, reserves ':' in the residual model on BOTH harness paths; tokenToChildModel carries effort for pi, returns null for claude (parent fallback, effort recorded inertly via DispatchDetails.childEffort). EMISSION (R342): child --model arg gets a ':<effort>' shorthand suffix on the pi path (e.g. --provider grok-build --model grok-build:xhigh) — NO '--thinking' flag (confirmed against the installed pi CLI: --model supports provider/id + optional :<thinking>; --thinking levels = PI_EFFORTS). Unsupported-effort: LENIENT (bad suffix -> null -> parent fallback), documented as diverging from @cq/config fail-fast. Verified via the in-workspace LOCAL REPLICA in cq-config/test/e2e-and-cross-layer.test.ts (the extension is outside the bun workspace, not importable); the replica cross-checks against the real parseReviewerToken on overlapping grammar. r2 added 2 rejection cases (pi token with claude-only effort 'max'; empty trailing colon).

### Reviews — reconciled go-ahead
- opus r1 (a071feac0e4bcc305) approve: emission is the --model :<effort> shorthand with NO --thinking (verified at the push site); inlined effort sets match @cq/config; reserved-':' both halves; claude inert; lenient policy documented; replica byte-faithful + cross-checked, not vacuous.
- minimax r1: returned verdict 'disapprove' BUT its rationale concluded "Approve" — self-contradictory, no concrete acceptance failure (criticism items self-confirmed correctness; one open question was already answered re: the pi shorthand). Two legitimate optional coverage suggestions ACCEPTED and addressed in r2 (cross-harness wrong-effort + empty-trailing-colon rejection tests). Adjudicated: the disapprove carried no acceptance failure; legitimate subset addressed.
- opus r2 (af6a84c7b72bf0fc7) approve: round-2 delta test-only (+30 lines), both cases trace correctly + faithfully mirror the per-harness isEffort guard; check green 1285/0.
- grok+codex abstained (no API key).
