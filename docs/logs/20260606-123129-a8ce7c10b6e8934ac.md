# Plan-flow session log
- **Goal:** G22 (M70) — planner (plan-advance) bootstrap — review-requested (no clarification needed)

### Session summary
- Grounded all 4 parts read-only; per the user's avoid-obvious-questions directive, filed NO clarifying questions (nothing genuinely ambiguous) and emitted the plan directly: clarifying→planning, 2 work milestones + 5 tasks. G22.fields.milestones=[M71,M72].
- M71 (web UI parts 1-3): T193 sidebar reorder → T194 help fixed size → T195 SVG left-align — serialized (all edit App.tsx+styles.css). M72 (renames part 4): T196 relocate 3 files + self-refs → T197 link-prompts.ts LINKS → T198 repo-wide cross-ref + README sweep. M71/M72 independent (disjoint files).
- Grounding facts:
  - Part 1: sidebar order HARDCODED in two visualLedgers.map blocks in App.tsx (~L785-1099), not from CANONICAL_LEDGERS. 'Q&A' is NOT a ledger — it's the batch-answer trigger button (.lw-batch-open / testid batch-open / openBatch over the questions ledger). ledgerCursor keyboard nav indexes visualLedgers → reorder must rebuild that memo + preserve cursor-index==visual-position.
  - Part 2: .lw-help (styles.css L161-169) has min/max-width but NO fixed height → content-driven jump per tab; .lw-help-statemachines already scrolls (max-height:70vh), shortcuts tab doesn't.
  - Part 3: diagrams are GENERATED (computeStateMachine→computeDagLayout); each <svg width=model.width viewBox> has per-ledger intrinsic width; .lw-statemachine-svg{max-width:100%} + default preserveAspectRatio xMidYMid centers scaled-down wide SVGs → inconsistent. Fix: preserveAspectRatio="xMinYMid meet" + consistent presented width (nodes already x=pad left).
  - Part 4: command keys derived purely from the commands/ dir tree by assets.nix collectMd (commands/cq/advance.md → /cq:advance); dev-llm.nix mergedCommands fan-out is key-driven, so RELOCATING files suffices — NO assets.nix/dev-llm.nix edit needed; commandKeyToStem collision assertion stays satisfied. Ripple: link-prompts.ts LINKS (3), self-refs in the 3 moved files, cross-refs/README across nix/pkg. DECISION (not a question): old names removed outright, no deprecated aliases.
- Issue: planner had no Bash/Grep/codegraph — exhaustive part-4 cross-ref count left to T198 (carries explicit grep commands + word-boundary caveats to avoid corrupting the staying *:advance/*:start names).
- Returned token: review-requested.
