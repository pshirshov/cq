## M69

### T188 — done

- createdAt: 2026-06-06T11:05:51.339Z
- updatedAt: 2026-06-06T11:29:10.867Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: Scaffold packages/cq-cli workspace package + `cq` subcommand dispatcher (init/reset/erase) + shared ResetIo confirmation helper
- suggestedModel: frontier
- ledgerRefs: ["goals:G20"]
- description: |
    Q108. Create a NEW workspace package packages/cq-cli: package.json (name `@cq/cli`, bin `cq` -> src/main.ts, dep on @cq/ledger workspace + @cq/config if needed), tsconfig.json, src/main.ts. Register the package in the root workspace (nix/pkg/cq-ledgers/package.json workspaces / bun.lock) so `bun run check` picks it up.
    
    src/main.ts: an argv dispatcher resolving the ledger root via --cwd > $LEDGER_ROOT > CWD (lift the EXACT precedence helper used by ledger-mcp/ledger-web; consider extracting a tiny shared resolveRoot or just replicate the ~6-line logic), then routing the FIRST positional arg to one of three subcommands: `init`, `reset`, `erase`. Unknown/absent subcommand -> usage to stderr + exit 2. Each subcommand parses its own flags (--cwd, --yes/-y where destructive).
    
    Move the injectable confirmation IO into this package: define ResetIo (rename to the more general ConfirmIo) + ResetOutcome + defaultConfirmIo here (lifted from ledger-mcp/src/main.ts), since BOTH `cq reset` and `cq erase` reuse the policy (--yes proceed / TTY prompt y/N / non-TTY-without-yes REFUSE exit 2, Q109+Q110). Keep it injectable so subcommand tests drive it without a real TTY. This task lands the package + dispatcher + the shared confirmation helper and STUB subcommand handlers (each throwing 'not implemented'); the three subcommands are filled by T189/T190/T191.
- acceptance: "Operational: `bun run packages/cq-cli/src/main.ts` (no subcommand) prints usage to stderr + exits 2; `... badcmd` likewise; the three subcommand names route to their (stub) handlers. Unit test the dispatcher + the ConfirmIo policy (injected non-TTY without --yes => refuse; --yes => proceed; TTY 'y' => proceed, 'n' => abort). `bun run check` green from nix/pkg/cq-ledgers/ (new package compiles + lints)."
- resultCommit: 8f60e59
- completion: "Scaffolded @cq/cli workspace package: cq dispatcher (init/reset/erase; unknown/absent → usage+exit 2), --cwd>$LEDGER_ROOT>CWD root resolution, stub handlers, self-contained ConfirmIo (copied from ledger-mcp; T190 relocates). 14 unit tests; registered in root workspaces+tsconfig. bun run check 945/0. flake.nix untouched (T192)."
- sessionLogs: ["docs/logs/20260606-112328-abb51676b9cd679f3.md","docs/logs/20260606-112328-aaf513d8ae012f677.md"]

### T189 — done

- createdAt: 2026-06-06T11:06:01.719Z
- updatedAt: 2026-06-06T11:45:07.906Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: Implement `cq init` — idempotent create-empty-ledgers-if-none
- suggestedModel: standard
- dependsOn: ["T188"]
- ledgerRefs: ["goals:G20"]
- description: "Q110. Fill the `init` subcommand handler in packages/cq-cli. `cq init` constructs `new FsLedgerStore({ root })` and calls `store.init()` then `store.dispose()`. FsLedgerStore.init() is already IDEMPOTENT: it bootstraps docs/ledgers.yaml + every canonical ledger (milestones seeded with the active group + ambient milestone; others empty) ONLY when files are absent, and is a no-op leaving existing data intact when present. Print a short summary to stdout (e.g. which ledgers were created vs already present, or just 'initialised ledgers at <root>'). `cq init` does NOT write cq.toml (Q111 — config authoring stays the user's). No confirmation needed (non-destructive)."
- acceptance: "Reproduce-first: a test against a tmp dir asserts (a) `cq init` on an EMPTY dir creates docs/ledgers.yaml + the canonical docs/*.md ledger files; (b) running it AGAIN is a no-op that preserves any items written between runs (idempotent); (c) no cq.toml is created. Run via the injectable store/root so the test uses a tmp root. `bun run check` green."
- resultCommit: da1aa82
- completion: "cq init: runInit constructs FsLedgerStore({root}).init()+dispose(), prints via injectable io.out (added out() seam to DispatchIo). Idempotent (no-op-if-present), writes no cq.toml. init.test.ts: empty-dir bootstrap + idempotency (item survives re-init) + no-cq.toml, via injectable tmp root. bun run check green (modulo then-pending T183 RED)."
- sessionLogs: ["docs/logs/20260606-113840-a6ed1ad2c76c67d0a.md","docs/logs/20260606-113840-a69c74e9c52519eb0.md"]

### T190 — done

- createdAt: 2026-06-06T11:06:15.947Z
- updatedAt: 2026-06-06T11:58:16.736Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: "Relocate `reset` CLI wrapper: implement `cq reset`, REMOVE --reset/--yes from ledger-mcp"
- suggestedModel: frontier
- dependsOn: ["T189"]
- ledgerRefs: ["goals:G20"]
- description: |
    Q109. TWO coordinated edits (same-file collisions noted):
    
    1. packages/cq-cli: fill the `reset` subcommand using the relocated runReset logic (now driven by the shared ConfirmIo from T188). It constructs FsLedgerStore({root}), init(), calls store.reset() (the LIBRARY method STAYS in @cq/ledger — do NOT move it), prints backupDir + per-ledger summary, disposes, returns the exit code. Confirmation policy unchanged: --yes proceed / TTY prompt 'Reset ledgers at <root>? Backup -> docs/.backup/ [y/N]' / non-TTY-without-yes REFUSE exit 2. Update the prompt/message prefix from 'ledger-mcp:' to 'cq reset:'.
    
    2. packages/ledger-mcp/src/main.ts: REMOVE the reset CLI surface entirely — drop ParsedArgs.reset/.yes + their parseArgs branches (--reset/--yes/-y), the ResetIo/ResetOutcome interfaces, defaultResetIo, runReset, and the main() --reset short-circuit; update the file header doc (L91-95 region) and the CLI usage comment. Move/adapt any ledger-mcp test that exercised runReset/parseArgs reset into the cq-cli test suite. This is an ACCEPTED breaking CLI change (consumers invoking `ledger-mcp --reset` must switch to `cq reset`).
    
    This task edits BOTH packages/cq-cli (shares files with T189/T191 — hence all three dependOn the T188 scaffold and should be serialized by the implement loop if they collide) and ledger-mcp/main.ts (no overlap with FEATURE 1). Grep the repo (commands/*, flake.nix apps, dev-llm.nix, .mcp.json, README) for any `ledger-mcp --reset` invocation and update it to `cq reset`.
- acceptance: "Reproduce-first: (a) a cq-cli test (injected ConfirmIo, tmp root) asserts `cq reset --yes` backs up to docs/.backup/<ts>/ and reinitialises empty, returning exit 0 + the summary; non-TTY without --yes refuses (exit 2); (b) a ledger-mcp test asserts `--reset` is NO LONGER a recognised flag (parseArgs ignores/does not set it; main no longer short-circuits) — reproduce the regression by confirming the old reset test now lives in cq-cli and passes there; (c) repo grep finds no remaining `ledger-mcp --reset` references. `bun run check` green; `nix build .#ledger-mcp` succeeds."
- resultCommit: 3d96f3c
- completion: "Relocated reset CLI wrapper: cq reset (FsLedgerStore.reset via shared ConfirmIo: --yes/TTY y-N/non-TTY-refuse exit 2; backupDir+summary) — library reset() stays in @cq/ledger. REMOVED ledger-mcp --reset/--yes surface entirely (ParsedArgs/parseArgs/ResetIo/runReset/main short-circuit + docs); reset coverage moved ledger-mcp→cq-cli; parseArgs.test asserts --reset unrecognised. grep clean (no live ledger-mcp --reset). bun run check 976/0. nix build deferred to T192."
- sessionLogs: ["docs/logs/20260606-115311-a1994ea8b82d3c7f3.md","docs/logs/20260606-115311-af80ef40f4ed8d0f9.md"]

### T191 — done

- createdAt: 2026-06-06T11:06:30.441Z
- updatedAt: 2026-06-06T12:10:28.787Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: Implement `cq erase` — destroy EVERYTHING under the ledger root (docs/ tree + cq.toml), no backup, no reinit
- suggestedModel: frontier
- dependsOn: ["T190"]
- ledgerRefs: ["goals:G20"]
- description: |
    Q110 (net-new, MOST destructive). Fill the `erase` subcommand. Per the user's answer ('erase should erase everything including archives and config'), erase DESTROYS, with NO backup and NO reinit:
    - the entire docs/ tree under <root>: docs/*.md (active ledgers), docs/ledgers.yaml, docs/archive/, docs/.backup/, docs/logs/, docs/.locks/;
    - AND the cq.toml config file at <root>/cq.toml (if present).
    Leave nothing re-initialised (do NOT call init() afterward). Implement as an explicit, bounded delete of those known paths under the resolved root (rm -rf docs/ + rm cq.toml) — NOT a blind wipe of the whole root dir (other files in the root, e.g. source, must survive). Dispose/avoid holding the FS lock while deleting (erase removes .locks/ itself). Reuse the shared ConfirmIo policy (T188): --yes proceed / TTY prompt 'ERASE all ledgers + config at <root>? This is IRREVERSIBLE. [y/N]' / non-TTY-without-yes REFUSE exit 2. Print what was removed to stdout.
    
    SAFETY: refuse (exit 2) if <root>/docs does not exist AND no cq.toml (nothing to erase) rather than silently succeeding; make the destructive set explicit and tested so it can never escape the resolved root.
- acceptance: "Reproduce-first: a test against a tmp root with a populated docs/ (active ledgers + archive/ + .backup/ + logs/) AND a cq.toml asserts that `cq erase --yes` removes docs/ ENTIRELY (incl. archive/.backup/logs) AND deletes cq.toml, leaving the root otherwise intact (a sentinel sibling file/dir under root survives); non-TTY without --yes refuses (exit 2) and deletes NOTHING; erase does NOT recreate any ledger. Assert the delete is bounded to <root>/docs + <root>/cq.toml (no path escape). `bun run check` green."
- resultCommit: e597b68
- completion: "cq erase: bounded irreversible delete of <root>/docs (rm -rf) + <root>/cq.toml (unlink) ONLY — no path escape, no whole-root wipe, no reinit, no FsLedgerStore (avoids re-creating docs/). confirmDestructive (--yes/TTY IRREVERSIBLE prompt/non-TTY-refuse exit 2); refuses exit 2 if nothing to erase. 6 reproduce-first fs tests (bounded-delete readdir before/after, sentinel survives, no-reinit, non-TTY-refuse, empty-root-refuse, TTY y/n). bun run check 986/0. nix build deferred to T192."
- sessionLogs: ["docs/logs/20260606-120726-afe5af12d4c17ada1.md","docs/logs/20260606-121013-ab9aaa9ab65ce14a0.md"]

### T192 — done

- createdAt: 2026-06-06T11:06:42.802Z
- updatedAt: 2026-06-06T12:22:12.047Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: Nix-package the `cq` bin (flake.nix derivation + apps.cq + node-modules FOD entry + hash refresh)
- suggestedModel: standard
- dependsOn: ["T185","T187","T188","T191"]
- ledgerRefs: ["goals:G20"]
- description: |
    Q108 packaging. In repo-root flake.nix add a `cqCli` derivation modelled on `ledgerMcp` (the template): cp packages/ledger + packages/cq-cli into $WORKSPACE, strip their node_modules, symlink @cq/ledger (+ its runtime deps from the FOD) + @cq/config under packages/cq-cli/node_modules/@cq, makeWrapper $out/bin/cq -> `bun run .../cq-cli/src/main.ts --` with PATH prefix [bun nodejs_22]. Expose packages.cq = cqCli and apps.cq.
    
    Update the node-modules FOD (bunNodeModules): add packages/cq-cli/package.json to the manifest fileset (around L56-62) and the cq-cli node_modules copy in installPhase (around L99-107, mkdir + cp -r packages/cq-cli/node_modules). 
    
    FOD HASH REFRESH — SINGLE consolidated point for this goal (R215 crit 2). The node-modules FOD output hash changes from THREE bun.lock edits in this plan: (i) smol-toml added to packages/cq-config (T185), (ii) the new @cq/cli workspace package (T188), and (iii) the @cq/config workspace edge added to ledger-web (T187). This task is the ONLY place the hash is refreshed, so it MUST run AFTER all three land — hence dependsOn now includes T185 (smol-toml), T187 (ledger-web @cq/config edge + flake.nix ledgerWeb symlink), T188 (cq-cli package scaffold) and T191 (the tail of the serialized cq-cli/main.ts chain T189->T190->T191, which transitively covers T188/T189/T190). Procedure: set outputHash to 52 'A's, run `nix build .#node-modules`, paste the reported got: hash back into flake.nix. Refreshing before T185/T187/T188 land would yield a hash omitting their deps and break `nix build .#ledger-mcp` / `.#ledger-web`.
    
    FLAKE.NIX COLLISION AVOIDANCE (R215 crit 3): T187 also edits flake.nix (the ledgerWeb derivation @cq/config symlink). This task's flake.nix edits are scoped to the cqCli derivation + bunNodeModules FOD + apps.cq. dependsOn:[T187] serializes the two flake.nix writers so they do not collide on merge-back.
    
    NOTE the smol-toml dep added in T185 (FEATURE 1) shares this single FOD refresh; embedServerClosure is unaffected (cq is standalone, not embedded).
- acceptance: "Operational: `nix build .#cq` builds and `result/bin/cq init --cwd <tmp>` / `cq reset --yes` / `cq erase --yes` run end-to-end against a tmp root (the wrapped bun bin resolves @cq/ledger + @cq/config from the staged node_modules). `nix build .#node-modules` succeeds with the refreshed hash (no hash mismatch). `nix build .#ledger-mcp` + `.#ledger-web` still build (FOD shared). `bun run check` green."
- resultCommit: bdd2720
- completion: "PART-2 closing gate: added cqCli flake.nix derivation + packages.cq + apps.cq + node-modules FOD manifest/installPhase entry; refreshed consolidated FOD outputHash (Ymy9b5It+… → R/phwlOIrTVkohhdyNi+YvtkTOaOENTrnP/x9T4SQjc=). nix build .#node-modules/.#cq/.#ledger-mcp/.#ledger-tui/.#ledger-web ALL exit 0; built cq bin runs init/reset/erase end-to-end against a tmp root (resolves @cq/ledger from staged node_modules, no module error). bun run check 986/0. (Reviewer re-ran all builds + e2e + check.) Non-blocking note: the @cq/config symlink under cq-cli is unused dead weight (cq-cli has no @cq/config dep) — harmless."
- sessionLogs: ["docs/logs/20260606-121721-a7c6a3696cea17492.md","docs/logs/20260606-121721-a01d3264adc34b351.md"]
