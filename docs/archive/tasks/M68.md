## M68

### T185 — done

- createdAt: 2026-06-06T11:05:02.533Z
- updatedAt: 2026-06-06T11:29:10.149Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: "Replace hand-rolled @cq/config TOML parser with the smol-toml library; add typed [webui] table"
- suggestedModel: frontier
- ledgerRefs: ["goals:G20"]
- description: |
    Q105: the user rejected the hand-rolled parser ('use a library'). Replace packages/cq-config/src/toml.ts parseToml internals with `smol-toml` (maintained pure-TS TOML 1.0 parser, zero native deps, Bun-compatible). Add `smol-toml` to packages/cq-config/package.json deps; refresh bun.lock.
    
    KEEP the SAME public surface + fail-fast boundary: parseToml(source) still returns RawToml, still throws a precise error (wrap smol-toml's TomlError into the existing TomlSyntaxError/CqConfigError style) on malformed input AND on UNEXPECTED top-level keys/tables (preserve the whitelist semantics: only [aliases], reviewers, planners, and the NEW [webui] are allowed; anything else throws — smol-toml itself accepts arbitrary TOML, so the whitelist/rejection must be re-implemented as a post-parse validation pass over the library's output object).
    
    Extend the typed model for [webui] (host string + port integer ONLY, Q105): add `webui: { host: string | null; port: number | null } | null` (or equivalent) to RawToml (toml.ts), CqConfig (types.ts), and populate it in parseConfig (config.ts). Validate at the boundary: host must be a string, port must be an INTEGER in 1..65535 (smol-toml yields a real JS number) — throw CqConfigError otherwise. Update the barrel index.ts only if new types are exported.
    
    COORDINATION (Q111): this edits the SAME files G18 PART 2 touches (toml.ts/config.ts/types.ts) and is a wholesale rewrite of toml.ts — milestone M68 dependsOn G18's M61/M62; implement only AFTER G18 has merged, then layer [webui] + the library swap on top of the planners-aware parser.
- acceptance: "Reproduce-first: add unit tests in packages/cq-config that (a) parse a cq.toml with `[webui]\\nhost = \"0.0.0.0\"\\nport = 5180` and assert webui={host:'0.0.0.0',port:5180} with port a number; (b) assert an unknown top-level table/key still THROWS; (c) assert port='5180' (string) or port=5180.5 (non-integer) or out-of-range THROWS CqConfigError; (d) existing [aliases]/reviewers/planners tests still pass unchanged. `bun run check` (tsc -b + eslint + bun test) green from nix/pkg/cq-ledgers/."
- resultCommit: 96b7031
- completion: "Replaced @cq/config hand-rolled TOML parser with smol-toml 1.6.1 (pure-TS, no native deps); whitelist re-implemented post-parse (aliases/reviewers/planners/webui else throws); typed [webui]={host,port} with integer-port 1..65535 validation → CqConfigError; G18 planners/aliases tests preserved. bun run check 943/0. FOD refresh deferred to T192."
- sessionLogs: ["docs/logs/20260606-112328-ac762f258887b0791.md","docs/logs/20260606-112328-a52e3f57146ac8caf.md"]

### T186 — done

- createdAt: 2026-06-06T11:05:19.377Z
- updatedAt: 2026-06-06T11:45:07.775Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: "ledger-web: per-field host/port resolution (CLI > cq.toml [webui] > default) + bounded port auto-increment scan"
- suggestedModel: frontier
- dependsOn: ["T185"]
- ledgerRefs: ["goals:G20"]
- description: |
    Q106 + Q107. In packages/ledger-web/src/serve.ts add two pure, unit-testable pieces (kept separate from the Bun.serve bind so they can be tested without opening sockets):
    
    1. PER-FIELD RESOLUTION (Q106): a helper resolveWebOpts(args, config) that resolves host and port INDEPENDENTLY with precedence CLI flag > cq.toml [webui] value > built-in default (DEFAULT_HOST 127.0.0.1, DEFAULT_PORT 5180). parseArgs must distinguish an EXPLICITLY-passed --host/--port from the default (e.g. return host/port as `string|null`/`number|null` = 'unset', or a `hostExplicit`/`portExplicit` flag) so the resolver can tell 'user set it' from 'fell through to default'. config is the loaded CqConfig.webui (null when no cq.toml / no [webui]).
    
    2. BOUNDED PORT SCAN (Q107): a helper that, given a desired host+port, tries to bind and on EADDRINUSE increments the port by 1, up to a cap of MAX_PORT_SCAN = 64 attempts, returning the actual bound port (or throwing a precise error when the cap is exhausted). ALWAYS-ON — applies even when --port was explicit (user: 'Explicit --port should increment too'). host is taken as-is (never incremented). Implementation note: Bun.serve throws synchronously on EADDRINUSE; the scan wraps the serve call (serveEmbedded/serveProxy both bind opts.port) in a retry loop, or probes a port for availability first then binds. Catch ONLY the address-in-use error; rethrow anything else.
    
    Do NOT wire loadConfig / stdout reporting here (that is the next task) — keep this task to the two pure helpers + their integration points' signatures.
- acceptance: "Reproduce-first unit tests (happy-dom not needed; pure logic): (a) resolveWebOpts precedence matrix — CLI-only, config-only, default-only, CLI-overrides-config, per-field mix (config port + default host) — each asserts the expected host/port; (b) port-scan: bind a throwaway Bun.serve on port P, then assert the scan starting at P returns P+1 (or first free), and that exhausting MAX_PORT_SCAN occupied ports throws the precise cap error; (c) host is never mutated by the scan. `bun run check` green."
- resultCommit: f71f9b9
- completion: "ledger-web serve.ts: resolveWebOpts (per-field CLI-explicit > cq.toml [webui] > default, null-as-unset) + scanForPort (bounded MAX_PORT_SCAN=64, EADDRINUSE-only, host-immutable, precise cap error); parseArgs tracks hostExplicit/portExplicit. Added @cq/config as a ledger-web dep (package.json+tsconfig). 21 reproduce-first tests. loadConfig/stdout wiring deferred to T187. bun run check green (modulo the then-pending T183 RED, now resolved)."
- sessionLogs: ["docs/logs/20260606-113840-a413e9423d424805f.md","docs/logs/20260606-113840-a13bf5fdbd0dfd94c.md"]

### T187 — done

- createdAt: 2026-06-06T11:05:33.772Z
- updatedAt: 2026-06-06T11:58:16.672Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: "ledger-web main(): load cq.toml, apply [webui] resolution + port scan, report actual bound URL to STDOUT"
- suggestedModel: standard
- dependsOn: ["T186"]
- ledgerRefs: ["goals:G20"]
- description: |
    Q106 + Q107 final wiring. In packages/ledger-web/src/serve.ts main()/parseArgs:
    
    1. Call loadConfig(resolvedCwd) (@cq/config) — the cq.toml root IS the resolved ledger root (--cwd > $LEDGER_ROOT > CWD), matching G18 Q99. loadConfig returns null when absent; treat webui as null then. Note loadConfig eagerly resolves reviewers/planners and may throw on a dangling alias — that is existing behaviour; surface it as a fatal startup error (consistent with the existing catch in the `import.meta.main` block).
    
    2. Use resolveWebOpts (T186) to compute effective host/port from CLI args + config.webui + defaults, then run the bounded port-scan (T186) so serve() binds the first free port from the resolved port.
    
    3. REPORT to STDOUT (Q107): after the server is bound, print the ACTUAL URL `http://<host>:<actualPort>/` to process.stdout (this is the new machine-readable line scripts capture). KEEP the existing human-readable `ledger-web: serving http://host:port/ → backend` line on STDERR (now using the actual bound port). Both embedded and proxy modes report the same way.
    
    Thread the resolved port through serve()/serveEmbedded/serveProxy so the value printed is the genuinely-bound port (use Bun server.port as today). cq.toml is read for [webui] only; reviewers/planners are untouched here.
    
    4. WIRE THE @cq/config DEPENDENCY (R215 crit 3 — currently MISSING). ledger-web does not yet depend on @cq/config, so loadConfig will not resolve under `bun run check` or `nix build .#ledger-web`. Add it BOTH ways:
      (a) DECLARE the dependency: add `"@cq/config": "workspace:*"` to packages/ledger-web/package.json `dependencies` (mirror how packages/ledger-mcp/package.json declares it). Refresh bun.lock for the new workspace edge (the FOD hash refresh itself is consolidated in T192 — do not paste a new hash here).
      (b) NIX SYMLINK: in repo-root flake.nix, the ledgerWeb derivation installPhase (≈L341-358) currently symlinks only @cq/ledger-mcp, @cq/ledger and @cq/ledger-live into packages/ledger-web/node_modules; the shared embedServerClosure stages cq-config SOURCE but symlinks @cq/config ONLY under ledger-mcp (≈L157-158). ADD an `@cq/config` workspace symlink into packages/ledger-web/node_modules/@cq/config in the ledgerWeb installPhase, mirroring the ledger-mcp wiring (≈L221-225), so `nix build .#ledger-web` resolves @cq/config from serve.ts at runtime.
    
    FLAKE.NIX COLLISION NOTE (R215 crit 3): this task's flake.nix edit is SCOPED to the ledgerWeb derivation only; T192's flake.nix edits are scoped to the cq/cqCli derivation + the bunNodeModules FOD (hash) + apps.cq. To avoid a same-file (flake.nix) merge collision, T192 dependsOn T187 (serialized) — see T192.
- acceptance: |
    Operational: (1) with no cq.toml and no flags, ledger-web binds 127.0.0.1:5180 and prints `http://127.0.0.1:5180/` to STDOUT + the serving line to STDERR; (2) with cq.toml `[webui] port = 5300` and no --port, binds 5300; (3) with --port 5300 AND cq.toml port=5180, binds 5300 (CLI wins); (4) when 5180 is occupied, binds 5181 and the STDOUT line shows 5181. Add a test (happy-dom web harness or a spawned-process/integration test) capturing stdout vs stderr separately and asserting the actual port.
    
    DEPENDENCY-WIRING acceptance (R215 crit 3): (5) packages/ledger-web/package.json `dependencies` includes `@cq/config: workspace:*`; (6) `bun run check` resolves loadConfig from @cq/config in serve.ts (no unresolved-module error); (7) `nix build .#ledger-web` SUCCEEDS and the built `result` has packages/ledger-web/node_modules/@cq/config symlinked (the ledgerWeb derivation links it, mirroring ledger-mcp) — verify the bound binary can loadConfig at runtime (e.g. start it against a tmp root containing a cq.toml [webui] and assert it honours the configured port). `bun run check` green; `nix build .#ledger-web` succeeds.
- resultCommit: 0c21f43
- completion: "ledger-web main() wires loadConfig(cwd) + resolveWebOpts (CLI>cq.toml [webui]>default per-field) + scanForPort (both embedded+proxy binds) so the ACTUAL bound port is reported as http://host:port/ to STDOUT (human serving line kept on STDERR); dangling-alias loadConfig throw is fatal. @cq/config symlink added to ledgerWeb installPhase in flake.nix (scoped to that derivation). 4 mainWiring tests. bun run check 982/0. nix build deferred to T192."
- sessionLogs: ["docs/logs/20260606-115311-a24c8f0fe9af23247.md","docs/logs/20260606-115311-a6149bb72c0b97597.md"]
