# Session log — T352 implement-worker (opus)

- **Task:** T352 (M145, G43); status pass; resultCommit a289b4c; bun run check green (1537/0)

### Session summary
Added `GitObjectLedgerBackend` extending `AbstractLedgerStore<GitPersistence>` plus `GitPersistence implements LedgerPersistence` over an orphan ref (refs/heads/cq-ledger, configurable) via GitPlumbing. Docs-relative tree paths (ledgers.yaml, <ledger>.md, archive/<ledger>/<id>.md — no docs/ prefix; leading ./ normalized). Each mutation is one atomic read-old-sha → rebuild-tree(all current files, one path replaced/added/removed) → commit-tree(orphan on first, child after) → CAS updateRef(ref,new,expectedOld) advance; StaleRefError propagates (lost-update surfaces loudly). init() seeds the ref from the empty tree when absent; reads stay SYNC from the in-memory map (cat-file only at init/coherence-reload). locksRoot() stays on the real FS (lockfiles never in the tree). backupCanonicalState tags refs/tags/cq-ledger-backup-<ts> at the ref head BEFORE reinit (Q195(1)). NO ~/.cache mirror (Q195(2)). DISCOVERED: GitPlumbing had only name-only lsTree — added lsTreeEntries(): TreeEntry[] (mode+sha+path) for tree reassembly; git tag takes a SHORT name (tagRef prepends refs/tags/). Test (gitObjectLedgerBackend.test.ts, +41) runs the shared abstract store suite against the git backend AND proves host checkout HEAD/working-tree/index byte-identical + status clean after create/update/archive, +1 commit per mutation, no .locks in `git ls-tree -r cq-ledger`, and a backup tag before divergence reinit. Based on main 32f108b.
