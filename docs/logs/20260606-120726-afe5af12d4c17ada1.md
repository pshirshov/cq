# Implement-flow session log
- **Task:** T191 (M69, G20) — worker, round 0 — pass (resultCommit e597b68)

### Session summary
- Filled runErase in cq-cli/src/main.ts: resolve root, bounded delete set [<root>/docs (rm -rf), <root>/cq.toml (unlink if present)], confirm via shared confirmDestructive (IRREVERSIBLE prompt / --yes / non-TTY-refuse exit 2), delete only those paths via node:fs, print removed paths, NO init() afterward (avoids re-creating docs/ + holding the lock it deletes). Refuses exit 2 when neither docs/ nor cq.toml exists. @cq/cli does not import @cq/config — cq.toml filename is a local constant (avoids package.json/bun.lock/FOD churn).
- resultCommit e597b688 (implement/T191). bun run check green: tsc+eslint clean, 986 pass / 0 fail. Reproduce-first erase.test.ts (6 tests, failed against the throwing stub): bounded-delete (post-erase readdir(root) == pre minus docs/+cq.toml — no path escape), sentinel-survives (SOURCE_KEEP_ME + src/index.ts survive while docs/{archive,.backup,logs,ledgers.yaml}+cq.toml gone), no-reinit, non-TTY-refuse (exit 2 deletes nothing), empty-root-refuse (exit 2), TTY y/n. Updated dispatch.test.ts stale routing assertion. nix build deferred to T192.
- Files: cq-cli/src/main.ts, test/erase.test.ts, test/dispatch.test.ts.
