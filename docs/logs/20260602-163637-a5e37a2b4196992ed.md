# plan-advance (planner) — goal G4 (D2 fix) — review-requested (revision per R75)

Agent a5e37a2b4196992ed. Rule 3: consumed revise R75, revised M22 tasks; phase stays planning.

### Session summary
- Did: revised D2 fix plan (M22) per R75's 2 criticisms (empty new_questions); no phase change.
- Achieved: T96 — added MANDATORY migration of existing parameterized suite canonical-ledgers.test.ts:332-356 (6 "divergence guard fires for a hand-edited <name> schema" cases re-targeted to construct with onSchemaDivergence:'abort', keeping rejects /different schema/ coverage; acceptance forbids any test asserting a DEFAULT-init throw on divergence). T95 — pinned re-load: T94's helper authoritatively mutates this.registry in place to canonical so the load loop at FsLedgerStore.ts:294-334 iterates the fresh registry; acceptance asserts post-reinit this.registry.ledgers schemas equal CANONICAL_LEDGERS and loaded ledger is fresh-canonical, not divergent prior. G4 stays planning.
- Discovered: suite at L332-356 seeds only docs/ledgers.yaml (no on-disk .md ledger file), default new FsLedgerStore({root:dir}) — corroborates both the migration need and T94's ENOENT-tolerance role; noted in T96.
- Issues: R75 defects bucket empty — no file-and-defer.
- Status token: review-requested.
