# Investigate-explorer session log

- **Defect:** D29 — empty/whitespace answer accepted on `answered` transition
- **Hypothesis:** H20 (frontend: TUI + web answer-submission paths permit empty submission)
- **Role:** explorer (read-only)
- **Returned lean:** supports

---

### Session summary
- **Did:** gathered evidence for hypothesis H20 (TUI + web answer-submission paths permit empty/whitespace submission) by reading the web `App.tsx` (detail panel answer box, BatchAnswerModal, per-suggestion pick/as-recommended), the TUI `app.tsx` (answer overlay, BatchAnswerOverlay, applyAnswer/applyBatchAnswer), `TextPrompt.tsx`, and the MCP `mcpClient.ts` update_item path.
- **Achieved:** 9 numbered evidence items; lean supports.
- **Discovered:** Every "save & mark answered" submit path is UNGUARDED against empty/whitespace. Web detail `submitAnswer` (App.tsx:2611) and its HoldButton (App.tsx:2629) have no `disabled`/non-empty check; web BatchAnswerModal `batch-answer-submit` (App.tsx:1732-1738) likewise unguarded; TUI `answer` overlay routes through `TextPrompt` whose Enter (TextPrompt.tsx:32-34) calls `onSubmit(value)` unconditionally; TUI `BatchAnswerOverlay` Enter (app.tsx:1952-1953) calls `onAnswer(row, value)` with `value` defaulted to the (often empty) persisted answer; both `applyAnswer` (app.tsx:549-566) and web `answerWith`/`batchSave` (App.tsx:543-551) forward the answer verbatim into `updateItem(status: answered, fields.answer)`; the client `updateItem` (mcpClient.ts:204-211) forwards `patch.fields` unmodified so an empty-string answer reaches `update_item`. The only client-side guards present (`answerHasText`) gate clobbering a draft, NOT empty submission.
- **Issues:** Could NOT complete item 4 (existing tests for empty-answer UI behavior): Glob/Grep/Bash unavailable in that runtime; recommend `rg -l answer .../packages/ledger-{web,tui}/test`.

Validated by orchestrator: citations #1 (App.tsx:2604-2611) and #2 (App.tsx:2628-2631) re-opened against source and confirmed exact (line 2626 `setAnswerHasText` confirms the draft-guard is for the 'as recommended' button, not empty-submission).
