# Session log — D56/H35 investigate-explorer (opus), round 1

- **Defect:** D56; **Hypothesis:** H35; role: explorer; lean **supports** (5 evidence items, no probeRequest).

### Session summary
- **Did:** gathered evidence for H35 (parseArgs has no --help/-h case and main() has no help-printing branch, so `ledger-mcp --help` falls through to launching the stdio MCP server; the only usage surface is a file-header doc-comment).
- **Achieved:** 5 numbered evidence items; lean supports.
- **Discovered:** parseArgs's arg-loop (main.ts:155-185) handles only --cwd/--http/--tool-prefix (+ = forms) and silently ignores any unrecognized flag — no --help/-h/help branch, no terminal else/throw. main() (L639-647) dispatches only the `restore` subcommand positionally; anything else (incl. --help) falls to the default path → createLedgerMcpServer + StdioServerTransport + server.connect. The only top-level usage text is the file-header JSDoc (L16-31), a comment never written to stdout. RESTORE_USAGE (L204-209) is a runtime string but scoped to the `restore` subcommand only — confirming the top-level gap.
- **Issues:** none. Orchestrator-validated citations 1 (parseArgs loop) + 2 (main dispatch) directly against source — both [correct], load-bearing, confirm H35.
