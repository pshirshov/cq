# plan candidate (pi:grok-build, alias grok) — G30

- goal: G30; role: candidate planner (pi shellout); 6 milestones
- alias: grok (pi: grok-build)

### Captured (summary)
6 milestones (Schema / Live ledger migration / Status rendering / Prompt threading / Tests and invariants / Verification). Notable: added an explicit "Locate the live handoffs ledger markdown file with embedded schema + HO records" discovery task before editing (defensive — folded into synthesized migration task). Split prompt threading: frontier advance.md (narrow-pinning + anti-laundering) + lighter per-flow table rows. Tests decomposed to the 3 acceptance gates + live-HO-survival check. Verify: bun run check + nix build .#llm-contexts/.#llm-context-with-env/.#llm-skills.
