# Session log — T369 + T370 implement (sonnet workers + opus reviews, both approve)

## T369 (M154) — register Stop hook in claude.nix; resultCommit 7b04682 → main; nix eval ok; review APPROVE (opus a1d2f7266be8af88f)
WORKER: added settings.hooks.Stop entry in nix/hm/claude.nix (matcher "*", {type="command"; command="${claudeStopGateHook}"}) mirroring SessionStart; flipped the T364 binding comment from "NOT yet wired" to "Registered (T369)". nix eval .#homeManagerModules.dev-llm → ok; parse ok.
REVIEW (opus approve): Stop entry well-formed + references claudeStopGateHook, exactly mirrors SessionStart structure + Claude Code settings.hooks schema; module evaluates; surgical (SessionStart untouched).

## T370 (M154) — advance.md marker/external-signal lifecycle; resultCommit 296d069 → 2011ba0 (main); check 1622/0; review APPROVE (opus ab1da6a1f1333c122)
WORKER: wired into advance.md — (1) §Bootstrap drops `touch "${XDG_RUNTIME_DIR:-/tmp}/cq-advance-active-$CLAUDE_CODE_SESSION_ID"` as the FIRST run-start action (before derive_predicates); (2) §The one write/§End-of-run `rm -f` the same marker ATOMICALLY coupled to the terminal handoffs create_item; (3) §Stop-condition gate cross-references the claudeStopGateHook mechanical enforcement + the `external-signal: "<verbatim harness quote>"` escape; D41 prose retained + one enforcement sentence; sub-commands untouched; regen agentsCatalogue.gen.ts.
REVIEW (opus approve): CRITICAL cross-task consistency verified — marker path in advance.md == advanceGate.ts (T362) BYTE-FOR-BYTE; external-signal format matches the gate's hasExternalSignal parser; unlink coupled to handoff create_item; D41 prose retained; gen.ts regenerates zero-diff; check 1622/0.
