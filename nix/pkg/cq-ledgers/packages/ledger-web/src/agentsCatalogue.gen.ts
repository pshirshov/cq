/**
 * GENERATED catalogue — PLACEHOLDER (T275 approach (a)).
 *
 * This file is committed as an empty placeholder so `agentsCatalogue.ts` can
 * re-export `AGENT_ROLES` and `bun run check` stays GREEN before the codegen
 * script exists. T276 OVERWRITES this file with the real generated array (walks
 * the `cq-assets` agent/command markdown, runs `parseAgentMarkdown`, derives each
 * `AgentRole`). Do NOT hand-author roles here — author the `## Catalogue` blocks
 * in the assets (T281) and regenerate.
 */

import type { AgentRole } from "./agentsCatalogue.js";

export const AGENT_ROLES: AgentRole[] = [];
