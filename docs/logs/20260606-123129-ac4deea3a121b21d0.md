# Plan-flow session log
- **Goal:** G22 (M70) — reviewer (plan-reviewer) round 0 — revise (R227)

### Session summary
- Reviewed G22 plan (M71 T193/T194/T195 web UI; M72 T196/T197/T198 renames). Verdict revise (R227); 1 criticism / 0 new questions / 0 defects.
- Parts 1-3 well-grounded + faithful: two visualLedgers.map blocks, .lw-batch-open/batch-open Q&A button (correctly = batch trigger, not a ledger), sidebar-divider, ledgerCursor indexing visualLedgers all match App.tsx L785-1099; requested group order reproduced with cursor-index==visual-position; .lw-help no fixed height (T194 correct); SVG width={model.width}+default preserveAspectRatio+max-width:100% diagnosis + xMinYMid-meet fix sound (nodes x=opts.pad). Part-4 nix grounding correct (collectMd path-driven; no assets.nix/dev-llm.nix edit; commandKeyToStem collision stays satisfied; word-boundary /advance vs /plan:advance handled).
- SOLE criticism (planner-fixable): cross-refs to OTHER renamed commands INSIDE the 3 relocated files are owned by neither T196 (own-name-only) nor T198 (excludes relocated files) → would survive dangling AND make T198's own `grep -rn '/investigate:start' nix/pkg/` acceptance unsatisfiable. Concrete: commands/plan/start.md (→cq/plan.md) references /investigate:start at frontmatter L2 + body L29/L35/L37. (investigate/start.md + advance.md unaffected — own-name or staying-names only.) Fix: assign cross-refs-to-other-renamed-commands within the relocated files to T196 (it already edits them), OR broaden T198's sweep to include them. Reconcile so every renamed-from ref is covered exactly once.
