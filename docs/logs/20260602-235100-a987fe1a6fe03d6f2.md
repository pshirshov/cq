# implement-worker — T114 (D18) — pass

- Task: T114 (M30, goal G7, defect D18) — batch modal per-suggestion pick buttons
- Returned: `pass` — resultCommit ae0e5f86e4a4d366a276d612cc98ca3723444def (landed on main, parent 56a3ded incl. T113)

### Session summary
- **Did:** implemented T114 (reset worktree to main first so it builds on T113), committed to main.
- **Achieved:** pass; resultCommit ae0e5f8; `bun run check` 690 pass, 0 fail.
- **Discovered:** the `suggestion` var inside the IIFE is typed string after the `Array.isArray(sv) ? sv : [sv]` split. `batchSave` passes the answer string directly to updateItem with ANSWERED_STATUS, so passing a suggestion string is the exact same code path as 'as recommended'.
- **Issues:** none.
- filesTouched: packages/ledger-web/src/App.tsx, packages/ledger-web/test/batchPickSuggestion.test.tsx
- summary: Replaced the plain <dd> suggestions in BatchAnswerModal with a <ul>, one <li>+pick button per suggestion entry (data-testid batch-pick-suggestion-{i}) calling onSave(row, suggestion); disabled by answerHasText. New test covers count/click-to-save/disabled-when-text for suggestions-only and suggestions+recommendation cases.
