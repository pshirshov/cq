# implement-worker — T316 (opus)

- Task T316; agent aae2834dc1f5e81ec; status **pass**; resultCommit eff7a63; check green 1316/0.
Flows tab now renders ROLE_FLOWS (roles + labelled action edges) via the existing DiagramSvg/layoutDiagram per flow's `model`, replacing the flowData state machines; testids help-tab-flows/help-flow-<id> stable; dropped unused flowData/FlowDefinition/FlowNodeKind/flowToModel/FLOW_KIND_FILL/BUCKET_HEX imports from App.tsx. flowData.ts retained (flowData.test.ts imports it). flowsTab.test.tsx rewritten to assert role + action-edge labels per flow + absence of old state-node labels. **Discovered + fixed a latent DiagramSvg multigraph defect:** ROLE_FLOWS has parallel edges between the same role pair; DiagramSvg keyed edges by `${from}->${to}` → React key collision; changed to `edge-${index}` (testid scheme `${idPrefix}-edge-${from}-${to}` preserved). Noted parallel edges now yield a duplicate testid (benign; left per the documented contract).
