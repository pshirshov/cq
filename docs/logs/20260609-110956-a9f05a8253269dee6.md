# Plan bootstrap log — G38 (role: planner, plan-advance; status: awaiting-answers)

- Goal G38 (M126), fresh bootstrap clarifying round. Filed Q167-Q173 (7 questions), grounding summary persisted on G38. Goal stays `clarifying`. agent a9f05a8253269dee6.

### Session summary
- **Did:** Ran the DEFAULT plan-flow state-machine step for G38 (clarifying, no prior questions → rule 2(a)): grounded read-only across all four work items, persisted a grounding summary on G38, then filed the first batch of clarifying questions.
- **Achieved:** Wrote `goals:G38.fields.grounding` (per-item repo findings + file loci); created seven open `questions` under M126 linked `goals:G38` — Q167 (1a worktree-removal ownership/locked-worktree), Q168 (1b backup granularity/format + write trigger), Q169 (1b path-scheme/rotation/restore-CLI), Q170 (2 flows-tab source-of-truth + visual form + replace-vs-add), Q171 (2 web-only-vs-TUI scope), Q172 (3 defect-routing), Q173 (3 exact key-map + the intentional-affordance reversal + Home/End feasibility). Goal stays `clarifying`.
- **Discovered:** (1a) Claude native isolation:worktree only auto-cleans UNCHANGED worktrees, so committed task worktrees accumulate — the gap is in implement/advance.md §7.3 (prompt-only fix). (1b) The existing docs/.backup is IN-REPO so a git reset wipes it too (the D43 failure mode), confirming the need for an out-of-repo ~/.cache mirror; the post-lock onMutation hook is the natural async trigger. (2) The Flows tab already exists (G23) rendering abstract state diagrams via a hand-authored TS module + elk renderer; Q145 already scoped help-dialog work web-only. (3) The requested keybinding change REVERSES an intentional no-Enter detail-scroll affordance, ink exposes no key.home/key.end (must match raw escape sequences), and there is no Home/End handling today.
- **Issues:** None blocking. Codegraph is not initialized for this repo (fell back to Read + ledger fts_search); Glob/Grep/Bash were unavailable in this runtime, so directory exploration was done via known file paths + ledger search. Item 3's defect-routing (Q172) and behavior-reversal (Q173) need user confirmation before the fix can be planned.
