# Implement worker+review log — T291 & T292

## T291 (M118/G34) — FakeClient getAgentModels (worker: sonnet, worktree agent-a000bf282d7ff8ba8)
- commit 8cd4045 (cherry-picked to main 60b16ca; base a0d7e2c, worktree-only reset — D43 honored). bun run check 1272/0.
- Replaced the T289 minimal stub with a configurable getAgentModels exposing all 5 modes (resolved / no-live-token / not-configured / not-model-configurable / throw). Default = not-configured (preserves T289 behavior → existing tests unaffected). 'resolved' synthetic entries DIFFER from the build-time catalogue for implement-worker (claude/sonnet-4.6 vs pi/grok-build) + plan-reviewer (opus-4.8[2m] vs opus-4.8[1m]) so T297 can prove overlay precedence. filesTouched: ledger-web/test/fakeClient.ts.
- Reviews: opus (a... ab00014d179c5f622) approve + minimax approve; grok+codex abstained. All 4 states + throw present; default preserves behavior; resolved differs from catalogue; agentsTab.test.tsx untouched. Reconciled go-ahead.

## T292 (M119/G36) — effort on get_planners/get_reviewers/get_config wire shapes (worker: sonnet, worktree agent-aa5e363abb11ef219)
- commit b0416f8 (cherry-picked to main e3b6a37; base a0d7e2c, worktree-only reset). bun run check 1280/0.
- Added optional effort?: string|null to ResolvedReviewer/ResolvedPlanner + GetConfigResult aliases entry + all three tiers slots (config-agnostic widening, NOT importing @cq/config Effort union); threaded token.effort ?? null through computeReviewers/computePlanners/projectConfig. All-effortless output byte-identical except the new optional key. New tests cover xhigh + null for all three tools. filesTouched: ledger/src/mcp/configCapability.ts, ledger-mcp/src/configCapability.ts, ledger-mcp/test/{configCapability,main}.test.ts.
- Reviews: opus (a5205942d4ec05bc9) approve + minimax approve; grok+codex abstained. Reconciled go-ahead.
