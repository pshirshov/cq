# Plan-flow session log

- **Goal:** G29 — Add provider-qualified token support to the cq config grammar (resolves D36)
- **Milestone:** M92
- **Role:** planner (plan-advance)
- **Returned status:** awaiting-answers
- **Agent id:** afd464efe85dce401

## Session summary
- **Did:** Grounded read-only in @cq/config (`toml.ts`/`config.ts`/`types.ts`), the `cq-subagent-dispatch.ts` extension resolver, and D36/G29, then filed the first batch of clarifying questions for fresh goal G29.
- **Achieved:** Created `questions` Q132–Q136 under M92, each `ledgerRefs:["goals:G29"]`, with grounded context/suggestions/recommendations. G29 remains `clarifying` (gated on five open questions). No goal-phase transition performed.
- **Discovered:** The user's "colon collides with the harness split" premise is false — both parsers split on the FIRST colon only, so `pi:<provider>:<model>` already parses unambiguously to `{harness:'pi', model:'<provider>:<model>'}`. The real reconciliation is the IN-MODEL provider separator: `tokenToChildModel` (cq-subagent-dispatch.ts L320-327) extracts provider on `/` (the T225-shipped, D36-suggestedFix form), while @cq/config extracts no provider at all and `ReviewerToken` has no `provider` field. The extension and @cq/config are deliberate COPIES (the extension is a store-path file outside the bun workspace, cannot import @cq/config — K46/T223 "mirror, don't import").
- **Issues:** none blocking. Could not confirm the exact `cq.toml.example` filename (Read of `nix/pkg/cq-ledgers/cq.toml.example` 404'd; planner resolves the precise path at plan time — noted in Q136). Acceptance scope (unit tests vs a fresh live minimax/ollama-cloud dispatch demo) depends on Q136 + ollama-cloud OAuth availability.

## Questions filed
- **Q132** (separator: colon vs slash vs both) — recommended slash `pi:<provider>/<model>` (already shipped in T225). [user answered: "as recommended"]
- **Q133** (scope: add a structured `provider` field to @cq/config ReviewerToken + extraction surfaced via get_reviewers/get_planners/tiers, vs parse-and-preserve only) — recommended the structured field + keep the extension's mirrored copy in sync.
- **Q134** (backward-compat of the bare `pi:<model>` form) — recommended keep it fully backward-compatible; qualified form is additive.
- **Q135** (apply to `pi:` only or also `claude:`) — recommended pi: only (provider is a pi runtime feature).
- **Q136** (acceptance bar: unit tests + cq.toml.example + docs, vs also a fresh live minimax dispatch demo) — recommended unit tests + example + docs, reuse T225's live `--provider ollama-cloud` evidence; live minimax run optional.
