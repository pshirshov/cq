# implement-flow batch 3 — workers T100 T96 T101 T93 (all pass)

Base = main 8e81f8f.

- **T100 (M24)** worker ab8ca9ee662c803d5 → 693e4da. App.tsx: replaced hand-maintained MILESTONE_STATUS_SCHEMA with import of MILESTONES_SCHEMA from @cq/ledger/constants (richer but statusBucket only needs terminalStatuses → identical behavior); deleted the dup. check 635.
- **T96 (M22)** worker a2beadad9ec2f9921 → 45a7ce1. New backup-reinit-init.test.ts (20 tests: divergence→backup+reinit default / abort opt-out / no-divergence / empty-dir regressions, byte-for-byte backup, one WARNING); formalized canonical-ledgers.test.ts comment (6 cases = abort coverage). check 655.
- **T101 (M24)** worker a6c7d82edef6afe4e → ffe0d83. New package-exports.test.ts: builds via tsc -b (beforeAll 60s) then asserts every main + exports[*].import/.types target exists; fails pre-fix layout, passes ./dist/src/*. check 636.
- **T93 (M21)** worker a2d1b40c1a893f408 → 6292f1e. styles.css `.lw-batch` tuned: width min(900px,90vw), max-height 85vh, font-size 0.95rem, overflow-y auto; new batchModalSizing.test.tsx. (NOTE: worker's worktree base was STALE — check 628 < main's 635; commit delta cherry-picks clean, re-validated on main.) item 19.

MERGE: all disjoint (App.tsx / 2 new ledger test files + canonical-ledgers comment / new ledger test / styles.css + new web test). Remaining: T104 (App.tsx, after T100) + T97 (gate, after T96) → batch 4.
