# plan-advance — goal G7 — review-requested (revision of R103)

- Goal: G7 (defect-seeded fix goal D14–D19)
- Role: planner (plan-advance subagent)
- Returned: `review-requested` (rule 3 — criticism-driven revision of R103)

### Session summary
- **Did:** Rule 3 — applied R103's 4 criticisms to the planning-phase tasks for goal G7; left the goal in `planning`.
- **Achieved:** Revised T110, T114, T111, T115 (descriptions + acceptance). T110: corrected the D16 fix mechanism to source milestone title/status from `docs/archive/milestones/<id>.md` by milestone id (NOT from the per-ledger group archive at `ptr.path`), with a reproduction-first acceptance that also fails under the naive ungate approach. T114: rewired batch-modal per-suggestion pick to the actual `onSave(row, suggestionText)` path (no nonexistent `answerWith`), gated by the `answerHasText` rule. T111: named all THREE `freePort()` call sites (pty.e2e:71, displayName:56, mcpClient:38). T115: added recompute-open-set acceptance covering mid-queue answers. R103 new_questions empty, no defects[] bucket → none filed.
- **Discovered:** Confirmed against source that `backfillLegacyArchivePointers` (FsLedgerStore.ts:246-272) parses `ptr.path` with `parseMilestoneItemArchive` (L260), which only extracts a title from the single-item milestones grammar — so a non-milestones group archive yields an empty title and fails-soft. The reviewer's mechanism critique was correct.
- **Issues:** none — plan re-emitted. Goal remains `planning`.
