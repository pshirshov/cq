# implement-worker — T328 (opus); FU-3 sidebar + scrollspy
agent a3a4c146c1a4e149a; status pass; resultCommit 7a76acc; check 1353/0; nix build .#ledger-web exit 0. New HelpDocsLayout (persistent left nav sidebar + scrolling content pane) for the Item-States/Flows/Agents tabs (Shortcuts unchanged). Sidebar entries from each tab's existing section source (ledger schemas / ROLE_FLOWS / AGENT_ROLES), reusing the verbatim help-item-state-/help-flow-/help-agent-<id> anchors (added DOM `id` alongside the existing data-testid — additive). Click → exported `scrollToHelpSection` helper; IntersectionObserver scrollspy (guarded for happy-dom; active-section setters exposed via the exported `helpActiveSectionSetters` Map) marks aria-current + active. Sidebar CSS added. Owned test helpDocsNav.test.tsx. Discovered: AGENT_ROLES already renders one help-agent-<id> per role (no missing FU-4b targets).
