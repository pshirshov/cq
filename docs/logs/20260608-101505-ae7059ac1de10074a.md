# implement-worker (opus) — T247 — pass (6c186a7→3f64515)
Added committed CI regression test handoffs-additive-widening-init.test.ts: primes a TEMP store with the canonical widened HANDOFFS_SCHEMA (incl. user-action-required) + a seeded HO1(drained), runs FsLedgerStore.init() again, asserts no docs/.backup/ + handoffs not emptied + HO1 still readable. Divergence detected via schemasEqual; negative control (forced extra status) confirmed backup fires + HO1 lost. Depends only on committed HANDOFFS_SCHEMA + tmpdir. ledger package 505/0.
