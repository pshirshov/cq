# implement-worker — T313 (opus)

- Task T313; agent a7451d211f2b0894f; status **pass**; resultCommit ceee96b; check green 1321/0; nix build .#ledger-mcp success.
Added positional-subcommand dispatch to ledger-mcp main.ts (default still launches server); new restore.ts handler for `restore --from-cache [--cwd]` reusing the shared cacheMirrorDir() from @cq/ledger (no dup hash) + atomicWrite (added to @cq/ledger index export) for byte-identical tmp+rename copy-back; refuses with CacheMirrorMissingError (exit 1) on absent/empty cache; per-ledger restored-count summary; updated main.ts header boundary per Q169. restore.test.ts drives the parseArgs/handler entrypoint. **Discovered:** the Nix ledger-mcp product copies source via the git flake tree, so an UNTRACKED new file is silently excluded — restore.ts must be committed before nix build (it is, in ceee96b).
