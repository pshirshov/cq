# Session log — 2026-06-01 16:06–17:26

## Original request
Build a portable (Claude Code + Codex) ledger-native "plan" workflow: goal →
clarifying Q&A loop → planning → adversarial review → planned, with formality
enforced in the MCP server + ledger data (NOT in any orchestrator), no Claude
dynamic Workflows. Run under the review-loop skill.

## Worked this session

### M1 — Server/data formality (COMPLETE, committed)
- **PR-01 / F1** (`d0139ba`) — declarative `transitions` map on `LedgerSchema`,
  enforced in `core.ts::applyUpdateItem` (`InvalidTransitionError`); plumbed through
  parse/serialize/clone/schemasEqual/validateSchema + both MCP Zod fragments; maps for
  all 7 then-canonical ledgers; terminal-no-outgoing validation; added
  `scripts/regen-bootstrap.ts`. Review: 2 rounds; 1 major (on-disk divergence → regen,
  no compat per user) + 2 minor + 3 nits, all resolved/accepted.
- **PR-02 / F2** (`0df0c6f`) — server-enforced goal preconditions at the store layer
  (both stores), shared `assertGoalPhasePreconditions` helper invoked AFTER the F1
  guard: can't leave `clarifying` with an open linked question; can't enter `planned`
  without a locked linked decision. `GoalPreconditionError`. Review: 1 round; 0 major,
  2 minor (per-process scope doc; test gaps) + 1 nit, all resolved/accepted.
- **PR-03 / F3** (`7aa0865`) — canonical `reviews` ledger, verdict-as-status
  (`go-ahead`/`revise`, both terminal), `idPrefix R`, fields new_questions/criticism;
  canonical set now 8. Review: 1 round; 0 major, 2 minor coverage gaps + 2 nits, fixed.
- **PR-04** — absorbed: the regen-bootstrap script (PR-01) + its invocation in the
  schema PRs IS the regeneration; `docs/ledgers.yaml` is gitignored (local dogfood).

### M4 — UI quick-transition buttons (COMPLETE, committed `6d67521`)
- TUI + web read `schema.transitions[item.status]` from the fetched MCP schema and
  render one-click actions for only the legal next statuses. Executed in an isolated
  worktree (which mis-branched off `main`; agent rebased onto `feat/plan-flow`), then
  transplanted onto the branch. Review: 0 major, 0 minor, 4 accepted nits.

## Final ledger state
- M1 [x], M4 [x]. M2 [ ] (prompts), M3 [ ] (watcher) — not started.
- All defects resolved/accepted. `bun run check` green throughout (final: 369 pass).
- Branch `feat/plan-flow`, 4 commits ahead of `main`. Not pushed (user hasn't asked).

## Why this session paused (blocker = user direction on M2/M3)
M1 + M4 are objective, test-verifiable work — ideal for the autonomous loop. M2 (the
three prompts: `/plan:start`, `/plan:advance`, `plan-reviewer`) is the design-defining
content co-designed with the user across the prior conversation; an adversarial
code-reviewer subagent verifies prompt *intent* poorly. M2 also has unresolved
specifics (exact Claude/Codex symlink layout; Codex prompt-file format). M3 (watcher)
is a new package with packaging/deployment choices. Returning for direction on how
autonomously to proceed with M2/M3 vs. collaborative prompt authoring.

## Question for the user
Proceed autonomously authoring M2 (+ M3) through the loop, or draft the prompts for
review/co-authoring before committing (since they encode the whole flow design)?
