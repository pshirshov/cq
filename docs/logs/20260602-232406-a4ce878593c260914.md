# implement-worker — T113 (D17) — pass

- Task: T113 (M30, goal G7, defect D17) — remove archived badge from id cell
- Returned: `pass` — resultCommit c071bf6 (branch implement/T113 / worktree-agent-a4ce878593c260914)

### Session summary
- **Achieved:** pass; resultCommit c071bf6; `bun run check` green — 677 pass, 0 fail.
- **Discovered:** the badge was genuinely redundant — archived rows are already gated behind the show-archived toggle and styled lw-row-terminal — so removing it entirely is cleaner than relocating. The test that asserted badge presence was replaced with an assertion that the id cell text is exactly the id and the badge element is absent; the "archived rows absent when off" test's archived-badge-MA1 check was removed.
- **Issues:** none.
- filesTouched: packages/ledger-web/src/App.tsx, packages/ledger-web/test/milestonesArchivedRows.test.tsx
- HEAD of the App.tsx serialization chain (T113→T114→T115).
