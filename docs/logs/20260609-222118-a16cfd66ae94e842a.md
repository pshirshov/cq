# Session log — T337 implement-worker (opus), SPIKE

- **Task:** T337 (M140); status pass; resultCommit 34e3a36; bun run check green (1486/0); decision K66 locked

### Session summary
SPIKE: assessed orphan-branch ledger storage without working-tree switches. Built + ran a throwaway /tmp-repo PoC proving the pure git-plumbing path (hash-object -w → scratch-index write-tree → commit-tree → CAS update-ref) advances an orphan ref TWO commits (70301c6→33fb332) while the main checkout's HEAD ref, working tree, and index stay BYTE-IDENTICAL and git status stays clean. Verdict FEASIBLE-WITH-CAVEATS recorded as locked decision K66 (decisions/M140, ledgerRefs goals:G41) with: the command sequence, byte-identical proof, locking assessment (existing AsyncMutex+lockfile + CAS update-ref), interaction with the per-merge/per-archive chore(ledger) commits, reflog/push-fetch edge cases, and a GO follow-up-goal recommendation. DISCOVERED: **/debug/ is gitignored (.gitignore:48) → PoC git add -f'd to carry it; the ledger MCP writes to the MAIN checkout's docs/ (its --cwd), so K66/M140 mutations are in the live ledger, not the worktree commit (orchestrator owns the ledger commit). NO production code under packages/*/src (git grep confirms PoC referenced only under debug/ + the findings doc). filesTouched: docs/drafts/20260609-221530-orphan-ledger-feasibility.md, nix/pkg/cq-ledgers/debug/20260609-221530-orphan-ledger-poc.sh. Based on main f20b3fc.
