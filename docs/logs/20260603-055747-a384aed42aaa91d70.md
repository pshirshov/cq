# implement-worker — T131 (reset non-canonical, D21) — pass

- Task T131 (M36, G8, defect D21); resultCommit 1bc5a68 (parent 137c7e0)
- pass; tsc+eslint clean; targeted tests 30/30 (reset.test.ts, reset-noncanonical.test.ts, backup-and-reinit.test.ts, backup-reinit-init.test.ts).
- Two-part fix: (1) backupAndReinit() now enumerates non-canonical registry ledgers (those not in CANONICAL_LEDGERS), backs them up, then unlinks their docs/<name>.md; (2) reset() calls searchIndex.removeLedger() for each non-canonical ledger before clearing this.ledgers (init() only re-indexes canonical). The backupAndReinit fix also covers the init() divergence path. New reset-noncanonical.test.ts: createLedger('ops') + item + reset() → no orphan file, no registry entry, no stale FTS hits.
- files: packages/ledger/src/store/FsLedgerStore.ts, packages/ledger/test/reset-noncanonical.test.ts
