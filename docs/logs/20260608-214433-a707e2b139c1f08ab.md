# Plan bootstrap log — G36 (planner, first clarifying round)

- Goal: G36 (milestone M115) — optional thinking-effort suffix in cq model-identifier tokens
- Role: planner (plan-advance) ; Returned: awaiting-answers

### Session summary
- **Did:** Performed the single `clarifying`-phase step for G36 — grounded read-only in the token-grammar code, then filed the first batch of clarifying questions.
- **Achieved:** Created six `open` questions Q160–Q165 under M115, each linked `goals:G36`, with context/suggestions/recommendation: (1) effort-suffix syntax + separator + pi/claude applicability, (2) effort VALUE set (enum vs opaque), (3) effort's participation in token identity (reviewerTokensEqual / D42 dup-guard / classifyToken), (4) per-harness invocation-flag mapping + unsupported-effort error-vs-ignore, (5) MCP wire-shape changes (get_planners/get_reviewers/get_config) + legal cq.toml locations for the suffix, (6) acceptance/test bar. Goal left in `clarifying` (no transition performed).
- **Discovered:** `parseReviewerToken` (config.ts:62) splits on the FIRST colon then the FIRST slash, and explicitly *preserves* any further colons in the model verbatim — so there is no reserved second colon today; a trailing `:<effort>` is genuinely ambiguous and needs an explicit delimiter rule plus a round-trip-safe `formatReviewerToken` (config.ts:390). The pi-extension's `tokenToChildModel` (cq-subagent-dispatch.ts:327) only maps pi tokens to `--provider`/`--model` and returns null for claude (falls back to parent model), so claude effort has no invocation path through that extension. The MCP get_* wire shapes (per tool descriptions) expose `{harness, model, alias}` but NOT even `provider` — a pre-existing gap noted, not in scope. The MCP server's loaded config root currently errors on an OLD-shape `[tiers]` (`unexpected key "opus"`), whereas this repo's `/home/pavel/work/safe/flakes/cq/cq.toml` already uses the new classifier shape — the live deployed cq.toml differs from the repo copy and appears mid-migration.
- **Issues:** Could not run codegraph (no index for this tree) or Grep/Glob/Bash (not available to this agent), so I could not locate the cq-config `cq.toml.example` file nor the concrete MCP get_* handler source; I relied on the authoritative parser sources plus the MCP tool descriptions for the wire shapes. Two facts the planner should confirm at plan time: the exact pi reasoning-effort CLI flag name, and the native-Claude effort/thinking knob — both flagged inside Q163. Relevant files: `nix/pkg/cq-ledgers/packages/cq-config/src/config.ts`, `.../src/types.ts`, `.../src/toml.ts`, `.../src/index.ts`, `nix/pkg/pi-extensions/cq-subagent-dispatch.ts`.

awaiting-answers
