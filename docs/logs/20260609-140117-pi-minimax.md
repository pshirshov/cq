# Plan candidate planner — G39 (minimax, pi ollama-cloud/minimax-m3 CANDIDATE)
Candidate plan emitted: 1 milestone, 1 task (opus-4.8[1m]). "Mirror layout.registryPath on 'create' op and add regression test" — relax the cacheMirror.ts:82 early return so registryPath mirrors on op==='create'||'archive' (createLedger + archiveLedger are the only ops that rewrite ledgers.yaml; 'update' must not); docstring L56-64; extend cache-mirror.test.ts. Acceptance has explicit REPRODUCE-BEFORE-FIX clause: new test FAILS on unpatched code for the right reason, PASSES with fix; existing tests pass; bun run check + nix build .#ledger-mcp exit 0; surgical diff (only the 2 files). Single fix task keeps the test inseparable from the fix (project guideline).
