# implement-worker — T310 retry (sonnet)

- Task T310; agent a38dc8212b005ac2e; status **pass**; resultCommit 32cdad0; check green 1314/0.
Reset worktree to base 6554eec (wave-1). Added 'Worktree lifetime' bullet + marker G38-1a-worker-ephemeral to implement-worker.md Boundaries region; ran `bun run gen-agents` → agentsCatalogue.gen.ts now carries BOTH G38-1a-post-done-cleanup (T308) and G38-1a-worker-ephemeral (cumulative). Freshness guard green.
