# Implement log — T282 (D42 fix: parseTiers fail-loud on dup-token classification)

- Task: T282 (M114 / G35 W1; fixes D42). Worker: opus (a97e26ad3419be833, commit a9633ac). Reviewers: opus[claude] (a9e460fe6fcd13227) + minimax[pi:ollama-cloud]; grok+codex[pi:grok-build] excluded (stall). Merged to main 93f7751. bun run check green 1224/0.

### Worker
Added a class-agnostic duplicate-token guard to parseTiers (config.ts ~L181, before entries.push): `entries.find(e => reviewerTokensEqual(e.token, token))` → if found, throw CqConfigError naming BOTH conflicting raw keys + their classes (symmetric to existing throws). classifyToken left as-is (parse-time dedup makes its first-match unambiguous; doc updated). Tests (config.test.ts T282 block): alias+direct dup throws (naming both keys), two-alias dup throws, same-class dup throws, single-token-one-class doesn't throw, distinct tokens don't false-positive. DISCOVERED the pre-existing VALID_TOML_WITH_TIERS fixture itself encoded the D42 contradiction (minimax alias=standard + pi:ollama-cloud/minimax-m3=fast → same token) — reworked it contradiction-free (grok=standard classifying the distinct pi:grok-build/grok-build token) + updated dependent assertions. Noted the literal-two-identical-direct-keys case is unreachable (TOML rejects dup literal keys before parseTiers) → substituted reachable cases.

### Reviews — both APPROVE
- opus: guard before push, reviewerTokensEqual (class-excluded → class-agnostic), CqConfigError names both keys; bun run check green 1224/0; cq.toml.example (3 distinct tokens) + reworked fixture parse without false-positive; throw-tests assert both keys + the marker substring (non-vacuous); the VALID_TOML_WITH_TIERS rework is a forced contradiction fix (not coverage-weakening); literal-dup unreachability confirmed (TomlSyntaxError pre-empts parseTiers).
- minimax: same conclusions — guard correct/class-agnostic, message names both keys+classes, classifyToken body untouched (doc-only), tests non-vacuous across the 3 reachable same-token shapes + no-throw regressions, fixture rework legitimate, substitution sound.
