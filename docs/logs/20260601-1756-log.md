# Session log (final) — 2026-06-01, plan-flow build

Continuation of `20260601-1726-log.md` (which paused for user direction on M2/M3).
User answered: M2 fully autonomous; M3 held; merge the work into `main`.

## Completed this session (all reviewed + committed, then merged to `main`)
- **M1 — server/data formality** (`d0139ba` F1, `0df0c6f` F2, `7aa0865` F3): transition
  guard (all 8 canonical ledgers), server-enforced goal preconditions, `reviews` ledger
  (verdict-as-status), `scripts/regen-bootstrap.ts`. PR-04 absorbed (regen script + its use).
- **M4 — UI quick-transition buttons** (`6d67521`): TUI + web read `transitions` from the
  fetched MCP schema; executed in an isolated worktree, transplanted onto the branch.
- **M2 — portable plan-flow prompts** (`dd559f4`): single-source `prompts/` (4 files)
  symlinked into Claude (`.claude/commands/plan/*`, `.claude/agents/*`) and Codex
  (`.codex/prompts/*`); `/plan:start`, `/plan:advance` (thin loop), `plan-advance` +
  `plan-reviewer` subagents; `scripts/link-prompts.ts`; `prompts/README.md`.

## Review summary (per PR)
- PR-01: 2 rounds; 1 major (on-disk divergence → regen, no compat) + 2 minor + 3 nit.
- PR-02: 1 round; 0 major, 2 minor + 1 nit.
- PR-03: 1 round; 0 major, 2 minor + 2 nit.
- M4: 1 round; 0 major, 0 minor, 4 nit.
- PR-05/M2: 2 rounds + 1 follow-up; 1 major (malformed `goals:G` link query → zero matches,
  fixed to `list_milestone_items` + `ledgerRefs` filter) + 4 minor + 2 nit. Follow-up D07
  fixed multi-milestone discovery (work milestones tracked in `goal.fields.milestones`).
- All defects resolved/accepted. `bun run check` green throughout (final 369 pass / 0 fail).

## Final state
- `main` == `feat/plan-flow` == `dd559f4` (fast-forward merge; 22 commits ahead of
  `origin/main`, NOT pushed — user did not request a push).
- Ledger: M1 [x], M2 [x], M4 [x]. **M3 [ ] (non-AI watcher) — HELD per user; deferred to a
  future session.** Manual `/plan:advance <G>` resume works without it.
- `docs/ledgers.yaml` and `.claude/` are gitignored (local); after clone, run
  `bun run regen-bootstrap` (dogfood server) and `bun run link-prompts` (Claude commands).

## How to use the flow
1. `/plan:start "<goal>"` → creates goal (clarifying) + first questions.
2. Answer questions in the TUI/web (status → answered + answer).
3. `/plan:advance <G>` → advances one step (more questions, or plan+review loop, or planned).
   Repeat after each answer batch until the goal reaches `planned`.

## Remaining / future work
- **M3** — non-AI watcher (`@cq/ledger-live` → re-invoke the open session on the resume
  predicate) for hands-off auto-resume. Held.
- Possible polish noted in defects: TUI/web milestone quick-transition parity (M4-N03);
  codegraph tool-name portability across hosts (PR-05-D03, mitigated by prose fallback).
