# plan-reviewer — goal G7 — R103 (revise)

- Goal: G7 (defect-seeded fix goal D14–D19)
- Role: reviewer (plan-reviewer subagent)
- Verdict: `revise` — review id **R103** (4 criticisms, 0 new questions, 0 out-of-scope defects)

### Session summary
- **Did:** reviewed the emitted plan (T110–T115 under work milestone M30) for defect-seeded goal G7, verifying each task's fix-locus + acceptance against the actual source (FsLedgerStore.ts, App.tsx, portHelpers.ts, app.test.tsx) and against the confirmed root causes in defect records D14–D19.
- **Achieved:** verdict `revise`, review id R103, 4 criticisms / 0 new questions / 0 out-of-scope defects filed.
- **Discovered:** The load-bearing defect is in T110 (D16). The plan instructs "drop the `isMilestones` gate so `backfillLegacyArchivePointers` runs for every loaded ledger," but that method (FsLedgerStore.ts:246-272) reads `ptr.path` and calls `parseMilestoneItemArchive` (L260), which parses the milestones single-ITEM grammar. For a non-milestones ledger `ptr.path` resolves to a GROUP archive (`docs/archive/tasks/M12.md` begins `## M12` then `### T50 — done`, carries NO milestone title), so the parse fails-soft to an empty title — the correct reproduction-first test would FAIL post-fix. The real fix must source the title from `docs/archive/milestones/<id>.md` (or the already-backfilled milestones pointer), not from `ptr.path`. The other three criticisms are under-specifications: T114 references a nonexistent `answerWith` handler in the batch-modal scope; T111 names "the HTTP tests" but `freePort()` has three call sites (pty.e2e, displayName, mcpClient); T115 omits the recompute-open-set acceptance D19 prefers. D17/D18/D19 loci, the App.tsx serialization chain (T113→T114→T115), and the T110/T111/T112 disjoint/parallel-safe claims all check out. D13's exclusion is correctly justified.
- **Issues:** Codegraph MCP tools not surfaced; fell back to Read + shell grep/jq. No blockers — all loci verified against source.
