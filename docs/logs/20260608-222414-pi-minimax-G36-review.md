# Plan review log — G36 (reviewer: pi:ollama-cloud/minimax-m3, configured mode) — round 1

- Goal G36 (M115). Parsed verdict: **revise** (exit 0). Reconciled with opus (strictest-wins). grok+codex abstained (no API key).

minimax verdict: revise. Concurs with opus that T292's dep should be T286 (not [T288,T290]). Independently CONFIRMED (given a generalized digest that omitted the literal '--thinking') that the correct pi emission is the `--model provider/model:level` shorthand, not a separate flag — corroborating opus's catch of the actual ledger task's `--thinking` error. Distinct valid additions folded into the reconciled review: name the per-harness enum single-source-of-truth (types.ts exports PI_EFFORTS/CLAUDE_EFFORTS so T286+T294 cannot drift); T294 should enumerate its test surface (pi token→--model:effort, claude token→inert no-flag, CqConfigError on unsupported); make the claude-inert failure mode explicit (ignore effort, pass none to child, do not error); T298 FOD-hash note. minimax DISAGREED with opus on reserving ':' in the pi model half (called it out of scope) — union keeps opus's fail-fast caution. Verbatim JSON captured in the run transcript (Bash result).
