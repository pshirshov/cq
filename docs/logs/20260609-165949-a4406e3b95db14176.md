# implement-worker — T327 (opus); FU-4a/c/d catalogue
agent a4406e3b95db14176; status pass; resultCommit 331eb37; check 1358/0; nix build .#ledger-web exit 0. Enriched ROLE_FLOWS (roleActions.ts): agentId on every concrete dispatched-subagent/sub-flow-command node (plan/implement/investigate subagents + advance lanes→sub-flow command ids), abstract nodes (user/main/worktree/ledger/flow-lane) agentId-free. Surfaced each cq-assets-formalized op as labeled edges against worktree/main/ledger infra nodes — implement: worktree create+teardown/prune, rebase, conflict-resolver dispatch, merge-by-SHA, per-task ledger commit, file-defect→investigate; investigate: prober worktree lifecycle + seed-goal→plan; plan: planning-lock commit + auto-investigate handoff. roleKind+fill=fillForRoleKind(roleKind) on every node. Preserved the flowsTab 'merges by SHA' orchestrator→main testid (kept node id `main`, roleKind external→main). All agentIds exist in AGENT_ROLES.
