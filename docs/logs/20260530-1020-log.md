# Session log — ledger-fts (in-memory FTS over ledgers, `fts_search` MCP tool)

Date: 2026-05-30. Branch `ledger-fts`, worktree off `cce4a6f`.
Discipline: vsm-loop (single-session). S4 plan + adversarial review run inline
(no Task tool), kept as distinct phases; S1 execution + S3* audit per PR.

## Goal
Full-text search over the ledgers, exposed as a new MCP tool `fts_search`.
In-memory MiniSearch v7.2.0 index as a derived projection of the store, kept
coherent by the existing onMutation (local) + invalidate (remote) hooks. NOT
SQLite. `includeArchived` IN v1.

## Cycles run (milestone M-FTS, one commit per PR)
- **fts-1** (b2f94a6) — `LedgerSearchIndex` (MiniSearch wrapper) + dep. Canonical
  headline/body/status field bucketing so heterogeneous per-ledger schemas index
  uniformly with consistent boosts. Per-ledger active/archived doc buckets with
  id-tracked discard/add replacement. Unit test (no store): headline>body
  ranking, fuzzy, prefix, status, includeArchived partition. 9/9.
- **fts-2** (8aa7c3d) — `ftsSearch` on `LedgerStore`; wired index into both
  adapters (init builds active+archived; onMutation rebuilds active guarded;
  archiveMilestone awaits archived refresh; invalidate rebuilds active+archived
  on BOTH branches incl. registry-reload; createLedger via its create mutation).
  Dual-tests (Fs + InMemory): ranked cross-ledger, single-ledger filter, boosts,
  fuzzy/prefix, status, local coherence, includeArchived. Found+fixed FTS-D01.
- **fts-3** (e40b937) — cross-process coherence test: two FsLedgerStore on one
  cwd, onMutation→peer.invalidate relay; B.ftsSearch finds A's item only AFTER
  invalidate (proves remote-path rebuild). Second case covers the registry-reload
  (new-ledger) branch (F1).
- **fts-4** (3a25795) — `fts_search` MCP tool on BOTH surfaces
  (`createLedgerMcpTools`+`LEDGER_TOOL_NAMES` for the Claude in-process bridge;
  `cq-mcp/src/main.ts::registerLedgerTools` for the stdio binary — it
  hand-redefines tools, so it does NOT inherit createLedgerMcpTools). Tool-count
  assertions bumped: ledger mcp-tools 13→14; cq-mcp main 13→14; cq-mcp
  submitRelay 14→15; server askProxy 14→15 (channel) / 13→14 (standalone).
- **fts-5** (this commit) — flake.nix: `minisearch` added to the ledger dep
  symlink loop (covers both @cq/ledger consumers via the shared workspace
  symlink); FOD outputHash refreshed
  (sha256-F1JSOq4Vyc53Wm6fDNf9KTVnW1OCg30/2bFHGHoww8A=). Discharge.

## Homeostat firings (S3↔S4)
- I6 (replan) ×1 — environmental-model correction during S4 scan: the brief
  assumed adding to `createLedgerMcpTools` exposes the tool on both backends
  automatically. FALSE: cq-mcp hand-redefines every tool via
  `server.registerTool`. Plan corrected before execution (fts-4 adds the tool in
  both places + bumps cq-mcp/server tool-count assertions). Counted as one
  replan; stabilized in one cycle.

## Defects
- **FTS-D01** (resolved, fts-2, major) — FTS archived docs stale immediately
  after `archiveMilestone` on the FS adapter (fire-and-forget async refresh).
  Reproduced via the dual-tests includeArchived case (failed for the right
  reason: archived bucket empty at search time; InMemory passed, sync refresh).
  Fixed: `archiveMilestone` awaits the archived-doc refresh per participant +
  the milestones ledger; the synchronous active rebuild stays in `fireMutation`
  (never unwinds the write).
- **FTS-D02** (resolved, fts-5, nit — harness artifact, NOT a product defect)
  — the initial nix-binary smoke test piped all JSON-RPC lines at once with no
  flow control; MCP `StdioServerTransport` dispatched the buffered requests
  concurrently, so `fts_search` ran before `create_item`'s awaited index
  rebuild completed → `{"results":[]}`. Diagnosed by reproducing against source
  (same empty result) then in-process via the same `@cq/ledger` import (D1
  FOUND), isolating the defect to request concurrency in the smoke harness, not
  the product. Confirmed: with ~0.4s between requests (as a real MCP client
  serializes call→response), the built binary returns ranked results and the
  archived-hit-with-include_archived scenario both work. No product change.

## Discharge
- `bun run check` exit 0 TWICE (deterministic): 1012 pass / 0 fail / 3560
  expect() across 115 files (baseline 989 + 23 new FTS tests).
- `bun run e2e` exit 0: 25 passed / 0 failed (WFL-D02 ordering / LOCK-D01 /
  Codex relay / continuation / workflow NOT regressed).
- `nix build .#default` exit 0; built `./result-cq/bin/cq-mcp` launches from the
  Nix closure (MiniSearch present for the cq-mcp @cq/ledger consumer) and
  `tools/list` shows 14 ledger tools incl. `fts_search` (+1). End-to-end
  fts_search against the built binary returns ranked cross-ledger results.

## Manual scenario (built Nix binary, request/response serialized)
Seeded `defects` + `tasks`; `fts_search("search index")` →
`defects:D1` (headline "full-text search index race", score 17.37,
matchedFields ["headline"]) ranked above `tasks:T1` (cross-ledger, ranked).
Archived a milestone: `fts_search("quokka")` → `[]` (archived hidden);
`fts_search("quokka", {include_archived:true})` → archived `defects:D1`.

## Tool count
14 ledger tools (was 13): +`fts_search`. cq-mcp standalone surface 14; with WS
channel + session 15 (adds ask_user_question); with workflow relay 15.

## Metrics
- WIP max 1 (one cycle per ledger group, serial); review rounds fts-1..5: 1 each
- S3-S4 firings research:0, replan:1; time-to-stabilize replan:1
- Material scope delta: none beyond plan (the cq-mcp dual-surface + the
  server/askProxy count bumps were folded in at S4 review, before execution)
- Verification complete; audit discrepancies 0; algedonic ordinary:0 bypass:0

## Resolved this session
- FTS-D01 → commit 8aa7c3d (fts-2)
- FTS-D02 → no code change (harness artifact; documented in defects.md + here)
