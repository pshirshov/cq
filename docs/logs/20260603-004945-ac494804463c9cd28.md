# plan-reviewer — goal G6 (follow-ups #2/#3/#4) — R111 (revise)

- Goal: G6 — full accumulated scope
- Role: reviewer (plan-reviewer subagent)
- Verdict: `revise` — review id **R111** (6 criticisms, 0 new questions, 0 out-of-scope defects)

### Session summary
- **Did:** adversarially reviewed the emitted G6 follow-up plan (M33/M32/M31, T116–T129) against locked answers Q55–Q72 and the actual repo (constants.ts, main.ts, FsLedgerStore.ts, link-prompts.ts, investigate/plan/implement prompts).
- **Achieved:** verdict `revise`, R111, 6 criticisms / 0 new questions / 0 out-of-scope defects.
- **Discovered:**
  - Blocking: Q67's locked transition map has NO `open → root-caused` edge, but the investigate flow never sets defect status and T119 instructs writing `root-caused` directly → would throw `InvalidTransitionError`; the path must be `open → wip → root-caused`.
  - T120 conflates the hypothesis-tree `confirmed` node (investigate adjudication / stop-predicate vocabulary at plan/advance.md:150-164) with the defect lifecycle; a blanket re-key to `status==root-caused` would corrupt predicates (b)/(c)/(d).
  - The defect terminal vocabulary at implement/advance.md:198 (`abandoned`) is a missed `abandoned → wontfix` touch-point not covered by any task.
  - T121's premise (implement reviewer writes rootCause markers) is false — that section only writes `status:open`; T127/T116 carry minor stale-`4`/contradictory-edge wrinkles.
  - Confirmed-correct: T116 schema delta, public reset() (T123) vs private backupAndReinit (FsLedgerStore.ts:694), parseArgs/--reset wiring (T124) at main.ts:109, .codex/prompts committed-symlink vs link-prompts.ts .claude-only convention (T126), N=4 at implement/advance.md:36, sweep predicate/guard (T128/T129).
- **Issues:** TUI/web color-map sites (T117/T118) not located precisely (Grep/Glob/Bash unavailable); left to execution-time grep — acceptable, existence established by prior G2 warning-bucket work.
