# plan candidate (pi:minimax-m3 via ollama-cloud, alias minimax) — G30

- goal: G30; role: candidate planner (pi shellout); 7 milestones
- alias: minimax (pi: ollama-cloud/minimax-m3)

### Captured (summary)
7 milestones starting with a "Confirm current state (read all 8 anchor files)" discovery milestone, then schema → migration → rendering → prompt threading → tests → verify. One task per Q142 acceptance gate (schema unit / grep-invariant / TUI render / web render) + separate bun run check + nix build tasks. NOTE: minimax's migration task targeted "docs/*handoffs*.md schema header" — INCORRECT (handoffs.md frontmatter holds only counters/archives; the live schema lives in the gitignored docs/ledgers.yaml registry, per opus's grounding + orchestrator verification). minimax also linked the migration task to defects:D37 (dropped in synthesis — G30 is not a D37 fix goal).
