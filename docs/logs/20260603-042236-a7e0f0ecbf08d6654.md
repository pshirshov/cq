# implement-worker — T123 (public reset()) — pass

- Task T123 (M32, G6); resultCommit 655dfe0c1a8b5fac57d68bc531f6a61b712fa1c9 (parent 6818f7a)
- pass; tsc+eslint clean; all 68 packages/ledger tests green in isolation (reset.test.ts + backup-and-reinit.test.ts). Full-suite redness was only the known D20 ledger-tui flake + an FsLedgerStore concurrency 5s-timeout under load (7/7 on isolated re-run).
- Added public FsLedgerStore.reset() → ResetSummary {backupDir, ledgers:[{name,itemCount}]}: pre-counts active items, reuses private backupAndReinit() verbatim for the timestamped docs/.backup/<ts>/ snapshot + canonical reinit, reloads via init(). backupAndReinit now returns its backup dir (init() call-site unaffected). ResetSummary exported from @cq/ledger. Test asserts backup contents, canonical empty set, matching counts, and post-reset defects status set [open,wip,root-caused,inconclusive,resolved,wontfix].
- files: packages/ledger/src/store/FsLedgerStore.ts, src/index.ts, test/reset.test.ts, test/backup-and-reinit.test.ts
