# Implement worker log — T271 (classifier resolvers)

- Task: T271 (M110 / G34 W2)
- Role: implement-worker (opus, frontier)
- Returned: pass; commit 1384611; `bun run check` green (1138 pass / 0 fail)

### Session summary
- **Did:** implemented T271 in worktree impl-T271 (replaced the resolveTierToken bridge with the Q149 classifier API in @cq/config).
- **Achieved:** pass; resultCommit 1384611; bun run check GREEN (typecheck + lint clean, 1138 pass / 0 fail). `rg resolveTierToken packages/cq-config/src` → no matches; none anywhere in workspace src.
- **Discovered:** resolveAgentModel/resolveTierToken had NO external consumers — all call sites inside cq-config (src + 2 test files). The E2E_TOML fixture keyed the same minimax token to two classes (pi:...=fast AND minimax-alias=standard); under the classifier's first-match semantics the direct pi: key would win as fast, contradicting the old test's "standard→minimax" expectation, so removed the redundant direct pi: key to keep each token unambiguously classified. [Orchestrator note: that contradiction is the root of filed defect D42 — fail-loud on duplicate-token classification, deferred.]
- **Signature decision:** resolveAgentModel(config, agentName, candidates) now REQUIRES candidates: readonly ReviewerToken[] (the relevant active set); filters by the agent's tier, first match (documented tie-break = candidate order); throws precise CqConfigError listing candidates on no-match (incl. [tiers]-absent).
- **Deferred:** workspace consumer threading = T272 (none exist today); exhaustive behaviour assertions = T273; added only minimal in-package tests to keep check green.
- filesTouched: config.ts, index.ts, config.test.ts, e2e-and-cross-layer.test.ts.
