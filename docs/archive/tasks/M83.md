## M83

### T207 — done

- createdAt: 2026-06-06T23:53:44.777Z
- updatedAt: 2026-06-07T00:32:07.969Z
- author: "opus-4.8[1m]"
- session: 059ff637-d28c-4785-8125-9c0d73ddf7a0
- headline: Add server-computed `progressTotal` to LedgerSummary and emit it from both MCP transports
- description: |
    Implement the denominator half of the D34 fix server-side (decision K38, approach A1), symmetric to the existing answered-only `computeCompletedCount`.
    
    1. `packages/ledger/src/types.ts` (LedgerSummary, ~L159-170): add an optional field `progressTotal?: number` with a doc comment mirroring the existing `completedCount`/`statusCounts` block — 'denominator for the progress bar, computed server-side against each ledger's schema; for the questions ledger this is open + answered (excludes the terminal `withdrawn`); for every other ledger it equals itemCount. Optional: absent on older servers (clients fall back to itemCount).'
    2. Add a `computeProgressTotal(ledgerName, sc, schema, itemCount)` helper next to `computeCompletedCount` in BOTH `packages/ledger/src/mcp/ledgerTools.ts` (~L66-86) and `packages/ledger/src/mcp/stdioLedgerTools.ts` (~L112-132), keeping the two byte-identical (same as the existing mirror). For QUESTIONS_LEDGER return `itemCount - (sc['withdrawn'] ?? 0)` (equivalently `(sc['open'] ?? 0) + (sc['answered'] ?? 0)`); for every other ledger return `itemCount`. Use the existing `QUESTIONS_LEDGER` constant; do not hardcode 'questions'. Reference the `withdrawn` terminal status via a named local constant (mirror the style of `QUESTIONS_ANSWERED_STATUS`).
    3. In each transport's `enumerate_ledgers` handler (ledgerTools.ts ~L186-207, stdioLedgerTools.ts ~L179-199) compute a `progressTotals[name]` alongside `completedCounts[name]`, and add `progressTotal: progressTotals[name] ?? 0` to each entry of the `ledgerSummaries` map.
    
    Leave `itemCount` and `completedCount` UNCHANGED. Do not touch the client in this task.
- acceptance: "`bun run typecheck` passes. LedgerSummary exposes optional `progressTotal: number`. Both transports' enumerate_ledgers emit `progressTotal` per ledger; the two `computeProgressTotal` helpers are textually identical. For the questions ledger progressTotal = open+answered (excludes withdrawn); for tasks/other ledgers progressTotal === itemCount. Verified by the new assertions added in the sibling test task."
- suggestedModel: standard
- ledgerRefs: ["goals:G27","defects:D34"]
- resultCommit: 26965e7f8e2cf2eb1f38e39a7fb87e06a9d23c55
- completion: Added server-computed progressTotal to LedgerSummary (questions = open+answered, excludes withdrawn; others = itemCount), byte-identical helper in both MCP transports.
- sessionLogs: ["docs/logs/20260607-002428-af4147400223b0c80.md","docs/logs/20260607-002809-a4c61365f30f9b8f4.md"]

### T208 — done

- createdAt: 2026-06-06T23:53:56.984Z
- updatedAt: 2026-06-07T00:44:04.238Z
- author: "opus-4.8[1m]"
- session: 059ff637-d28c-4785-8125-9c0d73ddf7a0
- headline: Render `progressTotal` as the questions progress-bar denominator in LedgerProgressBar
- description: |
    Consume the new server field in the web client so the bar reads 38/38 = 100% when only withdrawn questions remain unanswered.
    
    In `packages/ledger-web/src/App.tsx`, function `LedgerProgressBar` (~L1407-1433): change the denominator from `summary?.itemCount ?? 0` to `summary?.progressTotal ?? summary?.itemCount ?? 0` (graceful fallback to itemCount when the server omits the field). Leave the numerator (`summary?.completedCount ?? 0`) and the `pct`/title/aria-label rendering otherwise unchanged — they already derive from `total`/`done`. Preserve the existing 'No client-side schema lookup' invariant comment (the new denominator is still entirely server-classified); update that comment only to mention `progressTotal` is the denominator if it improves clarity, but do NOT add any questions-ledger-specific or schema branching in the client.
    
    Verify `LedgerSummary` is the type already imported by App.tsx (it is, via @cq/ledger), so the new optional field is visible without an import change.
- acceptance: "`bun run typecheck` passes. LedgerProgressBar uses `progressTotal` (falling back to `itemCount`) as the denominator; numerator unchanged. No client-side schema/status branching introduced. With a questions summary of {itemCount:39, completedCount:38, progressTotal:38} the rendered fraction is 38/38 and pct 100; with progressTotal absent it falls back to 38/39 (old behaviour). Confirmed by the web render test added in the test task (or, if no DOM test is added, by typecheck + the unit assertion on the summary values)."
- suggestedModel: standard
- dependsOn: ["T207"]
- ledgerRefs: ["goals:G27","defects:D34"]
- resultCommit: 5c8b458
- completion: LedgerProgressBar denominator now uses progressTotal (falls back to itemCount); withdrawn questions no longer inflate the bar.
- sessionLogs: ["docs/logs/20260607-004046-a121c3441cf37d503.md","docs/logs/20260607-004328-ab1d14f5902d837cd.md"]

### T209 — done

- createdAt: 2026-06-06T23:54:09.642Z
- updatedAt: 2026-06-07T00:55:35.917Z
- author: "opus-4.8[1m]"
- session: 059ff637-d28c-4785-8125-9c0d73ddf7a0
- headline: Extend enumerate-ledgers-summary test to assert progressTotal excludes withdrawn; run full check
- description: |
    Pin the regression with the case D34 reproduces, then verify the whole suite.
    
    1. `packages/ledger/test/enumerate-ledgers-summary.test.ts`: the first test already seeds the questions ledger with 2 answered + 1 open + 1 withdrawn (itemCount 4, completedCount 2). Add assertions in that same test: `expect(qs!.progressTotal).toBe(3)` (open + answered = 4 - 1 withdrawn) and, for the tasks ledger, `expect(ts!.progressTotal).toBe(ts!.itemCount)` (non-questions ledgers: progressTotal === itemCount). Update the file's top-of-file doc comment to mention the progressTotal denominator assertion. Also extend the type-safety test (the third `it`) to include `progressTotal` in the `full` LedgerSummary literal and assert it is optional/undefined on `minimal` (mirrors the existing completedCount/statusCounts checks).
    2. If a DOM render test for LedgerProgressBar exists under `packages/ledger-web/test/`, add a case asserting a summary with {itemCount:39, completedCount:38, progressTotal:38} renders 'Q: 38/38' and width 100%, plus a fallback case (progressTotal omitted) rendering 38/39. If no such test/harness exists, do NOT scaffold one — the server-side assertion plus typecheck is sufficient; note that in the task close-out.
    3. Run `bun run check` (tsc -b + eslint + bun test) from `nix/pkg/cq-ledgers/` and ensure it passes clean.
- acceptance: From `nix/pkg/cq-ledgers/`, `bun run check` passes (typecheck + lint + full test suite green). The enumerate-ledgers-summary test asserts `progressTotal === 3` for the 2-answered/1-open/1-withdrawn questions seed and `progressTotal === itemCount` for the tasks seed; both fail against the pre-fix code (no progressTotal field) and pass after T207. Type-safety test covers progressTotal as optional.
- suggestedModel: standard
- dependsOn: ["T207","T208"]
- ledgerRefs: ["goals:G27","defects:D34"]
- resultCommit: 1dac634
- completion: "Pinned the D34 regression: enumerate-ledgers-summary test asserts progressTotal=3 (excludes withdrawn) for questions and =itemCount for tasks; type-safety test covers the optional field. bun run check green."
- sessionLogs: ["docs/logs/20260607-005036-a8e2828fa594852f7.md","docs/logs/20260607-005408-a2c81c049306be6ff.md"]
