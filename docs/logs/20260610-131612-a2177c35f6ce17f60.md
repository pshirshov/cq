# Session log — T354 implement-reviewer (opus) — APPROVE

- **Task:** T354 (M147, G43); verdict **approve**; 0 criticism / 0 questions / 1 out-of-scope defect (low, → D52)

### Session summary
Reviewed T354 round 1 (diff ec785d2..039bfb6). Verdict approve. THE CRITICAL INVARIANT holds: the new GitPlumbing.add/rmCached/lsFiles (which DO touch the real git index) are called ONLY from moveLedger.ts (the deliberate migration), NEVER from GitObjectLedgerBackend/GitPersistence — the gitInvariants index-digest byte-identity suite passes 6/6, so the backend's host-byte-identity invariant is preserved. Lossless round-trip proven: the test seeds a real ledger (incl. archive/tasks/M1.md), asserts before-bytes===seeded, orphan-ref docs-relative bytes===seeded, `git ls-files docs/` empty after --to git, on-disk bytes unchanged (left-in-place per R418), then after --to local on-disk bytes===seeded + tracked-state restored. --to/--force guards tested both directions (R418 left-in-place means the reverse needs --force — coherent). .gitignore add/remove are marker-guarded byte-for-byte inverses. moveLedger writes docs-relative TreeEntry paths matching GitPersistence layout. tsc+lint clean; new tests 9/9. ONE out-of-scope defect filed (low, D52): gitObjectLedgerBackend.test.ts T143 snapshot test flaked once on the 5000ms default timeout under full-suite parallel load (passes 43/43 in isolation) — the git backend's per-op subprocess cost; not caused by this diff. Approve.
