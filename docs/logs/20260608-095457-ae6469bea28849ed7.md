# implement-reviewer (opus) — T232 round 1 — approve (0/0/0)
typecheck+lint clean; 5 new tests pass asserting provider through computeReviewers/computeConfig (ollama-cloud) + claude→null in the exact acceptance shape; @cq/config already carried provider (parseReviewerToken) so no config.ts edit needed; @cq/ledger stays config-agnostic; surgical, no scope creep.
