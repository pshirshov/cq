# cq — active defect ledger

Schema: one row per defect, id format `PR-NN-DMM`. Status: `[ ] open` / `[~] in-progress` / `[x] resolved` / `[!] blocked`. Severity: critical / major / minor / nit. Closed defects' PR groups migrate to `./docs/archive/defects-<milestone-id>.md` on milestone close.

| ID | Status | Severity | Location | Description | Root cause | Fix |
|----|--------|----------|----------|-------------|------------|-----|
| `PR-18-D01` | `[x]` resolved | minor | `packages/server/test/e2e/ws-resilience.test.ts` | M1 E2E does not drive a client `Manager` in-process; it observes server-side invariants only (clean close handling, immediate accept-after-drop, port re-bind). Manager-against-real-server integration is not tested at E2E level. | Importing `Manager` from `packages/web/src/ws/` into a server-side test requires making the web package a TS composite project; deemed out of scope for PR-18. | Resolved in post-discharge fix (commit `fix: web → composite TS project + ws-resilience drives real Manager`). `packages/web/tsconfig.json` made composite (`composite:true`, `declaration:true`, `declarationMap:true`, `rootDir:./src`); `packages/web/src/index.ts` created exporting `Manager`, `Connection`, `ManagerOpts`, `ManagerStats`, `ConnectionOpts`, `ConnectionState`, `ConnectionStats`, `SocketLike`; `packages/server/tsconfig.json` references `../web` and adds `@cq/web` path alias. Also fixed `isRetriable` in `packages/shared/src/close-codes.ts` to treat code 1006 (ABNORMAL_CLOSURE) as retriable — the stale-grace DEAD path now correctly schedules reconnection. All 3 E2E scenarios drive a real `Manager` with real Bun `WebSocket` transport against the real fixture server. |
| `PR-19-D01` | `[x]` resolved | minor | `packages/server/test/mcp-inheritance.test.ts` | G2c F-14 MCP-inheritance test was skipped. The test required a `MockAnthropicHTTP` stub server so the real SDK subprocess can initialize without network access. | Running the real bundled CLI requires either a valid Anthropic API key or an HTTP-level stub. The native binary (`@anthropic-ai/claude-agent-sdk-linux-x64`) is not installed in this environment. | Resolved in **PR-20** via **fallback path (b)**: `MockAnthropicHTTP` helper shipped; `agent/mcp.ts` implements `loadMcpServers()` which reads `~/.claude/mcp_servers.json`; `Bridge` merges the result into `Options.mcpServers`; `mcp-inheritance.test.ts` un-skipped and passing via captured-options query factory. Note: PR-20-D01 opened for the deferred real-SDK-against-stub path. |
| `PR-20-D01` | `[x]` resolved | minor | `packages/server/test/sdk-stub.test.ts` | The "real SDK invocation against MockAnthropicHTTP" path is deferred. The native Claude Code binary (`@anthropic-ai/claude-agent-sdk-linux-x64`) is not installed (only the JS wrapper). Calling the real `query()` throws "Native CLI binary for linux-x64 not found." | Optional platform-specific peer package is absent from `packages/server/node_modules`. | Resolved by pinning `@anthropic-ai/claude-agent-sdk-linux-x64@0.3.150` in `packages/server/package.json` (bun workspace installs it as a hard link at `packages/server/node_modules/@anthropic-ai/claude-agent-sdk-linux-x64/claude`). Bridge adds `resolveNativeBinaryPath()` to pass `pathToClaudeCodeExecutable` to the SDK, bypassing Bun's `child_process` spawn quirk. New test in `sdk-stub.test.ts` (describe "REAL SDK, PR-20-D01") drives the real subprocess against MockAnthropicHTTP: sends `chat.input`, waits for `chat.started` + assistant `chat.event`, calls `bridge.shutdown()`, asserts `chat.done{reason:'completed'}`. All 399 tests pass. Commit: `31ab30a` — `fix: install real SDK binary (PR-20-D01) + verify Candidate-A spike (PR-31-D01)` |
| `PR-31-D01` | `[x]` resolved | minor | `packages/server/src/agent/askUserQuestion.ts` | Candidate A answer injection (push synthetic `SDKUserMessage{type:'user', message:{role:'user', content:[{type:'tool_result', tool_use_id, content}]}}` onto the streaming-input queue) is untested against the real bundled CLI subprocess. The native `@anthropic-ai/claude-agent-sdk-linux-x64` binary is not installed in this environment (see PR-20-D01). `ask-question.test.ts` validates the injection path against MockQuery only. If real-SDK testing in PR-51 disconfirms Candidate A (subprocess rejects the synthetic tool_result), fall back to `Options.disallowedTools:['AskUserQuestion']`. | Same root cause as PR-20-D01: the native peer package is absent. | Resolved together with PR-20-D01. Spike test in `ask-question.test.ts` (describe "Candidate-A spike — AskUserQuestion via REAL SDK subprocess (PR-31-D01)") drives the real subprocess: mock returns AskUserQuestion SSE on main prompt, `bridge.handleChatQuestionReply()` injects the answer as a synthetic tool_result, mock returns confirmation SSE containing "Option B". Bridge's `for await` loop receives the confirmation `assistant` chat.event. Candidate A is confirmed: the real subprocess accepts the synthetic tool_result injection. Commit: `31ab30a` — `fix: install real SDK binary (PR-20-D01) + verify Candidate-A spike (PR-31-D01)` |
| `PR-31-D02` | `[x]` resolved | minor | `packages/server/src/agent/askUserQuestion.ts`, `packages/server/src/agent/bridge.ts` | Candidate A (synthetic `SDKUserMessage{tool_result}` injection) is functional but non-idiomatic. The SDK provides a native mechanism: `Options.toolAliases` to redirect built-in tool names to MCP tools, plus `createSdkMcpServer` to register in-process MCP servers with async handlers. Using Candidate A bypasses this documented path, incurs risk of subprocess protocol drift, and couples the bridge to undocumented SDKUserMessage internals. Two additional issues discovered during resolution: (1) the subprocess sends a `use_tool` permission control request for in-process MCP tools before calling them — the `canUseTool` callback must auto-allow `mcp__cq__*` tools with `updatedInput: {}` to avoid a Zod validation failure in the subprocess's permission-result schema; (2) the WS `chat.question_reply` arrives before the MCP handler calls `broker.ask()` (the assistant message surfaces to the iterator before the SDK bridge invokes the handler), requiring a buffered-reply slot in `AskBroker`. | Candidate A was retained after PR-31-D01 confirmed it works, but the `toolAliases + createSdkMcpServer` mechanism was always the intended native path. | Resolved. `askUserQuestion.ts` rewritten: `AskBroker` class with `ask/reply/rejectAll` + buffered-reply slot; `createAskUserQuestionMcpServer(broker)` builds an in-process `cq` MCP server. `bridge.ts`: constructs `AskBroker`, wires `Options.mcpServers = {...externalServers, cq: askMcpServer}` and `Options.toolAliases = { AskUserQuestion: 'mcp__cq__ask_user_question' }`; auto-allows `mcp__cq__*` tools in `canUseTool` with `updatedInput: {}`; `handleChatQuestionReply` calls `askBroker.reply()` instead of `injectAnswer`; `rejectAll()` on session end. `ask-question.test.ts` rewritten with `AskBroker` unit tests and new real-SDK spike (describe "toolAliases + SDK-MCP spike — AskUserQuestion via REAL SDK subprocess (PR-31-D02)") confirming the MCP round-trip. 403 tests pass. |
| `E2E-D01` | `[x]` resolved | minor | `packages/web/src/chat/SearchBar.tsx` | Search bar opened via Ctrl/Cmd+F does not close on Esc. Playwright `search.spec.ts` fails with `expect(searchBar).not.toBeVisible({timeout:3000})` after pressing Esc — the bar stays visible. | The input-level `onKeyDown` handler only fires when focus is on the `<input>`. After clicking the next-match button, focus moves to that button, so Esc events no longer reach the input handler. | Added `handleBarKeyDown` on the wrapping `<div>` (event bubbles up from all children): intercepts Esc → calls `onClose`. The input-level handler is retained as a fast path. Commit: `HEAD`. `bun x playwright test search` → 1 passed; `bun run e2e` → 4/6 (boot, markdown, send-receive, search). |
| `E2E-D02` | `[x]` resolved | minor | `packages/e2e/tests/scroll-anchor.spec.ts` + `packages/server/src/buildWeb.ts` | The "↓ Jump to latest" button did not appear in scroll-anchor.spec.ts after the test scrolled the stream to top. `expect(jumpToLatestBtn).toBeVisible({timeout:5000})` failed. | Two compounding causes: (1) `buildWeb.ts` wrote index.html with only a `<script src="/main.js">` tag, omitting the `<link rel="stylesheet" href="/main.css">` for the CSS asset Bun.build() emits (kind:`"asset"`). Without the stylesheet, `.root_b4FDoA` had browser-default styles (`overflow-y:visible; display:block`) so the stream div expanded to its content and was never scrollable — `scrollHeight` always equalled `clientHeight` regardless of message count. (2) Even with CSS loaded, 5 short one-liner replies would not have exceeded `clientHeight + 80px`. | Fixed `buildWeb.ts` to detect the CSS output (`kind === "asset" && path.endsWith(".css")`) and inject `<link rel="stylesheet">` into index.html. Fixed test to send 3 messages with 60-line replies ensuring genuine overflow, and added a precondition `expect(scrollHeight).toBeGreaterThan(clientHeight + 80)` assertion that fails loudly if the setup regresses. Commit: `822bb04`. |
| `E2E-D03` | `[x]` resolved | minor | `packages/e2e/tests/stop.spec.ts` | Stop test clicks Stop within ~50ms of sending the message; SDK is mid-HTTP-roundtrip and `Query.interrupt()` doesn't take effect within the 10s `toBeEnabled` timeout. Test never sees textarea re-enabled. | Two compounding causes: (1) without a warm-up, the SDK subprocess is still in init/HTTP-roundtrip phase when Stop fires — interrupt() in this phase does not cause subprocess to exit within 25s; (2) even with subprocess initialised, the instant mock delivers chat.event and chat.done in the same TCP packet, so they're processed in the same browser macrotask batch and the Stop button disappears in the same React render as the text — leaving no DOM window for Playwright's locator.click() to fire. | Fixed via three changes in `stop.spec.ts`: (1) warm-up message (`sendMessage("warm-up")` + `waitForTextInStream`) ensures the subprocess is fully initialised before the stop-test turn; (2) `MutationObserver` installed before `sendMessage` watches `stream-root` for the reply text and clicks Stop in the microtask boundary between the `chat.event` browser macrotask and the `chat.done` browser macrotask (separate browser tasks even when in the same TCP packet, per WebSocket spec); (3) `toBeEnabled` timeout bumped to 25s. Even when `chat.done{completed}` fires before the interrupt takes effect, the UI is already in idle state, so the test assertions (`toBeEnabled` + `stopButton.not.toBeVisible`) pass. Commit: HEAD. `bun x playwright test stop` → 1 passed (1.7s); `bun run e2e` → 6/6 pass. |
| `E2E-D04` | `[x]` resolved | major | server `bridge.ts` `handleChatStart` + client `ChatTab.tsx` lifecycle | Switching Chat → History → Chat (or clicking Resume from History) triggers a duplicate `chat.start` while the server still has `bridge.active !== null` → user sees red toast "A session is already active". | (a) ChatTab unmounts when the tab switcher hides it, losing in-memory `activeSessionId`; on remount the auto-start ALIVE-edge effect fires another `chat.start`. (b) `handleResumeSession` sends `chat.interrupt` synchronously then immediately sends `chat.start` — the bridge hasn't torn down `active` yet. | Server-side: `handleChatStart` now preempts an active session via `interruptActive() + await shutdown()`, then proceeds normally — old session ends with `chat.done{reason:'interrupted'}`, new `chat.started` follows. Client-side: `activeSessionId` and `inProgress` lifted from ChatTab local state into a new `SessionProvider`/`useSession()` context (`packages/web/src/chat/SessionContext.tsx`) mounted above the tab switcher in App.tsx. `bridge.test.ts` SESSION_BUSY assertion flipped to preempt-replace contract. `chat-autostart.test.ts` renders wrapped in `<SessionProvider>`. Commit: `efe35a2`. `bun run check` 456/456; `bun run e2e` 6/6. |
| `E2E-D05` | `[x]` resolved | major | `persist/invocations.ts` + `persist/SqlitePersistence.ts` startup path | History tab shows yesterday's invocation as `status:"running"` long after the cq process was killed. | Bridge writes `status:"running"` on `chat.start`; only the `runLoop` finally block transitions to `completed`/`errored`. On SIGKILL or unclean shutdown the finally block never runs. There's no orphan reaper at startup. | Added `InvocationStore.reapOrphans(now): number` running `UPDATE invocation SET status='errored', ended_at=COALESCE(ended_at, ?), duration_ms=COALESCE(duration_ms, ? - started_at) WHERE status='running'`. Wired into `SqlitePersistence` constructor (file-backed DBs only; `InMemoryPersistence.reapOrphans()` is a no-op). Reap count logged at `info`. Test in `persist-crud.test.ts` asserts running→errored with `endedAt`/`durationMs` set and completed unchanged. Also added `"errored"` to `InvocationRow.status` union, Zod `HistoryRow.status` enum, and `List.tsx` `statusClass` switch — bridge was already emitting that value without the type system including it. Commit: `bbb3567`. `bun run check` 455/455; `bun run e2e` 6/6. |
| `E2E-D06` | `[x]` resolved | major | `bridge.ts` SDK message → persistence path | History list shows `costUsd=0, toolCallCount=0` for every row, even sessions that ran multiple tool calls. | The InvocationRow is INSERTED with stub `costUsd:0, toolCallCount:0` and is never UPDATED. The bridge does emit `chat.usage` over WS (handled by ChatTab for the live counter) but doesn't write those values back into `invocation`. Similarly, the bridge never bumps `tool_call_count` on `tool_use` events. | Wired `runLoop`: on each `assistant` message with `tool_use` content blocks, increments `ActiveSession.toolCallCount` accumulator and patches the invocation row + emits `history.update`. On each `result` message (turn end), accumulates `total_cost_usd` + `usage.{input_tokens,output_tokens}` onto `ActiveSession` accumulators and patches the row. `ActiveSession` gains four accumulator fields. Test added in `bridge-persist.test.ts` (E2E-D06 case). Commit: `31d0e0d`. |
| `E2E-D07` | `[x]` resolved | nit | client `ChatTab.tsx` | Header's permission-mode default is "default"; user wants "bypassPermissions" so dogfooding doesn't require Allow on every tool call. | Initial state `useState<PermissionMode>("default")`. | Change initial state to `"bypassPermissions"`. |
| `E2E-D08` | `[x]` resolved | nit | `Header.tsx` SUPPORTED_MODELS | Model selector lacks 1M-context variants; only base names appear. | Hard-coded list of three short names; no `[1m]` suffix entries. | Add `claude-opus-4-7[1m]`, `claude-sonnet-4-6[1m]` (Anthropic's `[1m]` suffix selects the 1M-context tier). |
| `E2E-D09` | `[x]` resolved | minor | `Input.tsx` keymap + missing Send button | Current chord: Cmd/Ctrl+Enter to send; bare Enter inserts newline. User wants conventional chat UX: bare Enter to send, Shift+Enter for newline; AND an explicit "Send" button next to the textarea. | Deliberate product choice in PR-21 — was "unambiguous across IME and keyboard layouts." User disagrees; conventional chat UX wins. | Flipped `isSendChord` to `Enter && !shiftKey && !ctrlKey && !metaKey && !altKey && !isComposing`. Added explicit `<button>Send</button>` (disabled when textarea is empty, hidden when `disabled===true`). Updated `input.test.ts` (6 cases renamed to new contract). Updated `attachments.test.ts` `fireSend` to use bare Enter. Updated `cqPage.ts` `sendMessage` to press `Enter`. `bun run check` 453/453; `bun run e2e` 6/6. |
| `E2E-D15` | `[x]` resolved | major | `bridge.ts` `handleChatStart` + `persist/invocations.ts` + `persist/migrations.ts` | "Resume from history" created a new `chatSessionId` and a new `sessionRow`, making the resumed conversation look like an unrelated session in the history list. Violated Claude CLI semantics: resumption should continue the same session. | `handleChatStart` called `registry.create()` unconditionally, then inserted a fresh `sessionRow` and `invocationRow` regardless of `resumeFromInvocationId`. The SDK `resume` option was set but cq's own persistence was disconnected from it. | `SessionRegistry.register(id)` added (re-register an existing id, noop if already present). `handleChatStart` now: (1) looks up `priorInv`/`priorSession` from persistence; (2) calls `registry.register(priorSession.id)` to reuse the existing chatSessionId; (3) within `withTx`: calls `sessions.update(chatSessionId, {endedAt:null, endedReason:null})` to re-open the prior row instead of inserting a new one; (4) inserts the new invocation with `resumedFromInvocationId = frame.resumeFromInvocationId` and `parentInvocationId: null`; (5) falls back to fresh-session path with a warn log if prior inv/session are missing. Schema: `invocation` gains nullable `resumed_from_invocation_id` column (migration 2). `InvocationRow` and `HistoryRow` (Zod) gain `resumedFromInvocationId: string | null`. `List.tsx` renders a ↻ badge on resumed rows. Tests: `persist-crud` (round-trip of field, both adapters); `bridge-persist` (fresh + resume sequence asserting 1 session row, 2 invocations, link, SDK options). Commit: `e515cee`. `bun run check` 459/459; `bun run e2e` 6/6. Surprise: SQLite does not support `ALTER TABLE … ADD COLUMN IF NOT EXISTS`; the versioned migration system makes the guard unnecessary. |
| `D23` | `[x]` resolved | major | `packages/server/src/persist/invocations.ts`, `InMemoryPersistence.ts` | History list showed one row per invocation, so resuming a session three times produced three rows for the same session. | `list()` returned every invocation row without deduplication by session. | Added `ROW_NUMBER() OVER (PARTITION BY session_id ORDER BY started_at DESC) = 1` CTE to SQLite `list()` to deduplicate to the latest invocation per session. Added `agent_name='main' AND parent_invocation_id IS NULL` guards to exclude sub-agent children. Same logic applied to the FTS search branch. Mirrored in `InMemoryPersistence.list()` using a `Map<sessionId, latestRow>` accumulation. `total` now counts distinct sessions. Tests: `persist-crud.test.ts` D23 case (3-invocation session → 1 row, 2nd session → 2 rows sorted desc). Commit: `8142fcf`. |
| `D24` | `[x]` resolved | major | `packages/web/src/chat/Stream.tsx` | User's typed messages did not appear in the chat stream — `SDKUserMessage` frames (sdkType `"user"`) were falling through to `UnknownCard` (raw JSON blob). | `computeRenderedMessages` had no branch for `sdkType === "user"`; all non-stream_event/non-assistant types went to `unknownByKey`. | Added `user` variant to `RenderedMessage` union. In `computeRenderedMessages`, handle `sdkType === "user"`: extract text blocks, emit a `kind:"user"` entry when non-empty text present; tool_result-only messages fall through to `UnknownCard`. In `renderMessages`, render `kind:"user"` via `MessageBubble` with `role:"user"` and `Markdown`. Fixed `activeMatchKey` computation to handle both `assistant` and `user` `.messageId`. Tests in `stream-reflow.test.ts`. Commit: `400bfb4`. |
| `D25` | `[x]` resolved | minor | `packages/web/src/chat/Stream.tsx` | Assistant messages with only tool_use content blocks rendered as empty `MessageBubble` (no text, no thinking). | `extractFinalText` returned `""` for tool_use-only content; the empty bubble was still rendered. | In `renderMessages`, skip rendering a `MessageBubble` for `kind:"assistant"` when `text.trim() === "" && thinkingBlocks.length === 0`. Tool card renders as normal. Test in `stream-reflow.test.ts`. Commit: `4f9d2ef`. |
| `D26` | `[x]` resolved | minor | `UnknownCard.tsx`, `Header.tsx`, `ChatTab.tsx`, `Stream.tsx` | SDK event cards showed verbose `"SDK event: system (init)"` labels; no way to hide raw SDK event noise from the chat stream. | No label humanization; no hide toggle. | A) `UnknownCard.tsx`: replaced `"SDK event: <type>(<subtype>)"` summary with compact labels per type (system/user/result/rate_limit_event/default underscore-to-space). B) `Stream.tsx`: added `hideSdkEvents?: boolean` prop; when true, `renderMessages` skips `kind:"unknown"` entries. `Header.tsx`: added `hideSdkEvents`+`onHideSdkEventsChange` props and a compact checkbox labelled "Hide SDK events". `ChatTab.tsx`: wires local `hideSdkEvents` state into both. Session-scoped, no localStorage. Tests: `stream-reflow.test.ts` (UnknownCard label cases + Stream hideSdkEvents toggle), `header.test.ts` (defaultProps updated + D26 checkbox test). Commit: `d9436e2`. `bun run check` 473/473; `bun run e2e` 6/6. |
| `D28` | `[x]` resolved | major | `packages/server/src/agent/bridge.ts` | Subagent child invocation rows had `model=""`, `tool_call_count=0`, `cost=0`, `tokens=0/0`. Parent invocation row flipped to `status="failed"` with no visible error in the UI. | D28a: `runLoop` unconditionally credited `tool_use` blocks on assistant messages to the top-level invocation, ignoring `parent_tool_use_id`. `handleTaskStarted` did not build a `tool_use_id → childInvId` index, so subagent assistant messages could not be routed to the correct child row. D28b: when the SDK emits `result{subtype:"error_*"}`, the bridge emitted `chat.done{errored}` but no `chat.error` frame, so the UI toast never fired and the "failed" status appeared silently. | D28a: Added `toolUseInvocationMap: Map<tool_use_id, childInvId>` to `ActiveSession`; populated in `handleTaskStarted`. In the `assistant` handler, resolve `parent_tool_use_id` via the map: if it matches a child, credit `tool_call_count` to the child row and capture `msg.message.model` on the child's first message (stays 0 for cost/tokens — SDK only emits one result per top-level turn boundary; documented via code comment). D28b: After emitting `chat.done{errored}`, also emit `chat.error{code:subtype, message:errors[0]??subtype}` so the UI toast surfaces the failure. Tests: `bridge-persist.test.ts` D28a case (task_started + subagent assistant with parent_tool_use_id + task_notification → child row has `tool_call_count>0` and `model` non-empty); `bridge.test.ts` D28b case (result{error_max_turns} → both `chat.done{errored}` and `chat.error{code:"error_max_turns"}`). Commit: `4d3b028`. `bun run check` 475/475; `bun run e2e` 6/6. |
| `D-GC-N1` | `[x]` resolved | minor | popup permission-mode UX | Codex SDK exposes two orthogonal safety knobs (`sandboxMode` 3-value + `approvalPolicy` 4-value). This cycle surfaces only `sandboxMode` to the popup when a Codex model is selected. The `approvalPolicy` knob is not exposed; defaults to whatever the codex-sdk default is (`on-request`). | Brief specified "show ONLY the Codex enum"; the Codex API has two enums. Showing both would balloon the popup. | Resolved via outer-9 (gcn1-1 + gcn1-2 + gcn1-3). New `ApprovalPolicySchema` (`packages/shared/src/protocol.ts`) mirrors codex-sdk's `ApprovalMode` union; `ChatStart` carries the optional field. Migration #7 adds `session.approval_policy TEXT NULL`; both adapters honor it. `SessionRow` (TS) + `HistoryRow`/`HistoryRowFull` (Zod) gain the field. `CodexBridge.handleChatStart` forwards `frame.approvalPolicy` to `ThreadOptions.approvalPolicy` and persists on the new session row; facade refuses Claude + non-null approvalPolicy with `chat.error{code:'approval-policy-on-claude'}`. `SettingsPopup.tsx` renders a second select row "Approval policy" (4 SDK values) only when `modelToPlatform(model) === 'codex'`; default `on-request`. `ChatTab.tsx` hydrates from `localStorage['cq.codex.approvalPolicy']` and spreads `approvalPolicyFor(model, value)` onto every `chat.start` frame (5 construction sites). Tests: `persist-crud.test.ts` (round-trip + update + HistoryRow join), `codexBridge.test.ts` (forwarding + null-default), `bridge.test.ts` (facade refusal), `settingsPopup.test.ts` (absent-when-claude / present-when-codex / change-fires). Commits `688037e` (schema + persistence), `a8760e8` (bridge wiring), `<this>` (popup UX + close). |
| `D-GC-1` | `[x]` resolved | major | `packages/cq-mcp/*`, `packages/server/src/agent/codexBridge.ts` | Brief Q13 asks the existing `mcp__cq__*` (ledger MCP) tools to be reachable from a Codex session. The Codex SDK does NOT accept in-process MCP servers — Codex's MCP integration is config-file-driven (`~/.codex/config.toml` or `CodexOptions.config.mcp_servers.*`), and the CLI binary it spawns reads MCP config from there. We cannot inject `createSdkMcpServer` into a Codex thread. | Codex CLI architectural choice: MCP servers must be discoverable as external processes the CLI can spawn. The in-process SDK-MCP path is Claude-SDK-specific. | Resolved via outer-9 (cq-mcp-1+2+3 + gc1-1+2). New `@cq/cq-mcp` workspace ships a standalone stdio MCP binary (`packages/cq-mcp/src/main.ts`) that wraps `FsLedgerStore` and serves the 12 `mcp__cq__*` ledger tools through `@modelcontextprotocol/sdk@1.29.0` `McpServer` + `StdioServerTransport`. `CodexBridge` now constructs the `Codex` SDK per session with `CodexOptions.config.mcp_servers.cq = { command: <resolved cq-mcp>, args: ["--cwd", <abs>] }`; the SDK flattens this to `--config mcp_servers.cq.command=…` and `--config mcp_servers.cq.args=[…]` which the Codex CLI parses as a TOML mcp_servers table. Bin resolution walks `node_modules/.bin/cq-mcp` then falls back to `bun run packages/cq-mcp/src/main.ts`; injectable for tests via `CodexBridgeOpts.cqMcpBin`. `ask_user_question` is intentionally NOT exposed by the binary (requires a WS round-trip the standalone process cannot perform); Codex sessions use the native approvalPolicy flow instead — see D-GC-N1. Tests: `packages/cq-mcp/test/main.test.ts` spawns the binary end-to-end via MCP `Client`+`StdioClientTransport` (asserts 12 tool names, `enumerate_ledgers` round-trip, `create_milestone` on-disk write); `packages/server/test/codexBridge-mcp.test.ts` asserts the codexFactory receives the correct `config.mcp_servers.cq` shape (both with explicit `cqMcpBin` and the default resolver). README prerequisites updated. Commits `7e3f693` (binary + tests), `f2c5f61` (CodexBridge wiring). Follow-ups: `D-CQMCP-NIX` (verify Nix closure includes the bin), `D-CQMCP-E2E` (exercise via codex-roundtrip e2e once `OPENAI_API_KEY` is available). NOTE (outer-14): the `ask_user_question` sub-aspect deferred above ("requires a WS round-trip the standalone process cannot perform") is now closed for real via the outer-13 internal-WS channel — see `ASKPROXY-D01`. This row's history is unchanged. |
| `D-CQMCP-NIX` | `[x]` resolved | minor | `flake.nix`, `packages/server/src/agent/codexBridge.ts` | The cq-mcp stdio bin must be present in the Nix closure for `nix build .#default` users. Bun's workspace install links it under `node_modules/.bin` which the existing flake should pick up, but this has not been verified by running `nix build .#default` (heavyweight; out of scope for outer-9). | Nix verification deferred. | Resolved via outer-10 (cqmcp-nix-1 + cqmcp-nix-2). `flake.nix` FOD fileset extended to include `packages/{ledger,cq-mcp,e2e}/package.json` so `bun install` resolves the new workspaces inside the FOD; FOD `outputHash` refreshed to `sha256-qa7x+5dSTcZ/NfpdI6SL7wke8P9FtPiM8MvIZX3f1uM=`. Final `cq` derivation now (a) materialises per-workspace `node_modules` for `ledger` and `cq-mcp` (with workspace `@cq/ledger` symlink + npm deps zod, @modelcontextprotocol/sdk, remark-*, unified, yaml, anthropic-ai/claude-agent-sdk), (b) symlinks `@cq/ledger` and `@cq/cq-mcp` into the server's node_modules + `.bin/cq-mcp`, (c) installs a `makeWrapper`-built `$out/bin/cq-mcp` that runs `bun run $WORKSPACE/packages/cq-mcp/src/main.ts`. `defaultResolveCqMcpBin` (codexBridge.ts) reordered: `whichOnPath('cq-mcp')` now wins over `node_modules/.bin/cq-mcp` so a Nix-installed system bin beats a dev symlink. Verification: `nix build .#default` exit 0; `./result/bin/cq-mcp --help` → `cq-mcp: fatal: --cwd <absolute path> is required` (exit 0); `./result/bin/cq-mcp --cwd <tmp>` + JSON-RPC `initialize` → `{"serverInfo":{"name":"cq-mcp","version":"0.0.1"}, capabilities:{tools:{listChanged:true}}}`. Commits `b47aa42` (cqmcp-nix-1: flake.nix), `5bddf76` (cqmcp-nix-2: resolver order). |
| `D-CQMCP-E2E` | `[x]` resolved | minor | `packages/e2e/tests/codex-roundtrip.spec.ts`, `packages/e2e/tests/codex-mcp-roundtrip.spec.ts`, `packages/e2e/fixtures/codexAuth.ts`, `packages/e2e/globalSetup.ts`, `packages/server/src/agent/codexBridge.ts`, `packages/server/src/server.ts` | The codex-roundtrip e2e is auth-gated (skips when `OPENAI_API_KEY` / `CQ_E2E_RUN_CODEX` is unset). Once a credential is available, extend the spec to drive a Codex session that calls `mcp__cq__enumerate_ledgers` end-to-end and asserts the result reflects the cq server's view of the ledger registry. | Out of scope for outer-9 (no auth in env). | Resolved via outer-10 (cqmcp-e2e-1 + cqmcp-e2e-2). (1) `globalSetup.ts` symlinks `${realHome}/.codex` into the hermetic HOME (so `CodexBridge.defaultDetectCodexAuth` sees real auth) AND sets `CODEX_HOME=${realHome}/.codex` on the cq-server subprocess (the codex CLI refuses `codex_home` under /tmp; CODEX_HOME bypasses the derivation). (2) New `packages/e2e/fixtures/codexAuth.ts` exposes `hasCodexAuth()` (combines `~/.codex/auth.json` + `OPENAI_API_KEY` + `CQ_E2E_RUN_CODEX` checks) and `pickCodexModel()` (reads top-level `model = "..."` from `~/.codex/config.toml`, env override via `CQ_E2E_CODEX_MODEL`, fallback `gpt-5.1`). ChatGPT-account auth rejects most explicit `--model <id>` so we must use the user's CLI-declared default. (3) `codex-roundtrip.spec.ts` ungated: skip predicate now checks `hasCodexAuth()`, bypass the popup dropdown via `POST /__e2e/settings` (new admin endpoint) + `addInitScript` localStorage seeding, afterEach resets `ui_settings.model` to claude so subsequent specs don't inherit codex routing. (4) New `codex-mcp-roundtrip.spec.ts` drives `mcp__cq__create_ledger` end-to-end: pre-stages `permissionMode=codex-danger-full-access` + `approvalPolicy=never` so the codex CLI does not gate the tool call behind an approval prompt; sends a pinned prompt; asserts `${CQ_E2E_CWD}/docs/codex-e2e-ledger.md` exists with `ledger: codex-e2e-ledger` + `counters:` frontmatter AND `${CQ_E2E_CWD}/docs/ledgers.yaml` lists the entry — the authoritative signal that cq-mcp was actually spawned by the codex CLI and the tool call executed inside the cq server's --cwd. afterAll cleans up the ledger file + registry entry. (5) Bonus fix in `codexBridge.ts`: switched the default `CodexFactory` from sync `require("@openai/codex-sdk")` to async dynamic `import()`. The codex-sdk is ESM-only (its package.json `exports` has no `require` hook), so the previous code threw "Cannot find module" the moment a codex session started — masked until now because the codex-roundtrip spec was always skipped. (6) New `POST /__e2e/settings` admin endpoint (server.ts, gated on `CQ_E2E_HOOKS=1`) writes `ui_settings.{model,permissionMode,hideSdkEvents}` synchronously so specs can pre-stage server-side defaults before the page opens. Verification: `bun run check` → 689 pass / 0 fail / 2459 expect() across 86 files; `bun run e2e` → **20 passed / 0 skipped / 0 failed** (1.2m). Brief asked for 19/0/0; we went +1 because the new sibling spec is additive. Commits `fa64b6c` (cqmcp-e2e-1: server-side prereqs), `<this>` (cqmcp-e2e-2: e2e specs + globalSetup + fixtures). |
| `D-OUTER7-01` | `[x]` resolved | critical | `packages/web/test/*.test.ts` (33 files) + `packages/web/test/helpers/dom.ts` (new) | Full `bun test` regressed from 611/0/0 on main to 652/18/1 after outer-7 merged. 18 cross-package tests failed (isOriginAllowed×6, smoke×3, ws-origin×2, sdk-stub×3, dev-server×1, mcp-inheritance×1, ledger-MCP REAL SDK×1, toolAliases SDK-MCP spike×1) plus 1 HPE_UNEXPECTED_CONTENT_LENGTH error. Each failing file passed in isolation; failures were ordering- / global-state-dependent. | `@happy-dom/global-registrator`'s `GlobalRegistrator.register()` patches process-global symbols (`document`, `window`, `Request`, `fetch`, `Headers`, …) and the existing 33 web test files never called the symmetric `unregister()`. Bun's test-file discovery order is filesystem-dependent; on main, server tests ran before web tests by chance, so the leak was masked. Adding two new server test files in outer-7 (bridgeFacade.test.ts, codexBridge.test.ts) flipped the discovery order, exposing the leak. The polluted `Request` constructor strips forbidden headers (`Host`, `Origin`) — breaking origin.test.ts's pure unit tests; the polluted `fetch` throws HPE_UNEXPECTED_CONTENT_LENGTH against real Bun HTTP servers — breaking smoke / dev-server / ws-origin / sdk-stub / MockAnthropicHTTP tests. | Introduced `packages/web/test/helpers/dom.ts::registerDom()` which registers happy-dom if absent and installs `afterAll(GlobalRegistrator.unregister)`. All 33 web test files now call `registerDom()`; the shared-global guard pattern (`if (typeof globalThis.document === "undefined")`) is replaced. A short macrotask yield before unregister flushes React 19's scheduler microtasks so they do not throw `ReferenceError: window` between files. Regression test in `packages/web/test/helpers/dom.test.ts` registers DOM, then in a second `afterAll` asserts `globalThis.document === undefined` — fails locally if the cleanup contract regresses (verified by temporarily disabling unregister). Also adjusted `eslint.config.js` to honour `_`-prefixed unused-args (interface stubs in CodexBridge) and refactored `DummyThread.runStreamed`'s `const self = this` to remove a `no-this-alias` violation. `bun test`: 670 pass / 0 fail / 0 error / 2415 expect(). Commit: `1f66a9b`. |
| `D-OUTER7-02` | `[x]` resolved | major | `packages/server/src/agent/codexBridge.ts` (JSDoc) | Brief Q13 required the `mcp__cq__*` ledger MCP tool surface (12 ledger tools + ask_user_question) reachable from Codex sessions; outer-7 shipped Claude-only because `codexBridge.ts` could not wire an in-process MCP server. | `@openai/codex-sdk@0.134.0`'s public types — `ThreadOptions` (dist/index.d.ts line 239: `{model, sandboxMode, workingDirectory, skipGitRepoCheck, modelReasoningEffort, networkAccessEnabled, webSearchMode, webSearchEnabled, approvalPolicy, additionalDirectories}`) and `CodexOptions` (line 216: `{codexPathOverride, baseUrl, apiKey, config, env}`) — expose no `mcpServers` field, no in-process MCP transport, and no callback hook through which cq could surface the in-process LedgerStore. The only available path is `CodexOptions.config`, which is flattened into `--config key=value` overrides for the Codex CLI and would require an external stdio binary. | Resolved per brief option (b) — document the gap with citation. `codexBridge.ts` module JSDoc now cites the exact ThreadOptions / CodexOptions shapes that block in-process injection, plus the (a)+(b) external-binary path that would close the gap (`cq-mcp` stdio binary + per-session `config.mcp_servers.cq` entry). Building that binary is a substantial follow-up feature, not a defect fix, and is deferred. D-GC-1 remains open as the umbrella defect for the missing Codex MCP tools. Commit: `e52d651`. |
| `D-OUTER7-03` | `[x]` resolved | minor | `defects.md` | Prior D-GC-N0 row claimed `docs/drafts/20260528-1404-questions-gear-popup-and-codex.md` did not exist in the repo and called it a "sourcing inconsistency". | The Q&A doc exists on `main` as an UNTRACKED file (the user edits drafts in-place and commits them at their own cadence); the prior agent treated the absence-from-git as a defect rather than checking the working tree. The brief had the answers inlined verbatim — it was authoritative. | Deleted the D-GC-N0 row. The Q&A file itself is left untouched on main per the brief (user's call). Commit: `32d1377`. |
| `D-OUTER7-04` | `[x]` resolved | minor | `tasks.md`, `docs/logs/20260528-1432-gear-codex-log.md` | Outer-7 discharge row stated baseline as "593 pass / 18 fragile-env fail / 611 total" and framed the cycle as "+59 passing tests, no new failures". | Wrong: verified main (777231e) baseline is 611 pass / 0 fail / 0 error / 2202 expect() across 78 files. The "18 fragile-env" framing was a misattribution — those 18 failures were introduced by outer-7 via D-OUTER7-01, not pre-existing on main. | Corrected the outer-7 row in `tasks.md` to cite the verified baseline (611/0/0) and the actual delta (+41 net new passing tests; 18 new failures + 1 error attributed to D-OUTER7-01; fixed in outer-8). Added an outer-8 row covering this defect-fix cycle. Session log under `docs/logs/` updated correspondingly. Commit: `32d1377`. |
| `D-UNIFASYNC-01` | `[x]` resolved | minor | `packages/server/src/agent/codexBridge.ts`, `packages/server/test/codexBridge.test.ts`, `packages/server/test/codexBridge-mcp.test.ts` | `CodexFactory` was typed `(options?: CodexOptions) => Codex \| Promise<Codex>`. The production factory (lines 289–298) is already async (dynamic `import("@openai/codex-sdk")`) and the call site (line 382) already `await`s. The union existed only so the three test factories could return `Codex` synchronously. Violates the user's `feedback-uniform-async` rule: lazily-initialised singletons / memoised factories should be uniformly `Promise<T>`, not `T \| Promise<T>` unions. | Convenience-shaped type for hand-written test dummies — the cost (a sync/async union on a public factory type) was higher than the test convenience saved. | Tightened `CodexFactory` to `(options?: CodexOptions) => Promise<Codex>`. Updated the three test factories (`codexBridge.test.ts:120`, `codexBridge-mcp.test.ts:49,98`) to `async (options?): Promise<Codex> => …`. Verified no other call site narrowed on `instanceof Promise` or wrapped with `Promise.resolve(x)` in `packages/server/src/agent/`. `bun run check`: 689/0/0/2459 expect() across 86 files (unchanged from baseline). `bun run typecheck`: exit 0. Commit: `<this>`. |
| `ASKPROXY-D01` | `[x]` resolved | major | `packages/shared/src/internalProtocol.ts`, `packages/cq-mcp/src/{askBroker,main,internalWs}.ts`, `packages/server/src/agent/{askProxy,codexBridge,bridge}.ts`, `packages/server/src/{server,devServer}.ts` | `ask_user_question` was unavailable on Codex sessions: the cq-mcp stdio binary deliberately omitted it (D-GC-1) because answering a question needs a WebSocket round-trip to the user's browser that a standalone process spawned by the Codex CLI cannot perform. Codex sessions had no way to ask the user a multiple-choice question; only the native approval-policy flow was available. Now unblocked by the outer-13 internal-WS channel. | The outer-13 internal-WS channel (cq-server ↔ cq-mcp, originally for `ledger.changed` cache coherence) provides exactly the back-channel the standalone process lacked; D-GC-1 closed the ledger-tools sub-aspect but explicitly deferred `ask_user_question` to a "future cycle" pending that channel. | Resolved via outer-14 (askproxy-1..4). Two new `InternalWsMessage` variants — `ask.request` (cq-mcp→server: `{askId,toolUseId,sessionId,questions,sourcePid}`) and `ask.reply` (server→cq-mcp: `{askId,answers,sourcePid}`) — carry the proxy. cq-mcp: `CqMcpAskBroker` (two-slot race keyed by askId; `rejectAll` on channel close so the tool returns an error instead of hanging) + an `ask_user_question` tool registered ONLY when the channel is up and `CQ_SESSION_ID` is set; the handler sends `ask.request`, awaits the broker, and returns the Claude-identical `CallToolResult` (`{content:[{type:"text",text:JSON({questions,answers})}]}`). cq-server: `AskProxy` correlates `toolUseId→{askId,sessionId}`, emits the synthetic `AskUserQuestion` assistant tool_use (single factored `buildAskUserQuestionEvent`, rendered by the browser's existing `Stream.tsx` ask card path) via CodexBridge's `sendEvent`, and on the browser's `chat.question_reply` sends `ask.reply` upstream (askId discriminates across connected children). Dispatch is airtight: the Bridge facade already routes `chat.question_reply` to `this.active`, so a Claude-active reply reaches the in-process `AskBroker` and a Codex-active reply reaches the proxy — both directions tested (`bridgeFacade.test.ts`, `codexBridge.test.ts`). CodexBridge threads `CQ_SESSION_ID` into the spawned cq-mcp env alongside the existing `CQ_INTERNAL_WS_*`; no system prompt needed (Codex auto-discovers via tools/list). ClaudeBridge is UNTOUCHED (deferred step 2). Verification: `bun run check` 799/0/2757 across 93 files (baseline 761/0; +38); `bun run e2e` 20/0 (unchanged); `nix build .#default` exit 0, standalone `./result/bin/cq-mcp` tools/list = 13 (no ask_user_question); cross-process integration (`askProxy-integration.test.ts`) proves a real cq-mcp subprocess exposes the tool (14 w/ channel) and round-trips an answer. Commits `d315e03` (askproxy-1 protocol), `e018a77` (askproxy-2 cq-mcp), `2ec0aad` (askproxy-3 server), `13d3775` (askproxy-4 integration). |
| `ASKPROXY-D02` | `[x]` resolved (by-design; documented) | nit | `packages/cq-mcp/src/askBroker.ts`, `packages/server/src/agent/codexBridge.ts` | `ask.reply` is delivered via `InternalWsService.broadcast`, which sends to EVERY connected cq-mcp child; a child that did not originate the `askId` buffers the reply in `CqMcpAskBroker.bufferedReplies` and never consumes it (no matching `ask()` will ever come). In a long-lived process this would be an unbounded map leak. | `InternalWsService` keeps an unkeyed `Set` of sockets and has no per-session send; broadcast + askId-discrimination is the chosen delivery mechanism. | No fix needed: cq-mcp is short-lived per Codex session (one child per session, no reconnection — brief constraint), so the buffer dies with the process. The window in which two children are simultaneously connected is the brief cross-backend-preempt overlap; the stray buffered entry is bounded by that child's remaining lifetime. Documented in the askproxy plan doc and the askproxy-3 Completed entry. If cq-mcp ever becomes long-lived, route `ask.reply` to a specific socket (requires per-session socket tracking in `InternalWsService`) or have the broker drop replies for unknown askIds instead of buffering. |
| `ASKPROXY-D03` | `[x]` resolved (deferred; concrete follow-up) | minor | `packages/e2e/tests/` | No E2E exercises the Codex `ask_user_question` proxy through the browser. A real Codex model reliably emitting `ask_user_question` and a human answering in the UI is hard to make deterministic in Playwright (it depends on the model's choice to call the tool). | The brief explicitly states E2E is not required for this cycle and to defer with a concrete follow-up if no cheap deterministic test is feasible. | Deferred per the brief. Cross-process behaviour IS covered without a browser by `packages/server/test/askProxy-integration.test.ts` (real cq-mcp subprocess; full ask.request→ask.reply→resolved round-trip). Concrete follow-up: add `packages/e2e/tests/codex-ask-roundtrip.spec.ts` modelled on `codex-mcp-roundtrip.spec.ts` — pre-stage a Codex session, send a prompt engineered to force an `ask_user_question` call (e.g. an instruction to ask the user to pick between two options before proceeding), assert the ask card renders, click an option, assert the agent continues. Auth-gate it with the existing `hasCodexAuth()` fixture. Not attempted this cycle (no reliable forcing function for the model + auth-gated). |

## Milestone M-CANON (canon cycle)

| ID | Status | Severity | Location | Description | Root cause | Fix |
|----|--------|----------|----------|-------------|------------|-----|
| `CANON-D01` | `[x]` resolved (env hygiene; not a code defect) | nit | `packages/server/test/dev-server.test.ts:49,80` | The dev-server test uses a shared `cwd: "/tmp"`. With the new bootstrap+divergence guard, a stale `/tmp/docs/ledgers.yaml` left by an earlier msunify-era run (old milestones schema, no idPrefix) makes `FsLedgerStore.init()` throw `BootstrapViolationError`, crashing `startDevServer`. | Pre-existing test-hygiene issue: the test reuses a shared `/tmp` working directory instead of an isolated tmpdir; the divergence guard (correctly) refuses to boot against a divergent on-disk schema. | Cleared the stale `/tmp/docs` ledger files; the divergence guard then provisions fresh. Root-cause fix (isolated per-test tmpdir for dev-server.test.ts) is out of scope for this cycle — the test predates it and the guard behaviour is correct. Recorded for a future hygiene pass. |
| `ASKCARD-D01` | `[x]` resolved | major | `packages/web/src/chat/Cards/AskCard.tsx` | AskCard offers no free-form answer affordance. The `AskUserQuestion` tool contract promises the user can always select "Other" for custom text input (sdk-tools.d.ts:608 region), but cq's renderer only renders the model-supplied options. A user cannot answer with anything outside the provided radio/checkbox labels. Both backends (Claude in-process broker, Codex via the synthetic AskUserQuestion event) render through this single card, so the gap affects both. | The PR-31 renderer was scoped to the supplied options only; the "Other" affordance the tool contract guarantees was never implemented. | Appended a synthetic "Other…" option as the LAST control of every question (radio + checkbox) in `AskCard.tsx`. State: new `otherText: Record<number,string>`; an `OTHER_SENTINEL` (NUL-bearing, cannot collide with a real label) marks Other-active inside `selections`. Selecting/checking Other reveals an inline `<input type="text">` (testid `ask-other-text-<qIdx>`; control testid `ask-other-input-<qIdx>`). `isComplete()` treats an active-but-empty (trimmed) Other as UNANSWERED even when real checkboxes are checked (edge rule 1, code-commented). `handleSubmit` replaces the in-place sentinel with the trimmed text → radio payload `["<text>"]`, checkbox `["RealLabel","<text>"]`. `otherText` is independent of `selections` so radio switch-away preserves typed text (edge rule 2). Whitespace-only ⇒ empty (edge rule 3). No disabled-reason chrome existed; none added (edge rule 4). CSS `.otherText` added to `AskCard.module.css` matching the existing module vocabulary. Reply protocol/`QuestionReplyPayload`/both brokers UNCHANGED — the custom string rides the existing labels array; confirmed `normaliseAnswers` passes it through byte-identically on server (`packages/server/src/agent/askUserQuestion.ts:163`) and cq-mcp (`packages/cq-mcp/src/askBroker.ts:115`) with added lockstep tests. Tests: 6 web cases (`packages/web/test/ask-question.test.ts`), 1 cq-mcp + 1 server passthrough case. `bun run check` 807/0 (baseline 799 + 8). |
| `ASKCARD-D02` | `[x]` resolved (note-only; cosmetic, no functional change) | nit | `packages/web/src/chat/Cards/AskCard.tsx:160-195, 196-249` | If the model supplies a real option whose `label` is literally "Other…" (or "Other"), the card renders TWO "Other…" rows: the model's real radio/checkbox AND the synthetic one. No data corruption (the synthetic control keys on the NUL-bearing `OTHER_SENTINEL`, never on the visible label), but the UI shows a duplicate row. | The synthetic affordance is unconditionally appended; there is no de-duplication against a real option that happens to share the visible label. | No functional fix: the visible-label duplication is cosmetic and the AskUserQuestion contract treats "Other" as the affordance the *renderer* provides, so a model emitting a real "Other" option is already redundant. De-duplicating would require string-matching the visible label, which is fragile (locale/punctuation variants) and out of the brief's scope ("do not redesign the card beyond adding the Other affordance"). Recorded for future consideration only. |
| `ASKCARD-D03` | `[x]` resolved (by-design; documented) | nit | `packages/web/test/ask-question.test.ts` setText helper | The web test's `setText` helper invokes the controlled input's React `onChange` directly off the fiber `__reactProps$*` key rather than dispatching a DOM event. This couples the test to a React-internal property name. | happy-dom + React 19 + bun do not deliver a synthetically-dispatched `input` event to React's ChangeEventPlugin for text inputs (verified empirically: `<select>`/checkbox change propagates, text-input input/change does not, regardless of native-value-setter vs plain assignment). The fiber-props path is the deterministic alternative. | Resolved by design with a code comment explaining the harness limitation and the chosen approach. The helper throws a clear error if the props key or onChange is absent, so a React internals change surfaces immediately rather than silently passing. Confined to the one test helper; production code is unaffected. |
| `TESTHYG-D01` | `[x]` resolved | major | `packages/server/test/{smoke,ws-basic,ws-origin,dev-server}.test.ts` | Server-boot tests trip the canon-cycle schema-divergence guard / pollute the repo. `smoke`/`ws-basic`/`ws-origin` spawned `main.ts` with `--port/--host` but NO `--cwd`, so the server defaulted to `process.cwd()` (the repo root) and ran `FsLedgerStore.init()` against `<repo>/docs/`. A pre-§8c `docs/ledgers.yaml` there (old `blocked`/`depends` field names) makes `init()` throw `BootstrapViolationError` → the server never logs `"cq listening"` → `beforeAll` times out (reproduced: 3/3 server-boot suites fail with stale `docs/ledgers.yaml` present; manual spawn confirms `BootstrapViolationError: existing milestones ledger has a different schema than its canonical bootstrap schema` at `FsLedgerStore.ts:205`). Even when green, every run bootstrapped files into the repo `docs/`. `dev-server.test.ts` had the same hazard via a fixed `cwd:"/tmp"` shared across runs (flagged by CANON-D01): `startDevServer` constructs `new FsLedgerStore({root:cwd})` and `init()`s. | Tests depended on a shared, possibly-stale cwd (`process.cwd()` for spawns, `/tmp` for dev-server) instead of an isolated per-test directory. The divergence guard is correct (intended behavior, NOT weakened); the tests were wrong to read a shared docs root. | TEST-ONLY. Each server-boot test now `mkdtemp`s a fresh per-test cwd: spawned servers pass `--cwd <tmp>` (smoke/ws-basic/ws-origin); in-process `startDevServer` passes the tmp dir as `cwd`/`dbPath` (dev-server, via `beforeEach`/`afterEach`). All tmp dirs `rm -rf`'d in teardown. Verified: with a deliberately-divergent `docs/ledgers.yaml` present, the four suites are green (13 pass / 0 fail) and the repo `docs/` is no longer polluted. No production code changed. Refs CANON-D01. Commit: `3a6eec2`. |
| `TESTHYG-D02` | `[x]` resolved | minor | `packages/web/test/setup.ts` (removed); stale comments in `cards/header/grep-card/task-list/subagent.test.ts` | happy-dom global-leak landmine of the D-OUTER7-01 class. `setup.ts` called `GlobalRegistrator.register()` at module load with NO matching `unregister()`, patching process globals (`document`/`window`/`Request`/`fetch`/`Headers`/timers). Reproduced that this exact pattern leaks: a register-without-unregister file placed before `packages/server/test/origin.test.ts` in one bun-test process breaks 6 of its pure unit tests (polluted `Request` strips `Host`/`Origin`). `setup.ts` claimed it was "Referenced via bunfig.toml [test] preload", but the repo's `bunfig.toml` intentionally omits any preload and nothing imports `setup.ts` — it was orphaned dead code, a latent landmine that would re-introduce the leak the moment a preload/import was restored. Five web test files carried stale comments asserting the non-existent bunfig/setup.ts preload. | The D-OUTER7-01 fix moved all `*.test.ts` files onto the shared `registerDom()` + `afterAll(unregister)` helper (verified: under the full suite, register/unregister are paired 33/33 with zero conditional-skip; default + reversed + web-before-server file orders all green), but the superseded `setup.ts` preload script and its referencing comments were left behind. | TEST-ONLY. Removed the orphaned `packages/web/test/setup.ts` leaker; corrected the five stale comments to describe the actual per-file `registerDom()` mechanism. The `helpers/dom.test.ts` regression guard (fails if the helper stops unregistering) is retained and still passes. Verified `bun run check` green 2× deterministically (858/0). NOTE for reviewers: `bun test --randomize` surfaces failures that are NOT this leak — they are intra-file ordering contracts (e.g. `smoke.test.ts` boots one shared server in `beforeAll` and kills it in the SIGINT test; randomizing intra-file order runs the kill before the fetch tests → ConnectionRefused) and a timing-sensitive `ledger/concurrency.test.ts`; these are out of scope for the happy-dom file-order leak and `bun run check` does not randomize. Refs D-OUTER7-01. |
| `WF-D01` | `[x]` resolved (codexwf — Codex producer + all phase subagents via the cq-mcp `submit_*` relay) | major | `packages/shared/src/internalProtocol.ts`, `packages/cq-mcp/src/{submitBroker,main}.ts`, `packages/server/src/workflow/{workflowSubmitProxy,codexHeadless,codexProducer,codexPhaseSubagent,phases}.ts`, `packages/server/src/server.ts` | `/plan` worked on Claude but NOT Codex (was deferred). The Claude producer + phase subagents force structured output via in-process harness-owned `submit_*` MCP tools; the Codex SDK `@openai/codex-sdk@0.134.0` (`Thread.runStreamed`) has NO in-process forced-tool mechanism — its only MCP surface is the standalone `cq-mcp` stdio binary. Option (a) was approved: expose a harness-owned `submit_*` tool through cq-mcp that RELAYS the structured payload back to the cq server over the internal-WS channel (the askproxy pattern), where the WorkflowRuntime validates + writes — so the HARNESS still owns every ledger write; cq-mcp does NOT write ledgers for the submit tool. | The Codex CLI auto-discovers MCP tools via `tools/list` and runs them through the spawned cq-mcp child. A per-dispatch cq-mcp child is primed via env (`CQ_WORKFLOW_SUBMIT_ID` + `CQ_WORKFLOW_PHASE`, alongside the askproxy `CQ_INTERNAL_WS_URL/TOKEN`) and exposes a single harness-owned `submit_workflow_phase` tool. The model calls it; the child relays `workflow.submit{submitId,phase,payload}` over the internal WS and parks on a `CqMcpSubmitBroker` (submitId-keyed two-slot race machine, mirrors `CqMcpAskBroker`); the server's `WorkflowSubmitProxy` correlates submitId → the waiting headless-Codex dispatch, validates `payload` against THAT phase's Zod schema, resolves the dispatch (the runtime then writes the ledgers exactly as on Claude), and replies `workflow.submit_ack`. | RESOLVED (codexwf, 6 PRs). NEW internal-WS variants `workflow.submit`/`workflow.submit_ack` (Zod round-trip tested). cq-mcp `submit_workflow_phase` tool registered ONLY with the workflow env present (tools/list = 14 vs 13 standalone — proven by `submitRelay.e2e.test.ts` against a fake WS server). `WorkflowSubmitProxy` validates per-phase and ack{ok:false} on a malformed payload WITHOUT writing a ledger (the dispatch stays pending so the model can resubmit). `codexHeadless.dispatchCodexPhase` is the shared headless lane (Codex analogue of `headlessQuery.ts`): registers the correlation BEFORE starting the thread, spawns a headless Codex thread (own `Codex` instance — never the pool=1 interactive Bridge; sandbox `danger-full-access` + `approvalPolicy="never"` so the Codex CLI does not auto-CANCEL the model's MCP tool call — discovered empirically: the default approval/non-danger sandbox in the headless `exec` lane cancels the call with "user cancelled MCP tool call"), awaits the relayed+validated payload, and always `proxy.reject(submitId)` on teardown (no dangling correlation, no hang). `CodexProducer`/`CodexPhaseSubagent` are thin wrappers; they NEVER write ledgers. TESTS: `workflow-codex-relay-integration` drives the FULL produce→clarify→plan→review→planned loop on `platform="codex"` against a fake codex thread + the REAL proxy/runtime/loop (HARNESS-writes asserted; a malformed payload acked false + NO ledger write); `workflow-codex-pool1-regression` proves the Codex lane never disturbs interactive chat (mid-flight + on failure); `workflow-codex-real` (auth-gated) drives a REAL Codex producer through the relay and asserts goal+spec+questions land on disk, plus a real clarify-reviewer phase running via the relay. REAL-CODEX RELIABILITY (observed): the model RELIABLY calls `submit_workflow_phase` (13/13 phase dispatches in a long run); it typically makes ONE malformed first attempt (spreading the result's fields as separate tool args / flattening `goal` to a string) then RESUBMITS a correct payload after the proxy's ack{ok:false,error} — so the resubmit-on-validation-failure path is load-bearing and proven. Full loop convergence to `planned` is NOT asserted against real Codex because the real clarify-reviewer is a model-judgement (observed perpetually-unsatisfied with a generic answer batch — NOT a relay defect); convergence is the deterministic fake-driven test. No `LedgerBusyError` observed (LOCK-D01 lockfile-wait holds for the concurrent cq-server + cq-mcp child writes). Refs WF-D01 follow-up option (a), design-doc Q8. |
| `WF-D02` | `[x]` resolved (cycle 3 — ledger-as-state resume) | minor | `packages/server/src/workflow/workflowRuntime.ts` | Durable mid-flight workflow resume across a server restart was not implemented in phase 1 (a cycle-3 concern). The workflow's position was tracked only in an in-memory registry, so a restart mid-loop could strand a goal. | Phase 1 tracked active workflows only in memory (Q D); there was no derivation of the workflow position from durable state. | RESOLVED in cycle 3 (wfl-2/wfl-3). Workflow state is now DERIVED FROM THE LEDGERS (K-WFL-1): `WorkflowRuntime.derivePosition(goalId)` is a pure function of goal.status + the goal's open-question count, returning `terminal` / `awaiting_answers` / `clarify_ready` / `review_ready`. `reconcile()` runs once after `store.init()` in `server.ts`: every non-terminal goal that is `clarify_ready` or `review_ready` is re-dispatched; a goal with open questions sits idle until a `question.answer` arrives. The only in-memory state left is the global busy slot + per-goal in-flight latch (idempotency) + the per-goal no-progress fingerprint (in-memory by design — a fresh revise round after resume re-establishes it). Proven by `workflow-loops.test.ts` "resume-on-startup reconcile": a runtime is dropped mid-review (the reviewer threw), a NEW runtime is created against the SAME ledger dir, `derivePosition` returns `review_ready`, `reconcile()` resumes to `planned` with NO double-dispatch; a second test asserts a goal awaiting open questions sits idle (no dispatch) on reconcile. |
| `WFL-D01` | `[x]` resolved (escalation handler shipped, goals cycle — guard itself still USER-VETO-INVITED) | minor | `packages/server/src/workflow/workflowRuntime.ts` (`revisePlanWithGuard`) | Per Q6 there is NO hard round cap on the plan-review loop; convergence relies on the reviewer's verdict. To stop a pathological reviewer↔planner runaway from spinning without human input, a NO-PROGRESS liveness guard escalates: if a revise round produces a planner output whose structural fingerprint (milestone titles+descs, task headline/desc/acceptance/ref) is IDENTICAL to the prior revise round AND the reviewer raised no new questions, the runtime emits `workflow.event{status:"escalated"}` and STOPS the loop, asking the user to {proceed-as-is / give-guidance / abandon}. The goal stays `planning` so a future guidance answer can resume it. | A reviewer that is never satisfied and a planner that never changes its output would otherwise recurse unbounded (Q6 forbids a cap). The guard is the liveness backstop. | BY-DESIGN, flagged for USER VETO. The guard fires on the SECOND identical revise round (the first establishes the fingerprint). It is NOT a round cap — a loop that keeps making real progress (changing plans or raising questions) never escalates. The escalation `detail` enumerates the three options and the *handling* of the user's choice is NOW WIRED (goals cycle): the `workflow.escalation_reply{goalId, choice, guidance?}` inbound frame is handled in `workflowRuntime.ts` — `proceed` → goal `planned` + done; `guidance` → re-dispatch the planner with the appended guidance, resuming the plan-review loop; `abandon` → goal `abandoned`. The Goals-tab escalation banner emits this frame. Proven by `workflow-loops.test.ts` "no-progress liveness guard" (escalation fires, planner bounded ≤4×) plus the goals-cycle escalation_reply tests (proceed/guidance/abandon outcomes) and the Goals-tab UI test (banner emits the frame). The GUARD ITSELF (its existence + fingerprint definition) remains flagged for USER VETO — veto it here if undesired; the handler is independent of that decision. |
| `WFL-D02` | `[x]` resolved (wfl-6 — root cause isolated + deterministic fix) | minor | `packages/e2e/playwright.config.ts`, `packages/e2e/tests/plan-workflow-loop.spec.ts`, `packages/server/src/server.ts`, `packages/server/src/workflow/{workflowRuntime,phases,producer,claudeProducer,claudePhaseSubagent}.ts` | The e2e suite was intermittently red: `bun run e2e` run-to-run flipped between 23/23 green and a `stop.spec`/`plan-workflow*` failure. The wfl-5 row attributed this to generic "environmental CPU contention" and papered over it with raised timeouts — that diagnosis was WRONG and is corrected here. | TWO distinct defects, both rooted in subprocesses that outlive the spec that spawned them — confirmed empirically (cq-server logs + isolation runs), not assumed. **(1) Cross-process ledger-lock race.** A Codex spec (`codex-mcp-roundtrip`/`codex-roundtrip`) drives a REAL Codex model that calls `mcp__cq__*`; the Codex CLI spawns a long-lived `cq-mcp` child that opens its OWN `FsLedgerStore` on the shared `--cwd` and issues ledger writes, taking the cross-process advisory lockfile (`packages/ledger/src/store/lockfile.ts`). That child persists for the whole server lifetime. When a later `/plan` workflow producer wrote the goals ledger from the cq SERVER process, the two writers raced; the lockfile fails FAST on a live-pid `EEXIST` (no wait/retry) → `LedgerBusyError` → `workflow.errored` ("Ledger goals is locked by pid N on vm", observed in `cq-server` logs) → `questions_ready` banner never appears → `plan-workflow*` times out. Excluding the Codex specs made the workflow specs 21/21 green with ZERO lock errors across 3 runs (isolation proof). **(2) `stop.spec` starved by lingering subprocesses.** The lingering Codex CLI + `cq-mcp` child (and not-fully-reaped workflow `query()` subprocesses) kept consuming CPU, tipping `stop.spec`'s tight 5 s "Stop button DISABLED after drain" assertion (line 87, MutationObserver microtask race; see E2E-D03) into a spurious `toBeDisabled` timeout (observed: 5.8 s fail vs ~2 s normal). Secondary contributor: `WorkflowRuntime`/`ClaudeBridge` cleared their busy slot at the producer's submit-time, BEFORE `query().close()` finished reaping the child — so "idle" did not mean "subprocess gone". | Two surgical, in-scope fixes; NO change to workflow phase logic, loop semantics, the no-progress guard, or ledger writes; NO `stop.spec` timeout bumps; NO `.skip`. **(a) Uniform-async "fully drained" awaitable.** `ClaudeProducer`/`ClaudePhaseSubagent` now surface their post-`close()` `drain` promise (the SDK async-generator returning ⇒ subprocess reaped) via a new optional `registerTeardown: TeardownSink` on `ProduceRequest`/`PhaseRequest`. `WorkflowRuntime` tracks these in a `pendingTeardowns` set and exposes `whenDrained(): Promise<void>` (always `Promise<void>` — no sync/async union) that resolves only when no phase is dispatching AND every spawned subprocess is reaped. The producer/phase methods still resolve at submit-time (production timing UNCHANGED — `whenDrained` is auxiliary). New E2E hook `POST /__e2e/workflow-drain` awaits it; the loop spec's teardown calls it (replacing the unreliable `/__e2e/workflow-idle` busy-poll + 2 s magic-sleep), and `POST /__e2e/interrupt`'s afterEach now `await workflow.whenDrained()` after `abortActive()` so no workflow child leaks across specs. **(b) Ordering isolation (Playwright project split).** A `prelude` project runs `plan-workflow-loop`, `plan-workflow`, and `stop` FIRST; a `main` project (`dependencies: ["prelude"]`, `workers:1`) runs everything else (including the Codex specs) AFTER. With the workflow specs and `stop.spec` running before any Codex `cq-mcp` child exists, the cq server is the sole ledger writer at producer time (kills defect 1) and `stop.spec` runs against a fresh, unloaded server (kills defect 2). EVIDENCE (`bun run e2e`, foreground, read from durable per-run logs): 5 consecutive runs all 23/23 green, zero ledger-lock errors, `stop.spec` 2.4 s / 2.6 s / 2.1 s / 2.0 s / 2.2 s (was flaking at 5.8 s under load). Unit+integration `bun run check` 907 pass / 0 fail (baseline 905 + 2 new `whenDrained` tests in `workflow-runtime.test.ts`); `bun run typecheck` clean. Codex phase relay remains deferred (WF-D01). |
| `LOCK-D01` | `[x]` resolved (lockfile-wait cycle — repro on old code + deterministic fix + cross-instance regression) | major | `packages/ledger/src/store/lockfile.ts` (`acquire`, `readHolder`); tests `packages/ledger/test/lockfile.test.ts`, `packages/ledger/test/concurrency.test.ts` | `Lockfile.acquire()` failed FAST on an `EEXIST` whose holder PID was alive: it threw `LedgerBusyError` immediately, with no wait or retry. That was correct when only ONE process wrote ledgers, but cq now has TWO legitimate concurrent ledger writers on the same cwd — the cq SERVER's in-process `FsLedgerStore` (Claude path + the `/plan` `WorkflowRuntime`) and the long-lived `cq-mcp` CHILD's own `FsLedgerStore` (Codex sessions). Whenever the `cq-mcp` child held the advisory lockfile and the cq server tried to write (or vice versa), the second writer got `LedgerBusyError` and the operation FAILED instead of briefly waiting for the short write-through critical section to release. This is the production root cause behind WFL-D02 defect (1): a `/plan` producer write to `goals` raced a live `cq-mcp` child → `workflow.errored` ("Ledger goals is locked by pid N on vm"). REPRODUCED here (not assumed): with the old fail-fast code, the new cross-instance test threw `LedgerBusyError: Ledger __milestones__ is locked by pid N on vm` (stashed `lockfile.ts`, ran the LOCK-D01 tests → red for the expected reason). | Two writers contend on a per-ledger advisory file lock whose acquire primitive (`open(O_CREAT|O_EXCL)`) is correct but whose CONTENTION POLICY was fail-fast on a live holder, contradicting the ledger's founding requirement ("proper lockfiles, no race conditions"). Cross-process no-lost-write is a TWO-layer guarantee: the file lock serialises writes (no torn file) AND the D-COHERENCE channel (`onMutation`→peer `invalidate`, relayed over InternalWs) makes each store re-read the peer's committed state before its next write; the lock alone does not merge two stores' independent in-memory snapshots (verified: two stores firing concurrent writes with the relay DISABLED lose half the writes, 15/30). A second, latent fragility surfaced under the new waiting loop's repeated reads: `readHolder` parsed the lockfile body with `JSON.parse`, and an EMPTY body (a holder observed AFTER `open(O_EXCL)` but BEFORE its `writeFile` lands — a mid-write race far likelier when polling under load) threw `SyntaxError`, which the catch-all branch converted into a hard `LedgerBusyError(pid 0, unknown)`. This made the full-suite run flake (`pid 0 on unknown`) where the isolated run passed. | (1) `acquire()` now WAITS on a LIVE holder: on `EEXIST` it polls `tryOnce` every `pollIntervalMs` (default 25 ms) until it acquires or `acquireTimeoutMs` (default 5000 ms, BOUNDED so a genuinely stuck holder still surfaces `LedgerBusyError`) elapses. Dead-PID stale reclaim (unlink + retry) is PRESERVED unchanged. `O_CREAT|O_EXCL` remains the sole atomic acquire arbiter, so concurrent waiters cannot both win (no two-acquirer). All paths stay `Promise<T>` (no sync/async union); every wait is `await this.sleep(...)`. New injectable opts `acquireTimeoutMs`/`pollIntervalMs`/`sleep` (alongside existing `isPidAlive`/`now`/`selfPid`/`selfHostname`) make tests deterministic with a fake clock — no real-time sleeps. (2) `readHolder` now returns a discriminated `HolderProbe` (`gone` | `transient` | `holder`): an empty/unparseable body is classified `transient` (a holder mid-write) and the wait loop POLLS AGAIN WITHOUT reclaiming — reclaiming a mid-write lock would yank a live holder's in-flight lock and risk two acquirers. Only a confirmed dead `holder` is reclaimed; only a genuine I/O error (EACCES/EIO) still throws. TESTS: `lockfile.test.ts` — live-holder-never-releases → `LedgerBusyError` after a BOUNDED wait (asserts ≥1 poll, i.e. did NOT fail fast; fake clock); live-holder-releases-within-window → ACQUIRES (sleep deletes the lockfile on first poll); holder-dies-WHILE-waiting → reclaim + acquire (liveness flips false after first probe); dead-holder reclaim retained. `concurrency.test.ts` — TWO `FsLedgerStore` instances on ONE cwd (cq-server + cq-mcp simulation, both pids alive ⇒ the WAIT path, not dead-reclaim) with the D-COHERENCE relay wired: BOTH complete, final on-disk file parses, no lost write (2N items, N from each, unique ids) — and a sharper variant where two stores write DIFFERENT ledgers but contend on the shared `__milestones__.lock` and NEVER throw `LedgerBusyError` (this is the variant that was red on old code). Load-bearing proof: disabling the relay drops the no-lost-write count to 15/30; the old fail-fast code throws `LedgerBusyError` on the contention variant. DISCHARGE: `bun run check` exit 0 TWICE (911 pass / 0 fail; baseline 907 + 4 net new tests); `bun run e2e` 23/23 (the Codex cq-server↔cq-mcp specs pass — the workflow no longer ERRORS on a live `cq-mcp` holder); `nix build .#default` exit 0. SCOPE: ledger library + tests only; WorkflowRuntime/bridges/e2e specs untouched; schema-divergence guard untouched. NOTE: the WFL-D02 e2e Playwright project-ordering workaround is now belt-and-suspenders for the lock race (the lock waits instead of erroring) — it is RETAINED this cycle because it also addresses the WFL-D02 defect-(2) CPU-starvation half, but it is no longer load-bearing for the lock race. |
