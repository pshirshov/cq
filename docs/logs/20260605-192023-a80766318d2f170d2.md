# Implement-flow session log

- **Task:** T162 (milestone M54, goal G16, fixes D29) — backend StatusChangePrecondition
- **Role:** worker (implement-worker)
- **Returned status:** pass (resultCommit 0b5e98f9d71ea0729bda34b4c6725c04cd368379, branch implement/T162)

---

### Session summary
- **Did:** implemented task T162 in worktree branch `implement/T162` (local worktree branch `worktree-agent-a80766318d2f170d2`).
- **Achieved:** pass; resultCommit `0b5e98f9d71ea0729bda34b4c6725c04cd368379`; `bun run check` green (tsc clean, eslint clean, 889 tests pass / 0 fail).
- **Discovered:** Three pre-existing scaffolding sites transitioned a `questions` item to `answered` via `updateItem` with NO answer field (`goal-preconditions.test.ts` tests 2 & 10, `store-abstract.ts` snapshot suite). Under the strict invariant from D29 — effective answer `(patch.fields?.answer ?? item.fields.answer)` must be a non-empty trimmed string — these would have regressed, so I added a non-empty answer to each. `create_item`-at-`answered` paths are NOT affected because the precondition only fires on a status *change* via `applyUpdateItem`, not on create. Chose `SchemaValidationError` (answer-content semantic fit) over `GoalPreconditionError`.
- **Issues:** none. The guard treats an absent answer field (`undefined`) as a violation, consistent with the defect's effective-answer definition.

filesTouched: core.ts (assertQuestionAnswerPrecondition), FsLedgerStore.ts (statusChangePrecondition helper + wiring), InMemoryLedgerStore.ts (mirror), constants.ts (QUESTIONS_ANSWER_FIELD), test/question-answer-precondition.test.ts (new dual-test), test/goal-preconditions.test.ts + test/store-abstract.ts (scaffolding answer updates).
checkSummary: tsc clean; eslint clean; bun test 889 pass/0 fail. New dual-test: 14 pass (7 cases × 2 stores). Reproduce-first confirmed: 8 rejection assertions failed without the guard, green with it.
