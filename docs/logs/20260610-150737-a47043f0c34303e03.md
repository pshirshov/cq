# Session log — G44 plan-advance (opus), clarifying round 1

- **Goal:** G44 ("cq:advance turn-pause Stop-hook gate (fix D50)"); role: planner (single-planner mode); returned **awaiting-answers**

### Session summary
G44 was fresh in `clarifying` with no questions; read its full description + linked defect D50 (root-caused), advance.md (the gate prose), the cq-cli native-subcommand pattern, the D39 `assertHandoffInvariants` precedent (@cq/ledger store/core.ts), and nix/hm/dev-llm.nix (harness provisioning, candidate hook-install location). Filed the first batch of 7 clarifying questions (all `open`, linked `goals:G44`, under M150) and made no plan/transition:
- **Q198** — Stop-hook install location/mechanism: repo-level `.claude/settings.json` vs user-level `~/.claude` vs Nix `dev-llm.nix`; is install in-scope.
- **Q199** — `cq advance-gate` output contract + mapping to Claude Code's Stop-hook `{decision:'block',reason}` protocol; CLI-direct vs neutral-verdict-plus-wrapper.
- **Q200** — run-active marker location (`/tmp`/`$XDG_RUNTIME_DIR` keyed on `$CLAUDE_CODE_SESSION_ID` vs docs/ vs .git/) + lifecycle + how hook/advance.md derive the same path without racing.
- **Q201** — external-signal escape: exact verbatim format (`external-signal: "<quote>"`), where written so a no-MCP CLI sees it, and the self-attestation limit.
- **Q202** — predicate re-derivation without MCP: shared `derivePredicates` in @cq/ledger vs CLI-owned vs standalone; confirm "no-MCP" = no running server process, not no library import.
- **Q203** — scope of advance.md edits (exact sections for marker drop/remove + external-signal) and whether sibling standalone commands need the marker.
- **Q204** — acceptance/verification bar (dual-adapter unit tests of the gate verdict + hook stdout/exit integration test + documented manual repro + a D39-style grep-invariant), accepting the irreducible behavioral limit.

Codegraph index unavailable this session — fell back to Read/fts_search (no impact on the clarifying pass).
