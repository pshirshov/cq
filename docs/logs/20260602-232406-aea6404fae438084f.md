# implement-worker — T112 (D15) — pass

- Task: T112 (M30, goal G7, defect D15) — de-flake ledger-tui live-badge test
- Returned: `pass` — resultCommit af4220b55dbc6af1a54b1ad0f4d0d25e5d7a90e0 (worktree-agent-aea6404fae438084f)

### Session summary
- **Achieved:** pass; resultCommit af4220b; `bun run check` green (677 pass, 0 fail); deterministic across 10 repeated runs.
- **Discovered:** the race has TWO components — (1) React passive effects updating refreshRef.current with the fresh reloadItems closure hadn't fired by tick(30) because the ink render took ~100ms; (2) the push fired with a stale reloadItems (top.kind=ledgers) so it returned early without fetchLedger. Fix puts the push INSIDE the polling loop so each iteration gives effects more time; once refreshRef updates, the next push triggers the correct refetch.
- **Issues:** the approach (re-push every iteration, 50×20ms) is slightly broader than "single push + bounded wait" but is the minimal change that makes it deterministic.
- filesTouched: packages/ledger-tui/test/app.test.tsx
