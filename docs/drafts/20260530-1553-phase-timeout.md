# Cycle phase-timeout — raise workflow phase-dispatch timeout to 300s + env override

Worktree `/home/pavel/work/safe/cqe/cq1/.claude/worktrees/phase-timeout`,
branch `phase-timeout`, base `5d0408c`. Baseline `bun test` 1077/0; e2e 28/28.

## Why

In a live dogfood `clarify-reviewer#1` timed out. PLAN-D01 gave phase subagents
read-only repo access + an "explore the repository first" instruction, so each
phase reads CLAUDE.md / source / ledgers before submitting — on a large repo
this exceeds the current 120 s dispatch timeout. Symptom fix only; the deeper
"explore once in the producer, pass grounding to later phases" optimization is a
SEPARATE later cycle and is OUT OF SCOPE here.

## Change (one milestone, one PR)

- Raise default phase-dispatch timeout 120_000 → 300_000 (5 min) in all three
  lanes: `claudeProducer.ts` (`DEFAULT_TIMEOUT_MS`), `claudePhaseSubagent.ts`
  (`DEFAULT_TIMEOUT_MS`), `codexHeadless.ts` (`DEFAULT_CODEX_TIMEOUT_MS`).
  Update doc-comments mentioning "120 s".
- ENV override `CQ_WORKFLOW_PHASE_TIMEOUT_MS` via a small SHARED helper.
  Invalid/absent → 300_000 default.
- Resolution order per dispatch: explicit `opts.timeoutMs` (if provided) →
  `CQ_WORKFLOW_PHASE_TIMEOUT_MS` (valid positive integer) → 300_000.
- Keep timeout-rejection message format
  (`<label> timed out after <ms>ms without submitting`).
- ENV ONLY — do NOT touch `args.ts`/`main.ts`/`server.ts`/`FsLedgerStore`
  (in-flight cq-reset cycle owns those). Injectable `timeoutMs` opt still wins.

## Plan

1. Add shared helper `resolvePhaseTimeoutMs(optTimeoutMs?: number): number` in a
   new module `packages/server/src/workflow/phaseTimeout.ts`
   (const `DEFAULT_PHASE_TIMEOUT_MS = 300_000`, env var name constant).
   → verify: unit test on the helper (default / env-valid / env-invalid / opt-wins).
2. Wire the three lanes to the helper; remove their local `DEFAULT_*_TIMEOUT_MS`
   constants (re-export from the helper if they are imported elsewhere — check).
   Update doc-comments.
   → verify: `rg` shows no remaining `120_000` dispatch default; three lanes call helper.
3. Tests: helper unit + each lane resolves to 300_000 by default, honors env,
   honors explicit opt over env. try/finally env restore (no cross-test leak).
   → verify: `bun run check` exit 0 ×2 (determinism, no env leak).
4. Discharge: `bun run check` ×2, `bun run e2e` 28/28, `nix build .#default`.
   defects.md PHASE-TIMEOUT-01 row.

## Acceptance criteria

- All three lanes resolve to 300_000 when neither opt nor env set.
- `CQ_WORKFLOW_PHASE_TIMEOUT_MS=NNN` (valid positive int) overrides default in all three.
- Invalid env (non-numeric / ≤0 / float? — define: parseInt, require > 0) → 300_000.
- Explicit `opts.timeoutMs` wins over env in all three.
- No cross-test env pollution (set/restore in try/finally or before/afterEach).
- e2e timeouts unaffected (mock uses injected/short timeouts) — still fast.

## Out of scope (do NOT)

- CLI flag / `args.ts`/`main.ts`/`server.ts`/`FsLedgerStore` edits.
- Phase/loop semantics, explore-first prompt, read-only gate, ledger writes.
- The explore-once optimization (separate later cycle).
- sync/async unions. Touching main / other worktrees. History rewrite.
