## M60

### H22 — wrong

- createdAt: 2026-06-05T22:10:07.637Z
- updatedAt: 2026-06-05T22:15:24.496Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: T163 (D29 web UX guard) changed BatchAnswerModal so answering a non-last question dismisses the modal instead of advancing to the next unanswered row
- description: "TRUE if: the BatchAnswerModal's post-save behavior should ADVANCE to the next unanswered question (closing only when the batch is exhausted), but after T163's edits (adding `disabled={!answerHasText}` to the submit + changing `answerHasText` init/reset to read the stored answer + the batchSave/onSave path) the modal now CLOSES after a single non-last answer. Sub-possibilities: (a) the advance logic keys off a list/derivation that T163's answerHasText-reset effect or a `key={row.item.id}` change perturbs; (b) the modal's close condition (e.g. `rows.length===0` after the answered row drops out, or a reset-to-no-selection) fires too early; (c) the batchModalClose.test.tsx edits T163 made encode the BUGGY close-on-any-answer behavior, masking the regression. Confirmed by reading the BatchAnswerModal advance/close logic in ledger-web/src/App.tsx and diffing it against the pre-T163 (pre-6629483) version."
- ledgerRefs: ["defects:D31"]
- evidence: ["[correct] ledger-web/src/App.tsx:543-571 — batchSave: on save, adds row to batchAnsweredRef, computes `remaining`, `setBatchOpen(false)` ONLY when remaining.length===0, ELSE advances batchIndex to the next unanswered row. Non-last answer advances, does not dismiss. (re-validated, exact)","[correct] ledger-web/src/App.tsx:1695-1752 — ALL THREE modal save buttons call the SAME `onSave(row, ...)`=batchSave: 'save & mark answered' (1738), 'as recommended' (1747, value AS_RECOMMENDED_ANSWER), per-suggestion 'pick' (1699). None calls onClose. So the user's 'as recommended' uncertainty resolves to the identical correct advance path. (re-validated, exact)","[correct] `git log 6629483..HEAD -- ledger-web/src/App.tsx` returns EMPTY — NO commit touched this file after T163 (6629483). The current source IS the post-T163 source. (re-validated)","[correct] ledger-web/src/App.tsx:277-279,528,1012 — batchRows/batchOpen/batchSchema are captured state set at modal-open (531/528); reload() (469) refreshes ledger items but never resets them; modal render guard is `batchOpen && batchSchema!==null`. reload() cannot close the modal. (re-validated)","[correct] ledger-web/test/batchModalClose.test.tsx:233-257 — test 'mid-queue answer (Q2 of 3) advances to Q3, not back to Q1' asserts batch-overlay stays non-null after a non-last answer + progress 'X of 3' advances. This is EXACTLY the user-reported behavior, asserted CORRECT. (re-validated, exact)","[correct] `bun test batchModalClose.test.tsx` at HEAD => 6 pass / 0 fail — the green reproduction harness shows the modal advances on non-last answers and closes only on exhaustion. (re-run by orchestrator)"]
- sessionLogs: ["docs/logs/20260605-221259-a78481c5e1824403a.md"]

### H24 — confirmed

- createdAt: 2026-06-06T10:51:26.719Z
- updatedAt: 2026-06-06T10:54:56.073Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: "BatchAnswerModal closes on a backdrop click/mouseup that did NOT start on the backdrop: a press-and-hold on 'save & mark answered' that completes, advances to a shorter question, shrinks the modal, leaves the still-held pointer over the backdrop, and the release fires the close-on-outside-click handler"
- description: "What would make this true (per the user's confirmed repro): the BatchAnswerModal overlay registers a dismiss handler that closes the modal on a CLICK or MOUSEUP whose target is the backdrop/overlay (the area outside the dialog box), WITHOUT requiring that the corresponding MOUSEDOWN (press start) also occurred on the backdrop. Sequence: (1) user presses-and-holds the HoldButton 'save & mark answered' (HOLD_MS gate from G11/F1) — mousedown is INSIDE the dialog, on the button; (2) the hold completes → batchSave advances batchIndex to the next, SHORTER question → the dialog re-renders smaller (vertical resize); (3) the user is still holding the mouse button (can't release within the gate); (4) because the dialog shrank, the pointer is now over the BACKDROP, not the dialog; (5) the user releases → a click/mouseup fires with target = backdrop → the close-on-outside handler dismisses the modal. To confirm: locate the modal's backdrop/overlay close handler in ledger-web (App.tsx BatchAnswerModal / its overlay container) and verify it keys on a target-is-backdrop CLICK or MOUSEUP rather than a press that STARTED on the backdrop (mousedown→mouseup both on backdrop). The robust fix is to require the press to have begun on the backdrop (track mousedown target; only close if BOTH mousedown and mouseup/click hit the backdrop), or otherwise ignore a backdrop release that is the tail of a hold/drag started inside the dialog."
- ledgerRefs: ["defects:D31"]
- evidence: ["[correct] ledger-web/src/App.tsx (batch overlay) — `<div className=\"lw-help-backdrop\" data-testid=\"batch-overlay\" onClick={onClose}>` with inner dialog `onClick={(e) => e.stopPropagation()}` — backdrop closes on ANY click whose common-ancestor is the backdrop; NO mousedown-start guard. (orchestrator-validated via grep)","[correct] ledger-web/src/App.tsx — `onClose={() => setBatchOpen(false)}` — a backdrop-reachable click directly dismisses the modal (same setter as the queue-drain path).","[correct] ledger-web/src/HoldButton.tsx ~109-116 — onConfirm() fires when `elapsed >= HOLD_MS` (a TIMER), not on pointerup; so batchSave advances + the dialog resizes WHILE the user is still pressing; the eventual release lands wherever the pointer then is.","[correct] ledger-web/src/HoldButton.tsx ~185-191 — onPointerDown only preventDefault()s; onPointerUp={cancelHold}; never stopPropagation() on pointerup/click — nothing suppresses the synthesized backdrop click.","[correct] ledger-web/src/App.tsx batchSave (~538-559) advances batchIndex to the next open row; BatchAnswerModal renders rows[index], so a shorter next question renders less content.","[correct] ledger-web/src/styles.css ~237-248 `.lw-batch` has NO fixed height (only max-height:90vh, overflow-y:auto); backdrop `.lw-help-backdrop` is a centering flexbox — a shorter question shrinks the dialog so the area the pointer was over becomes backdrop.","[correct] ledger-web/test/batchModalClose.test.tsx ~82-89 `holdFull` dispatches only pointerdown + clock advance — never pointerup, never a backdrop click; no test clicks data-testid=\"batch-overlay\" — explains the green suite despite the real bug (vacuous-coverage class, cf. D24/H14).","[correct] SAME vulnerable `onClick={onClose}` backdrop pattern in HelpOverlay (App.tsx ~1485) and the log modal (~3021) — the whole overlay class shares the latent defect.","[correct] react-modal issue #466 (web) — external corroboration: a press started inside the dialog and released outside closes an onClick-only-backdrop modal because the native click targets the common ancestor; documented fix = gate on the mousedown target."]
- sessionLogs: ["docs/logs/20260606-105430-af751bea360049c4c.md"]
