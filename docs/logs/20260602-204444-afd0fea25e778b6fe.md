# plan-advance — goal G6 — review-requested (revision of R95)

- Goal: G6 (defect-seeded cleanup; follow-up fix unit D = D12)
- Role: planner (plan-advance subagent)
- Returned status: `review-requested` (rule 3 — criticism-driven revision of R95)

### Session summary
- **Did:** Rule 3 — applied R95 (revise, criticism-only, empty new_questions, empty defects[] bucket) to the G6 plan under work milestone M28; goal phase unchanged (stays `planning`).
- **Achieved:** Created prerequisite task **T109** (packages/ledger read-side ArchivePointer title/status backfill, reproduction-first acceptance); revised **T108** (ItemTable prop extension + pointer-shaped archived rows + legacy-path acceptance, `dependsOn: [T91, T109]`); updated **G6.grounding** with the confirmed live-data facts and revised parallel-safety; added **T109** to **D12.dependsOn** → `[T108, T109]`. No phase transition; no decision created.
- **Discovered:** Confirmed against live data: `docs/milestones.md` `archives:` entries persist only id/path/summary (M2 at docs/milestones.md:10-12); the archive `.md` carries the recoverable title + status (docs/archive/milestones/M2.md:5 `title:`; header `### M2 — done`). ItemTable (App.tsx:1687-1709) takes no archive props and its isMilestones branch (App.tsx:1747-1815) is an Item-shaped `<table>` that does not map onto an ArchivePointer. Codegraph/Grep/Bash were unavailable to this subagent; grounded via Read on docs + App.tsx + types.ts.
- **Issues:** D12 is no longer fully parallel-safe — T109 and T106 both touch `packages/ledger`; recorded in T109/T108/grounding (sequence or co-assign). The revised plan awaits re-review; no new user questions raised.
