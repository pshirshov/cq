# Session log — outer-9 (D-GC-1 + D-GC-N1 defect closure)

**Cycle:** outer-9 / defect-closure on D-GC-1 (Codex ledger MCP via external stdio binary) + D-GC-N1 (Codex approvalPolicy popup row).
**Worktree:** `/home/pavel/work/safe/cqe/cq1/.claude/worktrees/agent-a0e6ccecd124a96c8`
**Base commit:** `f5d02d7` (`docs: filled question batch for outer-7 (gear-popup + Codex SDK)`)
**Final commit:** `2822272` (`feat(gcn1-3): popup Approval-policy row + ChatStart plumbing; close D-GC-N1`)

## Carryover audit (G0) — setup correction

The worktree was reported in the brief as branched from `main@f5d02d7`, but `git log` on the worktree branch showed it actually branched from an earlier `4e3d4fe` and carried two `wip` commits adding obsolete drafts. The `f5d02d7` head and all of `main`'s subsequent refactors (introduction of `@cq/ledger`, `codexBridge.ts`, `claudeBridge.ts`, `SettingsPopup.tsx`) were NOT in the worktree.

Verification: both `wip` commits' content (one gitignore line subtracting from main's gitignore; one 396-line questions doc) was provably already present on `main@f5d02d7`. Resolution: `git reset --hard f5d02d7` to align the worktree with the brief's intended baseline. No information loss — the wip commits added nothing that was not already on main.

This was a setup mismatch the orchestrator resolved from inside the loop per `CLAUDE.md` + brief authority. Not escalated.

## Plan (G2) → execution

Plan committed to `tasks.md` as the outer-9 active section. Six PRs, six commits:

| PR | Commit | What shipped |
|----|--------|--------------|
| `cq-mcp-1+2+3` | `7e3f693` | New `@cq/cq-mcp` workspace package — stdio MCP binary wrapping `FsLedgerStore` (12 `mcp__cq__*` ledger tools). End-to-end test spawns the binary via `@modelcontextprotocol/sdk` `StdioClientTransport`. `ask_user_question` deliberately omitted (needs WS bridging). |
| `gc1-1` | `f2c5f61` | `CodexBridge` constructs `Codex` per session with `CodexOptions.config.mcp_servers.cq = { command, args: ["--cwd", <cwd>] }`. `defaultResolveCqMcpBin()` walks `node_modules/.bin/cq-mcp` with bun-source fallback. Factory signature widened to `(options?: CodexOptions) => Codex` so tests can capture options. |
| `gc1-2` | `563fc56` | README prerequisite note. D-GC-1 closed in `defects.md` with citations. Follow-ups filed: `D-CQMCP-NIX`, `D-CQMCP-E2E`. |
| `gcn1-1` | `688037e` | Migration #7 (`approval_policy TEXT NULL`). `SessionRow` (TS) + `HistoryRow`/`HistoryRowFull` (Zod) gain `approvalPolicy?: string \| null`. Both adapters' insert/update/select wired. Persist-crud round-trip + update + history-join tests. |
| `gcn1-2` | `a8760e8` | `ChatStart.approvalPolicy` optional Zod field with new `ApprovalPolicySchema`. Facade refusal on Claude + non-null approvalPolicy with `chat.error{code:'approval-policy-on-claude'}`. `CodexBridge` forwards `frame.approvalPolicy → ThreadOptions.approvalPolicy` and persists on the new session row. |
| `gcn1-3` | `2822272` | `SettingsPopup` renders the platform-gated "Approval policy" row (4 SDK values, default `on-request`). `ChatTab` hydrates from `localStorage['cq.codex.approvalPolicy']`, threads through Header, spreads onto all five `chat.start` construction sites via `approvalPolicyFor(model, value)`. D-GC-N1 closed. |

## Inner-loop homeostat firings

None of significance. All sub-cycles converged inside their I2 dispatch. One leaf-refinement: the brief said to "reuse `packages/ledger/src/mcp/ledgerTools.ts` verbatim" in the new binary, but inspection revealed the existing factory returns Claude-SDK-typed `SdkMcpToolDefinition` objects (via `@anthropic-ai/claude-agent-sdk`'s `tool()` helper), which `@modelcontextprotocol/sdk`'s `McpServer.registerTool` cannot consume. I duplicated the Zod schemas in `cq-mcp/src/main.ts` with a JSDoc note describing the necessary duplication and which test guards each path. This was a Surprises-line item, not a homeostat firing — the acceptance criterion ("12 tools served end-to-end") still applied verbatim and was met.

## Resolved this session

- `D-GC-1` → commit `f2c5f61` (+ supporting commits `7e3f693`, `563fc56`).
- `D-GC-N1` → commit `2822272` (+ supporting commits `688037e`, `a8760e8`).
- Carryover setup mismatch (worktree at `4e3d4fe + 2 wip` instead of `f5d02d7`) → `git reset --hard f5d02d7`.

## Opened this session (follow-ups)

- `D-CQMCP-NIX` (minor) — verify `nix build .#default` includes the `cq-mcp` bin in the closure.
- `D-CQMCP-E2E` (minor) — extend `codex-roundtrip.spec.ts` with an `mcp__cq__*` step once `OPENAI_API_KEY` is available in the e2e env.

## Discharge metrics

- `bun run check`: **689 pass / 0 fail / 0 error / 2459 expect() calls** across 86 files. Baseline (`f5d02d7`) was 672/0/0/2418 across 84 files. Δ = +17 new passing tests, +2 new test files (`cq-mcp/test/main.test.ts`, `server/test/codexBridge-mcp.test.ts`), +41 new expect() calls.
- `bun run e2e` (Playwright): **18 passed / 1 skipped / 0 failed** — unchanged from outer-8 baseline.
- `tsc -b`: clean.
- `eslint .`: 0 errors.
- `defects.md`: D-GC-1 + D-GC-N1 → `[x] resolved`; D-CQMCP-NIX + D-CQMCP-E2E opened as deferred follow-ups.

Metrics:
- WIP max 1; review rounds: cq-mcp-1+2+3:1, gc1-1:1, gc1-2:1, gcn1-1:1, gcn1-2:1, gcn1-3:1
- S3-S4 firings research:0, replan:0; time-to-stabilize n/a
- Material scope delta none (all PRs landed in their planned file set; `+packages/cq-mcp/` is the brief's prescribed new path)
- Verification complete (commands + counts cited per PR)
- Audit discrepancies 0; algedonic ordinary:0, bypass:0, depth-limit:0; bypass false-positives 0
