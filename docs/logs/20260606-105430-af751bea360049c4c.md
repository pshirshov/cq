# Investigate-flow session log
- **Defect:** D31 (M60) — hypothesis H24 — explorer — lean: supports (confirmed)

### Session summary
- Confirmed H24: BatchAnswerModal closes on a backdrop click whose target/common-ancestor is the backdrop, with NO guard that the press STARTED on the backdrop. 8 evidence items, lean supports.
- EXACT close handler: App.tsx `<div className="lw-help-backdrop" data-testid="batch-overlay" onClick={onClose}>` (dialog inner `onClick={(e)=>e.stopPropagation()}`); onClose === setBatchOpen(false). HoldButton fires onConfirm on a HOLD_MS TIMER (HoldButton.tsx ~109-116), not pointerup, and never stopPropagation()s the click; so batchSave advances batchIndex to the next (shorter) question and the content-driven `.lw-batch` (no fixed height, only max-height:90vh; backdrop is a centering flexbox) shrinks WHILE the user still presses → pointer ends over the backdrop → release synthesizes a click with common-ancestor=backdrop → onClose fires → premature dismiss.
- TEST GAP (green-suite-but-real-bug, like D24/H14): batchModalClose.test.tsx `holdFull` dispatches only pointerdown + clock advance, never pointerup and never a backdrop click; no test simulates a click on data-testid="batch-overlay". Every "modal closes" assertion exercises the queue-drain setBatchOpen(false), not the backdrop dismiss.
- SAME latent defect in two more overlays: HelpOverlay (App.tsx ~1485) and the log modal (~3021) — identical `onClick={onClose}` backdrop pattern.
- CORRECT FIX: require the backdrop press to START on the backdrop — track onMouseDown/onPointerDown target===currentTarget and only onClose when BOTH down-target AND up/click-target are the backdrop (extract a shared safe-backdrop wrapper; apply to batch + help + log overlays). External corroboration: react-modal #466 (drag-release-outside closes modal; fix = gate on the mousedown target).
- Citations validated by orchestrator (grep, working tree): batch-overlay onClick={onClose} present; help-overlay + log modal share it; HoldButton timer-fire; onClose=setBatchOpen(false). Line numbers shifted slightly by an unrelated uncommitted comment-cleanup; handlers exact.
