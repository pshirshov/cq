# implement-worker — T130 (de-flake ledger-tui tests, D20) — pass

- Task T130 (M36, G8, defect D20); resultCommit 67d5e16e3a8c0df3a3e95c64e8a57b1a88e3ab1e (parent 137c7e0)
- pass; `bun run check` green 723/0 on TWO consecutive runs.
- Replaced all fixed-tick settles with poll-until-condition (waitForFrame/waitFor) across the targeted AND adjacent test sites in app.test.tsx (MultiMilestoneClient, GoalsClient, ArchiveClient, summarize(), NoTransitionsClient all had the same race); added explicit 30s/20s timeouts to navMemo T85 + scrolling; reduced navMemo N=500→200, NAV=40→20. Documented bun's worker model: without --isolate all files run sequentially in ONE process → derivationCounters IS shared, but navMemo's existing resetDerivationCounters() handles it (no extra beforeEach needed). Zero product diff.
- files: packages/ledger-tui/test/app.test.tsx, navMemo.test.tsx
