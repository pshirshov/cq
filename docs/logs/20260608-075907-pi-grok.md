# plan candidate (pi:grok-build, alias grok) — G31 (D38 fix)

- goal: G31 (defect-seeded, resolves D38)
- role: candidate planner (multi-planner, pi shellout)
- alias: grok (pi: provider=grok-build, model=grok-build)
- outcome: candidate plan emitted (1 milestone, 4 tasks)

### Captured stdout (verbatim candidate)
1 milestone "Fix D38: Pin the cq verdict enum on the Pi subagent path" with 4 tasks:
1. Reinforce exact verdict enum in pi-context.md dispatching section (standard).
2. Add fail-loud verdict enum validation to plan/advance.md (standard) — synonym map pass/ok→go-ahead, fail/reject→revise; off-enum→abstention; reconcile operates on validated result.
3. Add fail-loud verdict enum validation to implement/advance.md (standard) — synonym map pass/ok→approve, fail/reject→disapprove; off-enum→abstention.
4. Verify checks and builds and document argument for the fix (fast) — bun run check + nix build of cq-assets/llm-contexts/llm-skills; dependsOn all 3 edit tasks.
All tasks ledgerRefs [goals:G31, defects:D38]. Rationale: single milestone groups the minimal complementary changes; three edit tasks fine-grained and sequenced before verification.
