# Session log — T357 implement-worker (opus)

- **Task:** T357 (M146, G43); status pass; resultCommit f406363 (worktree) → 16903fc (main); check 1577/0

### Session summary
Added `createLedgerStore(root)` factory in @cq/ledger selecting FsLedgerStore ('fs'/default) or GitObjectLedgerBackend ('git-object') from cq.toml's `[ledger]` backend (via @cq/config), init()-ing it. Routed ALL construction sites through it: ledger-mcp createEmbeddedStore/main, cq-cli runInit/runReset, embedded TUI (mcpClient) + web (serve). Added git-env fail-fast (assertGitWorkTree via `git rev-parse --is-inside-work-tree` → GitEnvironmentError); shared idempotent `ensureGitBackendGitignore(root)` (R418, marker-guarded, ignores docs/*.md + docs/ledgers.yaml + docs/.locks/) that T354 will reuse; generalised capability gating (read_log FS-only via instanceof FsLedgerStore; config/promptCatalog backend-independent via duck-typed rootDirOf — GitObjectLedgerBackend exposes rootDir); per-backend watcher selection (file-watch fs vs ref-sha-watch git, T353). runReset rejects git-object (EXIT_USAGE, duck-typed isResettable — reset() is FS-specific). New cross-package dep @cq/ledger→@cq/config (package.json + tsconfig project ref; no cycle — config depends on neither). backend='fs'/no-cq.toml byte-identical to before (same FsLedgerStore({root}) options). 20 new tests. DISCOVERED + FLAGGED: bun.lock gained the workspace edge → flagged Nix FOD refresh (orchestrator verified empirically NOT needed — internal edge, external closure unchanged). A pre-existing bun in-process-test quirk (loadConfig configured:false in-process) worked around in capabilityGating.test by asserting capability PRESENCE; git-object activation independently proven by createLedgerStore.test + init-git-object.test (real orphan-ref lands). Based on main 1af178c.
