# Plan candidate log — G36 (planner, candidate mode: opus[claude])

- Goal G36 (M115), multi-planner round 1, CANDIDATE mode. Emitted a 3-milestone/8-task candidate for the effort-suffix grammar. Wrote nothing; orchestrator synthesized + persisted.

### Session summary
- **Did:** Grounded read-only in the G36 code surface (parseReviewerToken/formatReviewerToken/reviewerTokensEqual/parseTiers/classifyToken, types.ts, configCapability.ts, the inlined parseCqToken/tokenToChildModel in cq-subagent-dispatch.ts), confirmed both harness effort vocabularies, proposed a candidate task-DAG.
- **Achieved:** 3-milestone/8-task sequenced candidate folding Q160–Q165 (incl. Q161's fixed validated per-harness enums + fail-fast).
- **Discovered:** pi flag asserted as `--thinking` (off/minimal/low/medium/high/xhigh); claude effort low/medium/high/xhigh/max (ultracode excluded). parser doc says "further colons preserved in model" — must invert (':' reserved). classifyToken + parseTiers D42 delegate to reviewerTokensEqual, so Q162 needs only that one change. Wire-shape result types live in @cq/ledger. pi-extension resolver is an inlined non-importing mirror.
- **NOTE (caught in review):** the `--thinking <effort>` emission in T294 was found WRONG by the opus reviewer — pi uses the `--model provider/model:level` shorthand, not a separate flag. Revised in round 2.
