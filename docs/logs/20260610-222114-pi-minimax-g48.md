# Session log — G48 plan candidate (pi:ollama-cloud / minimax-m3), planning round 1

- **Goal:** G48; candidate planner 1-of-3 via `pi --provider ollama-cloud --model minimax-m3`; 5 milestones / 11 tasks. FOLDED IN: the explicit "existing mcp/tui/web product test suites MUST stay UNCHANGED and exercise the embedded-MCP path (the Q218 byte-for-byte invariant)" acceptance clause.

### Captured stdout (verbatim, abridged)
```
W1 wire cq-cli dispatch (add 3 deps; mcp/tui/web routing w/ verbatim pass-through incl. cq mcp restore; embedded-mode triggers stay intact via exported main(argv)); W2 migrate call sites (.mcp.json/dev-llm/docs) while old bins still present; W3 remove bin fields (keep exported main/serve + import.meta.main guards); W4 collapse flake to one .#cq merged closure; W5 FOD-hash refresh + dispatch tests + bun run check + nix build .#cq, asserting existing product suites stay green + embedded MCP exercised.
```
Same in-process-delegation spine; contributed the explicit embedded-invariance test clause.
