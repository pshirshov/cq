# Implement-flow session log
- **Task:** T176 (M56, G15) — reviewer, round 0 — DISAPPROVE (1 criticism)

### Session summary
- reviewed T176 (commit bd75ef2). verdict disapprove; 1 criticism. Reconciliation/gate/record wiring correct + check green 875/0, BUT §3a resolves get_reviewers via "a Bash shellout to the cq-config CLI" — unworkable: packages/cq-config-mcp/src/main.ts is a pure stdio MCP server (registered `nix run .#cq-config-mcp`) with NO print-JSON CLI; @cq/config has no bin. A Bash shellout expecting JSON-on-stdout hangs. Fix: call MCP tool `mcp__cq-config__get_reviewers` (the T175/T177 mechanism) + add `mcp__cq-config__*` to frontmatter allowed-tools; update §3a wording (don't claim Bash covers it). This overrides the acceptance's "no new allowed-tools" line — the MCP-tool path is the only workable design.
