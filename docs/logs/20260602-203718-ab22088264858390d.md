# plan-advance — goal G6 — review-requested

- Goal: G6 (defect-seeded cleanup; follow-up fix unit D = D12)
- Role: planner (plan-advance subagent)
- Returned status: `review-requested`

### Session summary
- **Did:** Advanced defect-seeded goal G6 by one step (rule 2(b) / defect-aware planning for the appended follow-up D12). No clarifying questions were open (none exist under M27), and D12 is an already-CONFIRMED, grounded defect, so clarification was correctly skipped.
- **Achieved:** Created fix task **T108** under work milestone **M28** (status `planned`, `suggestedModel: standard`, `dependsOn: [T91]`, `ledgerRefs: [defects:D12, goals:G6]`); set **D12.dependsOn = [T108]** (bidirectional link); transitioned **G6 `clarifying → planning`**. M28's plan now covers all four fix units: T105 (D9), T106 (D10), T107 (D11), T108 (D12).
- **Discovered:** G6 had already completed a full planner↔reviewer round for D9/D10/D11 (R93 revise → R94 go-ahead, decision K17 locked); the follow-up appended D12 and re-opened the goal to `clarifying` per /plan:follow-up. The D12 fix surface was confirmed by reading `/home/pavel/work/safe/cqe/cq1/packages/ledger-web/src/App.tsx` — the `ArchiveSubsections` gate at L1182 carries `&& !isMilestones` (D7/T90), and the milestones `ItemTable` (L1160-1181) iterates only `view.milestones` (live), so `view.archivePointers` are unsurfaced for the milestones ledger. T108 is file-disjoint from T105/T106/T107 → parallel-safe; the lazy archive-detail path (`client.fetchLedgerArchive`, L1185) and `isArchived` DetailPanel (L1230-1240) already exist for reuse.
- **Issues:** D13 (TUI nav ~500ms, user issue 3) is intentionally NOT folded into G6 — its root cause is unknown and it is routed to /investigate per the goal description. T108 still needs reviewer sign-off; the existing K17 lock covers only D9/D10/D11, so reaching `planned` requires a fresh go-ahead review covering T108.
