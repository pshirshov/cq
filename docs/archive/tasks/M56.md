## M56

### T169 — done

- createdAt: 2026-06-05T18:43:05.681Z
- updatedAt: 2026-06-05T20:44:44.149Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: "SPIKE: verify `pi -p` runs non-interactively and emits parseable output in this sandbox"
- description: "De-risking spike (Q90 recommendation) BEFORE any reconciliation work depends on the pi-shellout mechanism. From a shell in this sandbox, verify that `pi` can be driven non-interactively to produce a parseable result, for BOTH target providers/models: (a) grok-build (default provider+model); (b) gpt-5.5 via the openai-codex provider already configured in pi (per dev-llm.nix programs.pi). Determine the EXACT non-interactive invocation (`pi -p <prompt>` or the correct flag set — read `pi --help`), how to SELECT provider+model per-invocation on the CLI, whether auth is already present (grok-build OAuth, openai-codex) or must be established, and whether stdout is cleanly capturable (no TUI escape codes polluting the JSON). Feed a tiny prompt that asks for a fixed JSON object and confirm the object is extractable from stdout. Capture findings to a docs/drafts/ note (exact command lines, provider/model selectors, auth state, any escape-stripping needed). Record a `decisions` item with the confirmed invocation contract so downstream tasks build on a fact, not a hypothesis. If pi CANNOT be driven non-interactively here, file a `defects` or escalate via a question rather than guessing — the rest of FEATURE 2's pi half depends on this being true."
- acceptance: "A docs/drafts/ note + a locked `decisions` ledger item record: the exact non-interactive `pi` command line for (a) grok-build and (b) gpt-5.5/openai-codex; how provider+model are selected per call; auth state; and a captured example where a fixed JSON object was extracted from stdout. Reproducible: the documented command, run again, yields the same parseable JSON. If non-interactive pi is NOT possible, that is recorded as the blocking finding instead."
- suggestedModel: frontier
- ledgerRefs: ["goals:G15"]
- resultCommit: 116691b
- completion: Spike CONFIRMED pi is non-interactively drivable for grok-build + gpt-5.5/openai-codex (byte-clean stdout, OAuth pre-auth); invocation contract recorded as decision K30; findings in docs/drafts/20260605-2130-pi-noninteractive-spike.md.
- sessionLogs: ["docs/logs/20260605-203620-a0bd2fd3ae68e33fa.md","docs/logs/20260605-204152-ab4bf2096a6f3e81a.md"]

### T170 — done

- createdAt: 2026-06-05T18:43:21.831Z
- updatedAt: 2026-06-05T20:44:47.734Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: Define cq.toml schema + parser/resolver (typed, tested) in a new cq-config package
- description: |
    Create a new Bun workspace package nix/pkg/cq-ledgers/packages/cq-config (add to root package.json `workspaces`), mirroring the layout of an existing leaf package (e.g. packages/ledger). Implement the cq.toml DATA MODEL + parse/resolve logic as a pure, typed module (NO MCP/transport yet — that is the next task): 
    - Named types (no catch-all): a `ReviewerToken` = `{ harness: "claude" | "pi", model: string }` parsed from a `"<harness>:<model>"` string; a `CqConfig` = `{ aliases: Record<string, ReviewerToken>, reviewers: ReviewerToken[] }`.
    - Parse cq.toml (Bun has builtin TOML import, or use a small TOML parser) with the schema: an `[aliases]` table mapping alias->"harness:model" string, and a top-level `reviewers = ["alias", ...]` list of ALIAS names. RESOLVE each reviewers entry THROUGH [aliases] to a ReviewerToken (fail fast with a precise error if an alias is undefined or a token isn't `harness:model` with a known harness).
    - A `resolveReviewers(config): ReviewerToken[]` and a top-level `loadConfig(repoRoot): CqConfig | null` (null = no cq.toml => feature OFF / single native Claude reviewer).
    - Validate at the boundary (malformed TOML, unknown harness, dangling alias) — throw early, do not silently default.
    Reproduce-first acceptance: write tests FIRST (dual fixtures: a valid cq.toml resolving codex/grok/opus aliases; an absent file => null; a dangling-alias file => throws; an unknown-harness token => throws). All `bun test` green.
- acceptance: "packages/cq-config exists in the workspace; `loadConfig`/`resolveReviewers` are typed (ReviewerToken/CqConfig named types, no `any`). Tests cover: valid [aliases]+reviewers resolves to the expected ReviewerToken[]; absent cq.toml => null; dangling alias => throws a precise error; unknown harness => throws. `bun run check` (tsc -b && eslint . && bun test) green from nix/pkg/cq-ledgers/."
- suggestedModel: frontier
- ledgerRefs: ["goals:G15"]
- resultCommit: 7ab056c
- completion: "New @cq/config package: typed cq.toml data model (ReviewerToken/CqConfig) + dependency-free TOML parser + loadConfig/resolveReviewers with fail-fast validation; reproduce-first tests (4 cases); wired into workspace; integrated check green 920/0."
- sessionLogs: ["docs/logs/20260605-203620-a18d7049505451d94.md","docs/logs/20260605-204152-a68634e221c3c193a.md"]

### T171 — done

- createdAt: 2026-06-05T18:43:35.592Z
- updatedAt: 2026-06-05T21:02:23.277Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: Implement cq-config MCP server exposing get_reviewers over the parser
- description: "On top of T170's parser, implement the `cq-config` MCP server (stdio transport, mirroring packages/ledger-mcp's server bootstrap — same MCP SDK, same --cwd convention defaulting to process CWD so one global server serves the per-repo cq.toml). Expose ONE tool, e.g. `get_reviewers` (and optionally `get_config`): it calls loadConfig(cwd) and returns the RESOLVED reviewer set as structured JSON — `{ configured: boolean, reviewers: [{harness, model, alias}] }`, where configured=false (empty/null) signals the orchestrators to use today's single native Claude reviewer. Add a `bin` (e.g. cq-config-mcp) and a Nix package (mirror nix/pkg/cq-ledgers' ledger-mcp packaging so `nix build .#cq-config-mcp` works — follow whatever flake attr/derivation pattern ledger-mcp uses; refresh the node-modules FOD hash per CLAUDE.md if bun.lock changes). Tests: the server responds to a get_reviewers MCP call against a fixture repo with a cq.toml (returns the resolved set) and against one without (returns configured:false). `bun run check` green. NOTE: depends on T170 (the parser) and shares the root package.json `workspaces` edit — sequence after T170 to avoid clobbering that file."
- acceptance: "`cq-config` MCP server starts over stdio; a get_reviewers call returns {configured:true, reviewers:[...]} for a fixture repo WITH cq.toml and {configured:false,...} WITHOUT. A Nix package builds it (`nix build .#cq-config-mcp` or the repo's equivalent attr). FOD hash refreshed if bun.lock changed. `bun run check` green."
- suggestedModel: frontier
- dependsOn: ["T170"]
- ledgerRefs: ["goals:G15"]
- resultCommit: 07a0621
- completion: "@cq/config-mcp stdio server (mirrors ledger-mcp) exposing get_reviewers/get_config over @cq/config; {configured,reviewers:[{harness,model,alias}]}; Nix package .#cq-config-mcp + FOD hash refreshed (also fixed T170's missing FOD fileset entry); verified nix build + MCP roundtrip; integrated check green 930/0."
- sessionLogs: ["docs/logs/20260605-205630-a88adc255223c45f8.md","docs/logs/20260605-210010-a404a9d2e8655f371.md"]

### T172 — done

- createdAt: 2026-06-05T18:43:48.657Z
- updatedAt: 2026-06-05T21:13:05.268Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: Register cq-config MCP server in dev-llm.nix AND this repo's .mcp.json
- description: |
    Wire the new cq-config MCP server into BOTH registries so it is reachable globally AND for in-repo dogfooding. (A) GLOBAL — nix/hm/dev-llm.nix programs.mcp.servers, mirroring the existing `servers.ledger` block: add `programs.mcp.servers.cq-config = { command = "${cqConfigPkg}/bin/cq-config-mcp"; args = [ ]; };` and bind `cqConfigPkg` from the ledger packages set (built in this flake — `self.packages.${system}.cq-config-mcp`, alongside ledgerPkg). Because the Pi-only piMcpJson re-maps `config.programs.mcp.servers` to keep-alive, the new server is automatically exposed to Pi too — confirm no extra wiring is needed there. Optionally add cq-config-mcp to ledgerTools/home.packages if a CLI is wanted on PATH (mirror ledger-mcp), else MCP-only is fine.
    
    (B) IN-REPO — THIS repo's project-local .mcp.json (R169 criticism 3): .mcp.json currently wires ONLY the ledger server (`command: nix run .#ledger-mcp`). For an in-repo /plan:advance or /implement:advance to call cq-config's get_reviewers during dogfooding, the orchestrator must reach the server in THIS repo's session. Add a .mcp.json entry for cq-config that mirrors the ledger entry, e.g. `"cq-config": { "command": "nix", "args": ["run", ".#cq-config-mcp"] }` (match the exact shape/style of the existing ledger entry; ensure the `.#cq-config-mcp` flake attr from T171 is the one referenced). Verify the entry parses and that an in-repo session can list the get_reviewers tool. (Without this, the in-repo reviewer feature is unreachable even though the global dev-llm.nix registration exists — the global registration alone does not guarantee merge into in-repo Claude sessions.)
    
    This edit touches dev-llm.nix; sequence it AFTER any other dev-llm.nix edit in this goal to serialize same-file writes (none other in FEATURE 2; the prober feature does NOT touch dev-llm.nix — confirmed via assets.nix auto-collection). Depends on T171 (the package + the .#cq-config-mcp attr must exist to reference).
- acceptance: dev-llm.nix declares programs.mcp.servers.cq-config pointing at the cq-config-mcp binary from this flake's package set; the Pi keep-alive re-map picks it up with no extra edit. THIS repo's .mcp.json gains a cq-config entry (mirroring the ledger entry, `nix run .#cq-config-mcp`) so an in-repo session can list/call get_reviewers — verified by listing the tool in an in-repo session. The home-manager module evaluates (no eval error introduced); `nix build` of the affected agent config attr succeeds. Surgical — only the two server registrations (and optional PATH tool) added.
- suggestedModel: standard
- dependsOn: ["T171"]
- ledgerRefs: ["goals:G15"]
- resultCommit: a4103cc
- completion: Registered cq-config MCP server in dev-llm.nix (programs.mcp.servers.cq-config -> cq-config-mcp, auto-exposed to Pi via piMcpJson) + this repo's .mcp.json (nix run .#cq-config-mcp); nix eval resolves, both files parse.
- sessionLogs: ["docs/logs/20260605-210751-a7cc382eecd7147f3.md","docs/logs/20260605-211047-ae8150e0cadc2509f.md"]

### T173 — done

- createdAt: 2026-06-05T18:44:05.007Z
- updatedAt: 2026-06-05T20:44:51.026Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: Extract a SHARED plan-reviewer rubric prompt consumable by pi; make native reviewer return-json in configured mode
- description: |
    The plan-reviewer rubric currently lives ONLY in nix/pkg/cq-assets/agents/plan-reviewer.md (a Claude-only AGENT — mergedAgents is not delivered to pi/codex). To let a pi reviewer judge a plan with the SAME rubric and emit the SAME JSON, extract the rubric into a SHARED prompt that pi receives as a promptTemplate. Create nix/pkg/cq-assets/commands/cq/plan-review.md (a /cq:* command, auto-delivered to Claude commands + Codex prompts + Pi promptTemplates by the materializer). Its body = the adversarial plan-review rubric (Fine-grained? / Sequenced? / Testable? / Grounded? / Complete?), the three-bucket classification (new_questions / criticism / defects-objects), and a STDOUT JSON contract a non-Claude harness can emit: a single fenced json {summary, verdict:"go-ahead"|"revise", new_questions:[], criticism:[], defects:[{headline,severity,rootCause?,suggestedFix?}]}.
    
    SINGLE-AGGREGATED-REVIEWS-ITEM INVARIANT (R169 criticism 1 — resolves the T173/T175 double-write): the reviews ledger must carry EXACTLY ONE aggregated reviews item per round. Therefore the native plan-reviewer's direct ledger-write is conditional on mode:
    - UNCONFIGURED single-reviewer FALLBACK (no cq.toml / get_reviewers configured=false): the native plan-reviewer.md AGENT writes the reviews item DIRECTLY, exactly as today. Keep this path.
    - CONFIGURED multi-reviewer mode: the native claude plan-reviewer must NOT write the reviews ledger — it RETURNS the verdict json (the same {summary,verdict,new_questions,criticism,defects} stdout/return shape this shared prompt defines) and ONLY the orchestrator writes the single aggregated reviews item (T175). This makes the plan side SYMMETRIC to the already-clean implement side, where implement-reviewer.md returns json and never writes the ledger.
    Update nix/pkg/cq-assets/agents/plan-reviewer.md so its 'write the reviews item' step is explicitly gated: 'if dispatched as the sole/unconfigured reviewer, write the reviews item; if dispatched as one of several configured reviewers, RETURN the verdict json and write NOTHING — the orchestrator reconciles and writes the single item.' The /cq:plan-review shared prompt's pi path ALWAYS returns json on stdout (pi has no MCP write path here). Declare one canonical rubric source shared between plan-reviewer.md and this file to avoid drift.
    
    The prompt takes the goal id + the plan context as input (the orchestrator fills these when shelling out).
- acceptance: "commands/cq/plan-review.md exists, contains the full adversarial plan rubric + 3-bucket classification + a stdout-JSON contract {summary,verdict,new_questions,criticism,defects}; explicitly states the pi path ALWAYS returns json on stdout (no ledger write). plan-reviewer.md's reviews-write step is gated on mode: writes the ledger ONLY in the unconfigured single-reviewer fallback, and RETURNS json (writes nothing) in configured multi-reviewer mode — so at most ONE reviews item is written per round (orchestrator-written when configured). Single canonical rubric source (no drift). Markdown renders."
- suggestedModel: frontier
- ledgerRefs: ["goals:G15"]
- resultCommit: aa227da
- completion: Shared commands/cq/plan-review.md rubric (stdout-json contract) + plan-reviewer.md write gated on mode (unconfigured=write-direct; configured=return-json/write-nothing) so exactly one aggregated reviews item per round; canonical rubric source (no drift).
- sessionLogs: ["docs/logs/20260605-203620-a59573920102771e3.md","docs/logs/20260605-204152-a94f773db05f3c674.md"]

### T174 — done

- createdAt: 2026-06-05T18:44:15.282Z
- updatedAt: 2026-06-05T20:44:54.011Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: Extract a SHARED implement-reviewer rubric prompt consumable by pi
- description: "Symmetric to T173 for the implement flow. Create nix/pkg/cq-assets/commands/cq/implement-review.md holding the implement-reviewer rubric (Meets acceptance? / Check truly green? / Correct & surgical? / Repro discipline? / Conventions?) + the 3-bucket classification (criticism / questions / defects-objects) + the EXISTING implement-reviewer stdout JSON contract {taskId, verdict:\"approve\"|\"disapprove\", criticism:[], questions:[], defects:[{headline,description,severity,suggestedFix?}], rationale, summary}. The implement-reviewer ALREADY returns json on stdout (it never writes the ledger), so the contract is the same shape pi must emit — this prompt just makes that rubric+contract available to the pi harness as a promptTemplate (since the agent file is Claude-only). The orchestrator fills task id / worktree diff / check output as input when shelling out to pi. Declare one canonical rubric source shared with implement-reviewer.md to avoid drift. Independent file from T173 — no dependency between them."
- acceptance: "commands/cq/implement-review.md exists with the full implement-review rubric + 3-bucket classification + the {taskId,verdict,criticism,questions,defects,rationale,summary} stdout-json contract identical in shape to implement-reviewer.md's. Native implement-reviewer.md unchanged. No rubric drift (one canonical source). Markdown renders."
- suggestedModel: frontier
- ledgerRefs: ["goals:G15"]
- resultCommit: b16feb5
- completion: Shared commands/cq/implement-review.md rubric + stdout-json contract byte-identical to native implement-reviewer.md (which already returns json, so unchanged); canonical source with DO-NOT-DRIFT note.
- sessionLogs: ["docs/logs/20260605-203620-afc984a344aced594.md","docs/logs/20260605-204152-a7b0bbcaa359d2c8c.md"]

### T175 — done

- createdAt: 2026-06-05T18:44:34.278Z
- updatedAt: 2026-06-05T21:33:25.680Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: Wire parallel reviewers + reconciliation into plan/advance.md (step 2)
- description: "Edit nix/pkg/cq-assets/commands/plan/advance.md step 2 (Spawn the reviewer) to support the pluggable multi-reviewer set. New behaviour: (1) At review time, the orchestrator calls the cq-config MCP `get_reviewers` tool. If configured=false (no cq.toml) OR the tool/file is absent => UNCHANGED behaviour: one native plan-reviewer Agent subagent writes the reviews item DIRECTLY (today's exact path; this is the single-reviewer fallback in which plan-reviewer.md still owns the ledger write per T173). (2) If configured, AND honoring any SESSION-ONLY override the user stated this run (see /cq:reviewers, T177) which supersedes the cq.toml default IN MEMORY for this run only, launch ALL active reviewers IN PARALLEL: for each `claude:*` token => an Agent subagent_type:plan-reviewer (native) instructed to RETURN the verdict json and NOT write the reviews ledger (configured mode per T173 — NOT 'in addition to' writing; the native reviewer writes NOTHING when it is one of several configured reviewers, so the only ledger writer is the orchestrator in step 4); for each `pi:*` token => shell out via Bash to `pi` (the confirmed non-interactive invocation from the T169 spike) feeding the SHARED /cq:plan-review rubric prompt (T173) + the goal/plan context, capturing the stdout json. (3) RECONCILE (Q91): STRICTEST-WINS verdict — any reviewer 'revise' => overall revise; 'go-ahead' requires ALL reviewers go-ahead. UNION all reviewers' new_questions/criticism/defects, each finding PREFIXED with its source reviewer (e.g. `[grok] ...`, `[opus] ...`). De-duplicate obvious near-identical findings but bias to keep. (4) The orchestrator writes the SINGLE aggregated `reviews` item (M-milestone, ledgerRef goals:G) carrying the reconciled verdict + unioned tagged findings — preserving the existing invariant that a `revise` has non-empty new_questions/criticism. RESULT: exactly ONE reviews item per round in BOTH modes — written by the native agent in the unconfigured fallback, written by the orchestrator (with every native reviewer returning json, never writing) in configured mode. Update §Session logs to log each pi reviewer's stdout too. Depends on T169 (pi invocation), T171/T172 (cq-config available), T173 (shared plan rubric + the gated native-reviewer write). Same-file edits to plan/advance.md are only here in FEATURE 2."
- acceptance: "plan/advance.md step 2: calls get_reviewers; absent/unconfigured => single native reviewer that writes the ledger directly (unchanged); configured => parallel claude-subagent (each RETURNS json and writes NOTHING) + pi-shellout reviewers reconciled STRICTEST-WINS + UNION-with-source-tags into ONE orchestrator-written reviews item. EXACTLY ONE reviews item is written per round in both modes (no double-write). Honors a session-only override; revise still requires non-empty findings. Doc references the T169 pi invocation, the cq-config tool, and /cq:plan-review. No new allowed-tools needed (Agent+Bash already present). Markdown renders. RECONCILIATION SEMANTICS CONFIRMED by Q95 (2026-06-05): strictest-wins verdict (any 'revise' blocks; 'go-ahead' requires unanimity) + union-of-findings-with-source-tags is the user-confirmed intended behaviour (over majority-vote / designated-primary) — this task's reconciliation spec stands as written, no change."
- suggestedModel: frontier
- dependsOn: ["T169","T172","T173"]
- ledgerRefs: ["goals:G15"]
- resultCommit: 60f870c
- completion: "plan/advance.md step 2: pluggable reviewers via get_reviewers (MCP tool, granted); unconfigured=single native reviewer writes directly; configured=parallel claude (return json) + pi (K30 shellout, /cq:plan-review) reconciled STRICTEST-WINS+tagged-UNION into ONE orchestrator-written reviews item; session-only override."
- sessionLogs: ["docs/logs/20260605-211925-aeef7be337eb3fdb0.md","docs/logs/20260605-212912-ad77e24aa3ffd2118.md","docs/logs/20260605-213245-ad5d8d780beefa6d3.md"]

### T176 — done

- createdAt: 2026-06-05T18:44:54.613Z
- updatedAt: 2026-06-05T21:33:28.957Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: Wire parallel reviewers + reconciliation into implement/advance.md (per-task review)
- description: "Symmetric to T175 for the implement flow. Edit nix/pkg/cq-assets/commands/implement/advance.md at the per-task review step (where it dispatches implement-reviewer and records the terminal review). Same pattern: (1) call cq-config get_reviewers; absent/unconfigured => single native implement-reviewer Agent (unchanged — it already returns json the orchestrator records). (2) configured (with session-only override applied) => launch all active reviewers in PARALLEL per task: `claude:*` => native implement-reviewer Agent (returns its json); `pi:*` => Bash shellout to `pi` (T169 invocation) feeding the SHARED /cq:implement-review rubric (T174) + task acceptance + worktree diff + check output, capturing stdout json. (3) RECONCILE (Q91) STRICTEST-WINS: any reviewer 'disapprove' => task disapproved (criticism loop continues / questions park it); 'approve' requires ALL reviewers approve AND green check. UNION criticism/questions/defects across reviewers, each finding tagged by source reviewer; preserve the existing 'approve requires empty criticism+questions+green check' and 'disapprove requires non-empty' invariants on the RECONCILED result. The orchestrator records the single terminal reviews item per task from the reconciled verdict. Update §Session logs to log each pi reviewer stdout. Depends on T169, T172, T174. Different file from T175 — may proceed in parallel with it."
- acceptance: "implement/advance.md per-task review: calls get_reviewers; unconfigured => single native implement-reviewer (unchanged); configured => parallel claude+pi reviewers per task reconciled STRICTEST-WINS+UNION-with-source-tags into the single terminal reviews item; approve still requires all-approve + green check, disapprove requires non-empty findings. References T169 pi invocation, cq-config, /cq:implement-review. No new allowed-tools (Agent+Bash present). Markdown renders. RECONCILIATION SEMANTICS CONFIRMED by Q95 (2026-06-05): strictest-wins verdict (any 'disapprove' blocks; 'approve' requires unanimity + green check) + union-of-findings-with-source-tags is the user-confirmed intended behaviour (over majority-vote / designated-primary) — this task's reconciliation spec stands as written, no change."
- suggestedModel: frontier
- dependsOn: ["T169","T172","T174"]
- ledgerRefs: ["goals:G15"]
- resultCommit: 14f8caa
- completion: "implement/advance.md per-task review: pluggable reviewers via get_reviewers (MCP tool, granted); configured=parallel claude+pi (K30 shellout, /cq:implement-review) reconciled STRICTEST-WINS+tagged-UNION; reconciled verdict drives criticism loop + success gate (all-approve+green) + single terminal reviews item."
- sessionLogs: ["docs/logs/20260605-211925-a89206ec7799ad28d.md","docs/logs/20260605-212912-aa6df76dacfe8e290.md","docs/logs/20260605-213245-a074ab6be09b601b6.md"]

### T177 — done

- createdAt: 2026-06-05T18:45:11.546Z
- updatedAt: 2026-06-05T21:25:52.241Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: "Author /cq:reviewers session-only override command"
- description: "Create nix/pkg/cq-assets/commands/cq/reviewers.md (net-new /cq:* namespace). Per Q93 the override is SESSION-ONLY — NO durable file, NO ledger item, NO gitignored state. The command's job: parse a natural-language reviewer-set instruction (e.g. `/cq:reviewers use grok and opus only`) into the canonical active reviewer set for THE CURRENT CHAINED RUN, by resolving alias names through the SAME cq.toml [aliases] table (via the cq-config get_reviewers / get_config tool) plus a small documented fallback alias map (grok->pi:grok-build, opus->claude:opus-4.8[1m], codex->pi:gpt-5.5). The command ECHOES/CONFIRMS the resolved active set and instructs that this override applies ONLY to the current run — the next fresh /plan:advance or /implement:advance reverts to cq.toml's default unless re-stated. Document precisely HOW the override is carried: because each orchestrator invocation is a fresh session, the override lives in the CONVERSATION CONTEXT of the current chained run — i.e. the user states it in the same session before/as they run /plan:advance|/implement:advance, and those orchestrators (T175/T176) read the stated active set from the run context, falling back to get_reviewers when none was stated. NO persistence is written. allowed-tools: at minimum the cq-config MCP tool + Read (to resolve aliases); NO Write (nothing is persisted). Depends on T172 (cq-config tool exists to resolve aliases)."
- acceptance: "commands/cq/reviewers.md exists; parses NL like 'use grok and opus only' into canonical harness:model tokens via cq.toml [aliases] + documented fallback map; ECHOES the resolved active set; states the override is SESSION-ONLY (no file/ledger persistence) and reverts next fresh run; writes NOTHING durable (no Write tool). Consistent with T175/T176's 'session-only override read from run context'. Markdown renders."
- suggestedModel: standard
- dependsOn: ["T172"]
- ledgerRefs: ["goals:G15"]
- resultCommit: 5ce023e
- completion: "Authored commands/cq/reviewers.md (/cq:reviewers): NL→harness:model via cq.toml [aliases] (cq-config get_config) + fallback map; echoes resolved active set; SESSION-ONLY override (no durable writes), reverts next fresh run."
- sessionLogs: ["docs/logs/20260605-211925-a239faff082ee8b30.md","docs/logs/20260605-212510-ac02457bde959f0c8.md"]

### T178 — done

- createdAt: 2026-06-05T18:45:26.654Z
- updatedAt: 2026-06-05T21:51:36.310Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: Add sample cq.toml + register new /cq commands in link-prompts.ts (verified root) + README
- description: "Final wiring/docs for FEATURE 2. (1) Add a sample/documented cq.toml at the repo root (or a cq.toml.example if a live one would alter THIS repo's own review behaviour — prefer .example so dogfooding stays opt-in) showing the schema: an [aliases] table (codex=\"pi:gpt-5.5\", grok=\"pi:grok-build\", opus=\"claude:opus-4.8[1m]\") and reviewers=[\"codex\",\"grok\",\"opus\"], with comments explaining alias resolution and that absence => single native Claude reviewer. (2) nix/pkg/cq-ledgers/scripts/link-prompts.ts: append LINKS entries for the three new commands — .claude/commands/cq/plan-review.md, .claude/commands/cq/implement-review.md, .claude/commands/cq/reviewers.md. Use the VERIFIED-correct source root (R169 criticism 2): the cq/* command source files live under nix/pkg/cq-assets/commands/cq/ — pin each entry's `source` to the path that ACTUALLY resolves to that file (the same verified root T168 establishes for cq-assets), NOT a stale 'llm/' root that would create a dangling link. (3) nix/pkg/cq-assets/README.md: add 'Current assets' + 'Three consumers' rows for cq/plan-review, cq/implement-review, cq/reviewers, and mention the new cq-config MCP server + cq.toml. Depends on T173, T174, T177 (the three command files must exist to link). Note: link-prompts.ts is ALSO edited by T168 (FEATURE 1) — serialize this AFTER T168 to avoid same-file clobber and to inherit T168's verified source root."
- acceptance: "A cq.toml.example (or root cq.toml) documents the [aliases]+reviewers schema with comments. link-prompts.ts has entries for cq/plan-review, cq/implement-review, cq/reviewers whose `source` paths are VERIFIED to resolve to existing files under nix/pkg/cq-assets/commands/cq/. `bun run link-prompts` runs clean AND each of the three created symlinks RESOLVES to an existing file (assert `test -e .claude/commands/cq/plan-review.md`, `... implement-review.md`, `... reviewers.md` all succeed — not merely that link-prompts ran). README lists all new cq/* assets + the cq-config MCP + cq.toml. `bun run check` green."
- suggestedModel: fast
- dependsOn: ["T173","T174","T177","T168"]
- ledgerRefs: ["goals:G15"]
- resultCommit: f2b69be
- completion: cq.toml.example (root) + 3 cq/* command LINKS entries (symlinks resolve) + README rows for cq/* assets + cq-config MCP + cq.toml; example verified to parse under @cq/config; final integrated check green 930/0.
- sessionLogs: ["docs/logs/20260605-213907-a9d2b9a8fa9c45647.md","docs/logs/20260605-214603-ac8f2ea24e4a37acf.md","docs/logs/20260605-214603-ae4ed344d7d90ac3a.md"]
