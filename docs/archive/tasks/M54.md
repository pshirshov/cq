## M54

### T162 — done

- createdAt: 2026-06-05T18:34:23.330Z
- updatedAt: 2026-06-05T19:29:38.198Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: "Backend (authoritative): reject empty/whitespace answer on questions `answered` transition via a StatusChangePrecondition, dual-store + reproduce-first dual-tests"
- description: |
    Primary invariant fix for D29. The questions answer field (constants.ts:217, QUESTIONS_SCHEMA.answer = {type:'string', required:false}) carries no content/status-conditional constraint, and the optional StatusChangePrecondition hook in the transition path (core.ts applyUpdateItem ~259-291; assertGoalPhasePreconditions is the goals-only precedent) is wired ONLY for GOALS_LEDGER (FsLedgerStore.ts ~571-581, full block ~559-588). validateFields/assertFieldType only type-check (core.ts ~831-859), so no answer-content check runs for questions.
    
    IMPLEMENT: add a questions-specific StatusChangePrecondition mirroring assertGoalPhasePreconditions that, on a transition where (from != 'answered') -> (to == 'answered'), asserts the EFFECTIVE answer = (patch.fields?.answer ?? item.fields.answer) is a non-empty trimmed string, and throws the same precondition/SchemaValidation error class assertGoalPhasePreconditions uses otherwise. Account for the answer arriving in patch.fields vs already present on the item. Wire it for QUESTIONS_LEDGER in BOTH FsLedgerStore.updateItem AND the in-memory store (InMemoryLedgerStore), so the guard is store-agnostic.
    
    REPRODUCE-FIRST (dual-tests, per the dual-tests skill): add a failing test asserting update_item(Q, status:'answered', fields:{answer:''}) AND {answer:'   '} both throw, while a non-empty answer still succeeds, AND that an answer already present on the item (no answer in patch) still permits the transition. Run the SAME abstract suite against BOTH FsLedgerStore and InMemoryLedgerStore. Verify the test fails for the RIGHT reason (no guard) before adding the precondition, then turns green after.
    
    Scope: this is the high-blast-radius, cross-cutting piece (new precondition contract wired into two stores) — design carefully. Cite all anchors above against live source before editing; the investigation validated them but line numbers may have drifted.
- acceptance: "Reproduce-first: the new dual-tests fail for the right reason WITHOUT the guard, pass WITH it; update_item to `answered` with empty/whitespace answer throws on BOTH FsLedgerStore and InMemoryLedgerStore, non-empty (in patch or already on item) succeeds. `bun run check` (test + typecheck + lint) green from nix/pkg/cq-ledgers/."
- suggestedModel: frontier
- ledgerRefs: ["defects:D29","goals:G16"]
- resultCommit: 0b5e98f9d71ea0729bda34b4c6725c04cd368379
- completion: Added questions-specific StatusChangePrecondition (assertQuestionAnswerPrecondition) rejecting empty/whitespace answer on from!=answered->answered, wired in both FsLedgerStore + InMemoryLedgerStore; reproduce-first dual-tests green; bun run check 889/0.
- sessionLogs: ["docs/logs/20260605-192023-a80766318d2f170d2.md","docs/logs/20260605-192809-a44c1e4e33cb44019.md"]

### T163 — done

- createdAt: 2026-06-05T18:34:34.038Z
- updatedAt: 2026-06-05T19:59:00.423Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: "Web UX: disable 'save & mark answered' when trimmed answer is empty (detail HoldButton + BatchAnswerModal, both in App.tsx)"
- description: |
    Complementary UX guard for D29 (prevents a server-rejected submission once T162's backend guard lands). Both web submit paths live in the SAME file (ledger-web App.tsx): the detail HoldButton (~App.tsx:2629, submitAnswer at ~2611) and the BatchAnswerModal submit (~App.tsx:1732). Because they share one file they MUST be a single task (repo reviewers serialize same-file edits strictly — R137/R138/R139).
    
    IMPLEMENT: disable the 'save & mark answered' affordance when the trimmed answer is empty, reusing the existing answerHasText/setAnswerHasText pattern already present for the 'as recommended'/'pick' buttons. Frontends are pure MCP clients (CLAUDE.md) — this is UX affordance only; the authoritative rejection is T162. Cite anchors against live source before editing (line numbers may have drifted).
    
    Testing: happy-dom; controlled TEXT inputs do not fire onChange under happy-dom — use uncontrolled inputs (refs) per CLAUDE.md when adding/adjusting any test.
- acceptance: In the web client, both the detail 'save & mark answered' HoldButton and the BatchAnswerModal submit are disabled when the trimmed answer is empty, enabled otherwise. `bun run check` green from nix/pkg/cq-ledgers/.
- suggestedModel: standard
- dependsOn: ["T162"]
- ledgerRefs: ["defects:D29","goals:G16"]
- resultCommit: 6629483b3daef8dcf84f43b5e73e43839d6f6ae6
- completion: "Web: detail + BatchAnswerModal 'save & mark answered' HoldButtons disabled={!answerHasText} (trimmed-non-empty); init reads stored answer; 11 new tests + 4 batchModalClose fixes; integrated check green."
- sessionLogs: ["docs/logs/20260605-195232-a6b11f636a62b2079.md","docs/logs/20260605-195232-a9056ac0ca8ea30d7.md"]

### T164 — done

- createdAt: 2026-06-05T18:34:40.425Z
- updatedAt: 2026-06-05T19:58:57.084Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: "TUI UX: block Enter-submit of an empty/whitespace answer (TextPrompt Enter + BatchAnswerOverlay Enter)"
- description: |
    Complementary UX guard for D29 (prevents a server-rejected submission once T162's backend guard lands). Two TUI submit paths in DIFFERENT files: the answer overlay via TextPrompt Enter (TextPrompt.tsx ~30-43) and the BatchAnswerOverlay Enter (ledger-tui app.tsx ~1952). They are separate files, so a single task may edit both without same-file serialization concern.
    
    IMPLEMENT: on Enter, refuse to submit (no update_item(status:'answered')) when the trimmed answer is empty, reusing the existing answerHasText/setAnswerHasText pattern. Frontends are pure MCP clients (CLAUDE.md) — UX affordance only; authoritative rejection is T162. Cite anchors against live source before editing (line numbers may have drifted).
    
    Testing: ink-testing-library for the TUI per CLAUDE.md.
- acceptance: In the TUI, pressing Enter with an empty/whitespace answer in both the TextPrompt answer overlay and the BatchAnswerOverlay does NOT submit (no `answered` transition fired); a non-empty answer submits as before. `bun run check` green from nix/pkg/cq-ledgers/.
- suggestedModel: standard
- dependsOn: ["T162"]
- ledgerRefs: ["defects:D29","goals:G16"]
- resultCommit: 46591fdccd1f389b6a74d2359b3785719430386f
- completion: "TUI: empty/whitespace Enter blocked in both answer paths (answer-overlay onSubmit + BatchAnswerOverlay key.return, v.trim()!==''); shared TextPrompt untouched; 6 new ink tests; integrated check green."
- sessionLogs: ["docs/logs/20260605-193948-a69fbb42cb47285a1.md","docs/logs/20260605-195232-a28945e6421cc5460.md"]
