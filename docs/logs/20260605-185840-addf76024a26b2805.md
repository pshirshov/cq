# Investigate-explorer session log

- **Defect:** D30 — link-prompts.ts + cq-assets/README.md still reference relocated `llm/` root
- **Hypothesis:** H21 (asset-tree relocation drift → dangling symlinks)
- **Role:** explorer (read-only)
- **Returned lean:** supports

---

### Session summary
- **Did:** gathered evidence for hypothesis H21 (asset tree relocated `llm/` → `nix/pkg/cq-assets/`, but `link-prompts.ts` + cq-assets README still reference `llm/`, producing dangling symlinks).
- **Achieved:** 8 numbered evidence items; lean **supports**.
- **Discovered:** (a) `link-prompts.ts` LINKS all set `source: "llm/..."`; REPO_ROOT resolves to `nix/pkg/cq-ledgers/` (script at `scripts/link-prompts.ts`, `dirname/..`), and `package.json` runs it from there. (b) `nix/pkg/cq-ledgers/llm/...` does NOT exist — the `llm/` root is gone. (c) `assets.nix` reads `./commands`/`./agents` under cq-assets, confirming relocation. (d) real assets exist under `nix/pkg/cq-assets/{commands,agents}/`. (d') the script never stats/tests the SOURCE target — `linkExists` checks the LINK path only; `symlink(2)` succeeds on a nonexistent target and line 73 logs success → DANGLING links on a "clean" run. (e) cq-assets README still cites `llm/commands/...`/`llm/agents/...`.
- **Issues:** explorer runtime had only `Read` (no Bash); proved target-absence by ENOENT. Orchestrator re-validated all citations with Bash (`ls`, `sed`, `test -e`, `grep`) — all confirmed exact.

Orchestrator validation (Bash, 2026-06-05T18:58Z): link-prompts.ts:19 REPO_ROOT=scriptdir/.. ✓; all 14 LINKS source "llm/..." ✓; `ls nix/pkg/cq-ledgers/llm` → No such file or directory ✓; symlink loop checks only `absLink` not `absSource` ✓; assets.nix:49-50 `collectMdIn ./commands`/`./agents` ✓; cq-assets/agents/plan-reviewer.md + commands/plan/start.md present ✓; README.md:1,10-11,40-42 still cite `llm/` ✓.
