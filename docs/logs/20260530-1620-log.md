# Session log — pcontent (`/plan` planning CONTENT visible + explore-once)

Date: 2026-05-30. Driver: VSM-loop (S5/S4). Worktree
`.claude/worktrees/planning-content`, branch `planning-content`, base `2bb2c4b`.
No Task tool in this harness → plan / adversarial-review / execute / review run
inline as explicit distinct steps.

## Goal

Make `/plan` planning CONTENT visible (the History Detail of a planning run was
EMPTY; answered questions were not recorded) AND fix the deeper inefficiency
where every phase re-explores the repo (the cause of PHASE-TIMEOUT-01's 300s
bump). Defects: WF-HIST-02 (empty Detail / Q&A) + PLAN-EXPLORE-01 (explore-once).

## Cycles run

- **G2 plan** (S4): plan doc `docs/drafts/20260530-1620-plan-content.md`;
  storage decision (grounding on GOALS_SCHEMA vs workflow_session table) resolved
  to the goal field with justification; adversarial pre-mortem R1–R5 in the doc.
- **G2b adversarial plan-review** (S3* on the plan, inline): surfaced R3
  (close-before-`result` race) as load-bearing → verified `SqliteEventLog.fdFor`
  reopens on a post-close append, so a late `result` still lands; close at settle
  is acceptable. R1 (GOAL-TITLE-01) mitigated by making `grounding` OPTIONAL.
- **M-PCONTENT execute** (S1):
  - pcontent-1+2+3 (commit 622d5f2): recorder event sink + lane forwarding +
    Q&A transcript + explore-once grounding. Delivered as one buildable commit
    (interwoven across shared files).
  - pcontent-4 (commit <this>): e2e + discharge.
- **S3\* audit**: read the recorded events back via `persistence.events.readAll`
  in the dual-adapter content test (phase children non-empty, root carries
  asked+answered); the real-SDK integration test proves grounding survives the
  SDK input-strip; the e2e proves the Detail is non-empty + Q&A visible in the
  real app.

## How non-empty-Detail + Q&A-visible + explore-once were proven

- **Non-empty Detail (WF-HIST-02a):** `workflow-content.test.ts` (dual adapter)
  forwards SDK messages through the lane → asserts every phase child's
  `events.readAll` is non-empty; the e2e clicks the producer child row and
  asserts `detail-body` contains the post-submit "submitted" text (empty before
  this cycle).
- **Q&A visible (WF-HIST-02b):** the content test asserts the root carries
  "Asked 1 clarifying question … Which platforms?" and "Answered <Qid>: web"
  (answer idempotent — no dup on re-answer); the e2e opens the root Detail and
  asserts the asked text renders.
- **Explore-once (PLAN-EXPLORE-01):** `workflow-phases.test.ts` asserts the
  producer keeps the full explore + asks for grounding, `renderGroundingPreamble`
  softens with grounding / falls back to full without, and every later-phase
  builder prepends grounding + "do NOT re-explore" when given. The content test
  asserts grounding is persisted on the goal, survives a runtime restart, and
  reaches the clarify-reviewer AND planner prompts after `reconcile`.
- **No GOAL-TITLE-01 repeat:** `workflow-integration.test.ts` (real-SDK
  MockAnthropic) submits a payload WITH grounding; the SDK strips to
  `submitPlanSchema`; grounding round-trips onto the goal (would be "" if dropped)
  → the producer does not time out from a stripped field.

## Manual scenario (operationalized by the e2e)

`/plan build a history probe app` (mocked backend) → questions_ready banner →
History tab → the run shows a "Plan" badge + a nested `producer` child →
clicking the producer row opens a Detail that REPLAYS the producer transcript
(non-empty), and clicking the run's root row opens a Detail showing the asked
Q&A ("Asked 1 clarifying question: Which platforms should it target?"). The
explore-once grounding is visible in later-phase context (the clarify/planner
prompts carry the persisted grounding + the softened "do NOT re-explore"
instruction — asserted by the restart test), so later phases do targeted reads
only instead of re-surveying the repo. Because GOALS_SCHEMA gained `grounding`,
an existing dev `docs/goals.md` requires `cq reset` once (gitignored,
regenerable; none existed in this worktree).

## Discharge

- `bun run check` exit 0 ×2 — run 1: 1120 pass / 1 fail (a pre-existing
  `heartbeat > previous-nonce hb.spong` timing flake under full-suite load,
  isolated 3/3 green, outside this cycle's scope — touches no workflow/persist/
  ledger code); run 2: 1120 pass / 0 fail. tsc + eslint clean both runs. Baseline
  1109 + 11 new tests.
- `bun run e2e` 28/28 (extended `plan-workflow-history.spec.ts` in-place; no count
  delta).
- `nix build .#default` exit 0 (`result` → cq-0.0.1; local build, remote SSH
  builder unreachable — same as prior cycles). No new SOURCE files (RESET-02:
  N/A — only edits to existing source + a new test file + a draft).

## Defects resolved this session

- `WF-HIST-02` → commit 622d5f2 (empty Detail / Q&A transcript).
- `PLAN-EXPLORE-01` → commit 622d5f2 (explore-once grounding).

## Metrics

- WIP max 1; review rounds <pcontent:1 (inline adversarial plan-review + S3* audit)>
- S3-S4 firings <research:0, replan:0>; time-to-stabilize <n/a>
- Material scope delta <none — touched exactly the files the plan named + the
  workflow `index.ts` barrel (grounding re-export) + the FakePhaseSubagent helper
  (fireEvents)>; verification <complete>
- Audit discrepancies <0>; algedonic <ordinary:0, bypass:0, depth-limit:0>;
  bypass false-positives <0>
