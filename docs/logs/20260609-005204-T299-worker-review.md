# Implement worker+review log — T299 (M120/G34) — DROP build-time model/modelMappings

- Worker: sonnet (worktree agent-a7fe70f42ec498364), commit bf496b6 (cherry-picked to main cfa8acb; base 72e5741, worktree-only reset — D43). bun run check 1290/0; integrated 1289/0.
- Removed model/modelMappings from AgentRole + gen-agents-catalogue.ts (incl. the now-unused deriveModelClass/deriveModelMappings + CONFIG_EXAMPLE + @cq/config parse imports), regenerated agentsCatalogue.gen.ts (no model fields on any role); removed the ModelClass/HarnessModelMappings types (unused); removed App.tsx's AgentModelMappings component + the static 'Model class'/'Model mappings' rows — the overlay AgentModelCell (help-agent-<id>-model) is now the SOLE model display; removed the T279 role.model assertion + narrowed the freshness test (dropped the model-field invariant; kept the regen byte-diff). KEPT the T285 AGENT_ROLE_TIERS roster + assertRosterMatchesShared (server-side anti-drift, separate). 7 files, +14/-232. filesTouched: gen-agents-catalogue.ts, App.tsx, agentsCatalogue.gen.ts, agentsCatalogue.ts, test/{agentsCatalogue.gen,agentsCatalogue,agentsTab}.test.*.
- Reviews: opus (aa1b94a86b4bbc794) approve + minimax approve; grok+codex abstained. Both verified: AgentRole + gen.ts carry no model/modelMappings; overlay cell is sole display; T279 role.model assertion removed without weakening other static-field assertions; freshness test narrowed; roster untouched; no residual AgentRole.model reads (entry.modelMappings is the overlay entry, correct); check green. Reconciled go-ahead.
- This is the cleanup that fulfills G34 follow-up #2's core goal: the Agents tab now shows ONLY live-configured models (resolved at runtime via get_agent_models), with no build-time/example-derived model data.
