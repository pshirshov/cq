# investigate-explorer session log

- **Defect:** D2 — ledger-mcp fails in uninitialized dir (should auto-init)
- **Hypothesis:** H2 — FsLedgerStore construction/index build throws on missing docs/dir/files
- **Role:** explorer (investigate-explorer)
- **Agent:** add0963e51e59c886 — returned lean: **contradicts**

### Session summary
- **Did:** gathered evidence for H2 (store construction/index build throws on missing docs/ or ledger files).
- **Achieved:** 6 numbered evidence items; lean contradicts.
- **Discovered:** The FsLedgerStore constructor (FsLedgerStore.ts:149-158) does NO fs I/O (only path joins). init() (254-340) auto-creates docs/.locks/archive via mkdir recursive, swallows ENOENT on registry + each ledger file (seeding freshLedger), bootstraps CANONICAL_LEDGERS. Server (main.ts:343-344) eagerly constructs+inits before serving. Passing test (canonical-ledgers.test.ts:451-465) runs init() against a fresh no-docs/ dir and asserts full provisioning. Only startup throw is BootstrapViolationError on existing-divergent schema. Reusable routine: FsLedgerStore.init()/createEmbeddedStore().
- **Issues:** H2 does not reproduce. Recommend reproducing `ledger-mcp --cwd <empty-tmpdir>` before forming a successor hypothesis — drill watcher/--cwd/handshake next.
