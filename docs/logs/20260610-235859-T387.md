# Session log — T387 implement-worker (opus) + 4-reviewer panel — UNANIMOUS approve

- **Task:** T387 (M166, G48; load-bearing dispatcher); worker a8cc36da status **pass**; resultCommit baf1077 (merged); check 1682/0. Review: opus af51d56 + codex/grok/minimax all approve.

### Worker
- Added a MODES (mcp|tui|web) routing layer to cq's `dispatch()` (packages/cq-cli/src/main.ts) running BEFORE native parseSubcommandArgs; each mode dynamically imports its product + calls exported `main(argv.slice(1))` VERBATIM (no re-parse), returns {exitCode:0} once the delegated main resolves. Injectable `ModeDelegates` seam (default = real dynamic imports; tests inject recorders). Refactored ledger-tui's module-private `run()` into `export async function main(argv)`; self-run → `main(process.argv.slice(2))`. Added `jsx:react-jsx` to cq-cli/tsconfig.json (fixes TS6305 from the first cross-package .tsx path-map import; cq-cli emits no JSX itself). dispatch.test.ts: 11 tests (verbatim argv.slice(1), mcp restore pass-through, bare-mode empty argv, native subcommands retained). check 1682/0.

### Review (UNANIMOUS approve)
opus (repo-grounded, re-ran check): mode branch precedes parseSubcommandArgs (L475-478 before L483); argv.slice(1) correct (dispatch operates on process.argv.slice(2)); returns {exitCode:0} fitting the contract; ModeDelegates seam minimal+justified; mcp path prints nothing (protocol-only); ledger-tui main(argv) export + self-run behavior-preserving (embedded mcpUrl===null gate unchanged); jsx:react-jsx reproduced/fixed TS6305 (verified by toggling); all 3 product imports resolve + expose main; mcp/web bins untouched; no scope creep/dead code; check 1682/0. codex/grok/minimax concur. (minimax noted a harmless 100644→100755 mode flip on main.tsx — a shebang bin entry, inconsequential.)
```json
{"taskId":"T387","verdict":"approve","criticism":[],"questions":[],"defects":[],"rationale":"mode-before-native-parse routing with verbatim argv.slice(1) (test-proven); tui main(argv) export + self-run behavior-preserving; jsx:react-jsx minimal load-bearing fix for TS6305; 3 product imports resolve; mcp/web bins untouched; check 1682/0.","summary":"T387 unanimous approve — load-bearing dispatcher correct."}
```
