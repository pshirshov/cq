# Session log — G48 plan-advance (opus), clarifying round 1

- **Goal:** G48 ("Unify the CLI tools into a single `cq` binary — cq tui / cq web / cq mcp / …"); role: planner; returned **awaiting-answers**. Filed Q214–Q218 under M164.

### Session summary
- **Did:** Grounded G48 read-only against the real repo (cq-cli dispatcher, ledger-mcp/tui/web mains, flake.nix, .mcp.json, package.json bins), then filed the first batch of clarifying questions. No plan emitted, no goal transition.
- **Achieved:** Created Q214 (unification mechanism: in-process delegation vs launcher vs absorb), Q215 (backward-compat of old `ledger-mcp`/`ledger-tui`/`ledger-web` names: alias vs remove, impact on `.mcp.json` + dev-llm + docs), Q216 (mode names + flag pass-through, `cq mcp restore`, `cq --help`), Q217 (Nix packaging: one fat `.#cq` vs lean per-product derivations + closure/FOD impact), Q218 (scope/behavior-invariance + incremental A-then-B sequencing + acceptance bar). All under M164, all `ledgerRefs: ["goals:G48"]`, each with concrete recommendation + suggestions. Goal remains `clarifying`.
- **Discovered:** The three product mains (`ledger-mcp/src/main.ts`, `ledger-tui/src/main.tsx`, `ledger-web/src/serve.ts`) all EXPORT `main(argv)`/`serve` and are guarded by `import.meta.main`, so in-process delegation from cq-cli is low-friction and behavior-preserving. cq-cli (`packages/cq-cli/src/main.ts`) is a flat first-positional dispatcher (`SUBCOMMANDS`/`HANDLERS`/`USAGE`) and constructs an `FsLedgerStore` directly (does not yet depend on the three product packages). `.mcp.json` launches `nix run .#ledger-mcp --`. The TUI and web both already import `@cq/ledger-mcp` for embedded mode (in-memory transport / co-hosted `/mcp`+`/ws`), so a merged closure must carry that embedded-server closure plus the web SPA (react/elkjs, Bun.build at startup) and Ink. flake.nix has five derivations sharing one FOD; each is a thin `makeWrapper bun run <main> --`.
- **Issues:** none — awaiting user answers before a plan can be written.
