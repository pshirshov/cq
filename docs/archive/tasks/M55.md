## M55

### T165 — done

- createdAt: 2026-06-05T18:41:42.922Z
- updatedAt: 2026-06-05T20:44:42.013Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: Extend investigate-explorer JSON contract with optional probeRequest field
- description: "In nix/pkg/cq-assets/agents/investigate-explorer.md, extend the returned fenced-json contract (currently {hypothesisId, evidence[], lean, notes}) with a NEW optional field `probeRequest` (object, omitted by default): { what: \"<commands/builds/tests the explorer needs RUN to gather decisive evidence>\", why: \"<why read-only inspection is insufficient>\" }. Document in the body: the explorer STILL writes nothing and runs nothing (its disallowedTools keep Bash/Edit blocked); when read-only inspection cannot settle H because evidence requires EXECUTION (run a repro, `bun test`, a build, git operations), the explorer sets `probeRequest` and returns `lean: insufficient`, leaving the actual execution to the orchestrator-dispatched prober. Keep the explorer's read-only/no-worktree invariant text intact. Do NOT add Bash to the explorer's tools. This is a contract/doc-only change to one file."
- acceptance: investigate-explorer.md documents the optional probeRequest field with what/why sub-keys; the explorer's disallowedTools line is UNCHANGED (still blocks Bash/Edit/worktree); the body explicitly states the explorer never executes — it only RETURNS a probeRequest. Markdown renders; no other asset references a probeRequest field the explorer doesn't document.
- suggestedModel: standard
- ledgerRefs: ["goals:G15"]
- resultCommit: 17bdf49
- completion: "investigate-explorer.md JSON contract extended with optional probeRequest {what,why}; explorer stays read-only (disallowedTools unchanged), only RETURNS the request with lean:insufficient."
- sessionLogs: ["docs/logs/20260605-203620-ac996d83316cba0a8.md","docs/logs/20260605-204152-a7b80da0000390eb5.md"]

### T166 — done

- createdAt: 2026-06-05T18:41:59.262Z
- updatedAt: 2026-06-05T21:02:19.943Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: Author investigate-prober.md agent (Bash + isolated worktree, read+execute)
- description: "Create nix/pkg/cq-assets/agents/investigate-prober.md, a NEW subagent modeled on investigate-explorer.md but EXECUTION-capable. Frontmatter: name=investigate-prober; description (one paragraph: dispatched by /investigate:advance ONLY on an explorer's probeRequest; runs READ+EXECUTE in an ISOLATED throwaway worktree; gathers evidence by RUNNING things; returns the SAME evidence-json shape; never spawns subagents; makes NO persisted edits to the main checkout). Tools: ALLOW Bash + Read/Grep/Glob (+ codegraph/web per explorer); the worktree isolation is supplied by the orchestrator at dispatch (Claude Agent isolation:\"worktree\"). Body must state the SCOPE constraints from Q89 verbatim in spirit: probe = read + EXECUTE (run the repro, `bun test`, builds, git inspection) INSIDE the throwaway worktree; NO persisted edits to the main checkout (any writes confined to the discardable worktree); LOCAL-ONLY, NO network (curl/git fetch) by default. The prober receives the probeRequest {what,why} + H's statement + branch context in its dispatch prompt, runs exactly what's needed, and RETURNS the same fenced-json {hypothesisId, evidence[], lean, notes} the explorer returns (each evidence item still a citation + verbatim excerpt + relevance; for a command result the 'citation' is the command + a verbatim excerpt of its output). It does NOT adjudicate and writes no ledger. Include the same Session summary handover block. Depends on T165 so the probeRequest field shape is settled first."
- acceptance: agents/investigate-prober.md exists with valid name/description/tools frontmatter; allows Bash, omits/blocks ledger writes (no mcp__ledger__* needed — it returns json like the explorer); body states read+execute-in-throwaway-worktree, NO persisted main-checkout edits, local-only/no-network; returns the SAME evidence-json shape as investigate-explorer; emits a Session summary block. Frontmatter parses (matches the shape of existing agents/*.md).
- suggestedModel: frontier
- dependsOn: ["T165"]
- ledgerRefs: ["goals:G15"]
- resultCommit: 108c0cb
- completion: "Authored agents/investigate-prober.md: execution-capable subagent (Bash + worktree isolation) for Q89 read+execute probes; returns the explorer's evidence-json shape; no persisted main-checkout edits, local-only/no-network; writes no ledger."
- sessionLogs: ["docs/logs/20260605-205630-a7346fe5fcba01d81.md","docs/logs/20260605-210010-a5ac7aec42e31eeaa.md"]

### T167 — done

- createdAt: 2026-06-05T18:42:12.933Z
- updatedAt: 2026-06-05T21:13:02.121Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: Wire prober dispatch into investigate/advance.md orchestrator
- description: "Edit nix/pkg/cq-assets/commands/investigate/advance.md to dispatch investigate-prober when an explorer returns a probeRequest. Add to step 3/4 (DISPATCH / VALIDATE): after an explorer returns, if its json carries a `probeRequest` (and the orchestrator judges the execution warranted for adjudicating H), dispatch an `investigate-prober` via Agent with `subagent_type: \"investigate-prober\"` and `isolation: \"worktree\"` (Claude native throwaway worktree; for Codex, the orchestrator does `git worktree add ../wt-probe-<H> ... ; git worktree remove` after), passing the probeRequest {what,why} + H's statement + branch context. The prober runs read+execute in that worktree and returns the SAME evidence-json; the orchestrator harvests it through the EXISTING citation-revalidation path (re-open cited file:line / re-run the cited command to confirm output) and then DISCARDS the worktree. State the scope guard: probes are LOCAL-ONLY, no network, no persisted main-checkout edits; the worktree is always removed after harvest. Update the §Session logs rule to also log the prober subagent (one log file per dispatched prober). Add `Agent` worktree isolation note consistent with implement/advance.md's worktree handling. NOTE: this also serializes after the explorer-contract change — depends on T165 (probeRequest shape) and T166 (the prober exists to dispatch)."
- acceptance: investigate/advance.md dispatches investigate-prober (subagent_type + worktree isolation) gated on a returned probeRequest; documents harvest-then-discard of the throwaway worktree; documents the local-only/no-network/no-persisted-edit guard; logs the prober per the Session logs convention. The allowed-tools line already includes Agent/Bash/Write — confirm no new tool grant is needed. No dangling reference to a prober behaviour not defined in T166's agent file.
- suggestedModel: frontier
- dependsOn: ["T165","T166"]
- ledgerRefs: ["goals:G15"]
- resultCommit: 565da97
- completion: "investigate/advance.md now dispatches investigate-prober (gated on an explorer's probeRequest, isolation:worktree), harvests via citation-revalidation then discards the worktree; Q89 local-only/no-network guard; logs the prober."
- sessionLogs: ["docs/logs/20260605-210751-a4c5d1972671574de.md","docs/logs/20260605-211047-a3bf57ee924b1799c.md"]

### T168 — done

- createdAt: 2026-06-05T18:42:43.686Z
- updatedAt: 2026-06-05T21:13:02.178Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: Register investigate-prober in link-prompts.ts (verified source root) + refresh cq-assets README
- description: "Two doc/wiring edits for FEATURE 1. (0) VERIFY THE REAL ASSET SOURCE ROOT FIRST (R169 criticism 2): scripts/link-prompts.ts currently resolves LINKS `source` as 'llm/commands/...'/'llm/agents/...' relative to nix/pkg/cq-ledgers/, but the assets were relocated to nix/pkg/cq-assets/{commands,agents}/ and the 'llm/' tree does NOT resolve under cq-ledgers (verified: Read of nix/pkg/cq-ledgers/llm/agents/plan-reviewer.md fails 'File does not exist' while nix/pkg/cq-assets/agents/plan-reviewer.md reads fine). Before adding any entry, determine the CORRECT source root against the current tree: check whether nix/pkg/cq-ledgers/llm is a (stale/missing) symlink to ../cq-assets or simply absent, and pin the new entry to the path that ACTUALLY resolves to a file (i.e. the nix/pkg/cq-assets/... path, unless an llm->cq-assets symlink is in fact present and resolves). Do NOT blindly replicate the existing 'llm/' root if it is stale. (NOTE: repointing the EXISTING stale entries + README 'llm/' references is the separately-filed out-of-scope defect D—; THIS task only needs its OWN new entry to resolve correctly, but if the chosen root differs from the stale existing entries, add the new entry on the verified-correct root and note the discrepancy.) (1) Append a LINKS entry for the new agent so in-repo Claude dogfooding discovers it: { link: \".claude/agents/investigate-prober.md\", source: \"<the VERIFIED-correct source path that resolves to nix/pkg/cq-assets/agents/investigate-prober.md>\" }. (2) nix/pkg/cq-assets/README.md: the 'Current assets' table is STALE — it omits ALL investigate/* assets that already exist. Add rows for the existing investigate assets (commands/investigate/start.md, commands/investigate/advance.md, agents/investigate-explorer.md, commands/advance.md) AND the NEW agents/investigate-prober.md, and add the matching Claude-link rows in the 'Three consumers' table. Keep edits surgical — only add rows, do not restructure."
- acceptance: link-prompts.ts has an investigate-prober LINKS entry whose `source` is VERIFIED to resolve to an existing file (nix/pkg/cq-assets/agents/investigate-prober.md) against the current tree — not a stale 'llm/' root that yields a dangling link. `bun run link-prompts` runs clean AND the created .claude/agents/investigate-prober.md symlink RESOLVES to an existing file (assert `test -e .claude/agents/investigate-prober.md` succeeds — i.e. the target exists, not merely that symlink() returned). README.md 'Current assets' + 'Three consumers' tables list the investigate/* assets and the new prober. `bun run check` green (link-prompts.ts is TS — must typecheck/lint).
- suggestedModel: fast
- dependsOn: ["T166"]
- ledgerRefs: ["goals:G15"]
- resultCommit: 4262ebc
- completion: Registered investigate-prober in link-prompts.ts (../cq-assets source, symlink resolves) + README Current-assets/Three-consumers tables list all investigate/* assets + prober; integrated check green.
- sessionLogs: ["docs/logs/20260605-210751-aafff80d4c318e49b.md","docs/logs/20260605-211047-af31e7820670fed1d.md"]
