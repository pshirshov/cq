# Implement worker+review log — T293 & T296

## T293 (M118/G34) — fetch get_agent_models on mount + overlay (worker: opus, worktree agent-aef18e1e1b957c8e0)
- commit 3dfeca4 (cherry-picked to main 4f5a39a; base 6882095, worktree-only reset — D43). bun run check 1286/0.
- App.tsx fetches client.getAgentModels() once on connect into AppState {agentOverlay:Map|null, agentOverlayError} with a catch-ALL (any throw -> overlayError, R341). Pure resolveAgentModelView (overlay-only, never reads role.model -> already T299-independent) + AgentModelCell rendering the 5 distinguished states under a new help-agent-<id>-model testid. TRANSITIONAL: kept the build-time Model class/mappings rows (T279 test asserts role.model; T293 must not edit it) — T299 removes them. filesTouched: ledger-web/src/App.tsx.
- Reviews: opus (aee35932b3490f1bc) approve (catch-ALL confirmed; overlay-only; 5 labels TS-exhaustive; transitional rows justified; re-ran check 1286/0). minimax DISAPPROVE — ADJUDICATED as a false-positive: (1) its "TS readonly-vs-mutable error" claim is REFUTED by the green tsc -b (a mutable string[] IS assignable to a readonly string[] field — minimax inverted the variance); (2) its "pre-fetch flicker to default/not-configured" is a minor UX point outside the acceptance and effectively non-visible (fetch is on app-mount; the Agents help-tab opens later, after the overlay resolves). Neither is an acceptance failure. grok+codex abstained. Reconciled go-ahead (opus authoritative + adjudicated false-positive). Optional future nicety: a 'loading' placeholder while overlay===null && !overlayError.

## T296 (M121/G36) — effort-suffix docs + cq.toml.example (worker: haiku, worktree agent-a2e34cc58ea6e6aba)
- commit 69348e4 (cherry-picked to main d208208; base 6882095, worktree-only reset). bun run check 1286/0.
- Documented the optional :<effort> grammar in cq.toml.example + the parseReviewerToken module-header jsdoc (config.ts, COMMENT-ONLY): grammar harness:[provider/]model[:effort]; ':' reserved on both model halves; per-harness enums (pi off/minimal/low/medium/high/xhigh; claude low/medium/high/xhigh/max); pi '--model provider/model:<effort>' shorthand (NO --thinking); claude inert in the pi-extension. Added grok-xhigh=pi:grok-build/grok-build:xhigh + opus-high=claude:opus-4.8[1m]:high to cq.toml.example (both parse; classified in [tiers]). filesTouched: cq.toml.example, cq-config/src/config.ts (comment-only).
- Reviews: opus (ae3fb9d93a21c1e04) approve + minimax approve; grok+codex abstained. No --thinking mention; both enums accurate; comment-only; edited the committed template not a ledger file. Reconciled go-ahead.
