## M36

### T130 — done

- createdAt: 2026-06-03T05:06:13.822Z
- updatedAt: 2026-06-03T06:13:33.770Z
- author: "opus-4.8[1m]"
- session: fe0aaf85-56b3-45ce-a7fc-718ab19c37e1
- headline: Stabilise flaky ledger-tui ink-testing-library tests for deterministic concurrent bun run check (D20)
- description: |
    ROOT CAUSE (confirmed by the D20 reviewers + R125 re-grounding): packages/ledger-tui/test/* uses ink-testing-library/happy-dom assertions whose settle budgets starve under concurrent `bun run check` load (multiple implement worktrees saturating CPU), so a rotating, DISJOINT set of tests times out non-deterministically. R125 corrected the mechanism on three points; this revision encodes those corrections. SCOPE: TEST-HARNESS ONLY — packages/ledger-tui/test/* — no product (src) diff expected; if a product change appears necessary, FLAG it rather than introduce it. At implementation time, RE-GREP packages/ledger-tui/test/ to confirm the current line numbers below (they drift).
    
    === TARGET SITES (verified by R125 re-grounding; all in app.test.tsx + navMemo.test.tsx — there is NO scrolling.test.tsx / suggestions.test.tsx / questionDetail.test.tsx) ===
    
    (A) navMemo.test.tsx (T85) — 'heavy derivations run once per data-change (N=500)' (~lines 132-171). IMPORTANT: this test ALREADY asserts ONLY the derivationCounters (~lines 153-168) and ALREADY uses a waitQuiescent() poll-until-condition (~lines 51-62) — there is NO in-body wall-clock assertion to drop. The RESIDUAL FLAKE is the bun DEFAULT per-test timeout (~5000ms) being exhausted under concurrent CPU contention by waitQuiescent (up to 200x15ms=3s) PLUS the N=500 mount PLUS the NAV=40 loop (each `await tick(8)` ~320ms+) — the test legitimately approaches/exceeds 5s when the box is saturated. FIX (wall-clock COST, not assertion): bound the per-test wall-clock — set an explicit generous per-test timeout (`it(name, fn, <ms>)` or `test(name, fn, { timeout })`, bun supports the 3rd-arg ms form) AND/OR reduce the work to be deadline-independent (lower NAV and/or N, tighten/cap the waitQuiescent poll budget so the worst case is well under the chosen timeout). Do NOT 'assert counters instead of wall-clock' — that is already done.
    
    (B) app.test.tsx, describe 'ledger-tui scrolling' > 'scrolls a long list and shows a scrollbar' (~lines 547-567). Uses fixed `await tick(40)` settles and a 59-step nav loop with `await tick(4)` each. POSITIVE assertions ('T0 ' visible, '█' scrollbar, then 'T59 ' after scroll). FIX: replace fixed tick budgets with poll-until-condition over r.lastFrame() (the T112 live-badge pattern, app.test.tsx waitForFrame ~lines 183-190) — poll until 'T0 ' appears before the first assertion, and until 'T59 ' appears after the nav loop instead of a fixed trailing tick.
    
    (C) app.test.tsx, 'filters the item list by status type (active/terminal)' (~lines 498-518). Fixed `await tick(30)` after each filter apply. Mixed assertions: POSITIVE ('[terminal]' / 'D1' visible) and NEGATIVE (`.not.toContain('D1')` after terminal filter). FIX: for the positive transitions poll-until ('[terminal]' chip appears; 'D1' reappears under active). For the negative (D1 hidden under terminal): poll-until the STABLE positive co-indicator first (the '[terminal]' header chip is present), THEN assert `.not.toContain('D1')` — i.e. settle on a positive invariant that only holds once the filter has applied, then check the negative.
    
    (D) app.test.tsx, 'toggles pane orientation and resizes without losing content' (~lines 520-534). Fixed `await tick(20)`. POSITIVE assertions (D1 + 'headline' still visible after 'o'/']'/'['). FIX: poll-until 'headline' (detail pane) is present after the orientation toggle instead of the fixed tick.
    
    (E) app.test.tsx, describe 'ledger-tui suggestions bulleted list (T57)' > 'renders suggestions string[]...' (~lines 1095-1113). Fixed `await tick(40)` after navigating to Q2. POSITIVE assertions ('• opt a' / '• opt b'). FIX: poll-until '• opt a' appears (waitFor) instead of the fixed tick; keep the `.not.toContain('opt a, opt b')` negative AFTER the positive poll settles.
    
    (F) app.test.tsx, describe 'ledger-tui archive view (T33)' > 'the \'s\' key is inert on an archived item' (~lines 936-956) AND 'the \'e\' key is inert on an archived item' (~lines 958-976). These are NEGATIVE-ASSERTION tests: they assert the overlay did NOT open after a fixed `await tick(40)` post-keypress ('s'/'e'). You CANNOT poll-until-a-frame-does-NOT-contain-X. FIX (settle-then-assert): the test already does a poll (waitForFrame on 'archived task') to reach a stable archived view; after pressing the inert key, SETTLE deterministically before the negative assertion — either await a BOUNDED number of ticks AND re-assert the STABLE positive invariant that must still hold ('[archived]' for 's', 'read-only' for 'e') as the settle signal, or poll-until that stable positive co-indicator is present and unchanged, THEN assert the overlay markers are absent. The negative assertion stays a negative; the determinism comes from settling on a stable positive frame, not from polling the negative.
    
    === CROSS-FILE COUNTER POLLUTION (R125 point 4) ===
    derivationCounters is a MODULE-GLOBAL shared across every test file in the bun process (navMemo.test.tsx comment ~lines 139-142 documents this). navMemo already guards itself with resetDerivationCounters() before each measurement, but App renders in OTHER files (app.test.tsx and any file importing App) mutate the SAME global concurrently. Specify the isolation mechanism CONCRETELY: do NOT leave it as 'isolate shared state'. Preferred: call resetDerivationCounters() in a beforeEach (or immediately before the measured window) in EVERY test file that mounts <App>, so a concurrent/preceding render cannot leave the global dirty when navMemo measures; alternatively, if bun runs files in separate workers such that the module-global is per-file (verify this at implementation time via `bun test` worker model — if confirmed, the cross-file hazard does not apply and navMemo's existing per-test reset suffices, in which case DOCUMENT that finding and drop this sub-item). Pick based on the verified bun execution model, not a guess.
- acceptance: "After the fix, repeated and concurrent `bun run check` runs are deterministic for the targeted tests (navMemo T85 N=500, scrolls-a-long-list, status-filter, pane-orientation, suggestions T57, the two inert-key archive tests). Specifically: (1) navMemo T85 has an explicit per-test timeout and/or reduced NAV/N so its worst-case wall-clock under saturation is comfortably under the timeout (no reliance on the implicit 5000ms default); (2) the POSITIVE-assertion sites (scroll, suggestions, pane-orientation, the positive halves of status-filter) use poll-until-condition over r.lastFrame() in place of fixed tick()/sleep budgets; (3) the NEGATIVE-assertion sites (the 's'/'e' inert-key tests, and the 'D1 hidden under terminal' check) use a settle-then-assert strategy keyed on a STABLE POSITIVE invariant before the negative assertion (no attempt to poll-until-a-negative); (4) derivationCounters cross-file pollution is handled by the concretely-specified mechanism (per-file reset before the measured window, OR a documented finding that bun's worker model makes the global per-file); (5) NO packages/ledger-tui/src product diff is introduced; (6) `bun run check` is green."
- suggestedModel: standard
- ledgerRefs: ["defects:D20","goals:G8"]
- resultCommit: bfa70ed
- completion: "De-flaked ledger-tui tests: fixed-tick settles → poll-until-condition (waitFor/waitForFrame) across targeted + adjacent sites; navMemo T85 explicit 30s timeout + N=500→200/NAV=40→20 (N-independent counter invariant); negative inert-key tests settle-then-assert on stable positives; bun runs files sequentially so navMemo's resetDerivationCounters() suffices. Test-only; 723/0 on two consecutive runs."

### T131 — done

- createdAt: 2026-06-03T05:06:24.264Z
- updatedAt: 2026-06-03T06:13:45.049Z
- author: "opus-4.8[1m]"
- session: fe0aaf85-56b3-45ce-a7fc-718ab19c37e1
- headline: FsLedgerStore reset path must handle NON-canonical ledgers — no orphan .md or stale FTS docs survive (D21)
- description: |
    ROOT CAUSE (confirmed by the T123 reviewer, see D21): FsLedgerStore.backupAndReinit (reused by the new public reset(), T123) enumerates only CANONICAL_LEDGERS for the backup and rewrites the registry to canonical-only. A ledger created via the public createLedger() is therefore (a) NOT backed up, (b) its docs/<name>.md orphaned on disk, and (c) its FTS docs SURVIVE the reset: reset() clears only this.ledgers; init() re-indexes only registry (canonical) ledgers; searchIndex.removeLedger is NEVER called for the dropped non-canonical ledger. A subsequent ftsSearch then returns hits for a wiped ledger, contradicting reset()'s doc-comment 'no stale FTS docs survive'. PRE-EXISTING property of backupAndReinit (G4/T94/T95 init-divergence path), not introduced by T123.
    
    FIX (per D21.suggestedFix; primary option): in the reset path, snapshot+drop EVERY REGISTRY ledger (not just CANONICAL_LEDGERS) and call searchIndex.removeLedger for each ledger absent from CANONICAL_LEDGERS before re-init — so no orphan docs/<name>.md file and no stale FTS docs survive. ALTERNATIVELY (lighter, acceptable if you judge it the sounder contract): explicitly DOCUMENT reset()/backupAndReinit as canonical-only and ASSERT that precondition (fail-fast if a non-canonical registry ledger is present at reset). Implementer's call between the two — but the chosen option must make the orphan/stale-FTS outcome impossible (either eliminated or rejected by assertion), not merely undocumented. Scope: packages/ledger/src/store/FsLedgerStore.ts (+ test).
- acceptance: "A new test creates a non-canonical ledger via createLedger('ops', ...), writes at least one item, then calls reset(): under the primary fix it asserts (a) no docs/ops.md orphan remains on disk, (b) no 'ops' registry entry remains, and (c) ftsSearch returns NO 'ops' hits; under the lighter contract option the test instead asserts reset() rejects (fail-fast) when a non-canonical ledger is present, with the contract documented. `bun run check` is green."
- suggestedModel: standard
- ledgerRefs: ["defects:D21","goals:G8"]
- resultCommit: 8c33435
- completion: "reset()/backupAndReinit now handle non-canonical ledgers: backupAndReinit backs up + unlinks docs/<name>.md for registry ledgers absent from CANONICAL_LEDGERS; reset() calls searchIndex.removeLedger() for each before clearing this.ledgers. New reset-noncanonical.test.ts (fails pre-fix). Init divergence path preserved (empty non-canonical set = no-op)."
