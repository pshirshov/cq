# plan-reviewer — goal G6 — R96 (revise)

- Goal: G6 (defect-seeded cleanup; follow-up fix unit D = D12 / tasks T108 + T109)
- Role: reviewer (plan-reviewer subagent)
- Verdict: `revise` — review id **R96** (3 criticisms, 0 new questions, 0 out-of-scope defects)

### Session summary
- **Did:** reviewed the revised plan for plan-flow goal G6 (T108 + T109 + D12), judging whether R95's 3 criticisms are resolved, grounded against live source in `packages/ledger` and `packages/ledger-web`.
- **Achieved:** verdict `revise`, review id R96, 3 criticisms / 0 new questions / 0 out-of-scope defects filed.
- **Discovered:** R95 criticisms 2 (acceptance non-reproduction) and 3 (ItemTable wiring) are fully resolved — T108's revised acceptance now exercises the T109 backfill path (no hand-fabricated inline title) and its ItemTable-prop/extension spec matches the confirmed `ItemTable` signature (App.tsx:1687-1709), the `&& !isMilestones` ArchiveSubsections gate (App.tsx:1182), the `selectedArchiveRow`/`isArchived` detail path (App.tsx:1230-1240), and the `p.title.length>0 ? ... : p.id` fallback (App.tsx:1990). Dependencies (T108.dependsOn=[T91,T109]; D12.dependsOn=[T108,T109]) and the T109↔T106 not-parallel-safe note are correct and complete. **However**, criticism 1's residue is unresolved: T109 mislocates the backfill. `LedgerStore.fetch()` is **synchronous** (LedgerStore.ts:121,130) and `FsLedgerStore.fetch()` (FsLedgerStore.ts:385-390) returns the synchronous, FS-free `materialiseFetchedLedger` (InMemoryLedgerStore.ts:570-609, archivePointers = a plain map at line 607). T109's preferred "read-side reconstruction per fetch by reading the archive .md at pointer.path" cannot run in a sync `fetch()`; the feasible locus is `FsLedgerStore.init()` (already async, already reads each archive .md via `collectArchivedItems`/`fetchArchive`, FsLedgerStore.ts:244-254/432-448) caching onto the in-memory pointers. T109 also names `materialiseFetchedLedger` as a candidate, which is impossible since it's shared with the FS-free InMemory adapter (would break the D10/T106 parity).
- **Issues:** No Bash/Grep/codegraph tools were available in this environment; verification was done via direct `Read` of named source files, which sufficed to ground every claim.
