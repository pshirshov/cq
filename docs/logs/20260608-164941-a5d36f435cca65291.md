# Plan session log — G34 candidate planner (opus, claude)

- Goal: G34 (milestone M108)
- Role: candidate planner (multi-planner panel, opus/claude), CANDIDATE mode
- Outcome: candidate plan emitted (4 milestones, 15 tasks)

### Session summary
- **Did:** Grounded read-only in the repo for G34 (web help-popup rename, new build-time-generated Agents tab, and the cq.toml [tiers] inversion) and produced a candidate task-DAG; wrote nothing to any ledger.
- **Achieved:** A 4-milestone candidate plan — (1) cq-config [tiers]→classifier inversion, (2) Agents-catalogue codegen from cq-assets, (3) web help-popup "State Machines"→"Item States" rename + new Agents tab, (4) docs/example/integration verification — with fine-grained, dependency-ordered, operationally-acceptance-tested tasks.
- **Discovered:** (a) ledger-web has NO compile step under Nix — `serve.ts` runs `Bun.build` over `src/` at startup, and the cq-assets `.md` files are OUTSIDE the ledger-web Nix closure (`nix/pkg/cq-assets/` is not staged into `nix/pkg/cq-ledgers`). Therefore Q147's "build-time codegen" must emit a CHECKED-IN generated `.ts` module under `packages/ledger-web/src/`, regenerated by a `bun run` script (added to `package.json` scripts, mirroring `regen-bootstrap`/`link-prompts`) that reads the asset `.md` files via relative path; a test enforces freshness (no drift). The codegen does NOT run inside the Nix sandbox. (b) Agent `.md` files carry YAML frontmatter (`name`, `description`, `disallowedTools`) + markdown body; per-role model class is NOT in frontmatter — it derives from cq.toml `[agent_tiers]` (agent→tier) classified through the new token→class `[tiers]`. (c) `[tiers]` dispatch consumers (`resolveTierToken`/`resolveAgentModel`) are exported from `@cq/config` and consumed mainly by orchestrator prompt-prose; the TS blast radius is cq-config + any ledger-mcp config capability re-export (must re-verify so `nix build .#ledger-mcp` stays green). (d) HelpOverlay tab state is `useState<"shortcuts" | "statemachines" | "flows">`; the Flows tab (`flowData.ts`) is the static-typed-catalogue precedent for the Agents tab.
- **Issues:** Codegraph is not indexed for this tree (fell back to Read). The cq-config↔Agents-tab coupling (showing each role's resolved class/per-harness mappings) means M2 codegen should also surface tier data; sequenced M3 (web) after both M1 and M2. Tie-break rule for "select among class-matching tokens" must be documented (M1) — flagged for the reviewer.

(Full candidate-plan JSON captured in the orchestrator transcript; synthesized plan persisted to the ledger by the orchestrator.)
