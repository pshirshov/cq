## M67

### T183 — done

- createdAt: 2026-06-06T10:57:52.496Z
- updatedAt: 2026-06-06T11:29:10.969Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: "Reproduce-first: failing happy-dom test for press-started-inside backdrop dismissal"
- description: |
    RED step of the TDD fix for D31. Add a happy-dom test in @cq/ledger-web (alongside batchModalClose.test.tsx, in nix/pkg/cq-ledgers/) that exercises the press-started-inside / released-on-backdrop path the existing suite never covers (vacuous-coverage gap, cf. D24/H14).
    
    The test must: (1) open the batch-answer modal (BatchAnswerModal / the `data-testid="batch-overlay"` `.lw-help-backdrop`); (2) dispatch a `pointerdown` (and `mousedown`) INSIDE the dialog — on the submit/hold button, target !== backdrop currentTarget; (3) then dispatch a `click` on `data-testid="batch-overlay"` (the backdrop itself), simulating the still-held pointer releasing over the shrunken backdrop; (4) assert the modal STAYS OPEN (batch dialog still in the DOM). Today this FAILS (the modal closes via onClick={onClose}). Do NOT change App.tsx in this task — the test asserts current-broken behaviour so the failure is observable and attributable. Keep the existing queue-drain close test and any genuine-backdrop-click test untouched here.
- acceptance: Run the ledger-web test suite from nix/pkg/cq-ledgers/ (bun test, or the targeted batch-modal test file). The NEW test FAILS pre-fix, and the failure message shows the modal closed (dialog removed) after the click on data-testid="batch-overlay" — i.e. it fails for the press-started-inside reason, NOT an import/setup error. All pre-existing batchModalClose tests remain green.
- suggestedModel: standard
- ledgerRefs: ["defects:D31","goals:G21"]
- resultCommit: d073a27
- completion: "Reproduce-first RED test for D31 in batchModalClose.test.tsx: pointerdown/mousedown inside the dialog then click on data-testid=batch-overlay, asserts modal stays open — FAILS today (modal closes via backdrop onClick={onClose}). App.tsx untouched. Right-reason failure (batch-overlay null). Becomes the GREEN regression guard once T184's fix lands. NOTE: leaves main transiently red (1 failing test) until T184."
- sessionLogs: ["docs/logs/20260606-112328-a42aada6db18cddeb.md","docs/logs/20260606-112328-a145faed1685113eb.md"]

### T184 — done

- createdAt: 2026-06-06T10:58:06.946Z
- updatedAt: 2026-06-06T11:45:07.175Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: "Shared safe-backdrop guard: dismiss only when press STARTED on the backdrop; apply to all 3 overlays"
- description: |
    GREEN step of the D31 fix (verbatim suggestedFix). Extract a small SHARED safe-backdrop wrapper/hook in @cq/ledger-web (a new small hook/helper file, e.g. useSafeBackdrop, plus minimal App.tsx wiring) that: on the backdrop element tracks onPointerDown/onMouseDown and records whether target === currentTarget (the press started on the backdrop itself); and only calls onClose() when BOTH the recorded down-target AND the up/click target are the backdrop. HoldButton's timer-fired release no longer dismisses because the down-target was the inner button, not the backdrop.
    
    Apply the guard to ALL THREE overlays so the whole react-modal #466 class is fixed, not just the reported one: (1) the batch overlay `data-testid="batch-overlay"` `.lw-help-backdrop`; (2) HelpOverlay (~App.tsx:1485); (3) the log modal (~App.tsx:3021). Escape-to-close and all other a11y behaviour UNCHANGED. Make the T183 reproduce-first test pass (modal stays open on press-started-inside release), and keep a genuine backdrop click (down AND up both on the backdrop) still closing the modal — add/keep that explicit assertion so the guard is not over-broad. SCOPE GUARD: ledger-web only (App.tsx + the test + the small new hook file). Do NOT touch the unrelated in-flight comment-cleanup code; branch off committed main (5809850) so App.tsx is the committed version — flag any later merge reconciliation with the uncommitted working-tree comment-cleanup.
- acceptance: "From nix/pkg/cq-ledgers/: (1) the T183 reproduce-first test now PASSES (batch modal stays open after pointerdown-inside-dialog then click-on-backdrop); (2) a genuine-backdrop-click test (pointerdown AND click both on data-testid=\"batch-overlay\") still CLOSES the modal; (3) the existing queue-drain close test stays green; (4) Escape-to-close still works for all three overlays; (5) `bun run check` (bun test + typecheck + lint) is green. The same guard is demonstrably wired on the help and log overlays (not only batch)."
- suggestedModel: standard
- dependsOn: ["T183"]
- ledgerRefs: ["defects:D31","goals:G21"]
- resultCommit: 99576bc
- completion: "D31 GREEN fix: new useBackdropDismiss hook (ref-based; closes only when press STARTED on the backdrop — target===currentTarget on both down and click) wired into all 3 overlays (help/batch/log), replacing bare onClick={onClose}. T183 reproduce-first test now PASSES; genuine-backdrop-click still closes; Escape intact. bun run check green 977/0/1skip."
- sessionLogs: ["docs/logs/20260606-113840-a9d3ade2ede18ee1f.md","docs/logs/20260606-113840-a10ea8a13d2c24d7b.md"]
