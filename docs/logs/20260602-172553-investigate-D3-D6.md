# investigate-flow — broadest-scope pass over D3, D4, D5, D6 (all CONFIRMED → seeded G5)

Standalone /investigate:start (no arg → broadest scope per the user). Worklist = open defects with no planned fix tasks = D3, D4, D5, D6 (D2/D7/D8 excluded — already have planned fix tasks). Formed hypotheses H5-H8, dispatched 4 read-only investigate-explorers (parallel), validated citations against source, adjudicated all CONFIRMED, seeded consolidated defect-seeded goal G5 (planning).

## H5 / D3 (explorer a5ee6a17fafbb5b29) — lean supports → CONFIRMED
9 evidence items. package.json:6-20 main+exports['.']=./dist/index.js, ['./relationships']=./dist/relationships.js; on disk (orchestrator-validated) dist/index.js + dist/relationships.js MISSING, dist/src/* EXIST; tsconfig outDir ./dist + include[src,test] + no rootDir → emits dist/src/. ['./columns'] already correct (./dist/src/columns.js) — internal inconsistency. ledger-web/tsconfig paths mask in-repo. No export-target test.

## H6 / D4 (explorer ad08b3dd188678f7a) — lean supports → CONFIRMED
7 evidence items. columns.ts:35-47 LONG_FIELD_DENYLIST + :55-72 eligibleColumnFields = fields minus denylist minus {id,status,summary}; headline/title/question pass. summarize() (tui :82-86 / web :181-185) picks headline first; tui :1200-1210 renders extra column cell AND summary → duplicate; picker mounts eligibleColumnFields (:1809). No columns.test.ts.

## H7 / D5 (explorer a851c1df8fb06e15d) — lean supports → CONFIRMED
9 evidence items. types.ts ArchivePointer {id,path,summary} + Milestone statusless; only ResolvedMilestone has status. core.ts:497 builds pointer w/o status. web active head passes status (:1853), archived head doesn't (:2002-2008); badge gated (:1659); comment (:1640) documents gap. NEW: T91 (M21) lands the ArchivePointer title+status DATA, but the @cq/shared fetch_ledger WIRE Zod mirror of archivePointers[] is NOT yet covered. D5 = render-only + wire half, sequenced after T91.

## H8 / D6 (explorer adc616f6bdea118ce) — lean supports → CONFIRMED
7 evidence items. package.json:8-21 no ./constants export; index.ts:50 re-exports FsLedgerStore → FsLedgerStore.ts:29-30 node:fs/node:path; constants.ts:25-27 type-only import (browser-safe) + :47 MILESTONES_SCHEMA; web App.tsx:69-82 duplicate; App.tsx:25/29 already use /relationships + /columns leaf subpaths; serve.ts:55-59 Bun.build browser. Same family as D3 (shared package.json edit).

## Adjudication / handoff
All H5-H8 → status confirmed (+ evidence). D3/D4/D5/D6 rootCause+suggestedFix refined (status stays wip — problem records until fixed). File-and-defer (standalone /investigate:start): seeded ONE consolidated defect-seeded goal G5 (M23, planning, clarify-skip K8 pt4) linking defects:D3-D6, with 3 fix units: A=packaging (D3+D6, shared package.json), B=column eligibility (D4), C=archived-head badge (D5, depends on T91/M21 + the wire-schema half). Standalone handback → user runs /plan:advance G5.
