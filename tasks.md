# cq — active task ledger

**Cycle:** outer-1 / inner M1 (M0 archived; PR-06 next).
**Goal:** build cq — TypeScript Web UI for the Claude Agent SDK on Bun + React + WebSocket per [`./prompt.md`](./prompt.md). Discharge condition: all five milestones `[x]` and archived; `bun test` clean; `bun run start --cwd <real-dir>` launches; sample prompt round-trips Chat tab + History tab drill-down.
**Accepted plan:** [`docs/drafts/20260526-0037-cq-plan.md`](docs/drafts/20260526-0037-cq-plan.md) (2294 lines, G2c-patched).
**Defects:** [`./defects.md`](./defects.md). _(none opened.)_

## Cross-cutting locks (non-negotiable, project-wide)

- TypeScript strict + `noUncheckedIndexedAccess` + `exactOptionalPropertyTypes`. No `any` outside justified library boundaries.
- Bun 1.3.13 (flake-pinned). React 19.2.6 fn+hooks only. `@anthropic-ai/claude-agent-sdk@0.3.150` (zod 4 peer).
- WebSocket-only application data. No HTTP endpoints for app data. No auth. No telemetry.
- One concurrent editor per ledger group; worktree-per-editor for parallel cycles; one ledger entry = one commit; sequential never-reused PR ids.
- Resilient-WS-UI Part-3 checklist is the spine of M1; every item is owned by a specific PR (see plan § 6 + coverage matrix in review).
- One open escalation: §11 **Q-1** (AskUserQuestion answer-injection) — *conditional, fires only if PR-31's spike disconfirms Candidate A*. Documented in plan with a recommended fallback.

## Milestones — stubs

- [x] **M0 — Bring-up** (closed: 2026-05-26; archive: [`./docs/archive/tasks-M0.md`](./docs/archive/tasks-M0.md)) — workspace, protocol, smoke server, logger, dev/HMR. *5 PRs.*
- [ ] **M1 — WebSocket spine** — full resilient-ws-ui Part 3 coverage. *PRs PR-06 … PR-18 (14, incl. PR-09a).*
- [ ] **M2 — Agent SDK integration (Chat MVP)** — SDK bridge, Chat shell, markdown + Shiki, basic cards, interrupt, header. *PRs PR-19 … PR-26 (9, incl. PR-22a/b).*
- [ ] **M3 — Chat full fidelity** — permission overlays, elicitation, AskUserQuestion, plan mode, thinking blocks, slash autocomplete, attachments, file-ref anchors, more tool cards, TaskList sidebar. *PRs PR-27 … PR-38 (12).*
- [ ] **M4 — Persistence + History tab** — DDL, adapters, bridge writes, list/detail/timing/export/delete, resume-from-history. *PRs PR-39 … PR-47 (9).*
- [ ] **M5 — Polish & harden** — graceful shutdown, error toasts, a11y, E2E suite, README, type/lint clean, stop-condition verify. *PRs PR-48 … PR-54 (7).*

Total PR count: 56 (PR-01 … PR-54 + PR-09a + PR-22b; PR-22a replaces old PR-22).

## M1 — WebSocket spine (current milestone)

Goal: full resilient-ws-ui Part-3 reliability + indicator coverage on both server and client. Close when all 14 PRs are `[x]`, all named tests pass, and PR-18's E2E (freeze + IP-change + server-restart) is green.

- [x] **PR-06** — Server WS endpoint + Zod inbound validation + Origin check (F-19). Tests: `ws-basic.test.ts`, `ws-origin.test.ts`. Deps: PR-02, PR-03. (44 tests, commit `c05b27a`)
- [x] **PR-07** — Server heartbeat (`hb.sping`/`hb.spong`) with setImmediate defer + nonce current+previous lookback `[ws R11]`. Test: `heartbeat.test.ts`. Deps: PR-06. (50 tests, commit HEAD, R11 ticked)
- [x] **PR-08** — Client `Connection` class: state machine NEW/ALIVE/STALE/DEAD + per-nonce ping + connect timeout `[ws R2,R3,R4]`. Test: `connection.test.ts`. Deps: PR-02. (8 tests, commit HEAD, R2+R3+R4 ticked)
- [x] **PR-09** — Client `Manager`: backoff + overlapping reconnect + close-code classify + pool cap=3 `[ws R5,R6,R7]`. Test: `manager.test.ts`. Deps: PR-08. (10 tests, commit HEAD, R5+R6+R7 ticked)
- [x] **PR-09a** — Server-side seq replay buffer (last-500 / 5 MB ring per session) (F-04). Test: `replay-buffer.test.ts`. Deps: PR-06, PR-09. (14 new tests; buffer empty until PR-19 wires chat.event traffic; commit HEAD)
- [x] **PR-10** — Page Lifecycle wiring + defer-while-hidden Phoenix pattern `[ws R9,R10]`. Test: `lifecycle.test.ts`. Deps: PR-09. (`pageLifecycle.ts` + Manager hooks: checkConnections, handleResume, closeForBFCache, reopenFromBFCache, runPendingReconnect, isVisible opt + defer in scheduleBackoff; 9 new tests; 27 web / 129 total; commit HEAD)
- [x] **PR-11** — Time-jump detector → proactive reconnect `[ws R8]`. Test: `time-jump.test.ts`. Deps: PR-09. (Manager: `startTimeJumpDetector`/`stopTimeJumpDetector`; TIME_JUMP_TICK_MS=1000, TIME_JUMP_THRESHOLD_MS=2000 exported; `setInterval`/`clearInterval`/`enableTimeJumpDetector` opts added; `_timeJumpStats` telemetry; destroy() clears tick; 4 new tests; 31 web / 133 total; commit HEAD) [ws P3-r-7] TICKED.
- [x] **PR-12** — Destroyed-flag guard + terminal-state truth `[ws R12,R13]`. Test: `destroy.test.ts`. Deps: PR-09. **Serial after PR-11.**
- [x] **PR-13** — Indicator widget: derived state + non-color channels + CSS modules infra `[ws V1,V2,V3]`. Test: `indicator.test.ts`. Deps: PR-09, PR-10, PR-11, PR-12.
- [x] **PR-14** — Countdown ring + 10 Hz rAF + event-driven refresh `[ws V4,V5]`. Test: `ring.test.ts`. Deps: PR-13. **Serial after PR-13.**
- [x] **PR-15** — Tooltip with pool, RTT windows, loss %, backoff, last close `[ws V6]`. Tests: `tooltip.test.ts`, `rtt-windows.test.ts`. Deps: PR-13, PR-14. **Serial after PR-14.**
- [x] **PR-16** — document.title mirror + bounded event log + never-lie labels. (14 tests; commit HEAD). [ws P3-i-7,8,9] TICKED.
- [ ] **PR-17** — `useConnection` hook + `<ConnectionProvider>` context (F-18). Test: `app-mount.test.ts`. Deps: PR-13..PR-16, PR-06, PR-07.
- [ ] **PR-18** — E2E: freeze + IP-change simulated + server restart. Test: `e2e/ws-resilience.test.ts`. Deps: PR-06 .. PR-17.

**Dispatch order (plan § 9).** Mostly serial. Possible parallel split: PR-07 (server) vs PR-08 (client) can run concurrently in separate worktrees once PR-06 lands (disjoint write scopes: `packages/server/src/ws/` vs `packages/web/src/ws/`). PR-13 onward serialises on `Indicator.tsx` per the F-17 write-scope map.

## In-progress / recent

- **PR-17** — `useConnection` hook + `<ConnectionProvider>` context (G2c: F-18). Deps: PR-13..PR-16, PR-06–PR-07.

## Recent completions (this cycle's worth)

- [x] **PR-16** — document.title mirror + bounded event log + never-lie labels. `ws/titleMirror.ts` (attachTitleMirror; never-lie labels: STOPPED/DEFERRED/STALE/CONNECTING/(alive/total); test-seam opts.doc; no-op when document undefined). `ws/eventLog.ts` (createEventLog; bounded ring 500 retained/100 displayed; append-evict-oldest; getAll()/getDisplayed() latest-first; pure data structure). `ws/Manager.ts` extended: event log field; spawn/alive/stale/dead/backoff/terminal/destroy entries appended; `events` getter + `ManagerStats.events` field. `ws/Tooltip.tsx` extended: scrollable event log section (max-height 120px). `styles/Tooltip.module.css`: `.eventLog`/`.event`/`.eventTs`/`.eventKind`. `App.tsx`: attachTitleMirror wired in useEffect with cleanup. `test/title.test.ts` 6 cases (test-seam doc; empty/alive/stale/terminal/deferred/connecting). `test/event-log.test.ts` 6 cases (empty/single/600→500-eviction/100-displayed/valid-kinds/ts-override/custom-limits). indicator.test.ts + ring.test.ts + tooltip.test.ts: makeStats updated with `events: []`. 101 web tests / 203 total. [ws P3-i-7] (document.title), [ws P3-i-8] (event log), [ws P3-i-9] (never-lie) TICKED. Commit: HEAD.
- [x] **PR-15** — Tooltip with pool, RTT windows, loss %, backoff, last close `[ws V6]`. `ws/Tooltip.tsx` (expanded panel: pool cards with active marker, active-connection detail, loss%, RTT windows table 30s/1m/5m min/median/max/count, backoff text V10-truthful: "stopped"/"deferred (tab hidden)"/"attempt N/max — retry in ETA", last close code+reason). `ws/Manager.ts` extended: `RttSummary` type + `computeRttSummary(samples,now,windowMs)→RttSummary|null` exported; `_rttSamples[]` ring (cap 1000); `_pingsSent`/`_pingsLost` counters; `_prevRtt`/`_prevInFlight`/`_prevState` per-connection telemetry maps; RTT sample appended on rtt change; `_pingsLost` incremented on ALIVE→STALE; `ManagerStats` extended with `rttWindows` + `lossPct`. `ws/Indicator.tsx` extended: `hovered`/`pinned` state; `onMouseEnter`/`onMouseLeave`/`onClick` handlers; `<Tooltip>` rendered when `hovered||pinned`. `styles/Tooltip.module.css` (fixed panel top-right below indicator dot; monospace dark theme; connCard/connCardActive; rttTable). `test/tooltip.test.ts`: 13 cases (Tooltip: role, active id+uptime+RTT, "stopped" label, "deferred" label, attempt ETA, null close code, numeric code+reason, no-connections, null RTT windows; Indicator: hidden initially, hover opens, mouseout closes, click pins open, second click unpins). `test/rtt-windows.test.ts`: 10 cases (empty→null, single sample, out-of-window→null, windowing 30s/1m/5m, even median, odd median, boundary inclusion, loss% formula: 0-pings, 2/10, 0/10, 3/3). indicator.test.ts + ring.test.ts: `makeStats` updated with `rttWindows`+`lossPct` defaults. 87 web tests / 189 total. [ws P3-i-6] TICKED. Commit: HEAD.
- [x] **PR-14** — Countdown ring + 10 Hz rAF + event-driven refresh `[ws V4,V5]`. `ws/computeRingRemaining.ts` (pure fn: `ManagerStats × {connectTimeoutMs,pongTimeoutMs,staleGraceMs} × now → RingInfo|null`; phase priority: connect > pong > stale > reconnect; remaining clamped to [0,total]). `ws/CountdownRing.tsx` (SVG ring; 2 circles: track + depleting arc; dashoffset=0 ↔ full, dashoffset=circumference ↔ empty; `position:absolute`, `pointerEvents:none`). `ws/Indicator.tsx` extended (CountdownRing overlay; `ringOpts` prop; two `useEffect` hooks: Manager.onUpdate subscription + rAF loop throttled to RAF_THROTTLE_MS=100; `useState<number>` for `now`). `styles/Indicator.module.css` extended (`.ring`, `.ringTrack`, `.ringIndicator`). `test/ring.test.ts`: 11 cases — 4 CountdownRing SVG (dashoffset at 50%, full, empty, monotonic depletion), 6 computeRingRemaining pure (connect, pong, stale, reconnect, null, clamp), 1 Indicator integration (ring appears on Manager.onUpdate). `test/helpers/MockWebSocket.ts` fixed: `_makeCloseEvent` now uses `Object.defineProperty` for code/reason/wasClean (happy-dom's CloseEvent init-dict ignores those fields, causing `isRetriable(undefined)→false` and spurious `isTerminal=true`). `Connection.ts`+`Manager.ts` extended: `oldestPendingPingSentAt`, `enteredStaleAt`, `connectedAt`, `retryScheduledAt` surfaced in stats. 62 web tests / 164 total. [ws P3-i-4] (countdown ring), [ws P3-i-5] (10 Hz rAF + event-driven) TICKED. Commit: HEAD.
- [x] **PR-13** — Indicator widget: derived state + non-color channels + CSS modules infra `[ws V1,V2,V3]`. `ws/deriveWidgetState.ts` (pure `deriveWidgetState(stats)→WidgetState`; 6-state enum; V2 algorithm verbatim). `ws/Indicator.tsx` (32×32 fixed-position widget; three channels: color class, `data-state`+glyph, `aria-label`; subscribes via `manager.onUpdate`; `useEffect` + `useState`). `src/styles/Indicator.module.css` (per-state color classes + circle layout). `src/App.tsx` (root composite; dev-time Manager pointing at `ws://${location.host}/ws`). `main.tsx` updated to `import App`. `src/types/css-module.d.ts` shim for tsc. happy-dom registered at module level in `indicator.test.ts` (not global preload — avoids GlobalRegistrator.register() replacing timers and breaking jest.useFakeTimers() in other test files). `test/indicator.test.ts`: 14 cases (7 pure `deriveWidgetState` unit tests + 7 component tests: data-state attr, state transition alive→dead, aria-label content, aria-label on connecting transition, unmount with no leaked subscribers, non-color glyph channel). 51 web tests / 153 total. [ws P3-i-1] (single compact widget + aria-label), [ws P3-i-2] (derived state, never stored), [ws P3-i-3] (two non-color channels) TICKED. Commit: HEAD.
- [x] **PR-12** — Destroyed-flag guard + terminal-state truth `[ws R12,R13]`. `ws/Manager.ts`: `_destroyed = true` set FIRST in `destroy()` (before any close side-effect); final `_cancelBackoff()` after closing connections (belt-and-suspenders); `_notify()` fires before clearing subscribers so `isTerminal: true` is delivered to any registered callbacks; `_deriveStats()` now derives `isTerminal: this._destroyed || this._isTerminal` so terminal state is truthfully labelled post-destroy (R13). `destroy()` comment documents R12 re-entry invariant. `test/destroy.test.ts`: 6 cases — backoff-cleared-on-destroy, synchronous-close-cascade (canonical R12 race), time-jump-detector-tick-cleared, max-attempts→isTerminal+send-false, non-retriable-isTerminal-sticks-across-destroy, onUpdate-after-destroy-never-fires. 37 web tests / 139 total. [ws R12] + [ws R13] TICKED. Commit: HEAD.
- [x] **PR-11** — Time-jump detector → proactive reconnect `[ws R8]`. `ws/Manager.ts`: `startTimeJumpDetector()`/`stopTimeJumpDetector()`; `TIME_JUMP_TICK_MS`=1000 + `TIME_JUMP_THRESHOLD_MS`=2000 exported; `setInterval`/`clearInterval`/`enableTimeJumpDetector` opts; `_timeJumpStats` telemetry counters; `destroy()` clears tick. `test/time-jump.test.ts`: FakeClock + FakeInterval seam; 4 cases (short gap → checkConnections, long gap → handleResume + spawn, normal cadence → neither, destroy clears interval). Hidden-tab: detector fires even when hidden (R8 rationale: connection ready before visibility). 31 web tests / 133 total. [ws P3-r-7] TICKED. Commit: HEAD.
- [x] **PR-10** — Page Lifecycle wiring + defer-while-hidden Phoenix pattern `[ws R9,R10]`. `src/lib/pageLifecycle.ts` (attachPageLifecycle: visibilitychange/freeze/resume/pagehide/pageshow/online/offline/NetworkInfo change; injectable `doc`/`win`/`nav` targets; returns `detach()`). `ws/Manager.ts` extended: `isVisible` opt (default reads document.visibilityState or true); `_pendingReconnectOnVisible` flag (private, surfaced in stats); `_scheduleBackoff()` defers when !isVisible → sets pendingReconnectOnVisible; `checkConnections()`, `handleResume(elapsedMs)`, `closeForBFCache()`, `reopenFromBFCache()`, `runPendingReconnect()`. Tests: `lifecycle.test.ts` 9 cases via MockDoc/MockWindow/MockNav helpers (hidden no-op, defer-while-hidden, visible-clears-pending, online→check, pagehide(persisted)→close, pageshow(persisted)→reopen, resume→handleResume, NI-API-change, detach removes all listeners). [ws P3-r-8] Page Lifecycle wiring, [ws P3-r-9] defer-while-hidden Phoenix pattern TICKED. 27 web tests / 129 total. Commit: HEAD.
- [x] **PR-09a** — Server-side seq replay buffer (last-500 / 5 MB ring per session) (F-04). `seq/replayBuffer.ts` (ring buffer: maxEntries=500, maxBytes=5 MB; O(1) eviction by subtraction; getSince(null|number)|"GAP_EXCEEDS"; append-before-send invariant documented). `seq/sessionRegistry.ts` (SessionRegistry: Map<id,SessionState>; create/get/delete/size; UUID keyed; process-lifetime). `ws/session.ts` extended (chatSessionId: string|null=null; registry injected via constructor; session.request_state dispatch: null→no-session state; within-buffer→replay entries+state; exceeds-buffer→gapDetected:true). 14 new tests in `replay-buffer.test.ts` (seq assignment, getSince ranges, GAP_EXCEEDS by maxEntries, eviction by maxBytes, registry UUID uniqueness, get/delete, 3 WsSession integrations: happy-replay + gap-exceeds + null-session). Total server tests: **64**. Total suite: **120**. `tsc -b` + `eslint` clean. Buffer empty until PR-19 wires chat.event traffic. Commit: HEAD.
- [x] **PR-09** — Client `Manager`: backoff + overlapping reconnect + close-code classify + pool cap=3 `[ws R5,R6,R7]`. `ws/Manager.ts` (pool Map<id,PoolEntry>; spawn on construct + on STALE; oldest-ALIVE rule for active selection; supersede-all on ALIVE; full-jitter backoff base 1s cap 30s ×0.5..1.0 max 15 attempts; isRetriable() classification; destroy() closes all + clears timers; pendingReconnectOnVisible exposed for PR-10). `test/manager.test.ts` 10 cases (construct, first-ALIVE, stale+recovery, stale+dead, backoff scheduling, backoff cap, max-attempts→terminal, non-retriable→terminal, pool-cap, destroy-while-retrying). [ws P3-r-4] (backoff/jitter/cap/terminal), [ws P3-r-5] (overlap failover + pool cap), [ws P3-r-6] (close-code classification) TICKED. Total web tests: 18. Total suite: 106. `tsc -b` + `eslint` clean. Commit: HEAD.
- [x] **PR-08** — Client `Connection` state machine NEW/ALIVE/STALE/DEAD `[ws R2,R3,R4]`. `ws/Connection.ts` (SocketLike structural interface; injectable socketFactory + clock; per-nonce Map<nonce,sentAt>; per-nonce pong timers; stale grace timer; connect timeout checking readyState; hb.sping→hb.spong R11 client side; onUpdate + onMessage subscribers; stats derived not stored). `test/helpers/MockWebSocket.ts` (simulateOpen/Message/Close). `connection.test.ts` 8 cases via fake timers (jest.useFakeTimers). tsconfig + package.json: bun-types + @cq/shared path mapping added to web package. [ws P3-r-1] (nonce heartbeat + per-ping timeouts), [ws P3-r-2] (four-state + grace), [ws P3-r-3] (connect timeout + readyState) TICKED. Total web tests: 8. Total suite: 96. `tsc -b` + `eslint` clean. Commit: HEAD.
- [x] **PR-07** — Server heartbeat with setImmediate defer + nonce lookback `[ws R11]`. `ws/heartbeat.ts` (createHeartbeat factory, WeakMap per-WS state: currentNonce, previousNonce, pendingFlag, pingTimerId, pendingTimerId; setImmediate injection seam for R11 race test), `ws/session.ts` wired (open→heartbeat.start, hb.spong→heartbeat.onPong, close→heartbeat.stop). 6 new tests in `heartbeat.test.ts` (schedule, current-nonce pong, previous-nonce lookback, unknown-nonce ignored, no-pong→1011, R11 race). Compressed real timers (pingIntervalMs:100, pongTimeoutMs:50); setImmediate injection for test 6. Total server tests: **50**. `tsc -b` + `eslint` clean. Operational smoke: WS open → saw hb.sping → close 1011 exit 0. [ws P3-r-10] TICKED. Commit: HEAD.
- [x] **PR-06** — Server WS endpoint + Zod inbound validation + Origin check (F-19). `ws/session.ts` (WsSession class, `open`/`message`/`close`, `sendFrame` helper), `ws/origin.ts` (isOriginAllowed), `server.ts` + `devServer.ts` extended with `/ws` upgrade path. Origin mismatch → HTTP 403 (option A; 1008 reserved for client-side). Zod `ClientFrame.safeParse` on every frame; failure → close 4000. `hb.ping` → `hb.pong` with `echoNonce`+`serverTs`. Tests: `ws-basic.test.ts` (4 cases) + `ws-origin.test.ts` (3 cases). Total server tests: **44** (37 prior + 7 new). `tsc -b` + `eslint` clean. Operational audit: WS open + 403 on bad Origin + SIGINT all exit 0. Commit: `c05b27a`.
- [x] **M0 closed + archived** to `docs/archive/tasks-M0.md`. 5 PRs, 113 tests total (76 shared + 37 server), 0 defects, 0 algedonic escalations. Both `start` and `dev` modes audit-green.
- [x] **G1/G2** — Plan accepted (committed `2181ae6`); G2b found 20 issues; G2c patched all 20 in-place; one conditional escalation (Q-1) documented.

## Archive

- M0 → [`./docs/archive/tasks-M0.md`](./docs/archive/tasks-M0.md)
