# Session log — D47/H34 investigate-explorer (opus)

- **Defect:** D47; **Hypothesis:** H34; role: explorer; lean: **supports**

### Session summary
Gathered read-only evidence for H34 (bootstrap divergence guard does not assert committed docs/ledgers.yaml is byte/canonical-identical to a fresh regen → constants.ts→fixture drift accumulates silently). 6 evidence items, lean supports: (1) `regen-bootstrap` is a MANUAL `bun run` script (package.json:12) NOT in `bun run check` (= tsc -b && eslint . && bun test); (2) regen-bootstrap.ts:32-38 writes docs/ledgers.yaml from CANONICAL_LEDGERS via serializeRegistry; (3) the only divergence guard is `schemasEqual` in FsLedgerStore.init() with DEFAULT `backup-reinit` (silent self-heal, FsLedgerStore.ts:430-454/200); (4) schemasEqual is a structural parsed-schema compare (FsLedgerStore.ts:1515), not a byte/regen compare; (5) onSchemaDivergence default = backup-reinit (silent); (6) D47 suggestedFix corroborates.

ORCHESTRATOR CITATION-VALIDATION refined the conclusion: the explorer (lacking Grep) MISSED an existing test — packages/ledger/test/canonical-ledgers.test.ts:504-522 ("repo docs/ledgers.yaml matches canon (no bootstrap divergence)") DOES read the committed fixture + boot a store against a temp copy. BUT it constructs `new FsLedgerStore({ root: dir })` WITHOUT `onSchemaDivergence: 'abort'`, so it runs in DEFAULT backup-reinit mode that SILENTLY self-heals instead of throwing — the test's own comment (L514 "init() throws BootstrapViolationError on any divergence") is WRONG for the mode it uses. So a stale committed fixture passes (store.enumerate() returns canon after the silent reinit). This is the precise mechanism; H34 CONFIRMED.
