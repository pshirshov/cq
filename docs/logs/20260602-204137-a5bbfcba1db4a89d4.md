# plan-reviewer — goal G6 — R95 (revise)

- Goal: G6 (defect-seeded cleanup; follow-up fix unit D = D12 / task T108)
- Role: reviewer (plan-reviewer subagent)
- Verdict: `revise` — review id **R95** (3 criticisms, 0 new questions, 0 out-of-scope defects)

### Session summary
- **Did:** reviewed the emitted plan for goal G6, focusing on the new follow-up fix task T108 (D12, web milestones-ledger archived-row rendering) under work milestone M28; verified it against the live ledger and the actual ledger-web/ledger source.
- **Achieved:** verdict `revise`, review id R95, 3 criticisms / 0 new questions / 0 out-of-scope defects.
- **Discovered:** T108's central grounding premise is false for the data the user sees. `fetch_ledger('milestones')` returns all 23 archive pointers (M2..M26) with `title:""` and `status:""`, and `docs/milestones.md` persists each `archives:` entry with only id/path/summary — T91 populates title/status only at archive time, so every pre-T91 archived milestone on disk lacks them. T108's rows would therefore fall back to the bare id and show no title (App.tsx:1990 `p.title.length>0 ? ... : p.id`), which is exactly D12 issue 1 ("titles never appear"). The titles ARE recoverable from the archive .md (e.g. `docs/archive/milestones/M2.md` line 5). The real fix lives in packages/ledger (FsLedgerStore pointer read / materialiseFetchedLedger or a one-shot migration), not App.tsx — which also undercuts T108's "App.tsx-only, disjoint, parallel-safe" claim. Secondary: the happy-dom acceptance as written is a non-reproduction (a fake pointer with a title passes while the app still shows blanks), and the task omits that `ItemTable` (App.tsx:1687-1816) takes no `archivePointers`/`showArchive`/fetch-handler props today. Confirmed-correct: T108 `dependsOn:[T91]` (T91 landed; store-abstract.ts:292-315 asserts it), the `!isMilestones` gate at App.tsx:1182 is preserved, and the lazy `fetchLedgerArchive`/`selectedArchiveRow` detail path (App.tsx:1185, 1230-1240) is viable for the milestones ledger (archive returns `kind:"item"`).
- **Issues:** the reviews ledger schema in this repo has no `defects` field, so I dropped the (empty) `defects` array from the review item; no out-of-scope faults to file regardless.

Relevant paths: `packages/ledger-web/src/App.tsx` (lines 1182, 1687-1816, 1990, 1230-1240), `docs/milestones.md` (archives entries lack title/status), `docs/archive/milestones/M2.md` (title present in archive), `packages/ledger/test/store-abstract.ts` (T91 assertions, store-abstract :261-328).
