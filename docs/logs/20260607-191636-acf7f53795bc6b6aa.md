# Plan-flow session log

- **Goal:** G28 — Enable subagent support in the pi agent harness for the cq flow
- **Milestone:** M86
- **Role:** planner (plan-advance, single-planner clarify round 2)
- **Returned status:** awaiting-answers
- **Agent id:** acf7f53795bc6b6aa

## Session summary
- **Did:** Grounded read-only in the actual Pi extension surface (dev-llm.nix `programs.pi` wiring, the two existing `nix/pkg/pi-extensions/*.ts`, pi-context.md, the `mergedAgents` frontmatter, the per-harness dispatch convention in `cq/investigate/advance.md`, and the live `cq.toml` via `get_config`), then filed the clarifying response answering Q125's counter-question.
- **Achieved:** Created `questions:Q130` (`open`, `goals:G28`, under M86) carrying the full grounded comparison of the three mechanisms against the now-fixed Q126/Q127/Q128/Q129 constraints, with suggestions enumerating all three and a justified recommendation for the bespoke in-repo extension. Goal G28 left in `clarifying` (no transition performed).
- **Discovered:** (1) The dispatch convention is already a per-harness split in the prompts (Claude `Agent`+`subagent_type`+`isolation:worktree`; Codex native multi_agent+manual worktree) with NO Pi branch — so the Pi mechanism must interpret that existing named-agent+task call shape, confirming Q126 is satisfiable Pi-side without prompt edits. (2) The extension API is compact and proven (two extant extensions, ~40 and ~75 lines, using `pi.on`/`pi.registerProvider`/payload-mutation); a subagent extension is ~150-300 lines in one file, the single unknown being whether Pi's `ExtensionAPI` can spawn a filtered-tool child session — which badlogic's example and nicobailon/pi-subagents both prove. (3) `cq.toml` already parses an `[aliases]` table + planner/reviewer lists via the ledger config layer, but has NO `fast/standard/frontier` tier mapping yet — Q127 requires adding a `[tiers]` table (additive).
- **Issues:** One genuine unknown to de-risk before the task DAG is committed: confirm Pi's `ExtensionAPI` child-session spawning primitive (filtered tools + captured result). The recommendation already proposes the first planned task be that read-only go/no-go spike. No other blockers.
