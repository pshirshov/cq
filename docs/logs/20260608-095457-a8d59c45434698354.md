# implement-worker (sonnet) — T232

- task: T232 (thread provider through resolvers + config capability); status: pass; resultCommit 719f821(…76e1)
### Session summary
Added `readonly provider: string|null` to ResolvedReviewer/ResolvedPlanner/GetConfigResult aliases+tier-slot shapes (ledger/src/mcp/configCapability.ts, config-agnostic plain string|null, no @cq/config import); ledger-mcp computeReviewers/computePlanners map token.provider, projectConfig copies provider into aliases+each tier slot. @cq/config resolvers already carry provider (return parseReviewerToken results) so config.ts untouched. 5 new tests assert provider flows end-to-end (ollama-cloud + claude→null). typecheck+lint clean; 23 legacy bare-pi fixtures deferred to T235.
