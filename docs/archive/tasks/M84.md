## M84

### T210 — done

- createdAt: 2026-06-07T00:00:03.541Z
- updatedAt: 2026-06-07T00:32:08.332Z
- author: "opus-4.8[1m]"
- session: 059ff637-d28c-4785-8125-9c0d73ddf7a0
- headline: Inventory the complete reference surface for the five retired skill names (full nix/ tree)
- description: |
    Before moving anything, produce the authoritative list of every file that references any of the five retired skill names (research-loop, vsm-loop, vsm-node, question-batch, review-loop), so the later scrub tasks are complete and verifiable.
    
    Scope = the ENTIRE nix/ tree (so the inventory covers every location the T215 final gate greps — scrub-scope must equal verify-scope; R248 criticism 1). Concretely search ALL of nix/, including: nix/pkg/cq-assets/** (commands/cq/*, agent prompts, agents/*, any skill manifests); nix/pkg/llm-skills/skills/** (surviving skills' content.md/meta.yaml); nix/pkg/llm-contexts/** (general-context.md, pi-context.md); nix/hm/**; and the flake itself (flake.nix). Also search docs/ for completeness. (Reviewer verified llm-contexts and flake.nix currently hold ZERO references, but the gate greps them, so they must still be owned by the inventory.)
    
    Search for: the bare names, [[wikilink]] forms ([[research-loop]] etc.), and any meta.yaml `name:` keys. Capture for each hit: file path, line, the form of the reference, and which cq successor (if any) it should repoint to per the Q118 map (research-loop->/cq:investigate; question-batch->questions-ledger flow; vsm-loop/vsm-node/review-loop->/cq:advance + plan/implement/review flow) vs. delete-only. Record the inventory in the task's description/notes (markdown) so downstream tasks consume one shared list.
    
    Note: also classify hits INSIDE the five skills' own content.md/meta.yaml separately — those bodies are being archived (Q119), not scrubbed in place, and their inter-skill [[wikilinks]] are handled by the archive task, not the cq-asset scrub.
- acceptance: "A written inventory exists listing every file+line under nix/ (cq-assets, llm-skills/skills, llm-contexts, hm, flake.nix) plus docs/ that references any of the five names, each tagged repoint-target-or-delete, with the five skills' own internal references called out separately. Spot-check: `grep -rn -E 'research-loop|vsm-loop|vsm-node|question-batch|review-loop' nix/` returns no hit absent from the inventory (matches the T215 gate's grep scope exactly)."
- suggestedModel: standard
- ledgerRefs: ["goals:G25"]
- resultCommit: da24c99
- completion: "Authored the retired-skill reference inventory (docs/drafts/...-T210-...md): 93 nix/ hits, all research-loop-only outside the five retired dirs; llm-contexts/hm/flake.nix + 7 surviving skills clean."
- sessionLogs: ["docs/logs/20260607-002428-a71607fa574f96288.md","docs/logs/20260607-002809-a72e6e3c30c107177.md"]

### T211 — done

- createdAt: 2026-06-07T00:00:18.502Z
- updatedAt: 2026-06-07T00:44:04.598Z
- author: "opus-4.8[1m]"
- session: 059ff637-d28c-4785-8125-9c0d73ddf7a0
- headline: Create docs/legacy-skills/ with each retired skill flattened to a single <name>.md (meta folded into header)
- description: |
    Per Q119: create the repo-root docs/legacy-skills/ directory (does not yet exist) and, for each of the five retired skills (research-loop, vsm-loop, vsm-node, question-batch, review-loop), produce docs/legacy-skills/<name>.md that folds the skill's meta.yaml into a header and appends its content.md body. Header should carry at least the original `name` and `description`/TRIGGER from meta.yaml (e.g. as front-matter or a titled block) so the archive is self-describing.
    
    This task PRODUCES the archive files from the current source content under nix/pkg/llm-skills/skills/<name>/{meta.yaml,content.md}; the source dirs are removed by the next task. Use git mv semantics where practical so history is rename-tracked (move content.md to the new path, then fold meta into its header), but the flatten means a literal single git mv per dir is not possible — preserve history for the body file at minimum.
    
    Handle the inter-skill [[wikilinks]] in the archived bodies (Q119 note): since the WHOLE family is retired (decision K40), [[research-loop]]/[[vsm-loop]]/[[vsm-node]]/[[review-loop]] links inside these bodies must NOT dangle — repoint each to the peer archive file (e.g. ./vsm-node.md) or annotate it as retired with its cq successor. No [[wikilink]] in the archive may point at a now-nonexistent installed skill.
    
    Also consider a short docs/legacy-skills/README.md noting these are retired skills, when, and their cq successors (optional but recommended for the archive to be self-explanatory).
- acceptance: "docs/legacy-skills/{research-loop,vsm-loop,vsm-node,question-batch,review-loop}.md all exist; each contains the skill's meta fields in a header plus the full content.md body; no [[wikilink]] inside any archive file points to a skill name that is no longer installed (each resolves to a peer ./<name>.md archive or is annotated as retired). git log --follow shows continuity for the moved body content."
- suggestedModel: standard
- dependsOn: ["T210"]
- ledgerRefs: ["goals:G25"]
- resultCommit: d36ae00
- completion: Archived the five retired skills to docs/legacy-skills/<name>.md (meta folded into header, bodies git-moved) + README; wikilinks repointed to peer archives. Source dirs left with meta.yaml for T212.
- sessionLogs: ["docs/logs/20260607-004046-acf9bbe72aa0d0dd2.md","docs/logs/20260607-004328-ace15266455339602.md"]

### T212 — done

- createdAt: 2026-06-07T00:00:30.133Z
- updatedAt: 2026-06-07T00:55:35.959Z
- author: "opus-4.8[1m]"
- session: 059ff637-d28c-4785-8125-9c0d73ddf7a0
- headline: Remove the five skill dirs from nix/pkg/llm-skills/skills/ to de-register them
- description: |
    Per Q117: delete the five source directories nix/pkg/llm-skills/skills/{research-loop,vsm-loop,vsm-node,question-batch,review-loop}/ (each a {meta.yaml,content.md} pair). Because default.nix discovers skills via `builtins.readDir ./skills` (filterAttrs directory) and inlines each dir's meta+content, removing the dir IS the de-registration — no manifest edit is required. validate-skills.sh runs on skills/*/meta.yaml, so once the dirs are gone there is nothing left to validate for them.
    
    Use git rm (the bodies were already captured into the docs/legacy-skills archive by T211, so this is the source-of-truth removal, not data loss). Do NOT edit default.nix or validate-skills.sh — the readDir mechanism handles registration automatically; touching them would be an out-of-scope change.
    
    Guard: confirm none of the five removed names is referenced by default.nix as a hardcoded attr (e.g. environmentContent only reads ./skills/environment/content.md — unrelated). If any hardcoded reference to a removed skill exists in the Nix, surface it (do not silently work around).
- acceptance: "nix/pkg/llm-skills/skills/ no longer contains research-loop, vsm-loop, vsm-node, question-batch, or review-loop directories; the other skill dirs are untouched. Verify de-registration via the `skills` ATTR SET (not `nix eval .#llm-skills`, which resolves to `.package`, a derivation, and does not expose skill names): evaluate the attr-name list directly with `nix eval --json --apply 'p: builtins.attrNames p.skills' -f nix/pkg/llm-skills/default.nix` (or `nix eval --json '.#llm-skills.skills' --apply builtins.attrNames` if that attr is exposed) and confirm it includes none of the five removed names; equivalently, after `nix build .#llm-skills`, confirm $out/skills/ contains no research-loop/vsm-loop/vsm-node/question-batch/review-loop entry. The build evaluates without error. default.nix and validate-skills.sh are unmodified."
- suggestedModel: fast
- dependsOn: ["T211"]
- ledgerRefs: ["goals:G25"]
- resultCommit: 94c5088
- completion: Removed the five retired skill dirs from nix/pkg/llm-skills/skills/ (de-registered via readDir); 7 survivors intact; nix build .#llm-skills green; default.nix/validate-skills.sh untouched.
- sessionLogs: ["docs/logs/20260607-005036-a396a7f403f155abe.md","docs/logs/20260607-005408-a58775308f4edd689.md"]

### T213 — done

- createdAt: 2026-06-07T00:00:52.960Z
- updatedAt: 2026-06-07T01:03:54.705Z
- author: "opus-4.8[1m]"
- session: 059ff637-d28c-4785-8125-9c0d73ddf7a0
- headline: Scrub/repoint references to the retired skills across cq-assets + the rest of the nix/ tree (excl. surviving skill bodies)
- description: |
    Per Q118, using the T210 inventory: for every reference to the five retired skill names anywhere in the nix/ tree EXCEPT surviving llm-skills/skills/*/content.md bodies (which T214 owns) and the retired skills' own dirs (archived by T211, removed by T212), either REPOINT it to its cq successor or DELETE it where no successor applies. This covers nix/pkg/cq-assets/** (commands/cq/*, agent prompts, agents/*, any skill manifests) AND the remaining nix/ locations the T215 gate greps: nix/pkg/llm-contexts/** (general-context.md, pi-context.md), nix/hm/**, and flake.nix. (Reviewer confirmed llm-contexts/hm/flake.nix currently hold ZERO references, so today this is a no-op there — but owning that scope makes scrub-scope ≡ the T215 verify-scope, so any future or missed reference outside cq-assets is assigned to a scrub task rather than failing the gate unowned; R248 criticism 1.)
    
    Repoint map: research-loop -> /cq:investigate (and :advance) flow; question-batch -> the questions-ledger clarification path (the plan-flow files questions directly as `questions` items); vsm-loop / vsm-node / review-loop -> the /cq:advance + plan/implement/review flow. Delete the reference outright only where no successor is a sensible target and the mention is purely vestigial (drop the sentence/[[wikilink]] so no dangling pointer remains, keeping surrounding prose coherent).
    
    Edits must be surgical (match surrounding style, no unrelated rewrites). [[wikilink]] forms that resolved to a now-removed skill must be removed or rewritten to the successor's command/skill name — they must not point at an uninstalled skill. Consume the inventory so coverage is complete; do not introduce new references to the retired names.
- acceptance: "grep -rn for each of the five retired names across nix/pkg/cq-assets, nix/pkg/llm-contexts, nix/hm, and flake.nix returns ZERO hits, EXCEPT references intentionally repointed to a cq successor (which by definition no longer contain the retired name) — i.e. effectively zero remaining mentions of the retired names in those locations. Every edited file remains coherent prose/config. The T210 inventory rows for these locations are all marked done (repointed or deleted). Note: surviving llm-skills/skills/* bodies are out of scope here (T214); the retired skills' own dirs are handled by T211/T212."
- suggestedModel: standard
- dependsOn: ["T210","T212"]
- ledgerRefs: ["goals:G25"]
- resultCommit: 703fa91
- completion: "Repointed the 5 research-loop references (3 cq-asset files: investigate-explorer.md, investigate-prober.md, investigate/advance.md) to /cq:investigate successors; grep across cq-assets/llm-contexts/hm/flake.nix now zero hits."
- sessionLogs: ["docs/logs/20260607-010120-af88a6a96c055db32.md","docs/logs/20260607-010323-a04b9af45409380ec.md"]

### T214 — done

- createdAt: 2026-06-07T00:01:02.575Z
- updatedAt: 2026-06-07T01:03:56.071Z
- author: "opus-4.8[1m]"
- session: 059ff637-d28c-4785-8125-9c0d73ddf7a0
- headline: Scrub references to the retired skills inside surviving llm-skills/skills/* bodies
- description: |
    Per Q118 and the T210 inventory, handle references to the five retired names that live in SURVIVING skills under nix/pkg/llm-skills/skills/*/content.md (and any meta.yaml description/TRIGGER text). These are distinct from the cq-assets surface (T213): a surviving skill's content.md may [[wikilink]] or name a now-retired peer.
    
    For each such hit: repoint to the cq successor per the Q118 map where one applies, or delete the mention where it is purely vestigial — leaving the surviving skill's prose coherent and free of [[wikilinks]] to uninstalled skills. Surgical edits only.
    
    Note: the retired skills' OWN bodies are NOT edited here (they were archived/flattened by T211 and their source dirs removed by T212); this task is strictly about the skills that remain installed.
- acceptance: "grep -rn for each of the five retired names across nix/pkg/llm-skills/skills (which now excludes the removed dirs) returns ZERO hits. No surviving skill body or meta contains a [[wikilink]] or mention resolving to a retired skill. The T210 inventory's surviving-skills rows are all marked done."
- suggestedModel: standard
- dependsOn: ["T210","T212"]
- ledgerRefs: ["goals:G25"]
- resultCommit: 703fa91
- completion: "Verified no-op: the 7 surviving llm-skills/skills/ bodies contain zero references to any of the five retired skill names (orchestrator-confirmed grep on main). No edit required."
- sessionLogs: ["docs/logs/20260607-010120-ad6cdc34458eb9e2a.md"]

### T215 — done

- createdAt: 2026-06-07T00:01:15.278Z
- updatedAt: 2026-06-07T01:10:05.489Z
- author: "opus-4.8[1m]"
- session: 059ff637-d28c-4785-8125-9c0d73ddf7a0
- headline: "Verify: Nix build evaluates, skills de-registered, and zero dangling references (final gate)"
- description: |
    Final acceptance gate for G25. Confirm the retirement is complete and the build is intact:
    1. BUILD: `nix build .#llm-skills` (and any cq-assets product that bundles skills, e.g. the home-manager skill set / `nix build .#ledger-*` if affected) evaluates and builds without error; the validate-skills.sh checkPhase passes against the remaining skills.
    2. DE-REGISTRATION: the installed/merged skill set no longer advertises research-loop, vsm-loop, vsm-node, question-batch, or review-loop (inspect the built $out/skills or the `skills` attr names).
    3. ARCHIVE: docs/legacy-skills/{research-loop,vsm-loop,vsm-node,question-batch,review-loop}.md all exist and are self-describing.
    4. NO DANGLING REFS: a repo-wide grep for each of the five retired names finds hits ONLY inside docs/legacy-skills/** (the intended archive) and the G25 ledger/log files — zero elsewhere in nix/pkg/cq-assets and nix/pkg/llm-skills/skills.
    
    If any check fails, the failing scrub/move task must be reopened rather than worked around.
- acceptance: "All four checks pass: (1) `nix build .#llm-skills` succeeds; (2) built skill set excludes all five names; (3) the five docs/legacy-skills/*.md archives exist; (4) `grep -rn -E 'research-loop|vsm-loop|vsm-node|question-batch|review-loop' nix/` returns no hit (all remaining hits are confined to docs/legacy-skills/ and ledger logs). Result recorded in the task."
- suggestedModel: standard
- dependsOn: ["T211","T212","T213","T214"]
- ledgerRefs: ["goals:G25"]
- resultCommit: 5f06ae1
- completion: "G25 final gate: nix build .#llm-skills exit 0 (7 surviving skills, 5 retired de-registered), bun run check green (1019/0), 5 docs/legacy-skills archives present, grep over nix/ zero dangling refs. Verification notes at docs/drafts/20260607-0130-T215-g25-final-gate-verification.md."
- sessionLogs: ["docs/logs/20260607-010810-acdf2bf2ce2f22088.md","docs/logs/20260607-010919-a5a0ded57faad3e9e.md"]
