## M58

### T179 — done

- createdAt: 2026-06-05T19:01:57.574Z
- updatedAt: 2026-06-05T19:32:05.595Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: "Reproduce-first: make link-prompts.ts import-safe (export LINKS + check logic, guard creation loop), add a --check mode + a test that FAILS on the current dangling output"
- description: |
    REPRODUCE BEFORE FIXING. Establish the failing repro that proves `bun run link-prompts` produces dangling symlinks under the CURRENT (unfixed) sources, then leave it in place as regression coverage. This requires an IMPORT-SAFETY REFACTOR of the script first, because the test must import the REAL LINKS array without firing symlink side effects.
    
    Scope (nix/pkg/cq-ledgers/scripts/link-prompts.ts):
    
    1. IMPORT-SAFETY REFACTOR (do this FIRST). The script currently runs its symlink-CREATION loop at MODULE TOP LEVEL (L56-74, a bare `for` with no guard), so importing the module from a test would EXECUTE that loop and mutate `.claude/` at test time. Fix this:
       - Export the `LINKS` array (the single source of truth — the test and the check mode MUST consume this exact exported array, never a duplicate).
       - Extract the per-link existence/check logic (resolve `absSource`, assert it exists via lstat/access) into an importable, side-effect-free exported function (e.g. `checkLinks(links): MissingTarget[]` returning every LINK whose target does NOT resolve).
       - GUARD the creation loop behind `import.meta.main` (or move it into a `main()` invoked only when run as a script), so merely importing the module performs ZERO filesystem mutation.
       After this refactor, `scripts/link-prompts.test.ts` can `import { LINKS, checkLinks }` with no `.claude/` side effects, and the red failure is unambiguously the missing `llm/...` targets (the D30 reason), NOT an import side effect or an unrelated throw.
    
    2. Add a `--check` mode to the script: when run with `--check`, call `checkLinks(LINKS)` and exit non-zero (or throw) listing every LINK whose target does NOT satisfy existence; otherwise (run as script, no --check) perform the guarded creation loop. The check operates on the SAME exported LINKS array the creation loop uses, so a stale source is caught. Do NOT yet repoint the LINKS sources or harden the creation loop's pre-symlink assertion in THIS task beyond what `checkLinks`/`--check` needs (that is T180) — though it is acceptable to land the shared existence-check helper here since T180 reuses it. Keep all edits to link-prompts.ts serialized within this goal.
    
    3. Add a bun test (`scripts/link-prompts.test.ts`) that imports the real exported `LINKS` + `checkLinks` and asserts every source target resolves (e.g. `expect(checkLinks(LINKS)).toEqual([])`). Against the CURRENT sources ('llm/...', nonexistent) this test MUST FAIL (red), reproducing D30. Read the failure: it must fail because the llm/ targets are missing, NOT an unrelated import/type error or a filesystem mutation — the import-safety refactor in step 1 is what guarantees this.
    
    Note: package.json test currently runs `bun test --pass-with-no-tests`; this is the first test touching this script.
- acceptance: "Import-safety holds: importing scripts/link-prompts.ts (as the new test does) performs ZERO filesystem mutation — `bun test` runs the new link-prompts test WITHOUT creating/changing any `.claude/` symlink (the creation loop is guarded behind import.meta.main / main()). The test consumes the REAL exported LINKS array (not a duplicate). Before any source repoint: `bun test` (or the new link-prompts test alone) FAILS, and the failure message enumerates the missing `llm/...` targets (fails for the D30 reason — missing targets — not an import side effect or unrelated error). `bun run link-prompts -- --check` (or equivalent) exits non-zero against current sources. Test file compiles under tsc -b and passes eslint."
- suggestedModel: standard
- ledgerRefs: ["defects:D30","goals:G17"]
- resultCommit: 7b66f3061f1e2f273a21c086a1edeb2fd322957e
- completion: Made link-prompts.ts import-safe (export LINKS+checkLinks, creation loop behind import.meta.main main()), added --check mode + scripts/link-prompts.test.ts reproducing D30 (14 missing llm/ targets); rebased onto T162; integrated bun run check green 891/0.
- sessionLogs: ["docs/logs/20260605-192023-a1f58d174db8c6b7b.md","docs/logs/20260605-192809-a6a836fd69d27ba3f.md"]

### T180 — done

- createdAt: 2026-06-05T19:02:15.074Z
- updatedAt: 2026-06-05T19:59:03.815Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: "Repoint link-prompts.ts LINKS sources onto ../cq-assets/{commands,agents}/ and harden the loop to assert the target exists"
- description: |
    Make the T179 repro pass. Two edits to nix/pkg/cq-ledgers/scripts/link-prompts.ts (which T179 has already refactored to be import-safe: `LINKS` and the `checkLinks` existence helper are EXPORTED, and the creation loop is guarded behind import.meta.main / main()):
    
    1. REPOINT the now-EXPORTED LINKS array (link-prompts.ts:29-44): change each of the 14 LINKS `source:` from `llm/commands/...` / `llm/agents/...` to `../cq-assets/commands/...` / `../cq-assets/agents/...`. Edit the exported LINKS declaration in place (do not duplicate it). cq-assets is a SIBLING of cq-ledgers and REPO_ROOT=nix/pkg/cq-ledgers/, so `path.join(REPO_ROOT, '../cq-assets/commands/plan/start.md')` resolves to the real file (assets.nix:49-50 confirms the tree). Update the interface PromptLink doc comments (L25 'Source file under `llm/`') to the new root. Prefer this explicit repoint over restoring a hidden `llm -> ../cq-assets` symlink (per D30 suggestedFix part 1, option a).
    2. HARDEN the (now guarded) creation loop: BEFORE `symlink(relTarget, absLink)`, assert absSource exists; if missing, THROW loud naming the link + missing target, so a future relocation fails fast instead of silently producing a dangling link. REUSE the existence-check logic factored into T179's exported `checkLinks` helper rather than duplicating it.
    
    SAME-FILE COORDINATION: this is the same file G15's T168/T178 edit (add investigate-prober / cq:* entries). A future implement pass MUST serialize this against G15 (no concurrent worktrees on scripts/link-prompts.ts). Keep G17's own link-prompts.ts edits (T179, this task) serialized too (this task dependsOn T179).
- acceptance: "After this task: the T179 test PASSES (every source target now resolves); `bun run link-prompts` recreates all 14 .claude/** symlinks and each target satisfies `test -e` (no dangling links); running link-prompts with a deliberately-missing source throws loud (does not silently symlink). `bun run check` (tsc -b && eslint . && bun test) green."
- suggestedModel: standard
- dependsOn: ["T179"]
- ledgerRefs: ["defects:D30","goals:G17"]
- resultCommit: f67a464bb3d821ef202d511bd7469276dfbafaaf
- completion: "Repointed 14 LINKS sources llm/ -> ../cq-assets/ (all resolve), hardened creation loop to throw loud on missing source (reuses checkLinks), flipped D30 repro test to checkLinks(LINKS) toEqual([]); integrated check green."
- sessionLogs: ["docs/logs/20260605-195232-aa6fe5ddb2750bbf0.md","docs/logs/20260605-195232-a569a9b61e99d57c4.md"]

### T181 — done

- createdAt: 2026-06-05T19:02:28.013Z
- updatedAt: 2026-06-05T20:10:46.952Z
- author: "opus-4.8[1m]"
- session: 58a3012b-08b8-4f7a-816b-008d6fb1d8d5
- headline: Update nix/pkg/cq-assets/README.md off the old `llm/` root to the cq-assets layout
- description: |
    Bring the docs in line with the relocated tree (README is doc-only; no code dependency, but kept after the code fix so it describes the final state). Edit nix/pkg/cq-assets/README.md:
    - Title L1 `# \`llm/\` — single-source LLM assets`: retitle to the cq-assets root.
    - Convention block L9-13: `llm/commands/<ns>/<name>.md` etc. -> the `nix/pkg/cq-assets/{commands,agents,skills}/...` (or repo-relative `commands/...`) layout the tree actually uses.
    - Narrative refs to `llm/` (L3 'this tree', L31 'Edit the files HERE', L54 `llmAssets`/assets.nix wording if it names llm/).
    - Three-consumers Claude-link table L38-49: the `→ source` column currently shows `llm/commands/...` / `llm/agents/...`; update to the sources link-prompts.ts now uses (`../cq-assets/commands/...` from cq-ledgers, or describe them relative to the asset root) so the table matches the repointed script from T180.
    Keep the current-assets table (L18-29) which already uses `commands/..`-relative paths unless its surrounding `llm/` framing is stale.
    
    No code change here; verify only that the README accurately reflects the post-fix link-prompts.ts source paths.
- acceptance: "README.md contains no stale `llm/` root references that misdescribe the current tree; the Claude-link table's source column matches the sources link-prompts.ts uses after T180. `bun run check` green (README is not linted but must not break any doc-driven test). Manual read confirms title/convention/three-consumers sections describe nix/pkg/cq-assets/{commands,agents}/."
- suggestedModel: fast
- dependsOn: ["T180"]
- ledgerRefs: ["defects:D30","goals:G17"]
- resultCommit: 24c1d5145fa0ac586fb54609dc621b35733e7557
- completion: "cq-assets/README.md de-staled off the llm/ root: title, convention block, narrative, and three-consumers Claude-link source column now describe nix/pkg/cq-assets/{commands,agents}/ and ../cq-assets/... sources matching post-T180 link-prompts.ts."
- sessionLogs: ["docs/logs/20260605-200638-ad1bff0f00fce8362.md","docs/logs/20260605-200638-ab0daafee8268ee2e.md"]
