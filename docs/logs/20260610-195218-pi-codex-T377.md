# T377 implement-reviewer (pi:grok-build / codex) — approve
```json
{"taskId":"T377","verdict":"approve","criticism":[],"questions":[],"defects":[],"rationale":"SERVER_INSTRUCTIONS->_TEMPLATE (byte-identical), buildServerInstructions(toolPrefix) asserts once, '' returns TEMPLATE directly, else longest-first LEDGER_TOOL_NAMES iteration + escapeRegExp + \\bname\\b replace via reused prefixToolName; buildServer calls buildServerInstructions(''); new test fixture + 4 tests cover '' byte-identical, myproj_create_item/snapshot, no bare whole-word, membership in prefixedToolNames('myproj'). check 1649/0.","summary":"Meets acceptance exactly, green, surgical+correct. approve."}
```
