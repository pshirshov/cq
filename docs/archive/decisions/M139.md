## M139

### K65 — locked

- createdAt: 2026-06-09T21:00:50.373Z
- updatedAt: 2026-06-09T21:00:50.373Z
- author: "opus-4.8[1m]"
- session: 7e451a99-b692-4ea6-b078-7776ebb17ca0
- headline: "T336: typed prompt-catalog model — 3 decisions LOCKED (role-scope split, validator=Ajv8, per-role sidecar storage)"
- rationale: |
    Foundational design for the typed prompt catalog that REPLACES the hand-authored agentsCatalogue/roleActions.ts prose as the single source of truth for agent prompts + I/O schemas (Q185, reconciling G38). Three decisions locked:
    
    (1) ROLE-SCOPE SPLIT. Two role kinds, only one with a parent-validated contract. DISPATCHED-SUBAGENT roles = non-null agentTierKey in @cq/config AGENT_ROLE_TIERS (plan-advance, plan-reviewer, implement-worker, implement-reviewer, implement-conflict-resolver, investigate-explorer, investigate-prober; plan-synthesizer as the chain generalizes) — a parent dispatches them with a supplied INPUT and consumes a validated OUTPUT, so ONLY these get formal inputSchema+outputSchema + the validate-in/out flow. ORCHESTRATOR-COMMAND roles = agentTierKey===null (the /cq:* commands) — never dispatched-with-validated-input by a parent, so they carry prompt+metadata but NO parent-validated input/output contract (inputSchema/outputSchema stay undefined). Encoded as RoleKind = 'dispatched-subagent' | 'orchestrator-command' + a redundant explicit `dispatched: boolean` on PromptCatalogEntry.
    
    (2) VALIDATOR = Ajv 8 (most-recent stable, ajv@^8.20.0), added as a DIRECT dependency of @cq/config. EVIDENCE that no in-workspace validator existed: zod@4 is a direct dep of @cq/ledger ONLY and is a code-first schema builder (not a JSON-Schema validator); ajv@8/ajv@6/@cfworker/json-schema existed only TRANSITIVELY via @modelcontextprotocol/sdk and eslint and were NOT resolvable from any workspace import (verified: `bun -e import ajv` and `import zod` both failed 'Cannot find package' from ledger-web). So Ajv 8 was added + the mandatory node-modules FOD refresh performed in the SAME task: flake.nix outputHash 52-A placeholder -> `nix build .#node-modules` -> got sha256-kG79B/z1rHAXOhb2dYvBvUkzjJVoyv8xLwu6RrCyk5U= pasted back; `nix build .#node-modules` GREEN. Schemas are plain JSON Schema draft 2020-12, compiled via Ajv's 2020 entrypoint (ajv/dist/2020).
    
    (3) STORAGE FORMAT = per-role typed TS sidecar co-located under packages/cq-config/src/schemas/<role>.ts (RoleSchemaSidecar { id, version, inputSchema, outputSchema }), consumed by gen-agents-catalogue.ts / catalog assembler — NOT embedded in the prose ## Catalogue blocks. Placed in @cq/config (the shared single-source-of-truth package where AGENT_ROLE_TIERS lives) so BOTH ledger-mcp and ledger-web can import it directly with no duplicate copy (satisfies T341's 'importable by ledger-mcp').
    
    PROOF-OF-ONE-ROLE (plan-advance, authored first from cq-assets/agents/plan-advance.md): inputSchema = { goalId (^G[0-9]+$, required), candidateMode (boolean, optional) }; outputSchema = oneOf[ DEFAULT-mode {mode:'default', status: awaiting-answers|review-requested|completed|noop}, CANDIDATE-mode {mode:'candidate', milestones[], tasks[] (headline/description/acceptance/suggestedModel/milestone/dependsOn?/ledgerRefs mirroring the asset's verbatim candidate-JSON contract), rationale} ]. PromptCatalogEntry { id, kind, dispatched, tier, version, promptTemplate, inputSchema?, outputSchema? } compiles. A test (packages/cq-config/test/promptCatalog.test.ts) compiles each schema with Ajv2020 and asserts accept-valid / reject-invalid for both. `bun run check` GREEN (1434 pass); `nix build .#node-modules` GREEN.
- ledgerRefs: ["goals:G41"]
