# implement-reviewer — T114 (D18) — approve

- Task: T114 (M30, goal G7, defect D18) — round 1; verdict `approve` (0/0/0)

### Session summary
- **Did:** reviewed task T114 round 1 (diff 56a3ded..ae0e5f8)
- **Achieved:** verdict approve; 0 criticisms / 0 questions
- **Discovered:** pick buttons wired to `onSave(row, suggestion)` (= batchSave → updateItem ANSWERED_STATUS, the real batch save path, mirroring 'as recommended'); one button per Array.isArray-split suggestion (handles string|string[]); disabled by the same `answerHasText` rule (resets on question change via useEffect([index])). New 10-case test fails 10/10 against base App.tsx and passes at HEAD (true discriminating repro). Diff confined to App.tsx batch region (lines 1432-1455) + new test; T113's archived-row change untouched. `bun run check` green (690/0). Suggestion text still rendered (`{suggestion}` per <li> in a <ul className="lw-field-list">).
- **Issues:** none
