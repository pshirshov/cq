# plan-advance — goal G8 (D20+D21) — review-requested (revision of R125)

- Goal: G8; Role: planner; Returned: `review-requested` (rule 3 revision of R125)

### Session summary
- **Did:** Rule 3 — re-grounded the ledger-tui test layout, revised T130 to fix R125's 3 mechanism/target errors; T131 untouched (sound).
- **Achieved:** T130 now: (A) navMemo T85 fix = bound the bun default 5000ms per-test timeout (explicit it(...,ms) and/or reduced N) since navMemo.test.tsx already polls with waitQuiescent + asserts only counters (no wall-clock assertion to drop); (B) names only real sites — all flaky tests in packages/ledger-tui/test/app.test.tsx + navMemo.test.tsx (scroll ~L547-567, status-filter ~L498-518, pane ~L520-534, suggestions T57 ~L1095-1113, question-detail T59 ~L1115-1151); (C) settle-then-assert (stable positive invariant) for negative-assertion inert-key tests ('s' ~L936-956, 'e' ~L958-976) + the status-filter negative half; concrete cross-file derivationCounters isolation. G8 stays planning.
- **Discovered:** R125 fully accurate — navMemo L51-62 waitQuiescent + L153-168 counter-only assertions confirm no wall-clock assertion; live-badge (L593-623) + multi-step (L450-467) already poll-until-condition (reference patterns, not fix targets).
- **Issues:** none; implementer must re-grep current line numbers + verify bun's per-file worker model.
