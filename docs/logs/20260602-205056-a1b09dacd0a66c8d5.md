# plan-advance — goal G6 — review-requested (revision of R96)

- Goal: G6 (defect-seeded cleanup; follow-up fix unit D = D12 / tasks T108 + T109)
- Role: planner (plan-advance subagent)
- Returned status: `review-requested` (rule 3 — criticism-driven revision of R96)

### Session summary
- **Did:** Applied R96 (revise, criticism-only, empty new_questions — rule 3): revised task T109 in place; left G6 in `planning`.
- **Achieved:** Updated T109 (M28) — relocated the legacy ArchivePointer title/status backfill from the synchronous read path (`materialiseFetchedLedger`/`fetch()`) to the async `FsLedgerStore.init()` (FS-only); explicitly removed `materialiseFetchedLedger` from the candidate locus and forbade touching it / the InMemory adapter to preserve D10/T106 parity; pinned the reproduction-first acceptance to an FsLedgerStore fixture (legacy `archives:` entry lacking title/status + an archive `.md` carrying them → non-empty after init, fail-soft on missing/malformed). T108/D12 deps (`dependsOn [T91,T109]`, refs `defects:D12`/`goals:G6`) confirmed intact. No goal phase transition.
- **Discovered:** Verified against live source that R96's diagnosis is correct: `FsLedgerStore.fetch()` (src/store/FsLedgerStore.ts:385-390) is synchronous and returns FS-free `materialiseFetchedLedger` (pointers spread plain at InMemoryLedgerStore.ts:607); `init()` already reads each archive `.md` via `collectArchivedItems`→`fetchArchive`, with `fetchArchive` calling `parseMilestoneItemArchive` (FsLedgerStore.ts:444) for the milestones ledger — so the title/status are already in hand at init. (Note: store sources live under `packages/ledger/src/store/`, not `src/`.)
- **Issues:** R96 has no `defects[]` bucket — nothing to file-and-defer. The revised plan now awaits the reviewer for re-review.
