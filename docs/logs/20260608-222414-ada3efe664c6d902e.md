# Plan review log — G34 follow-up #2 (reviewer: opus[claude], configured mode) — round 1

- Goal G34 (M108), review of follow-up #2 plan (M116/M118/M120, T283–T300). Verdict: **revise** (3 criticism / 0 new_questions / 0 defects). JSON returned; orchestrator wrote the aggregated reviews item.

### Verdict JSON
```json
{"summary":"Well-sequenced DAG that faithfully implements Q154/Q156/Q157/Q158/Q159 and covers embedded-mode serving + node-leak guard, but it CONTRADICTS the authoritative Q155 answer (DROP build-time model fields) by keeping them as a static fallback, and narrows its overlay-fallback to LedgerToolError when the primary older-server case throws a different error.","verdict":"revise","new_questions":[],"criticism":["Q155 answered 'as recommended' = DROP the build-time model/modelMappings fields; the plan does the OPPOSITE (T299 keeps gen-agents emitting them, T293/T295 render the static value on overlay error), reintroducing the example-vs-live divergence the follow-up exists to eliminate. Rework T293/T295/T299 (+T283 AgentRole typing) to DROP model/modelMappings + render the distinguished states on the no-overlay path.","T289/T293 key the overlay fallback on a thrown LedgerToolError, but the PRIMARY Q155 fallback (older/embedded server lacking get_agent_models) surfaces as a generic JSON-RPC unknown-tool error from the SDK callTool, NOT a LedgerToolError (McpLedgerClient only builds LedgerToolError on an isError result). Mount fetch must catch ANY thrown error.","T283 claims the server modelClass enum ('frontier'|'standard'|'fast'|null) is identical to web ModelClass so 'no remapping' — but web ModelClass adds 'N/A'/'default'. The status->label mapping must be the single source of truth (T293/T295), not a non-existent identity."],"defects":[]}
```

### Session summary
- **Did:** reviewed the ff#2 plan against answered Q154–Q159 + real repo (configCapability.ts, gen-agents-catalogue.ts, agentsCatalogue.ts, agentsCatalogue.gen.test.ts, mcpClient.ts, types.ts).
- **Achieved:** verdict revise; 3 criticisms, 0 new questions, 0 out-of-scope defects.
- **Discovered:** plan KEEPS build-time model fields (contradicts Q155=DROP); freshness guard is a full-file byte diff + a Part-(a) `model`-field assertion (so T299 must also update Part (a)); web ModelClass differs from the proposed server enum; fallback narrows to LedgerToolError missing older-server unknown-tool error. DAG sequencing/Q156/Q157/Q158/embedded-serving/node-leak-guard all faithfully covered.
