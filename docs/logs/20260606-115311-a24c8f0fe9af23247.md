# Implement-flow session log
- **Task:** T187 (M68, G20) — worker, round 0 — pass (resultCommit 0c21f43)

### Session summary
- ledger-web main() now calls loadConfig(cwd) + resolveWebOpts to apply cq.toml [webui] host/port with CLI-over-config precedence; serveEmbedded uses scanForPort internally (around the sync Bun.serve; serveEmbedded is async due to createEmbeddedStore), serveProxy uses scanForPort from serve(); actual bound URL printed to STDOUT, human serving line kept on STDERR (actual port). @cq/config symlink added to the ledgerWeb installPhase in flake.nix (scoped to ledgerWeb derivation only; @cq/config source already staged by embedServerClosure, only the node_modules symlink was missing). @cq/config dep was already declared in ledger-web/package.json (T186).
- resultCommit 0c21f43 (branch implement/T187). bun run check 982 pass / 0 fail. 4 new acceptance tests (mainWiring.test.ts) cover no-cq.toml/config-port/CLI-wins/occupied-port. nix build deferred to T192 (FOD hash stale on main until then). Files: flake.nix, ledger-web/src/serve.ts, test/mainWiring.test.ts.
