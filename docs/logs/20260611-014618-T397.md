# Session log — T397 acceptance gate (orchestrator operational verification) — PASS

- **Task:** T397 (M169, G48; final acceptance gate); verified directly by the orchestrator on merged main (3f8c7f5 code / docs on top). No code change — a verification gate. All criteria met.

### (1) bun run check — green
1687 pass / **1 skip** / **0 fail** / 6308 expect() across 144 files. The 1 skip is environment-conditional and pre-existing (`claude-stop-hook.test.ts` `it.skipIf(!HAVE_JQ)` when `jq` is absent on the direct run / the `ledger-tui/test/pty.e2e` suite gated on node-pty) — NOT a regression (0 fail; tsc -b + eslint also green, gated by `bun run check`). The unchanged ledger-mcp/ledger-tui/ledger-web product suites (mcp tool tests, ink-testing-library, happy-dom) still pass — the embedded-MCP byte-for-byte invariant holds.

### (2) nix build .#cq — green
Builds locally (`cq-0.0.1`; the `ssh-ng://…trx40` line is a remote-builder fallback, build proceeds locally). `result/bin/cq` produced.

### (3) Mode-parity launch evidence (all against examples/sample-ledger)
- **(a) `cq mcp` stdio** → `initialize` returns `serverInfo {name: ledger-mcp, version: 0.0.1, title: sample-ledger}`; `tools/list` returns **26 tools**. (Fixed by D59/T399 — pre-fix it served 0.)
- **(a) `cq mcp --http 7798`** → Streamable HTTP `/mcp` `initialize` returns `serverInfo` + `capabilities.tools.listChanged` + the ledger `instructions`.
- **(b) `cq tui --cwd`** → routes to the Ink TUI (headless: "Raw mode is not supported on process.stdin" — the EXPECTED non-TTY outcome, proving the tui delegate fired and the in-memory-transport embedded path is reached; full render parity is covered by the unchanged ink-testing-library suite in (1)).
- **(c) `cq web --cwd --port 5188`** embedded → `GET /` serves the SPA (`<!doctype html> … <title>ledger-web</title>`); `POST /mcp` `initialize` returns `serverInfo`; log: "ledger-web: serving … → embedded MCP (cwd=…)". (/ws co-hosted by the same embedded server.)
- **(c) `cq web --mcp-url http://127.0.0.1:7796/mcp` proxy** → reverse-proxies `/mcp` to the remote `cq mcp --http`; `initialize` returned proxied `serverInfo`; log: "serving … → MCP upstream http://127.0.0.1:7796/mcp".
- **(d) `cq mcp restore --from-cache`** → routes to the restore handler ("ledger-mcp restore: no cache mirror to restore from at ~/.cache/cq/ledgers/… — the directory is absent or empty" — correct behavior for an empty cache; the nested-subcommand routing works).
- **(e) old products gone** → `nix flake show` lists only `packages.{cq,default=cq-0.0.1,node-modules}` + `apps.{cq,default}` + the untouched LLM-asset/harness packages; NO ledger-mcp/ledger-tui/ledger-web packages or apps.

### Defects found + fixed during the gate
The gate surfaced TWO build/runtime defects invisible to the eval-only T391 acceptance and the byte-level reviews — both fixed and re-verified here:
- **D58** (T398) — cqCli staged cq-cli one level too deep (`mkdir` pre-created the cp dest → nesting); `Module not found`.
- **D59** (T399) — the cq dispatcher's `main()` `process.exit(0)`'d right after a mode delegate resolved post-`connect()`, tearing the stdio channel down (0 tools); fixed via a `longRunning` flag so modes govern their own process lifetime.

**Verdict: PASS** — all G48 acceptance criteria met; the unified `cq mcp|tui|web` product is behavior-equivalent to the three former standalone tools, embedded MCP intact, old products removed.
