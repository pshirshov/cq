# implement-worker — T117 (tui defect colors) — pass

- Task T117 (M33, G6); resultCommit 4aa3634 (parent ef4bc69)
- pass; `bun run check` 711/0. Added `ready` bucket (blueBright) for non-terminal `root-caused`; new PARKED_WARNING set maps non-terminal `inconclusive`→warning(magenta); added `wontfix` to DROPPED (gray). status.test.tsx asserts bucket/color for root-caused/inconclusive/wontfix + unchanged open/wip/resolved. `abandoned` kept in DROPPED (still valid for goals/tasks).
- files: packages/ledger-tui/src/status.ts, packages/ledger-tui/test/status.test.tsx
