# Plan review log — G34 (reviewer: opus/claude, configured mode)

- Goal: G34 (milestone M108), review round 1
- Role: plan-reviewer (multi-reviewer panel, claude/opus), CONFIGURED mode (returned JSON, wrote nothing)
- Verdict: **revise** (2 criticism, 0 new_questions, 0 defects)

### Session summary
- **Did:** reviewed the emitted plan for G34 (rename + Agents tab + [tiers] inversion); read goal, Q145–Q150, milestones M109–M112, tasks T267–T280; grounded against cq-config (types/config/toml/index), the ledger-web Bun.build/serve model, flowData.ts precedent, App.tsx HelpOverlay, and the cq-assets agent .md frontmatter.
- **Achieved:** verdict `revise`; 2 criticism / 0 new questions / 0 defects; returned JSON, wrote nothing.
- **Discovered:** sequencing is correct (M109∥M110, M111 dependsOn both, M112 last); part-3 inversion well-grounded (resolveTierToken/resolveAgentModel/TiersConfig/parseTiers exist; RawToml.tiers already Record<string,string> so token-keyed fits; tie-break specified in T271/tested in T273; T272's workspace rg+typecheck+nix-build gate catches all consumers though its "likely consumers" prose is loose — cq-cli doesn't even depend on cq-config). Codegen/Nix-closure reasoning sound. **Material gap (part 2):** the cq-assets agent .md files carry only frontmatter (name/description/disallowedTools) + prose body — NO structured inputs/outputs/IO-schema data — yet Q148/the goal require those as typed fields and Q147 chose codegen to avoid drift; the plan never names the source of truth for those fields, and a hardcoded-in-script table would be invisible to the T277 freshness guard.
- **Issues:** codegraph not initialized + no Grep/Bash in this reviewer; consumer audit relied on package.json dep edges + task descriptions; conclusion robust regardless.

Verdict JSON (criticism): (1) Part 2 Agents tab has no defined source of truth for structured inputs/outputs/IO-schema fields — must add structured frontmatter to assets (parsed by codegen, genuinely guarded by freshness) OR explicitly scope as hand-curated outside the freshness guard. (2) T275 parseAgentMarkdown spec says extract 'tools' but real frontmatter uses 'disallowedTools' (+ 'isolation' on worker) — fix the field mapping or 'tools' is always empty.
