# implement-worker — T89 (M19) TUI inert-when-answered — PASS

Agent adf13b9914590c709. resultCommit 25fa8b3, branch worktree-agent-adf13b9914590c709 (rebased onto 066bfe9). check 605 pass / 0 fail.

Added hasPersistedAnswer(item) helper; gated `r` (as-recommended) and 1-9 (number-pick) keys in both list/content focus on !hasPersistedAnswer(cur.item); same gate on Ctrl+R in BatchAnswerOverlay. Key hints suppress 'r as-recommended' + '1-9 pick suggestion' tokens when persisted answer non-empty. Fixture: Q3 already has non-empty persisted answer while open/answerable. 9 ink tests (all focus/key combos, positive empty-answer cases, hint suppression, batch overlay gate). Files: app.tsx, test/app.test.tsx.
