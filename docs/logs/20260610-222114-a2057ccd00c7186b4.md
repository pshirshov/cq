# Session log — G48 plan-advance (opus, CANDIDATE mode), planning round 1 — SYNTHESIS BASE

- **Goal:** G48 (unify CLI tools into one cq); candidate planner 1-of-3; emitted a 4-milestone / 12-task DAG. Selected as the synthesis base.

### Session summary
- **Did:** CANDIDATE-mode grounding for G48 — read the goal + the 5 locked answers (Q214-Q218) and the actual code (cq-cli dispatcher SUBCOMMANDS/USAGE/HANDLERS/dispatch, the three product mains all exporting main(argv) + import.meta.main-guarded, all 4 package.json bins, flake.nix's 5 derivations + node-modules FOD, .mcp.json, nix/hm/tools.nix).
- **Achieved:** 4-milestone (A dispatch+delegation → B delete bins + collapse flake → C migrate call sites → D FOD refresh + acceptance gate) / 12-task DAG.
- **Discovered:** the mains are side-effect-guarded + exported → delegation is `import('@cq/ledger-{mcp,tui,web}').main(post-mode-argv)` with zero re-parse; the cq dispatcher's parseSubcommandArgs is native-flag-specific (--cwd/--yes/--force/--to/--session) and must NOT touch mode argv → the mode branch must short-circuit BEFORE native parsing, passing argv.slice(1) verbatim. The dev-llm references actually live in **nix/hm/tools.nix** (the servers.ledger command `${ledgerPkg}/bin/ledger-mcp` + the ledgerTools PATH list lines 41-46), NOT dev-llm.nix. The merged .#cq closure must be the UNION of all per-product staging fragments (embedServerClosure for mcp+embedded, ink/react for tui, react/react-dom/react-markdown/remark-gfm/rehype-sanitize/elkjs for the web SPA Bun.build, @cq/config, the LEDGER_WEB_OUTDIR wrapper env).
- **Issues/risks:** (1) FOD-hash choreography after the 3 cq-cli deps; (2) web SPA build inputs must be in the .#cq closure or Bun.build fails at startup; (3) embedded-MCP invariance (TUI in-memory + web /mcp+/ws byte-for-byte); (4) cq mcp stdout must stay protocol-only (delegating to ledger-mcp's main preserves it; the wrapper must not print). The ledger-tui exported entry reads process.argv in some paths — the tui delegation must ensure cq's post-mode argv reaches parseArgs.
