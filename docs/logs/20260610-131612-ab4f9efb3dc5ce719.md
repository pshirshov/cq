# Session log — T354 implement-worker (opus)

- **Task:** T354 (M147, G43); status pass; resultCommit 039bfb6 (worktree) → 590c203 (main); check 1592/0

### Session summary
Added the native `cq move-ledger --to git|local` subcommand (cq-cli: SUBCOMMANDS/parser `--to`/HANDLERS/USAGE + new moveLedger.ts) — lossless bidirectional ledger transplant between docs/ working tree and the orphan ref. **--to git:** snapshot docs/*.md + docs/archive + docs/ledgers.yaml into the orphan ref via GitPlumbing (docs-relative tree), `git rm --cached` the docs files (LEFT IN PLACE on disk per R418), add the git-backend .gitignore block (ensureGitBackendGitignore, T357's shared helper), set [ledger] backend='git-object'. **--to local:** reverse — materialize the ref back to docs/, remove the .gitignore block (NEW removeGitBackendGitignore counterpart), re-track via git add, set backend='fs'. Refuses without --to; refuses non-empty target without --force; honours [ledger].branch; per-ledger moved-file summary; linked-worktree fallback in USAGE. Added GitPlumbing.lsFiles/add/rmCached (these touch the REAL index — the deliberate intent of re-track/untrack; reviewer CONFIRMED they are called ONLY from moveLedger.ts, NEVER the backend, so the host-byte-identity invariant holds). Round-trip test proves byte-losslessness (docs bytes before --to git === after --to local) + tracked→untracked→tracked. check green 1592/0 worktree, 1591/0 combined main. Based on main ec785d2.
