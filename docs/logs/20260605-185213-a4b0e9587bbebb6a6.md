# Plan-flow session log

- **Goal:** G15 (milestone M51) — Explorer RW + pluggable reviewers
- **Role:** reviewer (plan-reviewer)
- **Verdict:** revise (review R169) — 3 criticisms + 1 new question

---

### Session summary
- **Did:** adversarially reviewed the emitted plan for goal G15 (two-feature harness change — investigate two-tier explorer/prober + pluggable parallel reviewers via cq.toml), grounding every claim against the actual repo (investigate-explorer.md, investigate/advance.md, plan/advance.md, implement/advance.md, plan-reviewer.md, implement-reviewer.md, dev-llm.nix, assets.nix, link-prompts.ts, package.json, .mcp.json) and the answered questions Q88–Q93.
- **Achieved:** verdict **revise**, review id **R169**, 3 planner-fixable criticisms + 1 out-of-scope/pre-existing fault folded into criticism prose (the reviews schema has no `defects` field) + 1 new question.
- **Discovered:**
  - Grounding largely solid: explorer JSON contract `{hypothesisId, evidence[], lean, notes}` with `lean: insufficient` confirmed; orchestrator `allowed-tools` already include `Agent/Write/Bash`; `isolation:"worktree"` (Claude) + manual `git worktree add/remove` (Codex) precedent confirmed in implement/advance.md; `mergedAgents`→Claude-only (so the shared-rubric-prompt rationale holds); `programs.mcp.servers` + `piMcpJson` keep-alive remap confirm T172's wiring; `assets.nix` auto-collects new `agents/*.md` + `commands/cq/*.md` (no assets.nix edit needed). DAG is acyclic; all same-file edits serialized (T178→T168 for link-prompts.ts/README; T171→T170 for package.json/cq-config).
  - **T173/T175 double-write defect:** T173 keeps native plan-reviewer.md writing the reviews item directly while T175 has the orchestrator write the single aggregated item → two reviews items per round in multi-reviewer mode, breaking the reconciliation invariant. Implement side (T174/T176) is clean because implement-reviewer already returns json.
  - **T168/T178 stale source root:** link-prompts.ts `source:` paths use a `llm/` root that does NOT resolve under `nix/pkg/cq-ledgers/` (assets actually live under `nix/pkg/cq-assets/`); "match existing style" would replicate a broken root and the "runs clean" acceptance can pass while creating dangling symlinks. Same staleness in cq-assets/README.md is a pre-existing out-of-scope fault.
  - **cq-config not in `.mcp.json`:** this repo's `.mcp.json` wires only `ledger`; T172 only wires the home-manager global registry, leaving the in-repo reviewer feature unreachable during dogfooding.
  - Q91 was left empty by the user; planner adopted the conservative strictest-wins+union recommendation — reasonable default, raised one confirming question.
- **Issues:** could not run a shell to definitively classify whether `nix/pkg/cq-ledgers/llm` is a broken symlink vs simply absent (Bash/Glob not available); the conclusion (the `llm/` root does not resolve) is firm from repeated Read failures against it while the cq-assets equivalents read fine.
