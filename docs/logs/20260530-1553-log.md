# Session log — 2026-05-30 15:53 — phase-timeout cycle

## Original request

Small urgent reliability fix on cq's `/plan` workflow: raise the phase-dispatch
timeout so the explore-first phase subagents don't time out on a real repo. Run
under `/review-loop` discipline — one milestone. ENV-overridable, no CLI flag, do
NOT attempt the deeper "explore once" optimization (separate later cycle).

Worktree `.claude/worktrees/phase-timeout`, branch `phase-timeout`, base `5d0408c`.

## Milestone worked

M-PHASE-TIMEOUT — pt-1 (single PR). Plan doc:
`docs/drafts/20260530-1553-phase-timeout.md`.

## What shipped

- New shared helper `packages/server/src/workflow/phaseTimeout.ts`:
  `resolvePhaseTimeoutMs(optTimeoutMs?, env=process.env)`,
  `DEFAULT_PHASE_TIMEOUT_MS = 300_000`,
  `PHASE_TIMEOUT_ENV_VAR = "CQ_WORKFLOW_PHASE_TIMEOUT_MS"`.
  Resolution order: explicit opt → valid env (positive integer; non-numeric /
  ≤0 / float / `1e3` / `0x10` / safe-integer-overflow → default) → 300_000.
- Wired all three lanes to the helper: `claudeProducer`, `claudePhaseSubagent`,
  `codexHeadless` (`dispatchCodexPhase`). Removed their local
  `DEFAULT_TIMEOUT_MS` / `DEFAULT_CODEX_TIMEOUT_MS` constants. The two Codex
  wrappers (`codexProducer`/`codexPhaseSubagent`) only had stale doc-comments —
  updated; they pass `timeoutMs` through so resolution happens once in the shared
  dispatch.
- Removed dead `DEFAULT_CODEX_TIMEOUT_MS` barrel re-export (no consumers); export
  the new helper trio from `workflow/index.ts`.
- Tests: `phase-timeout.test.ts` (helper unit) + `phase-timeout-wiring.test.ts`
  (each lane honors the env via a never-ending subagent + explicit-opt-wins),
  env set/restore so nothing leaks.
- Timeout-rejection message format unchanged
  (`<label> timed out after <ms>ms without submitting`).

## Review rounds

One self-review round (no subagent-dispatch tool in this harness; adversarial
review run as an explicit per-milestone pass over the full diff).

- `PHASE-TIMEOUT-01` (major) — the feature itself: raise to 300 s + env override.
  Resolved.
- `PHASE-TIMEOUT-01-D01` (nit) — self-review found a latent (not live)
  cross-test env-leak footgun in the wiring test: the env snapshot was taken
  inside a per-test `setEnv()` call rather than the test lifecycle, so a future
  test omitting `setEnv` would restore a stale value. Hardened to an
  unconditional `beforeEach` snapshot. Resolved.

No open defects at close.

## Discharge

- `bun run check` exit 0 ×2 (1089 pass / 0 fail both; baseline 1077 + 12 new
  tests; deterministic, no env leak).
- `bun run e2e` 28/28 (1.8m; mock injects short timeouts → default bump does not
  slow e2e).
- `nix build .#default` exit 0 (`result` → cq-0.0.1; remote SSH builder
  unreachable, built locally — same as the dark cycle's baseline).

## Deferred / follow-up (out of scope this cycle)

Every /plan phase RE-EXPLORES the repo from scratch (redundant latency + token
cost). The real optimization is to explore ONCE in the producer and pass that
grounding context to later phases. This cycle deliberately shipped only the
symptom fix (raised timeout). Flagged in `PHASE-TIMEOUT-01` and the pt-1
Completed entry as a separate later cycle.

## Final ledger state

- `tasks.md`: M-PHASE-TIMEOUT CLOSED, pt-1 `[x]` with rich Completed entry.
- `defects.md`: `PHASE-TIMEOUT-01` `[x]` resolved, `PHASE-TIMEOUT-01-D01` `[x]`
  resolved.

Metrics: WIP max 1; review rounds pt-1:1; verification complete; audit
discrepancies 0; algedonic escalations 0.
