# Implement log — T277 (freshness test) + T278 (Agents tab UI)

Both dispatched in parallel worktrees from main dd49958; merged to main (T277 in 1e0b26e's parent range, T278 = 1e0b26e). bun run check green (T277 1213/0; T278 1201/0). Panel: opus[claude] + minimax[pi:ollama-cloud]; grok+codex[pi:grok-build] excluded (stall).

## T277 (M111) — freshness/drift test — worker a0c03e26f7e884877 (67cc722) — APPROVE (both)
Added packages/ledger-web/test/agentsCatalogue.gen.test.ts: (a) 11 role-set invariant tests (19 roles present, required fields, privilege spot-checks plan-reviewer=RO/implement-worker=RW); (b) freshness drift guard via save-run-compare-restore (beforeAll saves committed gen.ts, spawns `bun run gen-agents`, reads fresh, byte-compares; afterAll restores). Teeth VERIFIED (perturbing gen.ts → freshness assert fails; afterAll restores → clean tree). +12 tests.
- opus APPROVE: role-set assertions match real gen.ts; freshness guard sound + safe (afterAll restores even on beforeAll-throw + mid-suite failure; git status clean both times); save-restore is mutating but the gen-to-temp alternative is blocked by the T276 script's hardcoded OUT_FILE (out of scope); check green 1213/0.
- minimax APPROVE (with a non-blocking QUESTION on codegen cross-run determinism — out of T277 scope).

## T278 (M111) — Agents tab UI — worker aafd0f9f787e1e7ec (fd4f997) — APPROVE (reconciled)
Added 'agents' tab to App.tsx HelpOverlay: tab button (help-tab-agents) after Flows; AgentsTab maps all 19 AGENT_ROLES to one section per role (help-agent-<id>) with name/kind/description/inputs/outputs/ioSchema, model class + per-harness mappings (N/A/default fallback), an RO/RW privilege badge (help-agent-<id>-privilege), exposed-tools descriptor (help-agent-<id>-tools), and the prompt template in a <details> COLLAPSED by default (help-agent-<id>-prompt) via the Markdown component. Added lw-agent* CSS. Scope: App.tsx + styles.css only (verified by diff stat). bun run check green 1201/0.
- opus APPROVE (authoritative, full worktree): all 19 roles render, RO/RW badge + exposed-tools + folded <details> (no `open`); AgentModelMappings handles N/A/default/per-harness correctly; the shortcuts/item-states/flows panel branches are byte-identical (surgical ternary extension, no regression); key props present; scope clean.
- minimax DISAPPROVE — REJECTED as based on FACTUALLY FALSE premises: (1) "scope violation: new agentsCatalogue.ts" — FALSE: the diff stat shows T278 touches ONLY App.tsx+styles.css (2 files); agentsCatalogue.ts is the PRE-EXISTING T275 module T278 merely imports; (2) "fragile lowercase privilege class" — FALSE: privilege is the closed `Privilege` ('RO'|'RW') union from T275 (verified types.ts:105), so toLowerCase() is safe; (3) "renderListField undefined" — refuted by the green typecheck (it compiles); (4) "Source field extra" — Q148 explicitly permits additional useful fields. minimax reviewed a fragment without the T275 type context. opus (full-worktree, typecheck-verified) + the diff stat + the green check refute every point. Reconciled APPROVE.
