# plan-advance (planner) — goal G4 (D2 fix) — review-requested

Agent a216c7f3fec223109. Defect-seeded G4 (planning, clarify-skipped); emitted D2 fix-task plan under M22.

### Session summary
- Did: emitted the D2 fix-task plan for defect-seeded G4 (already planning, no review yet); created work milestone + fix tasks; wired bidirectional defect link; did NOT transition goal phase.
- Achieved: M22 ("G4-W: D2 backup-and-reinit") appended to G4.fields.milestones. 4 sequenced fix tasks: T94 (backup-and-reinit helper, standard), T95 (rewire init() divergence branch to backup-reinit-by-default + onSchemaDivergence opt-out, frontier), T96 (dual-tests: divergence→backup+reinit, abort opt-out, no-divergence+empty-dir regressions, standard), T97 (repo gate bun run check, fast). Each links defects:D2 + goals:G4; D2.dependsOn=[T94,T95,T96,T97]. Goal stays planning.
- Discovered: source confirms grounding — fatal throw at FsLedgerStore.ts:283-289 (!schemasEqual); reinit primitives (freshLedger, serializeRegistry/writeRegistry, writeLedgerFile, seedBootstrapGroup/applyEnsureAmbientMilestone, CANONICAL_LEDGERS) + path fields all exist; constructor opts (L150) is the home for onSchemaDivergence opt-out; store already mkdir's docs/archive so docs/.backup/<ts>/ follows the pattern.
- Issues: source fix doesn't retroactively repair a stale running global binary (needs rebuild) — out of scope, captured in G4 description.
- Status token: review-requested.
