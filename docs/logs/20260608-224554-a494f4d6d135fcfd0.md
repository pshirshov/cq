# Implement worker log — T284 (worker: sonnet[claude], worktree)

- Task T284 (M117/G36). Branch implement/T284, commit c6db66c, base b88c03f. status: pass. bun run check GREEN (1236 pass / 0 fail; tsc -b + eslint clean).

### Session summary
- Added PI_EFFORTS/CLAUDE_EFFORTS `as const` arrays, PiEffort/ClaudeEffort/Effort types, and isEffort(harness,value) guard to cq-config/src/types.ts; added optional `effort?: Effort | null` to ReviewerToken; exported all new symbols from index.ts; added test/effort.test.ts covering the five acceptance assertions + full-vocabulary + type-level checks.
- **Discovered:** `effort` had to be declared OPTIONAL (`effort?: Effort | null`) not required — ~40 existing ReviewerToken literal constructions in tests omit it, and bun:test `toEqual` fails when received has a property absent from expected. Optional-with-`?` matches the 'null = omitted = default; current behavior unchanged' semantic. (T286 will set effort during parse; reviewerTokensEqual identity in T290 must account for optional/undefined vs null.)
- pi effort vocabulary could not be confirmed from the Nix store; the six values pinned as specified. filesTouched: cq-config/src/types.ts, cq-config/src/index.ts, cq-config/test/effort.test.ts.
