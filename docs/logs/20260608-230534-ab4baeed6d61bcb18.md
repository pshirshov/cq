# Implement worker log — T286 (worker: opus[claude], worktree)

- Task T286 (M117/G36). Branch implement/T286, commit 0c757dc, base 005c19c. status: pass. bun run check GREEN (1246 pass / 0 fail).

### Session summary
- Rewrote parseReviewerToken: harness off the FIRST ':', candidate effort off the LAST ':' recognised ONLY via isEffort(harness, suffix); ':' reserved in the residual model on BOTH the claude model and the pi model half (R342); invalid effort or stray reserved ':' throws CqConfigError naming the bad value + legal set; omitted suffix yields effort:null. New tests cover all acceptance examples + the five rejection cases.
- **Discovered:** the previously-passing test `pi:prov/a:b preserves colons` encoded the OLD rule T286 reverses → changed to assert rejection (matches pi:prov/mo:del). Adding effort:null to parse output broke ~18 existing toEqual token-equality assertions across config/e2e/cq-toml-example tests → all updated to include effort:null (legitimate shape gain).
- **Worktree stale-base note:** worktree was on 087b889 'wip' (pre-T284); worker branched implement/T286 from 005c19c to get the effort symbols. Commit parent verified 005c19c.
- filesTouched: cq-config/src/config.ts + test/{config,e2e-and-cross-layer,cq-toml-example}.test.ts.
