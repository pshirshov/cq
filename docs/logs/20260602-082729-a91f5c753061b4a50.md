# plan-advance session log

- **Goal:** G2 — ledger-suite UI/schema enhancements
- **Role:** planner (plan-advance)
- **Agent:** a91f5c753061b4a50
- **Returned status:** awaiting-answers

---

### Session summary
- **Did:** Rule 2(a) — G2 was in `clarifying` with no questions and is not defect-seeded, so I grounded in the repo and filed the first batch of clarifying questions under M10. No phase transition.
- **Achieved:** Created 8 open questions Q29–Q36 (one per in-scope item #1,#1-detail,#3,#4,#5,#6,#7,#8), each linked `goals:G2`, with grounded context, suggestions, and a recommendation. Goal remains in `clarifying`; these gate the next phase.
- **Discovered:** `suggestions` is already `string[]` in the schema (item #4 is mostly normalization + list-rendering, not a schema add). `revise` and `go-ahead` both bucket to `done`→green (item #6 root, in `status.ts` `statusBucket`/`BUCKET_COLOR`, shared web+TUI). `DagView` (`packages/ledger-web/src/DagView.tsx`) uses an independent hardcoded `STATUS_COLORS` map ignorant of non-milestone statuses while `loadDagData` graphs every ledger (item #8 root). No `LedgerClient`/MCP method exposes the project/cwd name, so item #7 needs a new server-side surface. Column config does not exist anywhere; tables are hardcoded id/status/summary. `QUESTION_FIELD_ORDER` (the G1/T23 ordering) lives in `status.ts`.
- **Issues:** Item #7 requires a new MCP capability (server→client display name); flagged in Q35. Item #3's requested order is not just a constant edit — it requires restructuring leading metadata rows in both UIs (flagged in Q31). No blockers; awaiting user answers.

---

## Questions filed (Q29–Q36)
One per in-scope item: #1 column selector (+ default/persistence fork), #3 questions field order, #4 suggestions list normalization, #5 batch answer mode (modal vs full-screen; TUI equivalent), #6 revise color, #7 project-dir title (needs new MCP surface), #8 graph-view colorization (single shared status→color source). Item #2 (auto-init defect) deliberately excluded — routed to /investigate:start.
